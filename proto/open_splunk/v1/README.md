# Protobuf contracts

Versioned protobuf source files for SRouter request/response APIs, the binary search-progress WebSocket, shared typed values, and collector gRPC ingestion belong here.

Generated Go code is written to `gen/go`; generated TypeScript code is written to `gen/ts`.
