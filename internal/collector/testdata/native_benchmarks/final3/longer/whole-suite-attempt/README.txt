Stopped after six accepted whole-suite pairs because unrelated short jobs repeatedly overlapped the longer three-case window. No benchmark values were inspected from this attempt before changing sampling granularity. Replaced by 20 independently monitored baseline/candidate pairs per case, still two seconds per case and unchanged binaries. All samples and rejected workload observations remain here.
