@AGENTS.md

Claude Code specifics: prefer the Bash tool for grep/sed and file reads; run the verification block from `AGENTS.md` before reporting a change as done; when spawning agents that commit into the checkout, keep `pull.ff=only` and have exactly one writer per checkout.
