# Deployment

Local Docker Compose and later packaging assets belong here. The Open Splunk server remains a single application binary; ClickHouse is deployed as its external database service, and collectors are deployed separately on log-producing hosts.

