// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.0
// source: open_splunk/v1/search_ws.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { ApiWarning, FieldViolation } from "./common";
import {
  ExportArtifact,
  ExportFailure,
  ExportJobState,
  exportJobStateFromJSON,
  exportJobStateToJSON,
  ExportProgress,
} from "./export";
import { ResultRow, ResultSchema } from "./result";
import { SearchFailure, SearchJobState, searchJobStateFromJSON, searchJobStateToJSON, SearchProgress } from "./search";

export enum PreviewUpdateMode {
  PREVIEW_UPDATE_MODE_UNSPECIFIED = 0,
  PREVIEW_UPDATE_MODE_RESET = 1,
  PREVIEW_UPDATE_MODE_APPEND = 2,
  UNRECOGNIZED = -1,
}

export function previewUpdateModeFromJSON(object: any): PreviewUpdateMode {
  switch (object) {
    case 0:
    case "PREVIEW_UPDATE_MODE_UNSPECIFIED":
      return PreviewUpdateMode.PREVIEW_UPDATE_MODE_UNSPECIFIED;
    case 1:
    case "PREVIEW_UPDATE_MODE_RESET":
      return PreviewUpdateMode.PREVIEW_UPDATE_MODE_RESET;
    case 2:
    case "PREVIEW_UPDATE_MODE_APPEND":
      return PreviewUpdateMode.PREVIEW_UPDATE_MODE_APPEND;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PreviewUpdateMode.UNRECOGNIZED;
  }
}

export function previewUpdateModeToJSON(object: PreviewUpdateMode): string {
  switch (object) {
    case PreviewUpdateMode.PREVIEW_UPDATE_MODE_UNSPECIFIED:
      return "PREVIEW_UPDATE_MODE_UNSPECIFIED";
    case PreviewUpdateMode.PREVIEW_UPDATE_MODE_RESET:
      return "PREVIEW_UPDATE_MODE_RESET";
    case PreviewUpdateMode.PREVIEW_UPDATE_MODE_APPEND:
      return "PREVIEW_UPDATE_MODE_APPEND";
    case PreviewUpdateMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ResynchronizationReason {
  RESYNCHRONIZATION_REASON_UNSPECIFIED = 0,
  RESYNCHRONIZATION_REASON_SEQUENCE_EXPIRED = 1,
  RESYNCHRONIZATION_REASON_SERVER_RESTARTED = 2,
  RESYNCHRONIZATION_REASON_SLOW_CONSUMER = 3,
  RESYNCHRONIZATION_REASON_STATE_DIVERGED = 4,
  UNRECOGNIZED = -1,
}

export function resynchronizationReasonFromJSON(object: any): ResynchronizationReason {
  switch (object) {
    case 0:
    case "RESYNCHRONIZATION_REASON_UNSPECIFIED":
      return ResynchronizationReason.RESYNCHRONIZATION_REASON_UNSPECIFIED;
    case 1:
    case "RESYNCHRONIZATION_REASON_SEQUENCE_EXPIRED":
      return ResynchronizationReason.RESYNCHRONIZATION_REASON_SEQUENCE_EXPIRED;
    case 2:
    case "RESYNCHRONIZATION_REASON_SERVER_RESTARTED":
      return ResynchronizationReason.RESYNCHRONIZATION_REASON_SERVER_RESTARTED;
    case 3:
    case "RESYNCHRONIZATION_REASON_SLOW_CONSUMER":
      return ResynchronizationReason.RESYNCHRONIZATION_REASON_SLOW_CONSUMER;
    case 4:
    case "RESYNCHRONIZATION_REASON_STATE_DIVERGED":
      return ResynchronizationReason.RESYNCHRONIZATION_REASON_STATE_DIVERGED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ResynchronizationReason.UNRECOGNIZED;
  }
}

export function resynchronizationReasonToJSON(object: ResynchronizationReason): string {
  switch (object) {
    case ResynchronizationReason.RESYNCHRONIZATION_REASON_UNSPECIFIED:
      return "RESYNCHRONIZATION_REASON_UNSPECIFIED";
    case ResynchronizationReason.RESYNCHRONIZATION_REASON_SEQUENCE_EXPIRED:
      return "RESYNCHRONIZATION_REASON_SEQUENCE_EXPIRED";
    case ResynchronizationReason.RESYNCHRONIZATION_REASON_SERVER_RESTARTED:
      return "RESYNCHRONIZATION_REASON_SERVER_RESTARTED";
    case ResynchronizationReason.RESYNCHRONIZATION_REASON_SLOW_CONSUMER:
      return "RESYNCHRONIZATION_REASON_SLOW_CONSUMER";
    case ResynchronizationReason.RESYNCHRONIZATION_REASON_STATE_DIVERGED:
      return "RESYNCHRONIZATION_REASON_STATE_DIVERGED";
    case ResynchronizationReason.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum SearchWebSocketProtocolErrorCode {
  SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_UNSPECIFIED = 0,
  SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_INVALID_COMMAND = 1,
  SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_UNSUPPORTED_COMMAND = 2,
  SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_INVALID_TARGET = 3,
  SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_JOB_NOT_FOUND = 4,
  SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_TOO_MANY_SUBSCRIPTIONS = 5,
  SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_FRAME_TOO_LARGE = 6,
  SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_FORBIDDEN = 7,
  UNRECOGNIZED = -1,
}

export function searchWebSocketProtocolErrorCodeFromJSON(object: any): SearchWebSocketProtocolErrorCode {
  switch (object) {
    case 0:
    case "SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_UNSPECIFIED":
      return SearchWebSocketProtocolErrorCode.SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_UNSPECIFIED;
    case 1:
    case "SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_INVALID_COMMAND":
      return SearchWebSocketProtocolErrorCode.SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_INVALID_COMMAND;
    case 2:
    case "SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_UNSUPPORTED_COMMAND":
      return SearchWebSocketProtocolErrorCode.SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_UNSUPPORTED_COMMAND;
    case 3:
    case "SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_INVALID_TARGET":
      return SearchWebSocketProtocolErrorCode.SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_INVALID_TARGET;
    case 4:
    case "SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_JOB_NOT_FOUND":
      return SearchWebSocketProtocolErrorCode.SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_JOB_NOT_FOUND;
    case 5:
    case "SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_TOO_MANY_SUBSCRIPTIONS":
      return SearchWebSocketProtocolErrorCode.SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_TOO_MANY_SUBSCRIPTIONS;
    case 6:
    case "SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_FRAME_TOO_LARGE":
      return SearchWebSocketProtocolErrorCode.SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_FRAME_TOO_LARGE;
    case 7:
    case "SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_FORBIDDEN":
      return SearchWebSocketProtocolErrorCode.SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_FORBIDDEN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SearchWebSocketProtocolErrorCode.UNRECOGNIZED;
  }
}

export function searchWebSocketProtocolErrorCodeToJSON(object: SearchWebSocketProtocolErrorCode): string {
  switch (object) {
    case SearchWebSocketProtocolErrorCode.SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_UNSPECIFIED:
      return "SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_UNSPECIFIED";
    case SearchWebSocketProtocolErrorCode.SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_INVALID_COMMAND:
      return "SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_INVALID_COMMAND";
    case SearchWebSocketProtocolErrorCode.SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_UNSUPPORTED_COMMAND:
      return "SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_UNSUPPORTED_COMMAND";
    case SearchWebSocketProtocolErrorCode.SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_INVALID_TARGET:
      return "SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_INVALID_TARGET";
    case SearchWebSocketProtocolErrorCode.SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_JOB_NOT_FOUND:
      return "SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_JOB_NOT_FOUND";
    case SearchWebSocketProtocolErrorCode.SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_TOO_MANY_SUBSCRIPTIONS:
      return "SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_TOO_MANY_SUBSCRIPTIONS";
    case SearchWebSocketProtocolErrorCode.SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_FRAME_TOO_LARGE:
      return "SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_FRAME_TOO_LARGE";
    case SearchWebSocketProtocolErrorCode.SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_FORBIDDEN:
      return "SEARCH_WEB_SOCKET_PROTOCOL_ERROR_CODE_FORBIDDEN";
    case SearchWebSocketProtocolErrorCode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** JobTarget identifies the independent sequence space to subscribe to. */
export interface JobTarget {
  target: { $case: "searchJobId"; value: string } | { $case: "exportJobId"; value: string } | undefined;
}

export interface SearchSubscription {
  subscriptionId: string;
  target:
    | JobTarget
    | undefined;
  /**
   * The server sends events strictly after this per-target sequence. Zero asks
   * for current state followed by live events.
   */
  afterSequence: bigint;
  includePreviews: boolean;
  previewRowLimit?: number | undefined;
}

export interface SubscribeSearchJobsCommand {
  subscriptions: SearchSubscription[];
}

export interface UnsubscribeSearchJobsCommand {
  subscriptionIds: string[];
}

export interface SearchWebSocketPing {
  nonce: string;
}

/**
 * SearchWebSocketCommand is encoded as one binary WebSocket frame. request_id
 * correlates command acknowledgments and protocol errors.
 */
export interface SearchWebSocketCommand {
  requestId: string;
  payload:
    | { $case: "subscribe"; value: SubscribeSearchJobsCommand }
    | { $case: "unsubscribe"; value: UnsubscribeSearchJobsCommand }
    | { $case: "ping"; value: SearchWebSocketPing }
    | undefined;
}

export interface SubscriptionAcknowledged {
  requestId: string;
  subscriptionId: string;
  target: JobTarget | undefined;
  earliestAvailableSequence: bigint;
  latestSequence: bigint;
  replayWillFollow: boolean;
}

export interface SubscriptionRemoved {
  requestId: string;
  subscriptionId: string;
}

export interface SearchJobStateChanged {
  searchJobId: string;
  state: SearchJobState;
  stateVersion: bigint;
}

export interface ResultSchemaAvailable {
  searchJobId: string;
  schema: ResultSchema | undefined;
}

/**
 * ResultPreview is bounded and disposable. Durable/full pages are fetched via
 * GetSearchResults; a client may drop previews under backpressure.
 */
export interface ResultPreview {
  searchJobId: string;
  schemaId: string;
  previewRevision: bigint;
  updateMode: PreviewUpdateMode;
  rows: ResultRow[];
  truncated: boolean;
}

export interface SearchJobTerminal {
  searchJobId: string;
  state: SearchJobState;
  stateVersion: bigint;
  finalProgress: SearchProgress | undefined;
  failure?: SearchFailure | undefined;
  resultsExpireAt: Date | undefined;
}

export interface ExportJobStateChanged {
  exportJobId: string;
  state: ExportJobState;
  stateVersion: bigint;
}

export interface ExportJobTerminal {
  exportJobId: string;
  state: ExportJobState;
  stateVersion: bigint;
  finalProgress: ExportProgress | undefined;
  failure?: ExportFailure | undefined;
  artifact?: ExportArtifact | undefined;
}

export interface SearchWebSocketWarning {
  target: JobTarget | undefined;
  warning: ApiWarning | undefined;
}

export interface ResynchronizationRequired {
  subscriptionId: string;
  target: JobTarget | undefined;
  reason: ResynchronizationReason;
  earliestAvailableSequence: bigint;
  latestSequence: bigint;
  recoveryPath: string;
}

export interface SearchWebSocketPong {
  nonce: string;
  serverTime: Date | undefined;
}

export interface SearchWebSocketProtocolError {
  requestId: string;
  code: SearchWebSocketProtocolErrorCode;
  message: string;
  violations: FieldViolation[];
  connectionWillClose: boolean;
}

/**
 * SearchWebSocketEvent is one server-to-browser binary frame. sequence is
 * monotonically increasing per JobTarget. Control events not tied to a target
 * use sequence zero.
 */
export interface SearchWebSocketEvent {
  sequence: bigint;
  occurredAt: Date | undefined;
  subscriptionId?: string | undefined;
  target?: JobTarget | undefined;
  payload:
    | { $case: "subscriptionAcknowledged"; value: SubscriptionAcknowledged }
    | { $case: "subscriptionRemoved"; value: SubscriptionRemoved }
    | { $case: "searchStateChanged"; value: SearchJobStateChanged }
    | { $case: "searchProgress"; value: SearchProgress }
    | { $case: "resultSchemaAvailable"; value: ResultSchemaAvailable }
    | { $case: "resultPreview"; value: ResultPreview }
    | { $case: "searchTerminal"; value: SearchJobTerminal }
    | { $case: "exportStateChanged"; value: ExportJobStateChanged }
    | { $case: "exportProgress"; value: ExportProgress }
    | { $case: "exportTerminal"; value: ExportJobTerminal }
    | { $case: "warning"; value: SearchWebSocketWarning }
    | { $case: "resynchronizationRequired"; value: ResynchronizationRequired }
    | { $case: "pong"; value: SearchWebSocketPong }
    | { $case: "protocolError"; value: SearchWebSocketProtocolError }
    | undefined;
}

function createBaseJobTarget(): JobTarget {
  return { target: undefined };
}

export const JobTarget: MessageFns<JobTarget> = {
  encode(message: JobTarget, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.target?.$case) {
      case "searchJobId":
        writer.uint32(10).string(message.target.value);
        break;
      case "exportJobId":
        writer.uint32(18).string(message.target.value);
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobTarget {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobTarget();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.target = { $case: "searchJobId", value: reader.string() };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.target = { $case: "exportJobId", value: reader.string() };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobTarget {
    return {
      target: isSet(object.searchJobId)
        ? { $case: "searchJobId", value: globalThis.String(object.searchJobId) }
        : isSet(object.search_job_id)
        ? { $case: "searchJobId", value: globalThis.String(object.search_job_id) }
        : isSet(object.exportJobId)
        ? { $case: "exportJobId", value: globalThis.String(object.exportJobId) }
        : isSet(object.export_job_id)
        ? { $case: "exportJobId", value: globalThis.String(object.export_job_id) }
        : undefined,
    };
  },

  toJSON(message: JobTarget): unknown {
    const obj: any = {};
    if (message.target?.$case === "searchJobId") {
      obj.searchJobId = message.target.value;
    } else if (message.target?.$case === "exportJobId") {
      obj.exportJobId = message.target.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JobTarget>, I>>(base?: I): JobTarget {
    return JobTarget.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JobTarget>, I>>(object: I): JobTarget {
    const message = createBaseJobTarget();
    switch (object.target?.$case) {
      case "searchJobId": {
        if (object.target?.value !== undefined && object.target?.value !== null) {
          message.target = { $case: "searchJobId", value: object.target.value };
        }
        break;
      }
      case "exportJobId": {
        if (object.target?.value !== undefined && object.target?.value !== null) {
          message.target = { $case: "exportJobId", value: object.target.value };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseSearchSubscription(): SearchSubscription {
  return {
    subscriptionId: "",
    target: undefined,
    afterSequence: 0n,
    includePreviews: false,
    previewRowLimit: undefined,
  };
}

export const SearchSubscription: MessageFns<SearchSubscription> = {
  encode(message: SearchSubscription, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.subscriptionId !== "") {
      writer.uint32(10).string(message.subscriptionId);
    }
    if (message.target !== undefined) {
      JobTarget.encode(message.target, writer.uint32(18).fork()).join();
    }
    if (message.afterSequence !== 0n) {
      if (BigInt.asUintN(64, message.afterSequence) !== message.afterSequence) {
        throw new globalThis.Error("value provided for field message.afterSequence of type uint64 too large");
      }
      writer.uint32(24).uint64(message.afterSequence);
    }
    if (message.includePreviews !== false) {
      writer.uint32(32).bool(message.includePreviews);
    }
    if (message.previewRowLimit !== undefined) {
      writer.uint32(40).uint32(message.previewRowLimit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchSubscription {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchSubscription();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.subscriptionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.target = JobTarget.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.afterSequence = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.includePreviews = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.previewRowLimit = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchSubscription {
    return {
      subscriptionId: isSet(object.subscriptionId)
        ? globalThis.String(object.subscriptionId)
        : isSet(object.subscription_id)
        ? globalThis.String(object.subscription_id)
        : "",
      target: isSet(object.target) ? JobTarget.fromJSON(object.target) : undefined,
      afterSequence: isSet(object.afterSequence)
        ? BigInt(object.afterSequence)
        : isSet(object.after_sequence)
        ? BigInt(object.after_sequence)
        : 0n,
      includePreviews: isSet(object.includePreviews)
        ? globalThis.Boolean(object.includePreviews)
        : isSet(object.include_previews)
        ? globalThis.Boolean(object.include_previews)
        : false,
      previewRowLimit: isSet(object.previewRowLimit)
        ? globalThis.Number(object.previewRowLimit)
        : isSet(object.preview_row_limit)
        ? globalThis.Number(object.preview_row_limit)
        : undefined,
    };
  },

  toJSON(message: SearchSubscription): unknown {
    const obj: any = {};
    if (message.subscriptionId !== "") {
      obj.subscriptionId = message.subscriptionId;
    }
    if (message.target !== undefined) {
      obj.target = JobTarget.toJSON(message.target);
    }
    if (message.afterSequence !== 0n) {
      obj.afterSequence = message.afterSequence.toString();
    }
    if (message.includePreviews !== false) {
      obj.includePreviews = message.includePreviews;
    }
    if (message.previewRowLimit !== undefined) {
      obj.previewRowLimit = Math.round(message.previewRowLimit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchSubscription>, I>>(base?: I): SearchSubscription {
    return SearchSubscription.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchSubscription>, I>>(object: I): SearchSubscription {
    const message = createBaseSearchSubscription();
    message.subscriptionId = object.subscriptionId ?? "";
    message.target = (object.target !== undefined && object.target !== null)
      ? JobTarget.fromPartial(object.target)
      : undefined;
    message.afterSequence = (object.afterSequence !== undefined && object.afterSequence !== null)
      ? BigInt(object.afterSequence)
      : 0n;
    message.includePreviews = object.includePreviews ?? false;
    message.previewRowLimit = object.previewRowLimit ?? undefined;
    return message;
  },
};

function createBaseSubscribeSearchJobsCommand(): SubscribeSearchJobsCommand {
  return { subscriptions: [] };
}

export const SubscribeSearchJobsCommand: MessageFns<SubscribeSearchJobsCommand> = {
  encode(message: SubscribeSearchJobsCommand, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.subscriptions) {
      SearchSubscription.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubscribeSearchJobsCommand {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubscribeSearchJobsCommand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.subscriptions.push(SearchSubscription.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubscribeSearchJobsCommand {
    return {
      subscriptions: globalThis.Array.isArray(object?.subscriptions)
        ? object.subscriptions.map((e: any) => SearchSubscription.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SubscribeSearchJobsCommand): unknown {
    const obj: any = {};
    if (message.subscriptions?.length) {
      obj.subscriptions = message.subscriptions.map((e) => SearchSubscription.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubscribeSearchJobsCommand>, I>>(base?: I): SubscribeSearchJobsCommand {
    return SubscribeSearchJobsCommand.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubscribeSearchJobsCommand>, I>>(object: I): SubscribeSearchJobsCommand {
    const message = createBaseSubscribeSearchJobsCommand();
    message.subscriptions = object.subscriptions?.map((e) => SearchSubscription.fromPartial(e)) || [];
    return message;
  },
};

function createBaseUnsubscribeSearchJobsCommand(): UnsubscribeSearchJobsCommand {
  return { subscriptionIds: [] };
}

export const UnsubscribeSearchJobsCommand: MessageFns<UnsubscribeSearchJobsCommand> = {
  encode(message: UnsubscribeSearchJobsCommand, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.subscriptionIds) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UnsubscribeSearchJobsCommand {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnsubscribeSearchJobsCommand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.subscriptionIds.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnsubscribeSearchJobsCommand {
    return {
      subscriptionIds: globalThis.Array.isArray(object?.subscriptionIds)
        ? object.subscriptionIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.subscription_ids)
        ? object.subscription_ids.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: UnsubscribeSearchJobsCommand): unknown {
    const obj: any = {};
    if (message.subscriptionIds?.length) {
      obj.subscriptionIds = message.subscriptionIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnsubscribeSearchJobsCommand>, I>>(base?: I): UnsubscribeSearchJobsCommand {
    return UnsubscribeSearchJobsCommand.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnsubscribeSearchJobsCommand>, I>>(object: I): UnsubscribeSearchJobsCommand {
    const message = createBaseUnsubscribeSearchJobsCommand();
    message.subscriptionIds = object.subscriptionIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseSearchWebSocketPing(): SearchWebSocketPing {
  return { nonce: "" };
}

export const SearchWebSocketPing: MessageFns<SearchWebSocketPing> = {
  encode(message: SearchWebSocketPing, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nonce !== "") {
      writer.uint32(10).string(message.nonce);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchWebSocketPing {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchWebSocketPing();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nonce = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchWebSocketPing {
    return { nonce: isSet(object.nonce) ? globalThis.String(object.nonce) : "" };
  },

  toJSON(message: SearchWebSocketPing): unknown {
    const obj: any = {};
    if (message.nonce !== "") {
      obj.nonce = message.nonce;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchWebSocketPing>, I>>(base?: I): SearchWebSocketPing {
    return SearchWebSocketPing.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchWebSocketPing>, I>>(object: I): SearchWebSocketPing {
    const message = createBaseSearchWebSocketPing();
    message.nonce = object.nonce ?? "";
    return message;
  },
};

function createBaseSearchWebSocketCommand(): SearchWebSocketCommand {
  return { requestId: "", payload: undefined };
}

export const SearchWebSocketCommand: MessageFns<SearchWebSocketCommand> = {
  encode(message: SearchWebSocketCommand, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    switch (message.payload?.$case) {
      case "subscribe":
        SubscribeSearchJobsCommand.encode(message.payload.value, writer.uint32(82).fork()).join();
        break;
      case "unsubscribe":
        UnsubscribeSearchJobsCommand.encode(message.payload.value, writer.uint32(90).fork()).join();
        break;
      case "ping":
        SearchWebSocketPing.encode(message.payload.value, writer.uint32(98).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchWebSocketCommand {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchWebSocketCommand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.payload = { $case: "subscribe", value: SubscribeSearchJobsCommand.decode(reader, reader.uint32()) };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.payload = {
            $case: "unsubscribe",
            value: UnsubscribeSearchJobsCommand.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.payload = { $case: "ping", value: SearchWebSocketPing.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchWebSocketCommand {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      payload: isSet(object.subscribe)
        ? { $case: "subscribe", value: SubscribeSearchJobsCommand.fromJSON(object.subscribe) }
        : isSet(object.unsubscribe)
        ? { $case: "unsubscribe", value: UnsubscribeSearchJobsCommand.fromJSON(object.unsubscribe) }
        : isSet(object.ping)
        ? { $case: "ping", value: SearchWebSocketPing.fromJSON(object.ping) }
        : undefined,
    };
  },

  toJSON(message: SearchWebSocketCommand): unknown {
    const obj: any = {};
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.payload?.$case === "subscribe") {
      obj.subscribe = SubscribeSearchJobsCommand.toJSON(message.payload.value);
    } else if (message.payload?.$case === "unsubscribe") {
      obj.unsubscribe = UnsubscribeSearchJobsCommand.toJSON(message.payload.value);
    } else if (message.payload?.$case === "ping") {
      obj.ping = SearchWebSocketPing.toJSON(message.payload.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchWebSocketCommand>, I>>(base?: I): SearchWebSocketCommand {
    return SearchWebSocketCommand.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchWebSocketCommand>, I>>(object: I): SearchWebSocketCommand {
    const message = createBaseSearchWebSocketCommand();
    message.requestId = object.requestId ?? "";
    switch (object.payload?.$case) {
      case "subscribe": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = { $case: "subscribe", value: SubscribeSearchJobsCommand.fromPartial(object.payload.value) };
        }
        break;
      }
      case "unsubscribe": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = {
            $case: "unsubscribe",
            value: UnsubscribeSearchJobsCommand.fromPartial(object.payload.value),
          };
        }
        break;
      }
      case "ping": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = { $case: "ping", value: SearchWebSocketPing.fromPartial(object.payload.value) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseSubscriptionAcknowledged(): SubscriptionAcknowledged {
  return {
    requestId: "",
    subscriptionId: "",
    target: undefined,
    earliestAvailableSequence: 0n,
    latestSequence: 0n,
    replayWillFollow: false,
  };
}

export const SubscriptionAcknowledged: MessageFns<SubscriptionAcknowledged> = {
  encode(message: SubscriptionAcknowledged, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.subscriptionId !== "") {
      writer.uint32(18).string(message.subscriptionId);
    }
    if (message.target !== undefined) {
      JobTarget.encode(message.target, writer.uint32(26).fork()).join();
    }
    if (message.earliestAvailableSequence !== 0n) {
      if (BigInt.asUintN(64, message.earliestAvailableSequence) !== message.earliestAvailableSequence) {
        throw new globalThis.Error(
          "value provided for field message.earliestAvailableSequence of type uint64 too large",
        );
      }
      writer.uint32(32).uint64(message.earliestAvailableSequence);
    }
    if (message.latestSequence !== 0n) {
      if (BigInt.asUintN(64, message.latestSequence) !== message.latestSequence) {
        throw new globalThis.Error("value provided for field message.latestSequence of type uint64 too large");
      }
      writer.uint32(40).uint64(message.latestSequence);
    }
    if (message.replayWillFollow !== false) {
      writer.uint32(48).bool(message.replayWillFollow);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubscriptionAcknowledged {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubscriptionAcknowledged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.subscriptionId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.target = JobTarget.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.earliestAvailableSequence = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.latestSequence = reader.uint64() as bigint;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.replayWillFollow = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubscriptionAcknowledged {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      subscriptionId: isSet(object.subscriptionId)
        ? globalThis.String(object.subscriptionId)
        : isSet(object.subscription_id)
        ? globalThis.String(object.subscription_id)
        : "",
      target: isSet(object.target) ? JobTarget.fromJSON(object.target) : undefined,
      earliestAvailableSequence: isSet(object.earliestAvailableSequence)
        ? BigInt(object.earliestAvailableSequence)
        : isSet(object.earliest_available_sequence)
        ? BigInt(object.earliest_available_sequence)
        : 0n,
      latestSequence: isSet(object.latestSequence)
        ? BigInt(object.latestSequence)
        : isSet(object.latest_sequence)
        ? BigInt(object.latest_sequence)
        : 0n,
      replayWillFollow: isSet(object.replayWillFollow)
        ? globalThis.Boolean(object.replayWillFollow)
        : isSet(object.replay_will_follow)
        ? globalThis.Boolean(object.replay_will_follow)
        : false,
    };
  },

  toJSON(message: SubscriptionAcknowledged): unknown {
    const obj: any = {};
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.subscriptionId !== "") {
      obj.subscriptionId = message.subscriptionId;
    }
    if (message.target !== undefined) {
      obj.target = JobTarget.toJSON(message.target);
    }
    if (message.earliestAvailableSequence !== 0n) {
      obj.earliestAvailableSequence = message.earliestAvailableSequence.toString();
    }
    if (message.latestSequence !== 0n) {
      obj.latestSequence = message.latestSequence.toString();
    }
    if (message.replayWillFollow !== false) {
      obj.replayWillFollow = message.replayWillFollow;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubscriptionAcknowledged>, I>>(base?: I): SubscriptionAcknowledged {
    return SubscriptionAcknowledged.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubscriptionAcknowledged>, I>>(object: I): SubscriptionAcknowledged {
    const message = createBaseSubscriptionAcknowledged();
    message.requestId = object.requestId ?? "";
    message.subscriptionId = object.subscriptionId ?? "";
    message.target = (object.target !== undefined && object.target !== null)
      ? JobTarget.fromPartial(object.target)
      : undefined;
    message.earliestAvailableSequence =
      (object.earliestAvailableSequence !== undefined && object.earliestAvailableSequence !== null)
        ? BigInt(object.earliestAvailableSequence)
        : 0n;
    message.latestSequence = (object.latestSequence !== undefined && object.latestSequence !== null)
      ? BigInt(object.latestSequence)
      : 0n;
    message.replayWillFollow = object.replayWillFollow ?? false;
    return message;
  },
};

function createBaseSubscriptionRemoved(): SubscriptionRemoved {
  return { requestId: "", subscriptionId: "" };
}

export const SubscriptionRemoved: MessageFns<SubscriptionRemoved> = {
  encode(message: SubscriptionRemoved, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.subscriptionId !== "") {
      writer.uint32(18).string(message.subscriptionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubscriptionRemoved {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubscriptionRemoved();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.subscriptionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubscriptionRemoved {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      subscriptionId: isSet(object.subscriptionId)
        ? globalThis.String(object.subscriptionId)
        : isSet(object.subscription_id)
        ? globalThis.String(object.subscription_id)
        : "",
    };
  },

  toJSON(message: SubscriptionRemoved): unknown {
    const obj: any = {};
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.subscriptionId !== "") {
      obj.subscriptionId = message.subscriptionId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubscriptionRemoved>, I>>(base?: I): SubscriptionRemoved {
    return SubscriptionRemoved.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubscriptionRemoved>, I>>(object: I): SubscriptionRemoved {
    const message = createBaseSubscriptionRemoved();
    message.requestId = object.requestId ?? "";
    message.subscriptionId = object.subscriptionId ?? "";
    return message;
  },
};

function createBaseSearchJobStateChanged(): SearchJobStateChanged {
  return { searchJobId: "", state: 0, stateVersion: 0n };
}

export const SearchJobStateChanged: MessageFns<SearchJobStateChanged> = {
  encode(message: SearchJobStateChanged, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.searchJobId !== "") {
      writer.uint32(10).string(message.searchJobId);
    }
    if (message.state !== 0) {
      writer.uint32(16).int32(message.state);
    }
    if (message.stateVersion !== 0n) {
      if (BigInt.asUintN(64, message.stateVersion) !== message.stateVersion) {
        throw new globalThis.Error("value provided for field message.stateVersion of type uint64 too large");
      }
      writer.uint32(24).uint64(message.stateVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchJobStateChanged {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchJobStateChanged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.searchJobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.stateVersion = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchJobStateChanged {
    return {
      searchJobId: isSet(object.searchJobId)
        ? globalThis.String(object.searchJobId)
        : isSet(object.search_job_id)
        ? globalThis.String(object.search_job_id)
        : "",
      state: isSet(object.state) ? searchJobStateFromJSON(object.state) : 0,
      stateVersion: isSet(object.stateVersion)
        ? BigInt(object.stateVersion)
        : isSet(object.state_version)
        ? BigInt(object.state_version)
        : 0n,
    };
  },

  toJSON(message: SearchJobStateChanged): unknown {
    const obj: any = {};
    if (message.searchJobId !== "") {
      obj.searchJobId = message.searchJobId;
    }
    if (message.state !== 0) {
      obj.state = searchJobStateToJSON(message.state);
    }
    if (message.stateVersion !== 0n) {
      obj.stateVersion = message.stateVersion.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchJobStateChanged>, I>>(base?: I): SearchJobStateChanged {
    return SearchJobStateChanged.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchJobStateChanged>, I>>(object: I): SearchJobStateChanged {
    const message = createBaseSearchJobStateChanged();
    message.searchJobId = object.searchJobId ?? "";
    message.state = object.state ?? 0;
    message.stateVersion = (object.stateVersion !== undefined && object.stateVersion !== null)
      ? BigInt(object.stateVersion)
      : 0n;
    return message;
  },
};

function createBaseResultSchemaAvailable(): ResultSchemaAvailable {
  return { searchJobId: "", schema: undefined };
}

export const ResultSchemaAvailable: MessageFns<ResultSchemaAvailable> = {
  encode(message: ResultSchemaAvailable, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.searchJobId !== "") {
      writer.uint32(10).string(message.searchJobId);
    }
    if (message.schema !== undefined) {
      ResultSchema.encode(message.schema, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResultSchemaAvailable {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResultSchemaAvailable();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.searchJobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.schema = ResultSchema.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResultSchemaAvailable {
    return {
      searchJobId: isSet(object.searchJobId)
        ? globalThis.String(object.searchJobId)
        : isSet(object.search_job_id)
        ? globalThis.String(object.search_job_id)
        : "",
      schema: isSet(object.schema) ? ResultSchema.fromJSON(object.schema) : undefined,
    };
  },

  toJSON(message: ResultSchemaAvailable): unknown {
    const obj: any = {};
    if (message.searchJobId !== "") {
      obj.searchJobId = message.searchJobId;
    }
    if (message.schema !== undefined) {
      obj.schema = ResultSchema.toJSON(message.schema);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResultSchemaAvailable>, I>>(base?: I): ResultSchemaAvailable {
    return ResultSchemaAvailable.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResultSchemaAvailable>, I>>(object: I): ResultSchemaAvailable {
    const message = createBaseResultSchemaAvailable();
    message.searchJobId = object.searchJobId ?? "";
    message.schema = (object.schema !== undefined && object.schema !== null)
      ? ResultSchema.fromPartial(object.schema)
      : undefined;
    return message;
  },
};

function createBaseResultPreview(): ResultPreview {
  return { searchJobId: "", schemaId: "", previewRevision: 0n, updateMode: 0, rows: [], truncated: false };
}

export const ResultPreview: MessageFns<ResultPreview> = {
  encode(message: ResultPreview, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.searchJobId !== "") {
      writer.uint32(10).string(message.searchJobId);
    }
    if (message.schemaId !== "") {
      writer.uint32(18).string(message.schemaId);
    }
    if (message.previewRevision !== 0n) {
      if (BigInt.asUintN(64, message.previewRevision) !== message.previewRevision) {
        throw new globalThis.Error("value provided for field message.previewRevision of type uint64 too large");
      }
      writer.uint32(24).uint64(message.previewRevision);
    }
    if (message.updateMode !== 0) {
      writer.uint32(32).int32(message.updateMode);
    }
    for (const v of message.rows) {
      ResultRow.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.truncated !== false) {
      writer.uint32(48).bool(message.truncated);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResultPreview {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResultPreview();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.searchJobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.schemaId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.previewRevision = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.updateMode = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.rows.push(ResultRow.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.truncated = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResultPreview {
    return {
      searchJobId: isSet(object.searchJobId)
        ? globalThis.String(object.searchJobId)
        : isSet(object.search_job_id)
        ? globalThis.String(object.search_job_id)
        : "",
      schemaId: isSet(object.schemaId)
        ? globalThis.String(object.schemaId)
        : isSet(object.schema_id)
        ? globalThis.String(object.schema_id)
        : "",
      previewRevision: isSet(object.previewRevision)
        ? BigInt(object.previewRevision)
        : isSet(object.preview_revision)
        ? BigInt(object.preview_revision)
        : 0n,
      updateMode: isSet(object.updateMode)
        ? previewUpdateModeFromJSON(object.updateMode)
        : isSet(object.update_mode)
        ? previewUpdateModeFromJSON(object.update_mode)
        : 0,
      rows: globalThis.Array.isArray(object?.rows)
        ? object.rows.map((e: any) => ResultRow.fromJSON(e))
        : [],
      truncated: isSet(object.truncated) ? globalThis.Boolean(object.truncated) : false,
    };
  },

  toJSON(message: ResultPreview): unknown {
    const obj: any = {};
    if (message.searchJobId !== "") {
      obj.searchJobId = message.searchJobId;
    }
    if (message.schemaId !== "") {
      obj.schemaId = message.schemaId;
    }
    if (message.previewRevision !== 0n) {
      obj.previewRevision = message.previewRevision.toString();
    }
    if (message.updateMode !== 0) {
      obj.updateMode = previewUpdateModeToJSON(message.updateMode);
    }
    if (message.rows?.length) {
      obj.rows = message.rows.map((e) => ResultRow.toJSON(e));
    }
    if (message.truncated !== false) {
      obj.truncated = message.truncated;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResultPreview>, I>>(base?: I): ResultPreview {
    return ResultPreview.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResultPreview>, I>>(object: I): ResultPreview {
    const message = createBaseResultPreview();
    message.searchJobId = object.searchJobId ?? "";
    message.schemaId = object.schemaId ?? "";
    message.previewRevision = (object.previewRevision !== undefined && object.previewRevision !== null)
      ? BigInt(object.previewRevision)
      : 0n;
    message.updateMode = object.updateMode ?? 0;
    message.rows = object.rows?.map((e) => ResultRow.fromPartial(e)) || [];
    message.truncated = object.truncated ?? false;
    return message;
  },
};

function createBaseSearchJobTerminal(): SearchJobTerminal {
  return {
    searchJobId: "",
    state: 0,
    stateVersion: 0n,
    finalProgress: undefined,
    failure: undefined,
    resultsExpireAt: undefined,
  };
}

export const SearchJobTerminal: MessageFns<SearchJobTerminal> = {
  encode(message: SearchJobTerminal, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.searchJobId !== "") {
      writer.uint32(10).string(message.searchJobId);
    }
    if (message.state !== 0) {
      writer.uint32(16).int32(message.state);
    }
    if (message.stateVersion !== 0n) {
      if (BigInt.asUintN(64, message.stateVersion) !== message.stateVersion) {
        throw new globalThis.Error("value provided for field message.stateVersion of type uint64 too large");
      }
      writer.uint32(24).uint64(message.stateVersion);
    }
    if (message.finalProgress !== undefined) {
      SearchProgress.encode(message.finalProgress, writer.uint32(34).fork()).join();
    }
    if (message.failure !== undefined) {
      SearchFailure.encode(message.failure, writer.uint32(42).fork()).join();
    }
    if (message.resultsExpireAt !== undefined) {
      Timestamp.encode(toTimestamp(message.resultsExpireAt), writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchJobTerminal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchJobTerminal();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.searchJobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.stateVersion = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.finalProgress = SearchProgress.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.failure = SearchFailure.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.resultsExpireAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchJobTerminal {
    return {
      searchJobId: isSet(object.searchJobId)
        ? globalThis.String(object.searchJobId)
        : isSet(object.search_job_id)
        ? globalThis.String(object.search_job_id)
        : "",
      state: isSet(object.state) ? searchJobStateFromJSON(object.state) : 0,
      stateVersion: isSet(object.stateVersion)
        ? BigInt(object.stateVersion)
        : isSet(object.state_version)
        ? BigInt(object.state_version)
        : 0n,
      finalProgress: isSet(object.finalProgress)
        ? SearchProgress.fromJSON(object.finalProgress)
        : isSet(object.final_progress)
        ? SearchProgress.fromJSON(object.final_progress)
        : undefined,
      failure: isSet(object.failure) ? SearchFailure.fromJSON(object.failure) : undefined,
      resultsExpireAt: isSet(object.resultsExpireAt)
        ? fromJsonTimestamp(object.resultsExpireAt)
        : isSet(object.results_expire_at)
        ? fromJsonTimestamp(object.results_expire_at)
        : undefined,
    };
  },

  toJSON(message: SearchJobTerminal): unknown {
    const obj: any = {};
    if (message.searchJobId !== "") {
      obj.searchJobId = message.searchJobId;
    }
    if (message.state !== 0) {
      obj.state = searchJobStateToJSON(message.state);
    }
    if (message.stateVersion !== 0n) {
      obj.stateVersion = message.stateVersion.toString();
    }
    if (message.finalProgress !== undefined) {
      obj.finalProgress = SearchProgress.toJSON(message.finalProgress);
    }
    if (message.failure !== undefined) {
      obj.failure = SearchFailure.toJSON(message.failure);
    }
    if (message.resultsExpireAt !== undefined) {
      obj.resultsExpireAt = message.resultsExpireAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchJobTerminal>, I>>(base?: I): SearchJobTerminal {
    return SearchJobTerminal.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchJobTerminal>, I>>(object: I): SearchJobTerminal {
    const message = createBaseSearchJobTerminal();
    message.searchJobId = object.searchJobId ?? "";
    message.state = object.state ?? 0;
    message.stateVersion = (object.stateVersion !== undefined && object.stateVersion !== null)
      ? BigInt(object.stateVersion)
      : 0n;
    message.finalProgress = (object.finalProgress !== undefined && object.finalProgress !== null)
      ? SearchProgress.fromPartial(object.finalProgress)
      : undefined;
    message.failure = (object.failure !== undefined && object.failure !== null)
      ? SearchFailure.fromPartial(object.failure)
      : undefined;
    message.resultsExpireAt = object.resultsExpireAt ?? undefined;
    return message;
  },
};

function createBaseExportJobStateChanged(): ExportJobStateChanged {
  return { exportJobId: "", state: 0, stateVersion: 0n };
}

export const ExportJobStateChanged: MessageFns<ExportJobStateChanged> = {
  encode(message: ExportJobStateChanged, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exportJobId !== "") {
      writer.uint32(10).string(message.exportJobId);
    }
    if (message.state !== 0) {
      writer.uint32(16).int32(message.state);
    }
    if (message.stateVersion !== 0n) {
      if (BigInt.asUintN(64, message.stateVersion) !== message.stateVersion) {
        throw new globalThis.Error("value provided for field message.stateVersion of type uint64 too large");
      }
      writer.uint32(24).uint64(message.stateVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExportJobStateChanged {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExportJobStateChanged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.exportJobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.stateVersion = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExportJobStateChanged {
    return {
      exportJobId: isSet(object.exportJobId)
        ? globalThis.String(object.exportJobId)
        : isSet(object.export_job_id)
        ? globalThis.String(object.export_job_id)
        : "",
      state: isSet(object.state) ? exportJobStateFromJSON(object.state) : 0,
      stateVersion: isSet(object.stateVersion)
        ? BigInt(object.stateVersion)
        : isSet(object.state_version)
        ? BigInt(object.state_version)
        : 0n,
    };
  },

  toJSON(message: ExportJobStateChanged): unknown {
    const obj: any = {};
    if (message.exportJobId !== "") {
      obj.exportJobId = message.exportJobId;
    }
    if (message.state !== 0) {
      obj.state = exportJobStateToJSON(message.state);
    }
    if (message.stateVersion !== 0n) {
      obj.stateVersion = message.stateVersion.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExportJobStateChanged>, I>>(base?: I): ExportJobStateChanged {
    return ExportJobStateChanged.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExportJobStateChanged>, I>>(object: I): ExportJobStateChanged {
    const message = createBaseExportJobStateChanged();
    message.exportJobId = object.exportJobId ?? "";
    message.state = object.state ?? 0;
    message.stateVersion = (object.stateVersion !== undefined && object.stateVersion !== null)
      ? BigInt(object.stateVersion)
      : 0n;
    return message;
  },
};

function createBaseExportJobTerminal(): ExportJobTerminal {
  return {
    exportJobId: "",
    state: 0,
    stateVersion: 0n,
    finalProgress: undefined,
    failure: undefined,
    artifact: undefined,
  };
}

export const ExportJobTerminal: MessageFns<ExportJobTerminal> = {
  encode(message: ExportJobTerminal, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exportJobId !== "") {
      writer.uint32(10).string(message.exportJobId);
    }
    if (message.state !== 0) {
      writer.uint32(16).int32(message.state);
    }
    if (message.stateVersion !== 0n) {
      if (BigInt.asUintN(64, message.stateVersion) !== message.stateVersion) {
        throw new globalThis.Error("value provided for field message.stateVersion of type uint64 too large");
      }
      writer.uint32(24).uint64(message.stateVersion);
    }
    if (message.finalProgress !== undefined) {
      ExportProgress.encode(message.finalProgress, writer.uint32(34).fork()).join();
    }
    if (message.failure !== undefined) {
      ExportFailure.encode(message.failure, writer.uint32(42).fork()).join();
    }
    if (message.artifact !== undefined) {
      ExportArtifact.encode(message.artifact, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExportJobTerminal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExportJobTerminal();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.exportJobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.stateVersion = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.finalProgress = ExportProgress.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.failure = ExportFailure.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.artifact = ExportArtifact.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExportJobTerminal {
    return {
      exportJobId: isSet(object.exportJobId)
        ? globalThis.String(object.exportJobId)
        : isSet(object.export_job_id)
        ? globalThis.String(object.export_job_id)
        : "",
      state: isSet(object.state) ? exportJobStateFromJSON(object.state) : 0,
      stateVersion: isSet(object.stateVersion)
        ? BigInt(object.stateVersion)
        : isSet(object.state_version)
        ? BigInt(object.state_version)
        : 0n,
      finalProgress: isSet(object.finalProgress)
        ? ExportProgress.fromJSON(object.finalProgress)
        : isSet(object.final_progress)
        ? ExportProgress.fromJSON(object.final_progress)
        : undefined,
      failure: isSet(object.failure) ? ExportFailure.fromJSON(object.failure) : undefined,
      artifact: isSet(object.artifact) ? ExportArtifact.fromJSON(object.artifact) : undefined,
    };
  },

  toJSON(message: ExportJobTerminal): unknown {
    const obj: any = {};
    if (message.exportJobId !== "") {
      obj.exportJobId = message.exportJobId;
    }
    if (message.state !== 0) {
      obj.state = exportJobStateToJSON(message.state);
    }
    if (message.stateVersion !== 0n) {
      obj.stateVersion = message.stateVersion.toString();
    }
    if (message.finalProgress !== undefined) {
      obj.finalProgress = ExportProgress.toJSON(message.finalProgress);
    }
    if (message.failure !== undefined) {
      obj.failure = ExportFailure.toJSON(message.failure);
    }
    if (message.artifact !== undefined) {
      obj.artifact = ExportArtifact.toJSON(message.artifact);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExportJobTerminal>, I>>(base?: I): ExportJobTerminal {
    return ExportJobTerminal.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExportJobTerminal>, I>>(object: I): ExportJobTerminal {
    const message = createBaseExportJobTerminal();
    message.exportJobId = object.exportJobId ?? "";
    message.state = object.state ?? 0;
    message.stateVersion = (object.stateVersion !== undefined && object.stateVersion !== null)
      ? BigInt(object.stateVersion)
      : 0n;
    message.finalProgress = (object.finalProgress !== undefined && object.finalProgress !== null)
      ? ExportProgress.fromPartial(object.finalProgress)
      : undefined;
    message.failure = (object.failure !== undefined && object.failure !== null)
      ? ExportFailure.fromPartial(object.failure)
      : undefined;
    message.artifact = (object.artifact !== undefined && object.artifact !== null)
      ? ExportArtifact.fromPartial(object.artifact)
      : undefined;
    return message;
  },
};

function createBaseSearchWebSocketWarning(): SearchWebSocketWarning {
  return { target: undefined, warning: undefined };
}

export const SearchWebSocketWarning: MessageFns<SearchWebSocketWarning> = {
  encode(message: SearchWebSocketWarning, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.target !== undefined) {
      JobTarget.encode(message.target, writer.uint32(10).fork()).join();
    }
    if (message.warning !== undefined) {
      ApiWarning.encode(message.warning, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchWebSocketWarning {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchWebSocketWarning();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.target = JobTarget.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.warning = ApiWarning.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchWebSocketWarning {
    return {
      target: isSet(object.target) ? JobTarget.fromJSON(object.target) : undefined,
      warning: isSet(object.warning) ? ApiWarning.fromJSON(object.warning) : undefined,
    };
  },

  toJSON(message: SearchWebSocketWarning): unknown {
    const obj: any = {};
    if (message.target !== undefined) {
      obj.target = JobTarget.toJSON(message.target);
    }
    if (message.warning !== undefined) {
      obj.warning = ApiWarning.toJSON(message.warning);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchWebSocketWarning>, I>>(base?: I): SearchWebSocketWarning {
    return SearchWebSocketWarning.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchWebSocketWarning>, I>>(object: I): SearchWebSocketWarning {
    const message = createBaseSearchWebSocketWarning();
    message.target = (object.target !== undefined && object.target !== null)
      ? JobTarget.fromPartial(object.target)
      : undefined;
    message.warning = (object.warning !== undefined && object.warning !== null)
      ? ApiWarning.fromPartial(object.warning)
      : undefined;
    return message;
  },
};

function createBaseResynchronizationRequired(): ResynchronizationRequired {
  return {
    subscriptionId: "",
    target: undefined,
    reason: 0,
    earliestAvailableSequence: 0n,
    latestSequence: 0n,
    recoveryPath: "",
  };
}

export const ResynchronizationRequired: MessageFns<ResynchronizationRequired> = {
  encode(message: ResynchronizationRequired, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.subscriptionId !== "") {
      writer.uint32(10).string(message.subscriptionId);
    }
    if (message.target !== undefined) {
      JobTarget.encode(message.target, writer.uint32(18).fork()).join();
    }
    if (message.reason !== 0) {
      writer.uint32(24).int32(message.reason);
    }
    if (message.earliestAvailableSequence !== 0n) {
      if (BigInt.asUintN(64, message.earliestAvailableSequence) !== message.earliestAvailableSequence) {
        throw new globalThis.Error(
          "value provided for field message.earliestAvailableSequence of type uint64 too large",
        );
      }
      writer.uint32(32).uint64(message.earliestAvailableSequence);
    }
    if (message.latestSequence !== 0n) {
      if (BigInt.asUintN(64, message.latestSequence) !== message.latestSequence) {
        throw new globalThis.Error("value provided for field message.latestSequence of type uint64 too large");
      }
      writer.uint32(40).uint64(message.latestSequence);
    }
    if (message.recoveryPath !== "") {
      writer.uint32(50).string(message.recoveryPath);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResynchronizationRequired {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResynchronizationRequired();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.subscriptionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.target = JobTarget.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.earliestAvailableSequence = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.latestSequence = reader.uint64() as bigint;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.recoveryPath = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResynchronizationRequired {
    return {
      subscriptionId: isSet(object.subscriptionId)
        ? globalThis.String(object.subscriptionId)
        : isSet(object.subscription_id)
        ? globalThis.String(object.subscription_id)
        : "",
      target: isSet(object.target) ? JobTarget.fromJSON(object.target) : undefined,
      reason: isSet(object.reason) ? resynchronizationReasonFromJSON(object.reason) : 0,
      earliestAvailableSequence: isSet(object.earliestAvailableSequence)
        ? BigInt(object.earliestAvailableSequence)
        : isSet(object.earliest_available_sequence)
        ? BigInt(object.earliest_available_sequence)
        : 0n,
      latestSequence: isSet(object.latestSequence)
        ? BigInt(object.latestSequence)
        : isSet(object.latest_sequence)
        ? BigInt(object.latest_sequence)
        : 0n,
      recoveryPath: isSet(object.recoveryPath)
        ? globalThis.String(object.recoveryPath)
        : isSet(object.recovery_path)
        ? globalThis.String(object.recovery_path)
        : "",
    };
  },

  toJSON(message: ResynchronizationRequired): unknown {
    const obj: any = {};
    if (message.subscriptionId !== "") {
      obj.subscriptionId = message.subscriptionId;
    }
    if (message.target !== undefined) {
      obj.target = JobTarget.toJSON(message.target);
    }
    if (message.reason !== 0) {
      obj.reason = resynchronizationReasonToJSON(message.reason);
    }
    if (message.earliestAvailableSequence !== 0n) {
      obj.earliestAvailableSequence = message.earliestAvailableSequence.toString();
    }
    if (message.latestSequence !== 0n) {
      obj.latestSequence = message.latestSequence.toString();
    }
    if (message.recoveryPath !== "") {
      obj.recoveryPath = message.recoveryPath;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResynchronizationRequired>, I>>(base?: I): ResynchronizationRequired {
    return ResynchronizationRequired.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResynchronizationRequired>, I>>(object: I): ResynchronizationRequired {
    const message = createBaseResynchronizationRequired();
    message.subscriptionId = object.subscriptionId ?? "";
    message.target = (object.target !== undefined && object.target !== null)
      ? JobTarget.fromPartial(object.target)
      : undefined;
    message.reason = object.reason ?? 0;
    message.earliestAvailableSequence =
      (object.earliestAvailableSequence !== undefined && object.earliestAvailableSequence !== null)
        ? BigInt(object.earliestAvailableSequence)
        : 0n;
    message.latestSequence = (object.latestSequence !== undefined && object.latestSequence !== null)
      ? BigInt(object.latestSequence)
      : 0n;
    message.recoveryPath = object.recoveryPath ?? "";
    return message;
  },
};

function createBaseSearchWebSocketPong(): SearchWebSocketPong {
  return { nonce: "", serverTime: undefined };
}

export const SearchWebSocketPong: MessageFns<SearchWebSocketPong> = {
  encode(message: SearchWebSocketPong, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nonce !== "") {
      writer.uint32(10).string(message.nonce);
    }
    if (message.serverTime !== undefined) {
      Timestamp.encode(toTimestamp(message.serverTime), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchWebSocketPong {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchWebSocketPong();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nonce = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.serverTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchWebSocketPong {
    return {
      nonce: isSet(object.nonce) ? globalThis.String(object.nonce) : "",
      serverTime: isSet(object.serverTime)
        ? fromJsonTimestamp(object.serverTime)
        : isSet(object.server_time)
        ? fromJsonTimestamp(object.server_time)
        : undefined,
    };
  },

  toJSON(message: SearchWebSocketPong): unknown {
    const obj: any = {};
    if (message.nonce !== "") {
      obj.nonce = message.nonce;
    }
    if (message.serverTime !== undefined) {
      obj.serverTime = message.serverTime.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchWebSocketPong>, I>>(base?: I): SearchWebSocketPong {
    return SearchWebSocketPong.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchWebSocketPong>, I>>(object: I): SearchWebSocketPong {
    const message = createBaseSearchWebSocketPong();
    message.nonce = object.nonce ?? "";
    message.serverTime = object.serverTime ?? undefined;
    return message;
  },
};

function createBaseSearchWebSocketProtocolError(): SearchWebSocketProtocolError {
  return { requestId: "", code: 0, message: "", violations: [], connectionWillClose: false };
}

export const SearchWebSocketProtocolError: MessageFns<SearchWebSocketProtocolError> = {
  encode(message: SearchWebSocketProtocolError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.code !== 0) {
      writer.uint32(16).int32(message.code);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    for (const v of message.violations) {
      FieldViolation.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.connectionWillClose !== false) {
      writer.uint32(40).bool(message.connectionWillClose);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchWebSocketProtocolError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchWebSocketProtocolError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.violations.push(FieldViolation.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.connectionWillClose = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchWebSocketProtocolError {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      code: isSet(object.code) ? searchWebSocketProtocolErrorCodeFromJSON(object.code) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      violations: globalThis.Array.isArray(object?.violations)
        ? object.violations.map((e: any) => FieldViolation.fromJSON(e))
        : [],
      connectionWillClose: isSet(object.connectionWillClose)
        ? globalThis.Boolean(object.connectionWillClose)
        : isSet(object.connection_will_close)
        ? globalThis.Boolean(object.connection_will_close)
        : false,
    };
  },

  toJSON(message: SearchWebSocketProtocolError): unknown {
    const obj: any = {};
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.code !== 0) {
      obj.code = searchWebSocketProtocolErrorCodeToJSON(message.code);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.violations?.length) {
      obj.violations = message.violations.map((e) => FieldViolation.toJSON(e));
    }
    if (message.connectionWillClose !== false) {
      obj.connectionWillClose = message.connectionWillClose;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchWebSocketProtocolError>, I>>(base?: I): SearchWebSocketProtocolError {
    return SearchWebSocketProtocolError.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchWebSocketProtocolError>, I>>(object: I): SearchWebSocketProtocolError {
    const message = createBaseSearchWebSocketProtocolError();
    message.requestId = object.requestId ?? "";
    message.code = object.code ?? 0;
    message.message = object.message ?? "";
    message.violations = object.violations?.map((e) => FieldViolation.fromPartial(e)) || [];
    message.connectionWillClose = object.connectionWillClose ?? false;
    return message;
  },
};

function createBaseSearchWebSocketEvent(): SearchWebSocketEvent {
  return { sequence: 0n, occurredAt: undefined, subscriptionId: undefined, target: undefined, payload: undefined };
}

export const SearchWebSocketEvent: MessageFns<SearchWebSocketEvent> = {
  encode(message: SearchWebSocketEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sequence !== 0n) {
      if (BigInt.asUintN(64, message.sequence) !== message.sequence) {
        throw new globalThis.Error("value provided for field message.sequence of type uint64 too large");
      }
      writer.uint32(8).uint64(message.sequence);
    }
    if (message.occurredAt !== undefined) {
      Timestamp.encode(toTimestamp(message.occurredAt), writer.uint32(18).fork()).join();
    }
    if (message.subscriptionId !== undefined) {
      writer.uint32(26).string(message.subscriptionId);
    }
    if (message.target !== undefined) {
      JobTarget.encode(message.target, writer.uint32(34).fork()).join();
    }
    switch (message.payload?.$case) {
      case "subscriptionAcknowledged":
        SubscriptionAcknowledged.encode(message.payload.value, writer.uint32(82).fork()).join();
        break;
      case "subscriptionRemoved":
        SubscriptionRemoved.encode(message.payload.value, writer.uint32(90).fork()).join();
        break;
      case "searchStateChanged":
        SearchJobStateChanged.encode(message.payload.value, writer.uint32(98).fork()).join();
        break;
      case "searchProgress":
        SearchProgress.encode(message.payload.value, writer.uint32(106).fork()).join();
        break;
      case "resultSchemaAvailable":
        ResultSchemaAvailable.encode(message.payload.value, writer.uint32(114).fork()).join();
        break;
      case "resultPreview":
        ResultPreview.encode(message.payload.value, writer.uint32(122).fork()).join();
        break;
      case "searchTerminal":
        SearchJobTerminal.encode(message.payload.value, writer.uint32(130).fork()).join();
        break;
      case "exportStateChanged":
        ExportJobStateChanged.encode(message.payload.value, writer.uint32(138).fork()).join();
        break;
      case "exportProgress":
        ExportProgress.encode(message.payload.value, writer.uint32(146).fork()).join();
        break;
      case "exportTerminal":
        ExportJobTerminal.encode(message.payload.value, writer.uint32(154).fork()).join();
        break;
      case "warning":
        SearchWebSocketWarning.encode(message.payload.value, writer.uint32(162).fork()).join();
        break;
      case "resynchronizationRequired":
        ResynchronizationRequired.encode(message.payload.value, writer.uint32(170).fork()).join();
        break;
      case "pong":
        SearchWebSocketPong.encode(message.payload.value, writer.uint32(178).fork()).join();
        break;
      case "protocolError":
        SearchWebSocketProtocolError.encode(message.payload.value, writer.uint32(186).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchWebSocketEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchWebSocketEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.sequence = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.occurredAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.subscriptionId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.target = JobTarget.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.payload = {
            $case: "subscriptionAcknowledged",
            value: SubscriptionAcknowledged.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.payload = {
            $case: "subscriptionRemoved",
            value: SubscriptionRemoved.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.payload = {
            $case: "searchStateChanged",
            value: SearchJobStateChanged.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.payload = { $case: "searchProgress", value: SearchProgress.decode(reader, reader.uint32()) };
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.payload = {
            $case: "resultSchemaAvailable",
            value: ResultSchemaAvailable.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.payload = { $case: "resultPreview", value: ResultPreview.decode(reader, reader.uint32()) };
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.payload = { $case: "searchTerminal", value: SearchJobTerminal.decode(reader, reader.uint32()) };
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.payload = {
            $case: "exportStateChanged",
            value: ExportJobStateChanged.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.payload = { $case: "exportProgress", value: ExportProgress.decode(reader, reader.uint32()) };
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.payload = { $case: "exportTerminal", value: ExportJobTerminal.decode(reader, reader.uint32()) };
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.payload = { $case: "warning", value: SearchWebSocketWarning.decode(reader, reader.uint32()) };
          continue;
        }
        case 21: {
          if (tag !== 170) {
            break;
          }

          message.payload = {
            $case: "resynchronizationRequired",
            value: ResynchronizationRequired.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 22: {
          if (tag !== 178) {
            break;
          }

          message.payload = { $case: "pong", value: SearchWebSocketPong.decode(reader, reader.uint32()) };
          continue;
        }
        case 23: {
          if (tag !== 186) {
            break;
          }

          message.payload = {
            $case: "protocolError",
            value: SearchWebSocketProtocolError.decode(reader, reader.uint32()),
          };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchWebSocketEvent {
    return {
      sequence: isSet(object.sequence) ? BigInt(object.sequence) : 0n,
      occurredAt: isSet(object.occurredAt)
        ? fromJsonTimestamp(object.occurredAt)
        : isSet(object.occurred_at)
        ? fromJsonTimestamp(object.occurred_at)
        : undefined,
      subscriptionId: isSet(object.subscriptionId)
        ? globalThis.String(object.subscriptionId)
        : isSet(object.subscription_id)
        ? globalThis.String(object.subscription_id)
        : undefined,
      target: isSet(object.target) ? JobTarget.fromJSON(object.target) : undefined,
      payload: isSet(object.subscriptionAcknowledged)
        ? {
          $case: "subscriptionAcknowledged",
          value: SubscriptionAcknowledged.fromJSON(object.subscriptionAcknowledged),
        }
        : isSet(object.subscription_acknowledged)
        ? {
          $case: "subscriptionAcknowledged",
          value: SubscriptionAcknowledged.fromJSON(object.subscription_acknowledged),
        }
        : isSet(object.subscriptionRemoved)
        ? { $case: "subscriptionRemoved", value: SubscriptionRemoved.fromJSON(object.subscriptionRemoved) }
        : isSet(object.subscription_removed)
        ? { $case: "subscriptionRemoved", value: SubscriptionRemoved.fromJSON(object.subscription_removed) }
        : isSet(object.searchStateChanged)
        ? { $case: "searchStateChanged", value: SearchJobStateChanged.fromJSON(object.searchStateChanged) }
        : isSet(object.search_state_changed)
        ? { $case: "searchStateChanged", value: SearchJobStateChanged.fromJSON(object.search_state_changed) }
        : isSet(object.searchProgress)
        ? { $case: "searchProgress", value: SearchProgress.fromJSON(object.searchProgress) }
        : isSet(object.search_progress)
        ? { $case: "searchProgress", value: SearchProgress.fromJSON(object.search_progress) }
        : isSet(object.resultSchemaAvailable)
        ? { $case: "resultSchemaAvailable", value: ResultSchemaAvailable.fromJSON(object.resultSchemaAvailable) }
        : isSet(object.result_schema_available)
        ? { $case: "resultSchemaAvailable", value: ResultSchemaAvailable.fromJSON(object.result_schema_available) }
        : isSet(object.resultPreview)
        ? { $case: "resultPreview", value: ResultPreview.fromJSON(object.resultPreview) }
        : isSet(object.result_preview)
        ? { $case: "resultPreview", value: ResultPreview.fromJSON(object.result_preview) }
        : isSet(object.searchTerminal)
        ? { $case: "searchTerminal", value: SearchJobTerminal.fromJSON(object.searchTerminal) }
        : isSet(object.search_terminal)
        ? { $case: "searchTerminal", value: SearchJobTerminal.fromJSON(object.search_terminal) }
        : isSet(object.exportStateChanged)
        ? { $case: "exportStateChanged", value: ExportJobStateChanged.fromJSON(object.exportStateChanged) }
        : isSet(object.export_state_changed)
        ? { $case: "exportStateChanged", value: ExportJobStateChanged.fromJSON(object.export_state_changed) }
        : isSet(object.exportProgress)
        ? { $case: "exportProgress", value: ExportProgress.fromJSON(object.exportProgress) }
        : isSet(object.export_progress)
        ? { $case: "exportProgress", value: ExportProgress.fromJSON(object.export_progress) }
        : isSet(object.exportTerminal)
        ? { $case: "exportTerminal", value: ExportJobTerminal.fromJSON(object.exportTerminal) }
        : isSet(object.export_terminal)
        ? { $case: "exportTerminal", value: ExportJobTerminal.fromJSON(object.export_terminal) }
        : isSet(object.warning)
        ? { $case: "warning", value: SearchWebSocketWarning.fromJSON(object.warning) }
        : isSet(object.resynchronizationRequired)
        ? {
          $case: "resynchronizationRequired",
          value: ResynchronizationRequired.fromJSON(object.resynchronizationRequired),
        }
        : isSet(object.resynchronization_required)
        ? {
          $case: "resynchronizationRequired",
          value: ResynchronizationRequired.fromJSON(object.resynchronization_required),
        }
        : isSet(object.pong)
        ? { $case: "pong", value: SearchWebSocketPong.fromJSON(object.pong) }
        : isSet(object.protocolError)
        ? { $case: "protocolError", value: SearchWebSocketProtocolError.fromJSON(object.protocolError) }
        : isSet(object.protocol_error)
        ? { $case: "protocolError", value: SearchWebSocketProtocolError.fromJSON(object.protocol_error) }
        : undefined,
    };
  },

  toJSON(message: SearchWebSocketEvent): unknown {
    const obj: any = {};
    if (message.sequence !== 0n) {
      obj.sequence = message.sequence.toString();
    }
    if (message.occurredAt !== undefined) {
      obj.occurredAt = message.occurredAt.toISOString();
    }
    if (message.subscriptionId !== undefined) {
      obj.subscriptionId = message.subscriptionId;
    }
    if (message.target !== undefined) {
      obj.target = JobTarget.toJSON(message.target);
    }
    if (message.payload?.$case === "subscriptionAcknowledged") {
      obj.subscriptionAcknowledged = SubscriptionAcknowledged.toJSON(message.payload.value);
    } else if (message.payload?.$case === "subscriptionRemoved") {
      obj.subscriptionRemoved = SubscriptionRemoved.toJSON(message.payload.value);
    } else if (message.payload?.$case === "searchStateChanged") {
      obj.searchStateChanged = SearchJobStateChanged.toJSON(message.payload.value);
    } else if (message.payload?.$case === "searchProgress") {
      obj.searchProgress = SearchProgress.toJSON(message.payload.value);
    } else if (message.payload?.$case === "resultSchemaAvailable") {
      obj.resultSchemaAvailable = ResultSchemaAvailable.toJSON(message.payload.value);
    } else if (message.payload?.$case === "resultPreview") {
      obj.resultPreview = ResultPreview.toJSON(message.payload.value);
    } else if (message.payload?.$case === "searchTerminal") {
      obj.searchTerminal = SearchJobTerminal.toJSON(message.payload.value);
    } else if (message.payload?.$case === "exportStateChanged") {
      obj.exportStateChanged = ExportJobStateChanged.toJSON(message.payload.value);
    } else if (message.payload?.$case === "exportProgress") {
      obj.exportProgress = ExportProgress.toJSON(message.payload.value);
    } else if (message.payload?.$case === "exportTerminal") {
      obj.exportTerminal = ExportJobTerminal.toJSON(message.payload.value);
    } else if (message.payload?.$case === "warning") {
      obj.warning = SearchWebSocketWarning.toJSON(message.payload.value);
    } else if (message.payload?.$case === "resynchronizationRequired") {
      obj.resynchronizationRequired = ResynchronizationRequired.toJSON(message.payload.value);
    } else if (message.payload?.$case === "pong") {
      obj.pong = SearchWebSocketPong.toJSON(message.payload.value);
    } else if (message.payload?.$case === "protocolError") {
      obj.protocolError = SearchWebSocketProtocolError.toJSON(message.payload.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchWebSocketEvent>, I>>(base?: I): SearchWebSocketEvent {
    return SearchWebSocketEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchWebSocketEvent>, I>>(object: I): SearchWebSocketEvent {
    const message = createBaseSearchWebSocketEvent();
    message.sequence = (object.sequence !== undefined && object.sequence !== null) ? BigInt(object.sequence) : 0n;
    message.occurredAt = object.occurredAt ?? undefined;
    message.subscriptionId = object.subscriptionId ?? undefined;
    message.target = (object.target !== undefined && object.target !== null)
      ? JobTarget.fromPartial(object.target)
      : undefined;
    switch (object.payload?.$case) {
      case "subscriptionAcknowledged": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = {
            $case: "subscriptionAcknowledged",
            value: SubscriptionAcknowledged.fromPartial(object.payload.value),
          };
        }
        break;
      }
      case "subscriptionRemoved": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = {
            $case: "subscriptionRemoved",
            value: SubscriptionRemoved.fromPartial(object.payload.value),
          };
        }
        break;
      }
      case "searchStateChanged": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = {
            $case: "searchStateChanged",
            value: SearchJobStateChanged.fromPartial(object.payload.value),
          };
        }
        break;
      }
      case "searchProgress": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = { $case: "searchProgress", value: SearchProgress.fromPartial(object.payload.value) };
        }
        break;
      }
      case "resultSchemaAvailable": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = {
            $case: "resultSchemaAvailable",
            value: ResultSchemaAvailable.fromPartial(object.payload.value),
          };
        }
        break;
      }
      case "resultPreview": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = { $case: "resultPreview", value: ResultPreview.fromPartial(object.payload.value) };
        }
        break;
      }
      case "searchTerminal": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = { $case: "searchTerminal", value: SearchJobTerminal.fromPartial(object.payload.value) };
        }
        break;
      }
      case "exportStateChanged": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = {
            $case: "exportStateChanged",
            value: ExportJobStateChanged.fromPartial(object.payload.value),
          };
        }
        break;
      }
      case "exportProgress": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = { $case: "exportProgress", value: ExportProgress.fromPartial(object.payload.value) };
        }
        break;
      }
      case "exportTerminal": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = { $case: "exportTerminal", value: ExportJobTerminal.fromPartial(object.payload.value) };
        }
        break;
      }
      case "warning": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = { $case: "warning", value: SearchWebSocketWarning.fromPartial(object.payload.value) };
        }
        break;
      }
      case "resynchronizationRequired": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = {
            $case: "resynchronizationRequired",
            value: ResynchronizationRequired.fromPartial(object.payload.value),
          };
        }
        break;
      }
      case "pong": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = { $case: "pong", value: SearchWebSocketPong.fromPartial(object.payload.value) };
        }
        break;
      }
      case "protocolError": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = {
            $case: "protocolError",
            value: SearchWebSocketProtocolError.fromPartial(object.payload.value),
          };
        }
        break;
      }
    }
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
