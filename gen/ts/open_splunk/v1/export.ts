// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.0
// source: open_splunk/v1/export.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../../google/protobuf/duration";
import { Timestamp } from "../../google/protobuf/timestamp";

export enum ExportFormat {
  EXPORT_FORMAT_UNSPECIFIED = 0,
  EXPORT_FORMAT_CSV = 1,
  EXPORT_FORMAT_JSON_LINES = 2,
  UNRECOGNIZED = -1,
}

export function exportFormatFromJSON(object: any): ExportFormat {
  switch (object) {
    case 0:
    case "EXPORT_FORMAT_UNSPECIFIED":
      return ExportFormat.EXPORT_FORMAT_UNSPECIFIED;
    case 1:
    case "EXPORT_FORMAT_CSV":
      return ExportFormat.EXPORT_FORMAT_CSV;
    case 2:
    case "EXPORT_FORMAT_JSON_LINES":
      return ExportFormat.EXPORT_FORMAT_JSON_LINES;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ExportFormat.UNRECOGNIZED;
  }
}

export function exportFormatToJSON(object: ExportFormat): string {
  switch (object) {
    case ExportFormat.EXPORT_FORMAT_UNSPECIFIED:
      return "EXPORT_FORMAT_UNSPECIFIED";
    case ExportFormat.EXPORT_FORMAT_CSV:
      return "EXPORT_FORMAT_CSV";
    case ExportFormat.EXPORT_FORMAT_JSON_LINES:
      return "EXPORT_FORMAT_JSON_LINES";
    case ExportFormat.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum CsvHeaderMode {
  CSV_HEADER_MODE_UNSPECIFIED = 0,
  CSV_HEADER_MODE_FIELD_NAMES = 1,
  CSV_HEADER_MODE_DISPLAY_NAMES = 2,
  CSV_HEADER_MODE_NONE = 3,
  UNRECOGNIZED = -1,
}

export function csvHeaderModeFromJSON(object: any): CsvHeaderMode {
  switch (object) {
    case 0:
    case "CSV_HEADER_MODE_UNSPECIFIED":
      return CsvHeaderMode.CSV_HEADER_MODE_UNSPECIFIED;
    case 1:
    case "CSV_HEADER_MODE_FIELD_NAMES":
      return CsvHeaderMode.CSV_HEADER_MODE_FIELD_NAMES;
    case 2:
    case "CSV_HEADER_MODE_DISPLAY_NAMES":
      return CsvHeaderMode.CSV_HEADER_MODE_DISPLAY_NAMES;
    case 3:
    case "CSV_HEADER_MODE_NONE":
      return CsvHeaderMode.CSV_HEADER_MODE_NONE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CsvHeaderMode.UNRECOGNIZED;
  }
}

export function csvHeaderModeToJSON(object: CsvHeaderMode): string {
  switch (object) {
    case CsvHeaderMode.CSV_HEADER_MODE_UNSPECIFIED:
      return "CSV_HEADER_MODE_UNSPECIFIED";
    case CsvHeaderMode.CSV_HEADER_MODE_FIELD_NAMES:
      return "CSV_HEADER_MODE_FIELD_NAMES";
    case CsvHeaderMode.CSV_HEADER_MODE_DISPLAY_NAMES:
      return "CSV_HEADER_MODE_DISPLAY_NAMES";
    case CsvHeaderMode.CSV_HEADER_MODE_NONE:
      return "CSV_HEADER_MODE_NONE";
    case CsvHeaderMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum JsonIntegerEncoding {
  JSON_INTEGER_ENCODING_UNSPECIFIED = 0,
  JSON_INTEGER_ENCODING_NUMBER_WHEN_SAFE = 1,
  JSON_INTEGER_ENCODING_STRING = 2,
  UNRECOGNIZED = -1,
}

export function jsonIntegerEncodingFromJSON(object: any): JsonIntegerEncoding {
  switch (object) {
    case 0:
    case "JSON_INTEGER_ENCODING_UNSPECIFIED":
      return JsonIntegerEncoding.JSON_INTEGER_ENCODING_UNSPECIFIED;
    case 1:
    case "JSON_INTEGER_ENCODING_NUMBER_WHEN_SAFE":
      return JsonIntegerEncoding.JSON_INTEGER_ENCODING_NUMBER_WHEN_SAFE;
    case 2:
    case "JSON_INTEGER_ENCODING_STRING":
      return JsonIntegerEncoding.JSON_INTEGER_ENCODING_STRING;
    case -1:
    case "UNRECOGNIZED":
    default:
      return JsonIntegerEncoding.UNRECOGNIZED;
  }
}

export function jsonIntegerEncodingToJSON(object: JsonIntegerEncoding): string {
  switch (object) {
    case JsonIntegerEncoding.JSON_INTEGER_ENCODING_UNSPECIFIED:
      return "JSON_INTEGER_ENCODING_UNSPECIFIED";
    case JsonIntegerEncoding.JSON_INTEGER_ENCODING_NUMBER_WHEN_SAFE:
      return "JSON_INTEGER_ENCODING_NUMBER_WHEN_SAFE";
    case JsonIntegerEncoding.JSON_INTEGER_ENCODING_STRING:
      return "JSON_INTEGER_ENCODING_STRING";
    case JsonIntegerEncoding.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ExportJobState {
  EXPORT_JOB_STATE_UNSPECIFIED = 0,
  EXPORT_JOB_STATE_QUEUED = 1,
  EXPORT_JOB_STATE_RUNNING = 2,
  EXPORT_JOB_STATE_COMPLETED = 3,
  EXPORT_JOB_STATE_FAILED = 4,
  EXPORT_JOB_STATE_CANCELED = 5,
  EXPORT_JOB_STATE_EXPIRED = 6,
  UNRECOGNIZED = -1,
}

export function exportJobStateFromJSON(object: any): ExportJobState {
  switch (object) {
    case 0:
    case "EXPORT_JOB_STATE_UNSPECIFIED":
      return ExportJobState.EXPORT_JOB_STATE_UNSPECIFIED;
    case 1:
    case "EXPORT_JOB_STATE_QUEUED":
      return ExportJobState.EXPORT_JOB_STATE_QUEUED;
    case 2:
    case "EXPORT_JOB_STATE_RUNNING":
      return ExportJobState.EXPORT_JOB_STATE_RUNNING;
    case 3:
    case "EXPORT_JOB_STATE_COMPLETED":
      return ExportJobState.EXPORT_JOB_STATE_COMPLETED;
    case 4:
    case "EXPORT_JOB_STATE_FAILED":
      return ExportJobState.EXPORT_JOB_STATE_FAILED;
    case 5:
    case "EXPORT_JOB_STATE_CANCELED":
      return ExportJobState.EXPORT_JOB_STATE_CANCELED;
    case 6:
    case "EXPORT_JOB_STATE_EXPIRED":
      return ExportJobState.EXPORT_JOB_STATE_EXPIRED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ExportJobState.UNRECOGNIZED;
  }
}

export function exportJobStateToJSON(object: ExportJobState): string {
  switch (object) {
    case ExportJobState.EXPORT_JOB_STATE_UNSPECIFIED:
      return "EXPORT_JOB_STATE_UNSPECIFIED";
    case ExportJobState.EXPORT_JOB_STATE_QUEUED:
      return "EXPORT_JOB_STATE_QUEUED";
    case ExportJobState.EXPORT_JOB_STATE_RUNNING:
      return "EXPORT_JOB_STATE_RUNNING";
    case ExportJobState.EXPORT_JOB_STATE_COMPLETED:
      return "EXPORT_JOB_STATE_COMPLETED";
    case ExportJobState.EXPORT_JOB_STATE_FAILED:
      return "EXPORT_JOB_STATE_FAILED";
    case ExportJobState.EXPORT_JOB_STATE_CANCELED:
      return "EXPORT_JOB_STATE_CANCELED";
    case ExportJobState.EXPORT_JOB_STATE_EXPIRED:
      return "EXPORT_JOB_STATE_EXPIRED";
    case ExportJobState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ExportFailureCode {
  EXPORT_FAILURE_CODE_UNSPECIFIED = 0,
  EXPORT_FAILURE_CODE_SEARCH_NOT_FOUND = 1,
  EXPORT_FAILURE_CODE_SEARCH_NOT_READY = 2,
  EXPORT_FAILURE_CODE_SEARCH_EXPIRED = 3,
  EXPORT_FAILURE_CODE_INVALID_COLUMNS = 4,
  EXPORT_FAILURE_CODE_ROW_LIMIT = 5,
  EXPORT_FAILURE_CODE_BYTE_LIMIT = 6,
  EXPORT_FAILURE_CODE_STORAGE_UNAVAILABLE = 7,
  EXPORT_FAILURE_CODE_INTERNAL = 8,
  EXPORT_FAILURE_CODE_SEARCH_UNAVAILABLE = 9,
  UNRECOGNIZED = -1,
}

export function exportFailureCodeFromJSON(object: any): ExportFailureCode {
  switch (object) {
    case 0:
    case "EXPORT_FAILURE_CODE_UNSPECIFIED":
      return ExportFailureCode.EXPORT_FAILURE_CODE_UNSPECIFIED;
    case 1:
    case "EXPORT_FAILURE_CODE_SEARCH_NOT_FOUND":
      return ExportFailureCode.EXPORT_FAILURE_CODE_SEARCH_NOT_FOUND;
    case 2:
    case "EXPORT_FAILURE_CODE_SEARCH_NOT_READY":
      return ExportFailureCode.EXPORT_FAILURE_CODE_SEARCH_NOT_READY;
    case 3:
    case "EXPORT_FAILURE_CODE_SEARCH_EXPIRED":
      return ExportFailureCode.EXPORT_FAILURE_CODE_SEARCH_EXPIRED;
    case 4:
    case "EXPORT_FAILURE_CODE_INVALID_COLUMNS":
      return ExportFailureCode.EXPORT_FAILURE_CODE_INVALID_COLUMNS;
    case 5:
    case "EXPORT_FAILURE_CODE_ROW_LIMIT":
      return ExportFailureCode.EXPORT_FAILURE_CODE_ROW_LIMIT;
    case 6:
    case "EXPORT_FAILURE_CODE_BYTE_LIMIT":
      return ExportFailureCode.EXPORT_FAILURE_CODE_BYTE_LIMIT;
    case 7:
    case "EXPORT_FAILURE_CODE_STORAGE_UNAVAILABLE":
      return ExportFailureCode.EXPORT_FAILURE_CODE_STORAGE_UNAVAILABLE;
    case 8:
    case "EXPORT_FAILURE_CODE_INTERNAL":
      return ExportFailureCode.EXPORT_FAILURE_CODE_INTERNAL;
    case 9:
    case "EXPORT_FAILURE_CODE_SEARCH_UNAVAILABLE":
      return ExportFailureCode.EXPORT_FAILURE_CODE_SEARCH_UNAVAILABLE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ExportFailureCode.UNRECOGNIZED;
  }
}

export function exportFailureCodeToJSON(object: ExportFailureCode): string {
  switch (object) {
    case ExportFailureCode.EXPORT_FAILURE_CODE_UNSPECIFIED:
      return "EXPORT_FAILURE_CODE_UNSPECIFIED";
    case ExportFailureCode.EXPORT_FAILURE_CODE_SEARCH_NOT_FOUND:
      return "EXPORT_FAILURE_CODE_SEARCH_NOT_FOUND";
    case ExportFailureCode.EXPORT_FAILURE_CODE_SEARCH_NOT_READY:
      return "EXPORT_FAILURE_CODE_SEARCH_NOT_READY";
    case ExportFailureCode.EXPORT_FAILURE_CODE_SEARCH_EXPIRED:
      return "EXPORT_FAILURE_CODE_SEARCH_EXPIRED";
    case ExportFailureCode.EXPORT_FAILURE_CODE_INVALID_COLUMNS:
      return "EXPORT_FAILURE_CODE_INVALID_COLUMNS";
    case ExportFailureCode.EXPORT_FAILURE_CODE_ROW_LIMIT:
      return "EXPORT_FAILURE_CODE_ROW_LIMIT";
    case ExportFailureCode.EXPORT_FAILURE_CODE_BYTE_LIMIT:
      return "EXPORT_FAILURE_CODE_BYTE_LIMIT";
    case ExportFailureCode.EXPORT_FAILURE_CODE_STORAGE_UNAVAILABLE:
      return "EXPORT_FAILURE_CODE_STORAGE_UNAVAILABLE";
    case ExportFailureCode.EXPORT_FAILURE_CODE_INTERNAL:
      return "EXPORT_FAILURE_CODE_INTERNAL";
    case ExportFailureCode.EXPORT_FAILURE_CODE_SEARCH_UNAVAILABLE:
      return "EXPORT_FAILURE_CODE_SEARCH_UNAVAILABLE";
    case ExportFailureCode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface CsvExportOptions {
  /**
   * Formula-injection protection is always enabled and cannot be disabled by a
   * browser request.
   */
  headerMode: CsvHeaderMode;
}

export interface JsonLinesExportOptions {
  integerEncoding: JsonIntegerEncoding;
  includeTypeMetadata: boolean;
}

export interface ExportDefinition {
  searchJobId: string;
  columns: string[];
  rowLimit?: bigint | undefined;
  byteLimit?: bigint | undefined;
  formatOptions:
    | { $case: "csv"; value: CsvExportOptions }
    | { $case: "jsonLines"; value: JsonLinesExportOptions }
    | undefined;
}

export interface ExportFailure {
  code: ExportFailureCode;
  message: string;
  retryable: boolean;
}

export interface ExportProgress {
  rowsWritten: bigint;
  bytesWritten: bigint;
  percentComplete?: number | undefined;
  elapsed: Duration | undefined;
  updatedAt: Date | undefined;
}

export interface ExportArtifact {
  fileName: string;
  mediaType: string;
  sizeBytes: bigint;
  rowCount: bigint;
  expiresAt: Date | undefined;
}

export interface ExportJob {
  exportJobId: string;
  stateVersion: bigint;
  definition: ExportDefinition | undefined;
  format: ExportFormat;
  state: ExportJobState;
  progress: ExportProgress | undefined;
  artifact?: ExportArtifact | undefined;
  failure?: ExportFailure | undefined;
  createdAt: Date | undefined;
  startedAt: Date | undefined;
  finishedAt: Date | undefined;
  expiresAt: Date | undefined;
}

function createBaseCsvExportOptions(): CsvExportOptions {
  return { headerMode: 0 };
}

export const CsvExportOptions: MessageFns<CsvExportOptions> = {
  encode(message: CsvExportOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.headerMode !== 0) {
      writer.uint32(8).int32(message.headerMode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CsvExportOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCsvExportOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.headerMode = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CsvExportOptions {
    return {
      headerMode: isSet(object.headerMode)
        ? csvHeaderModeFromJSON(object.headerMode)
        : isSet(object.header_mode)
        ? csvHeaderModeFromJSON(object.header_mode)
        : 0,
    };
  },

  toJSON(message: CsvExportOptions): unknown {
    const obj: any = {};
    if (message.headerMode !== 0) {
      obj.headerMode = csvHeaderModeToJSON(message.headerMode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CsvExportOptions>, I>>(base?: I): CsvExportOptions {
    return CsvExportOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CsvExportOptions>, I>>(object: I): CsvExportOptions {
    const message = createBaseCsvExportOptions();
    message.headerMode = object.headerMode ?? 0;
    return message;
  },
};

function createBaseJsonLinesExportOptions(): JsonLinesExportOptions {
  return { integerEncoding: 0, includeTypeMetadata: false };
}

export const JsonLinesExportOptions: MessageFns<JsonLinesExportOptions> = {
  encode(message: JsonLinesExportOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.integerEncoding !== 0) {
      writer.uint32(8).int32(message.integerEncoding);
    }
    if (message.includeTypeMetadata !== false) {
      writer.uint32(16).bool(message.includeTypeMetadata);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JsonLinesExportOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJsonLinesExportOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.integerEncoding = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.includeTypeMetadata = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JsonLinesExportOptions {
    return {
      integerEncoding: isSet(object.integerEncoding)
        ? jsonIntegerEncodingFromJSON(object.integerEncoding)
        : isSet(object.integer_encoding)
        ? jsonIntegerEncodingFromJSON(object.integer_encoding)
        : 0,
      includeTypeMetadata: isSet(object.includeTypeMetadata)
        ? globalThis.Boolean(object.includeTypeMetadata)
        : isSet(object.include_type_metadata)
        ? globalThis.Boolean(object.include_type_metadata)
        : false,
    };
  },

  toJSON(message: JsonLinesExportOptions): unknown {
    const obj: any = {};
    if (message.integerEncoding !== 0) {
      obj.integerEncoding = jsonIntegerEncodingToJSON(message.integerEncoding);
    }
    if (message.includeTypeMetadata !== false) {
      obj.includeTypeMetadata = message.includeTypeMetadata;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JsonLinesExportOptions>, I>>(base?: I): JsonLinesExportOptions {
    return JsonLinesExportOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JsonLinesExportOptions>, I>>(object: I): JsonLinesExportOptions {
    const message = createBaseJsonLinesExportOptions();
    message.integerEncoding = object.integerEncoding ?? 0;
    message.includeTypeMetadata = object.includeTypeMetadata ?? false;
    return message;
  },
};

function createBaseExportDefinition(): ExportDefinition {
  return { searchJobId: "", columns: [], rowLimit: undefined, byteLimit: undefined, formatOptions: undefined };
}

export const ExportDefinition: MessageFns<ExportDefinition> = {
  encode(message: ExportDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.searchJobId !== "") {
      writer.uint32(10).string(message.searchJobId);
    }
    for (const v of message.columns) {
      writer.uint32(18).string(v!);
    }
    if (message.rowLimit !== undefined) {
      if (BigInt.asUintN(64, message.rowLimit) !== message.rowLimit) {
        throw new globalThis.Error("value provided for field message.rowLimit of type uint64 too large");
      }
      writer.uint32(24).uint64(message.rowLimit);
    }
    if (message.byteLimit !== undefined) {
      if (BigInt.asUintN(64, message.byteLimit) !== message.byteLimit) {
        throw new globalThis.Error("value provided for field message.byteLimit of type uint64 too large");
      }
      writer.uint32(32).uint64(message.byteLimit);
    }
    switch (message.formatOptions?.$case) {
      case "csv":
        CsvExportOptions.encode(message.formatOptions.value, writer.uint32(82).fork()).join();
        break;
      case "jsonLines":
        JsonLinesExportOptions.encode(message.formatOptions.value, writer.uint32(90).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExportDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExportDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.searchJobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.columns.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.rowLimit = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.byteLimit = reader.uint64() as bigint;
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.formatOptions = { $case: "csv", value: CsvExportOptions.decode(reader, reader.uint32()) };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.formatOptions = { $case: "jsonLines", value: JsonLinesExportOptions.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExportDefinition {
    return {
      searchJobId: isSet(object.searchJobId)
        ? globalThis.String(object.searchJobId)
        : isSet(object.search_job_id)
        ? globalThis.String(object.search_job_id)
        : "",
      columns: globalThis.Array.isArray(object?.columns) ? object.columns.map((e: any) => globalThis.String(e)) : [],
      rowLimit: isSet(object.rowLimit)
        ? BigInt(object.rowLimit)
        : isSet(object.row_limit)
        ? BigInt(object.row_limit)
        : undefined,
      byteLimit: isSet(object.byteLimit)
        ? BigInt(object.byteLimit)
        : isSet(object.byte_limit)
        ? BigInt(object.byte_limit)
        : undefined,
      formatOptions: isSet(object.csv)
        ? { $case: "csv", value: CsvExportOptions.fromJSON(object.csv) }
        : isSet(object.jsonLines)
        ? { $case: "jsonLines", value: JsonLinesExportOptions.fromJSON(object.jsonLines) }
        : isSet(object.json_lines)
        ? { $case: "jsonLines", value: JsonLinesExportOptions.fromJSON(object.json_lines) }
        : undefined,
    };
  },

  toJSON(message: ExportDefinition): unknown {
    const obj: any = {};
    if (message.searchJobId !== "") {
      obj.searchJobId = message.searchJobId;
    }
    if (message.columns?.length) {
      obj.columns = message.columns;
    }
    if (message.rowLimit !== undefined) {
      obj.rowLimit = message.rowLimit.toString();
    }
    if (message.byteLimit !== undefined) {
      obj.byteLimit = message.byteLimit.toString();
    }
    if (message.formatOptions?.$case === "csv") {
      obj.csv = CsvExportOptions.toJSON(message.formatOptions.value);
    } else if (message.formatOptions?.$case === "jsonLines") {
      obj.jsonLines = JsonLinesExportOptions.toJSON(message.formatOptions.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExportDefinition>, I>>(base?: I): ExportDefinition {
    return ExportDefinition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExportDefinition>, I>>(object: I): ExportDefinition {
    const message = createBaseExportDefinition();
    message.searchJobId = object.searchJobId ?? "";
    message.columns = object.columns?.map((e) => e) || [];
    message.rowLimit = (object.rowLimit !== undefined && object.rowLimit !== null)
      ? BigInt(object.rowLimit)
      : undefined;
    message.byteLimit = (object.byteLimit !== undefined && object.byteLimit !== null)
      ? BigInt(object.byteLimit)
      : undefined;
    switch (object.formatOptions?.$case) {
      case "csv": {
        if (object.formatOptions?.value !== undefined && object.formatOptions?.value !== null) {
          message.formatOptions = { $case: "csv", value: CsvExportOptions.fromPartial(object.formatOptions.value) };
        }
        break;
      }
      case "jsonLines": {
        if (object.formatOptions?.value !== undefined && object.formatOptions?.value !== null) {
          message.formatOptions = {
            $case: "jsonLines",
            value: JsonLinesExportOptions.fromPartial(object.formatOptions.value),
          };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseExportFailure(): ExportFailure {
  return { code: 0, message: "", retryable: false };
}

export const ExportFailure: MessageFns<ExportFailure> = {
  encode(message: ExportFailure, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.retryable !== false) {
      writer.uint32(24).bool(message.retryable);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExportFailure {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExportFailure();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.retryable = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExportFailure {
    return {
      code: isSet(object.code) ? exportFailureCodeFromJSON(object.code) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      retryable: isSet(object.retryable) ? globalThis.Boolean(object.retryable) : false,
    };
  },

  toJSON(message: ExportFailure): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = exportFailureCodeToJSON(message.code);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.retryable !== false) {
      obj.retryable = message.retryable;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExportFailure>, I>>(base?: I): ExportFailure {
    return ExportFailure.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExportFailure>, I>>(object: I): ExportFailure {
    const message = createBaseExportFailure();
    message.code = object.code ?? 0;
    message.message = object.message ?? "";
    message.retryable = object.retryable ?? false;
    return message;
  },
};

function createBaseExportProgress(): ExportProgress {
  return { rowsWritten: 0n, bytesWritten: 0n, percentComplete: undefined, elapsed: undefined, updatedAt: undefined };
}

export const ExportProgress: MessageFns<ExportProgress> = {
  encode(message: ExportProgress, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rowsWritten !== 0n) {
      if (BigInt.asUintN(64, message.rowsWritten) !== message.rowsWritten) {
        throw new globalThis.Error("value provided for field message.rowsWritten of type uint64 too large");
      }
      writer.uint32(8).uint64(message.rowsWritten);
    }
    if (message.bytesWritten !== 0n) {
      if (BigInt.asUintN(64, message.bytesWritten) !== message.bytesWritten) {
        throw new globalThis.Error("value provided for field message.bytesWritten of type uint64 too large");
      }
      writer.uint32(16).uint64(message.bytesWritten);
    }
    if (message.percentComplete !== undefined) {
      writer.uint32(25).double(message.percentComplete);
    }
    if (message.elapsed !== undefined) {
      Duration.encode(message.elapsed, writer.uint32(34).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExportProgress {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExportProgress();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.rowsWritten = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.bytesWritten = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.percentComplete = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.elapsed = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExportProgress {
    return {
      rowsWritten: isSet(object.rowsWritten)
        ? BigInt(object.rowsWritten)
        : isSet(object.rows_written)
        ? BigInt(object.rows_written)
        : 0n,
      bytesWritten: isSet(object.bytesWritten)
        ? BigInt(object.bytesWritten)
        : isSet(object.bytes_written)
        ? BigInt(object.bytes_written)
        : 0n,
      percentComplete: isSet(object.percentComplete)
        ? globalThis.Number(object.percentComplete)
        : isSet(object.percent_complete)
        ? globalThis.Number(object.percent_complete)
        : undefined,
      elapsed: isSet(object.elapsed) ? Duration.fromJSON(object.elapsed) : undefined,
      updatedAt: isSet(object.updatedAt)
        ? fromJsonTimestamp(object.updatedAt)
        : isSet(object.updated_at)
        ? fromJsonTimestamp(object.updated_at)
        : undefined,
    };
  },

  toJSON(message: ExportProgress): unknown {
    const obj: any = {};
    if (message.rowsWritten !== 0n) {
      obj.rowsWritten = message.rowsWritten.toString();
    }
    if (message.bytesWritten !== 0n) {
      obj.bytesWritten = message.bytesWritten.toString();
    }
    if (message.percentComplete !== undefined) {
      obj.percentComplete = message.percentComplete;
    }
    if (message.elapsed !== undefined) {
      obj.elapsed = Duration.toJSON(message.elapsed);
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExportProgress>, I>>(base?: I): ExportProgress {
    return ExportProgress.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExportProgress>, I>>(object: I): ExportProgress {
    const message = createBaseExportProgress();
    message.rowsWritten = (object.rowsWritten !== undefined && object.rowsWritten !== null)
      ? BigInt(object.rowsWritten)
      : 0n;
    message.bytesWritten = (object.bytesWritten !== undefined && object.bytesWritten !== null)
      ? BigInt(object.bytesWritten)
      : 0n;
    message.percentComplete = object.percentComplete ?? undefined;
    message.elapsed = (object.elapsed !== undefined && object.elapsed !== null)
      ? Duration.fromPartial(object.elapsed)
      : undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    return message;
  },
};

function createBaseExportArtifact(): ExportArtifact {
  return { fileName: "", mediaType: "", sizeBytes: 0n, rowCount: 0n, expiresAt: undefined };
}

export const ExportArtifact: MessageFns<ExportArtifact> = {
  encode(message: ExportArtifact, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fileName !== "") {
      writer.uint32(10).string(message.fileName);
    }
    if (message.mediaType !== "") {
      writer.uint32(18).string(message.mediaType);
    }
    if (message.sizeBytes !== 0n) {
      if (BigInt.asUintN(64, message.sizeBytes) !== message.sizeBytes) {
        throw new globalThis.Error("value provided for field message.sizeBytes of type uint64 too large");
      }
      writer.uint32(24).uint64(message.sizeBytes);
    }
    if (message.rowCount !== 0n) {
      if (BigInt.asUintN(64, message.rowCount) !== message.rowCount) {
        throw new globalThis.Error("value provided for field message.rowCount of type uint64 too large");
      }
      writer.uint32(32).uint64(message.rowCount);
    }
    if (message.expiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expiresAt), writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExportArtifact {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExportArtifact();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.fileName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.mediaType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sizeBytes = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.rowCount = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.expiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExportArtifact {
    return {
      fileName: isSet(object.fileName)
        ? globalThis.String(object.fileName)
        : isSet(object.file_name)
        ? globalThis.String(object.file_name)
        : "",
      mediaType: isSet(object.mediaType)
        ? globalThis.String(object.mediaType)
        : isSet(object.media_type)
        ? globalThis.String(object.media_type)
        : "",
      sizeBytes: isSet(object.sizeBytes)
        ? BigInt(object.sizeBytes)
        : isSet(object.size_bytes)
        ? BigInt(object.size_bytes)
        : 0n,
      rowCount: isSet(object.rowCount)
        ? BigInt(object.rowCount)
        : isSet(object.row_count)
        ? BigInt(object.row_count)
        : 0n,
      expiresAt: isSet(object.expiresAt)
        ? fromJsonTimestamp(object.expiresAt)
        : isSet(object.expires_at)
        ? fromJsonTimestamp(object.expires_at)
        : undefined,
    };
  },

  toJSON(message: ExportArtifact): unknown {
    const obj: any = {};
    if (message.fileName !== "") {
      obj.fileName = message.fileName;
    }
    if (message.mediaType !== "") {
      obj.mediaType = message.mediaType;
    }
    if (message.sizeBytes !== 0n) {
      obj.sizeBytes = message.sizeBytes.toString();
    }
    if (message.rowCount !== 0n) {
      obj.rowCount = message.rowCount.toString();
    }
    if (message.expiresAt !== undefined) {
      obj.expiresAt = message.expiresAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExportArtifact>, I>>(base?: I): ExportArtifact {
    return ExportArtifact.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExportArtifact>, I>>(object: I): ExportArtifact {
    const message = createBaseExportArtifact();
    message.fileName = object.fileName ?? "";
    message.mediaType = object.mediaType ?? "";
    message.sizeBytes = (object.sizeBytes !== undefined && object.sizeBytes !== null) ? BigInt(object.sizeBytes) : 0n;
    message.rowCount = (object.rowCount !== undefined && object.rowCount !== null) ? BigInt(object.rowCount) : 0n;
    message.expiresAt = object.expiresAt ?? undefined;
    return message;
  },
};

function createBaseExportJob(): ExportJob {
  return {
    exportJobId: "",
    stateVersion: 0n,
    definition: undefined,
    format: 0,
    state: 0,
    progress: undefined,
    artifact: undefined,
    failure: undefined,
    createdAt: undefined,
    startedAt: undefined,
    finishedAt: undefined,
    expiresAt: undefined,
  };
}

export const ExportJob: MessageFns<ExportJob> = {
  encode(message: ExportJob, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exportJobId !== "") {
      writer.uint32(10).string(message.exportJobId);
    }
    if (message.stateVersion !== 0n) {
      if (BigInt.asUintN(64, message.stateVersion) !== message.stateVersion) {
        throw new globalThis.Error("value provided for field message.stateVersion of type uint64 too large");
      }
      writer.uint32(16).uint64(message.stateVersion);
    }
    if (message.definition !== undefined) {
      ExportDefinition.encode(message.definition, writer.uint32(26).fork()).join();
    }
    if (message.format !== 0) {
      writer.uint32(32).int32(message.format);
    }
    if (message.state !== 0) {
      writer.uint32(40).int32(message.state);
    }
    if (message.progress !== undefined) {
      ExportProgress.encode(message.progress, writer.uint32(50).fork()).join();
    }
    if (message.artifact !== undefined) {
      ExportArtifact.encode(message.artifact, writer.uint32(58).fork()).join();
    }
    if (message.failure !== undefined) {
      ExportFailure.encode(message.failure, writer.uint32(66).fork()).join();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(74).fork()).join();
    }
    if (message.startedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startedAt), writer.uint32(82).fork()).join();
    }
    if (message.finishedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.finishedAt), writer.uint32(90).fork()).join();
    }
    if (message.expiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expiresAt), writer.uint32(98).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExportJob {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExportJob();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.exportJobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.stateVersion = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.definition = ExportDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.format = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.progress = ExportProgress.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.artifact = ExportArtifact.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.failure = ExportFailure.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.startedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.finishedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.expiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExportJob {
    return {
      exportJobId: isSet(object.exportJobId)
        ? globalThis.String(object.exportJobId)
        : isSet(object.export_job_id)
        ? globalThis.String(object.export_job_id)
        : "",
      stateVersion: isSet(object.stateVersion)
        ? BigInt(object.stateVersion)
        : isSet(object.state_version)
        ? BigInt(object.state_version)
        : 0n,
      definition: isSet(object.definition) ? ExportDefinition.fromJSON(object.definition) : undefined,
      format: isSet(object.format) ? exportFormatFromJSON(object.format) : 0,
      state: isSet(object.state) ? exportJobStateFromJSON(object.state) : 0,
      progress: isSet(object.progress) ? ExportProgress.fromJSON(object.progress) : undefined,
      artifact: isSet(object.artifact) ? ExportArtifact.fromJSON(object.artifact) : undefined,
      failure: isSet(object.failure) ? ExportFailure.fromJSON(object.failure) : undefined,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      startedAt: isSet(object.startedAt)
        ? fromJsonTimestamp(object.startedAt)
        : isSet(object.started_at)
        ? fromJsonTimestamp(object.started_at)
        : undefined,
      finishedAt: isSet(object.finishedAt)
        ? fromJsonTimestamp(object.finishedAt)
        : isSet(object.finished_at)
        ? fromJsonTimestamp(object.finished_at)
        : undefined,
      expiresAt: isSet(object.expiresAt)
        ? fromJsonTimestamp(object.expiresAt)
        : isSet(object.expires_at)
        ? fromJsonTimestamp(object.expires_at)
        : undefined,
    };
  },

  toJSON(message: ExportJob): unknown {
    const obj: any = {};
    if (message.exportJobId !== "") {
      obj.exportJobId = message.exportJobId;
    }
    if (message.stateVersion !== 0n) {
      obj.stateVersion = message.stateVersion.toString();
    }
    if (message.definition !== undefined) {
      obj.definition = ExportDefinition.toJSON(message.definition);
    }
    if (message.format !== 0) {
      obj.format = exportFormatToJSON(message.format);
    }
    if (message.state !== 0) {
      obj.state = exportJobStateToJSON(message.state);
    }
    if (message.progress !== undefined) {
      obj.progress = ExportProgress.toJSON(message.progress);
    }
    if (message.artifact !== undefined) {
      obj.artifact = ExportArtifact.toJSON(message.artifact);
    }
    if (message.failure !== undefined) {
      obj.failure = ExportFailure.toJSON(message.failure);
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.startedAt !== undefined) {
      obj.startedAt = message.startedAt.toISOString();
    }
    if (message.finishedAt !== undefined) {
      obj.finishedAt = message.finishedAt.toISOString();
    }
    if (message.expiresAt !== undefined) {
      obj.expiresAt = message.expiresAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExportJob>, I>>(base?: I): ExportJob {
    return ExportJob.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExportJob>, I>>(object: I): ExportJob {
    const message = createBaseExportJob();
    message.exportJobId = object.exportJobId ?? "";
    message.stateVersion = (object.stateVersion !== undefined && object.stateVersion !== null)
      ? BigInt(object.stateVersion)
      : 0n;
    message.definition = (object.definition !== undefined && object.definition !== null)
      ? ExportDefinition.fromPartial(object.definition)
      : undefined;
    message.format = object.format ?? 0;
    message.state = object.state ?? 0;
    message.progress = (object.progress !== undefined && object.progress !== null)
      ? ExportProgress.fromPartial(object.progress)
      : undefined;
    message.artifact = (object.artifact !== undefined && object.artifact !== null)
      ? ExportArtifact.fromPartial(object.artifact)
      : undefined;
    message.failure = (object.failure !== undefined && object.failure !== null)
      ? ExportFailure.fromPartial(object.failure)
      : undefined;
    message.createdAt = object.createdAt ?? undefined;
    message.startedAt = object.startedAt ?? undefined;
    message.finishedAt = object.finishedAt ?? undefined;
    message.expiresAt = object.expiresAt ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
