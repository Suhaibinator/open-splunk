// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.0
// source: open_splunk/v1/app.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { TimeRangeSpec } from "./common";

export enum AppState {
  APP_STATE_UNSPECIFIED = 0,
  APP_STATE_ACTIVE = 1,
  APP_STATE_ARCHIVED = 2,
  UNRECOGNIZED = -1,
}

export function appStateFromJSON(object: any): AppState {
  switch (object) {
    case 0:
    case "APP_STATE_UNSPECIFIED":
      return AppState.APP_STATE_UNSPECIFIED;
    case 1:
    case "APP_STATE_ACTIVE":
      return AppState.APP_STATE_ACTIVE;
    case 2:
    case "APP_STATE_ARCHIVED":
      return AppState.APP_STATE_ARCHIVED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AppState.UNRECOGNIZED;
  }
}

export function appStateToJSON(object: AppState): string {
  switch (object) {
    case AppState.APP_STATE_UNSPECIFIED:
      return "APP_STATE_UNSPECIFIED";
    case AppState.APP_STATE_ACTIVE:
      return "APP_STATE_ACTIVE";
    case AppState.APP_STATE_ARCHIVED:
      return "APP_STATE_ARCHIVED";
    case AppState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface AppSelector {
  selector: { $case: "appId"; value: string } | { $case: "slug"; value: string } | undefined;
}

export interface AppDefinition {
  /** slug is immutable after creation and safe for durable navigation links. */
  slug: string;
  displayName: string;
  description?: string | undefined;
  defaultIndexNames: string[];
  defaultTimeRange: TimeRangeSpec | undefined;
}

/**
 * AppWorkspace is a UI/knowledge-object scope and is deliberately independent
 * from physical storage indexes.
 */
export interface AppWorkspace {
  appId: string;
  version: bigint;
  definition: AppDefinition | undefined;
  state: AppState;
  createdAt: Date | undefined;
  updatedAt: Date | undefined;
}

export interface AppSummary {
  appId: string;
  slug: string;
  displayName: string;
  defaultIndexNames: string[];
  state: AppState;
}

function createBaseAppSelector(): AppSelector {
  return { selector: undefined };
}

export const AppSelector: MessageFns<AppSelector> = {
  encode(message: AppSelector, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.selector?.$case) {
      case "appId":
        writer.uint32(10).string(message.selector.value);
        break;
      case "slug":
        writer.uint32(18).string(message.selector.value);
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppSelector {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppSelector();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.selector = { $case: "appId", value: reader.string() };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.selector = { $case: "slug", value: reader.string() };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppSelector {
    return {
      selector: isSet(object.appId)
        ? { $case: "appId", value: globalThis.String(object.appId) }
        : isSet(object.app_id)
        ? { $case: "appId", value: globalThis.String(object.app_id) }
        : isSet(object.slug)
        ? { $case: "slug", value: globalThis.String(object.slug) }
        : undefined,
    };
  },

  toJSON(message: AppSelector): unknown {
    const obj: any = {};
    if (message.selector?.$case === "appId") {
      obj.appId = message.selector.value;
    } else if (message.selector?.$case === "slug") {
      obj.slug = message.selector.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AppSelector>, I>>(base?: I): AppSelector {
    return AppSelector.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AppSelector>, I>>(object: I): AppSelector {
    const message = createBaseAppSelector();
    switch (object.selector?.$case) {
      case "appId": {
        if (object.selector?.value !== undefined && object.selector?.value !== null) {
          message.selector = { $case: "appId", value: object.selector.value };
        }
        break;
      }
      case "slug": {
        if (object.selector?.value !== undefined && object.selector?.value !== null) {
          message.selector = { $case: "slug", value: object.selector.value };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseAppDefinition(): AppDefinition {
  return { slug: "", displayName: "", description: undefined, defaultIndexNames: [], defaultTimeRange: undefined };
}

export const AppDefinition: MessageFns<AppDefinition> = {
  encode(message: AppDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.slug !== "") {
      writer.uint32(10).string(message.slug);
    }
    if (message.displayName !== "") {
      writer.uint32(18).string(message.displayName);
    }
    if (message.description !== undefined) {
      writer.uint32(26).string(message.description);
    }
    for (const v of message.defaultIndexNames) {
      writer.uint32(34).string(v!);
    }
    if (message.defaultTimeRange !== undefined) {
      TimeRangeSpec.encode(message.defaultTimeRange, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.slug = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.defaultIndexNames.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.defaultTimeRange = TimeRangeSpec.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppDefinition {
    return {
      slug: isSet(object.slug) ? globalThis.String(object.slug) : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
      defaultIndexNames: globalThis.Array.isArray(object?.defaultIndexNames)
        ? object.defaultIndexNames.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.default_index_names)
        ? object.default_index_names.map((e: any) => globalThis.String(e))
        : [],
      defaultTimeRange: isSet(object.defaultTimeRange)
        ? TimeRangeSpec.fromJSON(object.defaultTimeRange)
        : isSet(object.default_time_range)
        ? TimeRangeSpec.fromJSON(object.default_time_range)
        : undefined,
    };
  },

  toJSON(message: AppDefinition): unknown {
    const obj: any = {};
    if (message.slug !== "") {
      obj.slug = message.slug;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.defaultIndexNames?.length) {
      obj.defaultIndexNames = message.defaultIndexNames;
    }
    if (message.defaultTimeRange !== undefined) {
      obj.defaultTimeRange = TimeRangeSpec.toJSON(message.defaultTimeRange);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AppDefinition>, I>>(base?: I): AppDefinition {
    return AppDefinition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AppDefinition>, I>>(object: I): AppDefinition {
    const message = createBaseAppDefinition();
    message.slug = object.slug ?? "";
    message.displayName = object.displayName ?? "";
    message.description = object.description ?? undefined;
    message.defaultIndexNames = object.defaultIndexNames?.map((e) => e) || [];
    message.defaultTimeRange = (object.defaultTimeRange !== undefined && object.defaultTimeRange !== null)
      ? TimeRangeSpec.fromPartial(object.defaultTimeRange)
      : undefined;
    return message;
  },
};

function createBaseAppWorkspace(): AppWorkspace {
  return { appId: "", version: 0n, definition: undefined, state: 0, createdAt: undefined, updatedAt: undefined };
}

export const AppWorkspace: MessageFns<AppWorkspace> = {
  encode(message: AppWorkspace, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.appId !== "") {
      writer.uint32(10).string(message.appId);
    }
    if (message.version !== 0n) {
      if (BigInt.asUintN(64, message.version) !== message.version) {
        throw new globalThis.Error("value provided for field message.version of type uint64 too large");
      }
      writer.uint32(16).uint64(message.version);
    }
    if (message.definition !== undefined) {
      AppDefinition.encode(message.definition, writer.uint32(26).fork()).join();
    }
    if (message.state !== 0) {
      writer.uint32(32).int32(message.state);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(42).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppWorkspace {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppWorkspace();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.appId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.definition = AppDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppWorkspace {
    return {
      appId: isSet(object.appId)
        ? globalThis.String(object.appId)
        : isSet(object.app_id)
        ? globalThis.String(object.app_id)
        : "",
      version: isSet(object.version) ? BigInt(object.version) : 0n,
      definition: isSet(object.definition) ? AppDefinition.fromJSON(object.definition) : undefined,
      state: isSet(object.state) ? appStateFromJSON(object.state) : 0,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      updatedAt: isSet(object.updatedAt)
        ? fromJsonTimestamp(object.updatedAt)
        : isSet(object.updated_at)
        ? fromJsonTimestamp(object.updated_at)
        : undefined,
    };
  },

  toJSON(message: AppWorkspace): unknown {
    const obj: any = {};
    if (message.appId !== "") {
      obj.appId = message.appId;
    }
    if (message.version !== 0n) {
      obj.version = message.version.toString();
    }
    if (message.definition !== undefined) {
      obj.definition = AppDefinition.toJSON(message.definition);
    }
    if (message.state !== 0) {
      obj.state = appStateToJSON(message.state);
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AppWorkspace>, I>>(base?: I): AppWorkspace {
    return AppWorkspace.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AppWorkspace>, I>>(object: I): AppWorkspace {
    const message = createBaseAppWorkspace();
    message.appId = object.appId ?? "";
    message.version = (object.version !== undefined && object.version !== null) ? BigInt(object.version) : 0n;
    message.definition = (object.definition !== undefined && object.definition !== null)
      ? AppDefinition.fromPartial(object.definition)
      : undefined;
    message.state = object.state ?? 0;
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    return message;
  },
};

function createBaseAppSummary(): AppSummary {
  return { appId: "", slug: "", displayName: "", defaultIndexNames: [], state: 0 };
}

export const AppSummary: MessageFns<AppSummary> = {
  encode(message: AppSummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.appId !== "") {
      writer.uint32(10).string(message.appId);
    }
    if (message.slug !== "") {
      writer.uint32(18).string(message.slug);
    }
    if (message.displayName !== "") {
      writer.uint32(26).string(message.displayName);
    }
    for (const v of message.defaultIndexNames) {
      writer.uint32(34).string(v!);
    }
    if (message.state !== 0) {
      writer.uint32(40).int32(message.state);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppSummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppSummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.appId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.slug = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.defaultIndexNames.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppSummary {
    return {
      appId: isSet(object.appId)
        ? globalThis.String(object.appId)
        : isSet(object.app_id)
        ? globalThis.String(object.app_id)
        : "",
      slug: isSet(object.slug) ? globalThis.String(object.slug) : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      defaultIndexNames: globalThis.Array.isArray(object?.defaultIndexNames)
        ? object.defaultIndexNames.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.default_index_names)
        ? object.default_index_names.map((e: any) => globalThis.String(e))
        : [],
      state: isSet(object.state) ? appStateFromJSON(object.state) : 0,
    };
  },

  toJSON(message: AppSummary): unknown {
    const obj: any = {};
    if (message.appId !== "") {
      obj.appId = message.appId;
    }
    if (message.slug !== "") {
      obj.slug = message.slug;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.defaultIndexNames?.length) {
      obj.defaultIndexNames = message.defaultIndexNames;
    }
    if (message.state !== 0) {
      obj.state = appStateToJSON(message.state);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AppSummary>, I>>(base?: I): AppSummary {
    return AppSummary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AppSummary>, I>>(object: I): AppSummary {
    const message = createBaseAppSummary();
    message.appId = object.appId ?? "";
    message.slug = object.slug ?? "";
    message.displayName = object.displayName ?? "";
    message.defaultIndexNames = object.defaultIndexNames?.map((e) => e) || [];
    message.state = object.state ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
