// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.0
// source: open_splunk/v1/collector.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../../google/protobuf/duration";
import { Timestamp } from "../../google/protobuf/timestamp";
import { FieldViolation } from "./common";
import { LogEvent } from "./event";

export enum CollectorCapability {
  COLLECTOR_CAPABILITY_UNSPECIFIED = 0,
  COLLECTOR_CAPABILITY_FILE_INPUT = 1,
  COLLECTOR_CAPABILITY_DURABLE_QUEUE = 2,
  COLLECTOR_CAPABILITY_PARTIAL_EVENT_REJECTION = 3,
  COLLECTOR_CAPABILITY_GZIP = 4,
  COLLECTOR_CAPABILITY_MULTILINE = 5,
  COLLECTOR_CAPABILITY_TYPED_FIELDS = 6,
  UNRECOGNIZED = -1,
}

export function collectorCapabilityFromJSON(object: any): CollectorCapability {
  switch (object) {
    case 0:
    case "COLLECTOR_CAPABILITY_UNSPECIFIED":
      return CollectorCapability.COLLECTOR_CAPABILITY_UNSPECIFIED;
    case 1:
    case "COLLECTOR_CAPABILITY_FILE_INPUT":
      return CollectorCapability.COLLECTOR_CAPABILITY_FILE_INPUT;
    case 2:
    case "COLLECTOR_CAPABILITY_DURABLE_QUEUE":
      return CollectorCapability.COLLECTOR_CAPABILITY_DURABLE_QUEUE;
    case 3:
    case "COLLECTOR_CAPABILITY_PARTIAL_EVENT_REJECTION":
      return CollectorCapability.COLLECTOR_CAPABILITY_PARTIAL_EVENT_REJECTION;
    case 4:
    case "COLLECTOR_CAPABILITY_GZIP":
      return CollectorCapability.COLLECTOR_CAPABILITY_GZIP;
    case 5:
    case "COLLECTOR_CAPABILITY_MULTILINE":
      return CollectorCapability.COLLECTOR_CAPABILITY_MULTILINE;
    case 6:
    case "COLLECTOR_CAPABILITY_TYPED_FIELDS":
      return CollectorCapability.COLLECTOR_CAPABILITY_TYPED_FIELDS;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CollectorCapability.UNRECOGNIZED;
  }
}

export function collectorCapabilityToJSON(object: CollectorCapability): string {
  switch (object) {
    case CollectorCapability.COLLECTOR_CAPABILITY_UNSPECIFIED:
      return "COLLECTOR_CAPABILITY_UNSPECIFIED";
    case CollectorCapability.COLLECTOR_CAPABILITY_FILE_INPUT:
      return "COLLECTOR_CAPABILITY_FILE_INPUT";
    case CollectorCapability.COLLECTOR_CAPABILITY_DURABLE_QUEUE:
      return "COLLECTOR_CAPABILITY_DURABLE_QUEUE";
    case CollectorCapability.COLLECTOR_CAPABILITY_PARTIAL_EVENT_REJECTION:
      return "COLLECTOR_CAPABILITY_PARTIAL_EVENT_REJECTION";
    case CollectorCapability.COLLECTOR_CAPABILITY_GZIP:
      return "COLLECTOR_CAPABILITY_GZIP";
    case CollectorCapability.COLLECTOR_CAPABILITY_MULTILINE:
      return "COLLECTOR_CAPABILITY_MULTILINE";
    case CollectorCapability.COLLECTOR_CAPABILITY_TYPED_FIELDS:
      return "COLLECTOR_CAPABILITY_TYPED_FIELDS";
    case CollectorCapability.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum CollectorInputType {
  COLLECTOR_INPUT_TYPE_UNSPECIFIED = 0,
  COLLECTOR_INPUT_TYPE_FILE = 1,
  UNRECOGNIZED = -1,
}

export function collectorInputTypeFromJSON(object: any): CollectorInputType {
  switch (object) {
    case 0:
    case "COLLECTOR_INPUT_TYPE_UNSPECIFIED":
      return CollectorInputType.COLLECTOR_INPUT_TYPE_UNSPECIFIED;
    case 1:
    case "COLLECTOR_INPUT_TYPE_FILE":
      return CollectorInputType.COLLECTOR_INPUT_TYPE_FILE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CollectorInputType.UNRECOGNIZED;
  }
}

export function collectorInputTypeToJSON(object: CollectorInputType): string {
  switch (object) {
    case CollectorInputType.COLLECTOR_INPUT_TYPE_UNSPECIFIED:
      return "COLLECTOR_INPUT_TYPE_UNSPECIFIED";
    case CollectorInputType.COLLECTOR_INPUT_TYPE_FILE:
      return "COLLECTOR_INPUT_TYPE_FILE";
    case CollectorInputType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum CollectorInputState {
  COLLECTOR_INPUT_STATE_UNSPECIFIED = 0,
  COLLECTOR_INPUT_STATE_STARTING = 1,
  COLLECTOR_INPUT_STATE_HEALTHY = 2,
  COLLECTOR_INPUT_STATE_MISSING = 3,
  COLLECTOR_INPUT_STATE_UNREADABLE = 4,
  COLLECTOR_INPUT_STATE_PAUSED = 5,
  COLLECTOR_INPUT_STATE_ERROR = 6,
  UNRECOGNIZED = -1,
}

export function collectorInputStateFromJSON(object: any): CollectorInputState {
  switch (object) {
    case 0:
    case "COLLECTOR_INPUT_STATE_UNSPECIFIED":
      return CollectorInputState.COLLECTOR_INPUT_STATE_UNSPECIFIED;
    case 1:
    case "COLLECTOR_INPUT_STATE_STARTING":
      return CollectorInputState.COLLECTOR_INPUT_STATE_STARTING;
    case 2:
    case "COLLECTOR_INPUT_STATE_HEALTHY":
      return CollectorInputState.COLLECTOR_INPUT_STATE_HEALTHY;
    case 3:
    case "COLLECTOR_INPUT_STATE_MISSING":
      return CollectorInputState.COLLECTOR_INPUT_STATE_MISSING;
    case 4:
    case "COLLECTOR_INPUT_STATE_UNREADABLE":
      return CollectorInputState.COLLECTOR_INPUT_STATE_UNREADABLE;
    case 5:
    case "COLLECTOR_INPUT_STATE_PAUSED":
      return CollectorInputState.COLLECTOR_INPUT_STATE_PAUSED;
    case 6:
    case "COLLECTOR_INPUT_STATE_ERROR":
      return CollectorInputState.COLLECTOR_INPUT_STATE_ERROR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CollectorInputState.UNRECOGNIZED;
  }
}

export function collectorInputStateToJSON(object: CollectorInputState): string {
  switch (object) {
    case CollectorInputState.COLLECTOR_INPUT_STATE_UNSPECIFIED:
      return "COLLECTOR_INPUT_STATE_UNSPECIFIED";
    case CollectorInputState.COLLECTOR_INPUT_STATE_STARTING:
      return "COLLECTOR_INPUT_STATE_STARTING";
    case CollectorInputState.COLLECTOR_INPUT_STATE_HEALTHY:
      return "COLLECTOR_INPUT_STATE_HEALTHY";
    case CollectorInputState.COLLECTOR_INPUT_STATE_MISSING:
      return "COLLECTOR_INPUT_STATE_MISSING";
    case CollectorInputState.COLLECTOR_INPUT_STATE_UNREADABLE:
      return "COLLECTOR_INPUT_STATE_UNREADABLE";
    case CollectorInputState.COLLECTOR_INPUT_STATE_PAUSED:
      return "COLLECTOR_INPUT_STATE_PAUSED";
    case CollectorInputState.COLLECTOR_INPUT_STATE_ERROR:
      return "COLLECTOR_INPUT_STATE_ERROR";
    case CollectorInputState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum CollectorGoodbyeReason {
  COLLECTOR_GOODBYE_REASON_UNSPECIFIED = 0,
  COLLECTOR_GOODBYE_REASON_SHUTDOWN = 1,
  COLLECTOR_GOODBYE_REASON_RESTART = 2,
  COLLECTOR_GOODBYE_REASON_CONFIGURATION_RELOAD = 3,
  UNRECOGNIZED = -1,
}

export function collectorGoodbyeReasonFromJSON(object: any): CollectorGoodbyeReason {
  switch (object) {
    case 0:
    case "COLLECTOR_GOODBYE_REASON_UNSPECIFIED":
      return CollectorGoodbyeReason.COLLECTOR_GOODBYE_REASON_UNSPECIFIED;
    case 1:
    case "COLLECTOR_GOODBYE_REASON_SHUTDOWN":
      return CollectorGoodbyeReason.COLLECTOR_GOODBYE_REASON_SHUTDOWN;
    case 2:
    case "COLLECTOR_GOODBYE_REASON_RESTART":
      return CollectorGoodbyeReason.COLLECTOR_GOODBYE_REASON_RESTART;
    case 3:
    case "COLLECTOR_GOODBYE_REASON_CONFIGURATION_RELOAD":
      return CollectorGoodbyeReason.COLLECTOR_GOODBYE_REASON_CONFIGURATION_RELOAD;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CollectorGoodbyeReason.UNRECOGNIZED;
  }
}

export function collectorGoodbyeReasonToJSON(object: CollectorGoodbyeReason): string {
  switch (object) {
    case CollectorGoodbyeReason.COLLECTOR_GOODBYE_REASON_UNSPECIFIED:
      return "COLLECTOR_GOODBYE_REASON_UNSPECIFIED";
    case CollectorGoodbyeReason.COLLECTOR_GOODBYE_REASON_SHUTDOWN:
      return "COLLECTOR_GOODBYE_REASON_SHUTDOWN";
    case CollectorGoodbyeReason.COLLECTOR_GOODBYE_REASON_RESTART:
      return "COLLECTOR_GOODBYE_REASON_RESTART";
    case CollectorGoodbyeReason.COLLECTOR_GOODBYE_REASON_CONFIGURATION_RELOAD:
      return "COLLECTOR_GOODBYE_REASON_CONFIGURATION_RELOAD";
    case CollectorGoodbyeReason.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum AckDurability {
  ACK_DURABILITY_UNSPECIFIED = 0,
  ACK_DURABILITY_CLICKHOUSE_COMMITTED = 1,
  UNRECOGNIZED = -1,
}

export function ackDurabilityFromJSON(object: any): AckDurability {
  switch (object) {
    case 0:
    case "ACK_DURABILITY_UNSPECIFIED":
      return AckDurability.ACK_DURABILITY_UNSPECIFIED;
    case 1:
    case "ACK_DURABILITY_CLICKHOUSE_COMMITTED":
      return AckDurability.ACK_DURABILITY_CLICKHOUSE_COMMITTED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AckDurability.UNRECOGNIZED;
  }
}

export function ackDurabilityToJSON(object: AckDurability): string {
  switch (object) {
    case AckDurability.ACK_DURABILITY_UNSPECIFIED:
      return "ACK_DURABILITY_UNSPECIFIED";
    case AckDurability.ACK_DURABILITY_CLICKHOUSE_COMMITTED:
      return "ACK_DURABILITY_CLICKHOUSE_COMMITTED";
    case AckDurability.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum EventRejectionCode {
  EVENT_REJECTION_CODE_UNSPECIFIED = 0,
  EVENT_REJECTION_CODE_INVALID_EVENT_ID = 1,
  EVENT_REJECTION_CODE_INVALID_INDEX = 2,
  EVENT_REJECTION_CODE_UNAUTHORIZED_INDEX = 3,
  EVENT_REJECTION_CODE_INVALID_TIMESTAMP = 4,
  EVENT_REJECTION_CODE_EVENT_TOO_LARGE = 5,
  EVENT_REJECTION_CODE_TOO_MANY_FIELDS = 6,
  EVENT_REJECTION_CODE_FIELD_NAME_INVALID = 7,
  EVENT_REJECTION_CODE_NESTING_TOO_DEEP = 8,
  EVENT_REJECTION_CODE_VALUE_INVALID = 9,
  EVENT_REJECTION_CODE_REDACTION_POLICY = 10,
  UNRECOGNIZED = -1,
}

export function eventRejectionCodeFromJSON(object: any): EventRejectionCode {
  switch (object) {
    case 0:
    case "EVENT_REJECTION_CODE_UNSPECIFIED":
      return EventRejectionCode.EVENT_REJECTION_CODE_UNSPECIFIED;
    case 1:
    case "EVENT_REJECTION_CODE_INVALID_EVENT_ID":
      return EventRejectionCode.EVENT_REJECTION_CODE_INVALID_EVENT_ID;
    case 2:
    case "EVENT_REJECTION_CODE_INVALID_INDEX":
      return EventRejectionCode.EVENT_REJECTION_CODE_INVALID_INDEX;
    case 3:
    case "EVENT_REJECTION_CODE_UNAUTHORIZED_INDEX":
      return EventRejectionCode.EVENT_REJECTION_CODE_UNAUTHORIZED_INDEX;
    case 4:
    case "EVENT_REJECTION_CODE_INVALID_TIMESTAMP":
      return EventRejectionCode.EVENT_REJECTION_CODE_INVALID_TIMESTAMP;
    case 5:
    case "EVENT_REJECTION_CODE_EVENT_TOO_LARGE":
      return EventRejectionCode.EVENT_REJECTION_CODE_EVENT_TOO_LARGE;
    case 6:
    case "EVENT_REJECTION_CODE_TOO_MANY_FIELDS":
      return EventRejectionCode.EVENT_REJECTION_CODE_TOO_MANY_FIELDS;
    case 7:
    case "EVENT_REJECTION_CODE_FIELD_NAME_INVALID":
      return EventRejectionCode.EVENT_REJECTION_CODE_FIELD_NAME_INVALID;
    case 8:
    case "EVENT_REJECTION_CODE_NESTING_TOO_DEEP":
      return EventRejectionCode.EVENT_REJECTION_CODE_NESTING_TOO_DEEP;
    case 9:
    case "EVENT_REJECTION_CODE_VALUE_INVALID":
      return EventRejectionCode.EVENT_REJECTION_CODE_VALUE_INVALID;
    case 10:
    case "EVENT_REJECTION_CODE_REDACTION_POLICY":
      return EventRejectionCode.EVENT_REJECTION_CODE_REDACTION_POLICY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return EventRejectionCode.UNRECOGNIZED;
  }
}

export function eventRejectionCodeToJSON(object: EventRejectionCode): string {
  switch (object) {
    case EventRejectionCode.EVENT_REJECTION_CODE_UNSPECIFIED:
      return "EVENT_REJECTION_CODE_UNSPECIFIED";
    case EventRejectionCode.EVENT_REJECTION_CODE_INVALID_EVENT_ID:
      return "EVENT_REJECTION_CODE_INVALID_EVENT_ID";
    case EventRejectionCode.EVENT_REJECTION_CODE_INVALID_INDEX:
      return "EVENT_REJECTION_CODE_INVALID_INDEX";
    case EventRejectionCode.EVENT_REJECTION_CODE_UNAUTHORIZED_INDEX:
      return "EVENT_REJECTION_CODE_UNAUTHORIZED_INDEX";
    case EventRejectionCode.EVENT_REJECTION_CODE_INVALID_TIMESTAMP:
      return "EVENT_REJECTION_CODE_INVALID_TIMESTAMP";
    case EventRejectionCode.EVENT_REJECTION_CODE_EVENT_TOO_LARGE:
      return "EVENT_REJECTION_CODE_EVENT_TOO_LARGE";
    case EventRejectionCode.EVENT_REJECTION_CODE_TOO_MANY_FIELDS:
      return "EVENT_REJECTION_CODE_TOO_MANY_FIELDS";
    case EventRejectionCode.EVENT_REJECTION_CODE_FIELD_NAME_INVALID:
      return "EVENT_REJECTION_CODE_FIELD_NAME_INVALID";
    case EventRejectionCode.EVENT_REJECTION_CODE_NESTING_TOO_DEEP:
      return "EVENT_REJECTION_CODE_NESTING_TOO_DEEP";
    case EventRejectionCode.EVENT_REJECTION_CODE_VALUE_INVALID:
      return "EVENT_REJECTION_CODE_VALUE_INVALID";
    case EventRejectionCode.EVENT_REJECTION_CODE_REDACTION_POLICY:
      return "EVENT_REJECTION_CODE_REDACTION_POLICY";
    case EventRejectionCode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum BatchRejectionCode {
  BATCH_REJECTION_CODE_UNSPECIFIED = 0,
  BATCH_REJECTION_CODE_INVALID_BATCH_ID = 1,
  BATCH_REJECTION_CODE_SEQUENCE_CONFLICT = 2,
  BATCH_REJECTION_CODE_BATCH_TOO_LARGE = 3,
  BATCH_REJECTION_CODE_TOO_MANY_EVENTS = 4,
  BATCH_REJECTION_CODE_EVENT_ID_DIGEST_MISMATCH = 5,
  BATCH_REJECTION_CODE_COLLECTOR_ID_MISMATCH = 6,
  BATCH_REJECTION_CODE_PROTOCOL_VIOLATION = 7,
  BATCH_REJECTION_CODE_NO_AUTHORIZED_EVENTS = 8,
  UNRECOGNIZED = -1,
}

export function batchRejectionCodeFromJSON(object: any): BatchRejectionCode {
  switch (object) {
    case 0:
    case "BATCH_REJECTION_CODE_UNSPECIFIED":
      return BatchRejectionCode.BATCH_REJECTION_CODE_UNSPECIFIED;
    case 1:
    case "BATCH_REJECTION_CODE_INVALID_BATCH_ID":
      return BatchRejectionCode.BATCH_REJECTION_CODE_INVALID_BATCH_ID;
    case 2:
    case "BATCH_REJECTION_CODE_SEQUENCE_CONFLICT":
      return BatchRejectionCode.BATCH_REJECTION_CODE_SEQUENCE_CONFLICT;
    case 3:
    case "BATCH_REJECTION_CODE_BATCH_TOO_LARGE":
      return BatchRejectionCode.BATCH_REJECTION_CODE_BATCH_TOO_LARGE;
    case 4:
    case "BATCH_REJECTION_CODE_TOO_MANY_EVENTS":
      return BatchRejectionCode.BATCH_REJECTION_CODE_TOO_MANY_EVENTS;
    case 5:
    case "BATCH_REJECTION_CODE_EVENT_ID_DIGEST_MISMATCH":
      return BatchRejectionCode.BATCH_REJECTION_CODE_EVENT_ID_DIGEST_MISMATCH;
    case 6:
    case "BATCH_REJECTION_CODE_COLLECTOR_ID_MISMATCH":
      return BatchRejectionCode.BATCH_REJECTION_CODE_COLLECTOR_ID_MISMATCH;
    case 7:
    case "BATCH_REJECTION_CODE_PROTOCOL_VIOLATION":
      return BatchRejectionCode.BATCH_REJECTION_CODE_PROTOCOL_VIOLATION;
    case 8:
    case "BATCH_REJECTION_CODE_NO_AUTHORIZED_EVENTS":
      return BatchRejectionCode.BATCH_REJECTION_CODE_NO_AUTHORIZED_EVENTS;
    case -1:
    case "UNRECOGNIZED":
    default:
      return BatchRejectionCode.UNRECOGNIZED;
  }
}

export function batchRejectionCodeToJSON(object: BatchRejectionCode): string {
  switch (object) {
    case BatchRejectionCode.BATCH_REJECTION_CODE_UNSPECIFIED:
      return "BATCH_REJECTION_CODE_UNSPECIFIED";
    case BatchRejectionCode.BATCH_REJECTION_CODE_INVALID_BATCH_ID:
      return "BATCH_REJECTION_CODE_INVALID_BATCH_ID";
    case BatchRejectionCode.BATCH_REJECTION_CODE_SEQUENCE_CONFLICT:
      return "BATCH_REJECTION_CODE_SEQUENCE_CONFLICT";
    case BatchRejectionCode.BATCH_REJECTION_CODE_BATCH_TOO_LARGE:
      return "BATCH_REJECTION_CODE_BATCH_TOO_LARGE";
    case BatchRejectionCode.BATCH_REJECTION_CODE_TOO_MANY_EVENTS:
      return "BATCH_REJECTION_CODE_TOO_MANY_EVENTS";
    case BatchRejectionCode.BATCH_REJECTION_CODE_EVENT_ID_DIGEST_MISMATCH:
      return "BATCH_REJECTION_CODE_EVENT_ID_DIGEST_MISMATCH";
    case BatchRejectionCode.BATCH_REJECTION_CODE_COLLECTOR_ID_MISMATCH:
      return "BATCH_REJECTION_CODE_COLLECTOR_ID_MISMATCH";
    case BatchRejectionCode.BATCH_REJECTION_CODE_PROTOCOL_VIOLATION:
      return "BATCH_REJECTION_CODE_PROTOCOL_VIOLATION";
    case BatchRejectionCode.BATCH_REJECTION_CODE_NO_AUTHORIZED_EVENTS:
      return "BATCH_REJECTION_CODE_NO_AUTHORIZED_EVENTS";
    case BatchRejectionCode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum RetryBatchReason {
  RETRY_BATCH_REASON_UNSPECIFIED = 0,
  RETRY_BATCH_REASON_SERVER_BUSY = 1,
  RETRY_BATCH_REASON_STORAGE_UNAVAILABLE = 2,
  RETRY_BATCH_REASON_RATE_LIMITED = 3,
  RETRY_BATCH_REASON_SHUTTING_DOWN = 4,
  UNRECOGNIZED = -1,
}

export function retryBatchReasonFromJSON(object: any): RetryBatchReason {
  switch (object) {
    case 0:
    case "RETRY_BATCH_REASON_UNSPECIFIED":
      return RetryBatchReason.RETRY_BATCH_REASON_UNSPECIFIED;
    case 1:
    case "RETRY_BATCH_REASON_SERVER_BUSY":
      return RetryBatchReason.RETRY_BATCH_REASON_SERVER_BUSY;
    case 2:
    case "RETRY_BATCH_REASON_STORAGE_UNAVAILABLE":
      return RetryBatchReason.RETRY_BATCH_REASON_STORAGE_UNAVAILABLE;
    case 3:
    case "RETRY_BATCH_REASON_RATE_LIMITED":
      return RetryBatchReason.RETRY_BATCH_REASON_RATE_LIMITED;
    case 4:
    case "RETRY_BATCH_REASON_SHUTTING_DOWN":
      return RetryBatchReason.RETRY_BATCH_REASON_SHUTTING_DOWN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RetryBatchReason.UNRECOGNIZED;
  }
}

export function retryBatchReasonToJSON(object: RetryBatchReason): string {
  switch (object) {
    case RetryBatchReason.RETRY_BATCH_REASON_UNSPECIFIED:
      return "RETRY_BATCH_REASON_UNSPECIFIED";
    case RetryBatchReason.RETRY_BATCH_REASON_SERVER_BUSY:
      return "RETRY_BATCH_REASON_SERVER_BUSY";
    case RetryBatchReason.RETRY_BATCH_REASON_STORAGE_UNAVAILABLE:
      return "RETRY_BATCH_REASON_STORAGE_UNAVAILABLE";
    case RetryBatchReason.RETRY_BATCH_REASON_RATE_LIMITED:
      return "RETRY_BATCH_REASON_RATE_LIMITED";
    case RetryBatchReason.RETRY_BATCH_REASON_SHUTTING_DOWN:
      return "RETRY_BATCH_REASON_SHUTTING_DOWN";
    case RetryBatchReason.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ThrottleReason {
  THROTTLE_REASON_UNSPECIFIED = 0,
  THROTTLE_REASON_SERVER_LOAD = 1,
  THROTTLE_REASON_STORAGE_LOAD = 2,
  THROTTLE_REASON_TOKEN_QUOTA = 3,
  THROTTLE_REASON_INDEX_QUOTA = 4,
  UNRECOGNIZED = -1,
}

export function throttleReasonFromJSON(object: any): ThrottleReason {
  switch (object) {
    case 0:
    case "THROTTLE_REASON_UNSPECIFIED":
      return ThrottleReason.THROTTLE_REASON_UNSPECIFIED;
    case 1:
    case "THROTTLE_REASON_SERVER_LOAD":
      return ThrottleReason.THROTTLE_REASON_SERVER_LOAD;
    case 2:
    case "THROTTLE_REASON_STORAGE_LOAD":
      return ThrottleReason.THROTTLE_REASON_STORAGE_LOAD;
    case 3:
    case "THROTTLE_REASON_TOKEN_QUOTA":
      return ThrottleReason.THROTTLE_REASON_TOKEN_QUOTA;
    case 4:
    case "THROTTLE_REASON_INDEX_QUOTA":
      return ThrottleReason.THROTTLE_REASON_INDEX_QUOTA;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ThrottleReason.UNRECOGNIZED;
  }
}

export function throttleReasonToJSON(object: ThrottleReason): string {
  switch (object) {
    case ThrottleReason.THROTTLE_REASON_UNSPECIFIED:
      return "THROTTLE_REASON_UNSPECIFIED";
    case ThrottleReason.THROTTLE_REASON_SERVER_LOAD:
      return "THROTTLE_REASON_SERVER_LOAD";
    case ThrottleReason.THROTTLE_REASON_STORAGE_LOAD:
      return "THROTTLE_REASON_STORAGE_LOAD";
    case ThrottleReason.THROTTLE_REASON_TOKEN_QUOTA:
      return "THROTTLE_REASON_TOKEN_QUOTA";
    case ThrottleReason.THROTTLE_REASON_INDEX_QUOTA:
      return "THROTTLE_REASON_INDEX_QUOTA";
    case ThrottleReason.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ServerNoticeType {
  SERVER_NOTICE_TYPE_UNSPECIFIED = 0,
  SERVER_NOTICE_TYPE_INFORMATION = 1,
  SERVER_NOTICE_TYPE_MAINTENANCE = 2,
  SERVER_NOTICE_TYPE_SHUTTING_DOWN = 3,
  UNRECOGNIZED = -1,
}

export function serverNoticeTypeFromJSON(object: any): ServerNoticeType {
  switch (object) {
    case 0:
    case "SERVER_NOTICE_TYPE_UNSPECIFIED":
      return ServerNoticeType.SERVER_NOTICE_TYPE_UNSPECIFIED;
    case 1:
    case "SERVER_NOTICE_TYPE_INFORMATION":
      return ServerNoticeType.SERVER_NOTICE_TYPE_INFORMATION;
    case 2:
    case "SERVER_NOTICE_TYPE_MAINTENANCE":
      return ServerNoticeType.SERVER_NOTICE_TYPE_MAINTENANCE;
    case 3:
    case "SERVER_NOTICE_TYPE_SHUTTING_DOWN":
      return ServerNoticeType.SERVER_NOTICE_TYPE_SHUTTING_DOWN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ServerNoticeType.UNRECOGNIZED;
  }
}

export function serverNoticeTypeToJSON(object: ServerNoticeType): string {
  switch (object) {
    case ServerNoticeType.SERVER_NOTICE_TYPE_UNSPECIFIED:
      return "SERVER_NOTICE_TYPE_UNSPECIFIED";
    case ServerNoticeType.SERVER_NOTICE_TYPE_INFORMATION:
      return "SERVER_NOTICE_TYPE_INFORMATION";
    case ServerNoticeType.SERVER_NOTICE_TYPE_MAINTENANCE:
      return "SERVER_NOTICE_TYPE_MAINTENANCE";
    case ServerNoticeType.SERVER_NOTICE_TYPE_SHUTTING_DOWN:
      return "SERVER_NOTICE_TYPE_SHUTTING_DOWN";
    case ServerNoticeType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface CollectorInputRegistration {
  inputId: string;
  inputType: CollectorInputType;
  indexName: string;
  source?: string | undefined;
  sourcetype?: string | undefined;
}

export interface CollectorHello {
  collectorId: string;
  instanceId: string;
  protocolMajor: number;
  protocolMinor: number;
  collectorVersion: string;
  hostname: string;
  operatingSystem: string;
  architecture: string;
  startedAt: Date | undefined;
  capabilities: CollectorCapability[];
  inputs: CollectorInputRegistration[];
  lastAcknowledgedBatchSequence?: bigint | undefined;
}

/**
 * EventBatch is one durable queue unit. batch_id and batch_sequence remain
 * stable across reconnects and retries. event_ids_sha256 is SHA-256 over each
 * UTF-8 event_id prefixed by its unsigned 32-bit big-endian byte length.
 */
export interface EventBatch {
  collectorId: string;
  batchId: string;
  batchSequence: bigint;
  createdAt: Date | undefined;
  events: LogEvent[];
  uncompressedSizeBytes: bigint;
  eventIdsSha256: Uint8Array;
  protocolMajor: number;
  protocolMinor: number;
}

export interface CollectorQueueStats {
  queuedEvents: bigint;
  queuedBytes: bigint;
  oldestEventAge?: Duration | undefined;
  sentEventsTotal: bigint;
  acknowledgedEventsTotal: bigint;
  retriedBatchesTotal: bigint;
  rejectedEventsTotal: bigint;
  droppedEventsTotal: bigint;
}

export interface CollectorInputHealth {
  inputId: string;
  state: CollectorInputState;
  statusMessage: string;
  discoveredSources: bigint;
  activeSources: bigint;
  eventsReadTotal: bigint;
  bytesReadTotal: bigint;
  lastEventAt: Date | undefined;
  lastErrorAt: Date | undefined;
}

export interface CollectorHeartbeat {
  collectorId: string;
  instanceId: string;
  observedAt: Date | undefined;
  queue: CollectorQueueStats | undefined;
  inputs: CollectorInputHealth[];
  lastSentBatchSequence?: bigint | undefined;
  lastAcknowledgedBatchSequence?: bigint | undefined;
  processResidentMemoryBytes: bigint;
  processCpuPercent: number;
}

export interface CollectorGoodbye {
  reason: CollectorGoodbyeReason;
  message?: string | undefined;
}

/**
 * CollectRequest.stream_sequence starts at 1 for each connection and must
 * increase by one. It is independent of durable EventBatch.batch_sequence.
 */
export interface CollectRequest {
  streamSequence: bigint;
  sentAt: Date | undefined;
  payload:
    | { $case: "hello"; value: CollectorHello }
    | { $case: "batch"; value: EventBatch }
    | { $case: "heartbeat"; value: CollectorHeartbeat }
    | { $case: "goodbye"; value: CollectorGoodbye }
    | undefined;
}

/** CollectorReady completes stream negotiation and declares hard server limits. */
export interface CollectorReady {
  streamId: string;
  serverInstanceId: string;
  serverVersion: string;
  protocolMajor: number;
  protocolMinor: number;
  serverTime: Date | undefined;
  heartbeatInterval: Duration | undefined;
  maxInFlightBatches: number;
  maxBatchEvents: number;
  maxBatchBytes: bigint;
  maxEventBytes: bigint;
  authorizedIndexes: string[];
  acknowledgmentDurability: AckDurability;
  resumeAfterBatchSequence?: bigint | undefined;
}

export interface EventRejection {
  eventIndex: number;
  eventId: string;
  code: EventRejectionCode;
  message: string;
  violations: FieldViolation[];
}

/**
 * BatchAck is terminal for the batch. Rejected events must be dead-lettered;
 * accepted and duplicate events may be removed from the durable queue.
 */
export interface BatchAck {
  batchId: string;
  batchSequence: bigint;
  acknowledgedThroughBatchSequence?: bigint | undefined;
  durability: AckDurability;
  acceptedEventCount: number;
  duplicateEventCount: number;
  rejectedEvents: EventRejection[];
  committedAt: Date | undefined;
}

/** BatchReject is a permanent, terminal rejection of the complete batch. */
export interface BatchReject {
  batchId: string;
  batchSequence: bigint;
  code: BatchRejectionCode;
  message: string;
  violations: FieldViolation[];
}

/**
 * RetryBatch is non-terminal. The collector must retain and resend the exact
 * same durable batch after at least retry_after.
 */
export interface RetryBatch {
  batchId: string;
  batchSequence: bigint;
  reason: RetryBatchReason;
  retryAfter: Duration | undefined;
  message?: string | undefined;
}

/**
 * Throttle applies to subsequent sends until effective_until or another
 * throttle message. Zero limits mean the previously negotiated limit remains.
 */
export interface Throttle {
  reason: ThrottleReason;
  minimumSendDelay: Duration | undefined;
  effectiveUntil: Date | undefined;
  maxInFlightBatches: number;
  maxBatchEvents: number;
  maxBatchBytes: bigint;
  message?: string | undefined;
}

export interface ServerNotice {
  type: ServerNoticeType;
  code: string;
  message: string;
  reconnectAfter?: Duration | undefined;
}

/**
 * CollectResponse.stream_sequence is connection-local and monotonically
 * increasing. Responses may arrive independently of collector requests.
 */
export interface CollectResponse {
  streamSequence: bigint;
  sentAt: Date | undefined;
  payload:
    | { $case: "ready"; value: CollectorReady }
    | { $case: "batchAck"; value: BatchAck }
    | { $case: "batchReject"; value: BatchReject }
    | { $case: "retryBatch"; value: RetryBatch }
    | { $case: "throttle"; value: Throttle }
    | { $case: "notice"; value: ServerNotice }
    | undefined;
}

function createBaseCollectorInputRegistration(): CollectorInputRegistration {
  return { inputId: "", inputType: 0, indexName: "", source: undefined, sourcetype: undefined };
}

export const CollectorInputRegistration: MessageFns<CollectorInputRegistration> = {
  encode(message: CollectorInputRegistration, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.inputId !== "") {
      writer.uint32(10).string(message.inputId);
    }
    if (message.inputType !== 0) {
      writer.uint32(16).int32(message.inputType);
    }
    if (message.indexName !== "") {
      writer.uint32(26).string(message.indexName);
    }
    if (message.source !== undefined) {
      writer.uint32(34).string(message.source);
    }
    if (message.sourcetype !== undefined) {
      writer.uint32(42).string(message.sourcetype);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CollectorInputRegistration {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCollectorInputRegistration();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.inputId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.inputType = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.indexName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.sourcetype = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CollectorInputRegistration {
    return {
      inputId: isSet(object.inputId)
        ? globalThis.String(object.inputId)
        : isSet(object.input_id)
        ? globalThis.String(object.input_id)
        : "",
      inputType: isSet(object.inputType)
        ? collectorInputTypeFromJSON(object.inputType)
        : isSet(object.input_type)
        ? collectorInputTypeFromJSON(object.input_type)
        : 0,
      indexName: isSet(object.indexName)
        ? globalThis.String(object.indexName)
        : isSet(object.index_name)
        ? globalThis.String(object.index_name)
        : "",
      source: isSet(object.source) ? globalThis.String(object.source) : undefined,
      sourcetype: isSet(object.sourcetype) ? globalThis.String(object.sourcetype) : undefined,
    };
  },

  toJSON(message: CollectorInputRegistration): unknown {
    const obj: any = {};
    if (message.inputId !== "") {
      obj.inputId = message.inputId;
    }
    if (message.inputType !== 0) {
      obj.inputType = collectorInputTypeToJSON(message.inputType);
    }
    if (message.indexName !== "") {
      obj.indexName = message.indexName;
    }
    if (message.source !== undefined) {
      obj.source = message.source;
    }
    if (message.sourcetype !== undefined) {
      obj.sourcetype = message.sourcetype;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CollectorInputRegistration>, I>>(base?: I): CollectorInputRegistration {
    return CollectorInputRegistration.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CollectorInputRegistration>, I>>(object: I): CollectorInputRegistration {
    const message = createBaseCollectorInputRegistration();
    message.inputId = object.inputId ?? "";
    message.inputType = object.inputType ?? 0;
    message.indexName = object.indexName ?? "";
    message.source = object.source ?? undefined;
    message.sourcetype = object.sourcetype ?? undefined;
    return message;
  },
};

function createBaseCollectorHello(): CollectorHello {
  return {
    collectorId: "",
    instanceId: "",
    protocolMajor: 0,
    protocolMinor: 0,
    collectorVersion: "",
    hostname: "",
    operatingSystem: "",
    architecture: "",
    startedAt: undefined,
    capabilities: [],
    inputs: [],
    lastAcknowledgedBatchSequence: undefined,
  };
}

export const CollectorHello: MessageFns<CollectorHello> = {
  encode(message: CollectorHello, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.collectorId !== "") {
      writer.uint32(10).string(message.collectorId);
    }
    if (message.instanceId !== "") {
      writer.uint32(18).string(message.instanceId);
    }
    if (message.protocolMajor !== 0) {
      writer.uint32(24).uint32(message.protocolMajor);
    }
    if (message.protocolMinor !== 0) {
      writer.uint32(32).uint32(message.protocolMinor);
    }
    if (message.collectorVersion !== "") {
      writer.uint32(42).string(message.collectorVersion);
    }
    if (message.hostname !== "") {
      writer.uint32(50).string(message.hostname);
    }
    if (message.operatingSystem !== "") {
      writer.uint32(58).string(message.operatingSystem);
    }
    if (message.architecture !== "") {
      writer.uint32(66).string(message.architecture);
    }
    if (message.startedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startedAt), writer.uint32(74).fork()).join();
    }
    writer.uint32(82).fork();
    for (const v of message.capabilities) {
      writer.int32(v);
    }
    writer.join();
    for (const v of message.inputs) {
      CollectorInputRegistration.encode(v!, writer.uint32(90).fork()).join();
    }
    if (message.lastAcknowledgedBatchSequence !== undefined) {
      if (BigInt.asUintN(64, message.lastAcknowledgedBatchSequence) !== message.lastAcknowledgedBatchSequence) {
        throw new globalThis.Error(
          "value provided for field message.lastAcknowledgedBatchSequence of type uint64 too large",
        );
      }
      writer.uint32(96).uint64(message.lastAcknowledgedBatchSequence);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CollectorHello {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCollectorHello();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.collectorId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.instanceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.protocolMajor = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.protocolMinor = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.collectorVersion = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.hostname = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.operatingSystem = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.architecture = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.startedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag === 80) {
            message.capabilities.push(reader.int32() as any);

            continue;
          }

          if (tag === 82) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.capabilities.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.inputs.push(CollectorInputRegistration.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.lastAcknowledgedBatchSequence = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CollectorHello {
    return {
      collectorId: isSet(object.collectorId)
        ? globalThis.String(object.collectorId)
        : isSet(object.collector_id)
        ? globalThis.String(object.collector_id)
        : "",
      instanceId: isSet(object.instanceId)
        ? globalThis.String(object.instanceId)
        : isSet(object.instance_id)
        ? globalThis.String(object.instance_id)
        : "",
      protocolMajor: isSet(object.protocolMajor)
        ? globalThis.Number(object.protocolMajor)
        : isSet(object.protocol_major)
        ? globalThis.Number(object.protocol_major)
        : 0,
      protocolMinor: isSet(object.protocolMinor)
        ? globalThis.Number(object.protocolMinor)
        : isSet(object.protocol_minor)
        ? globalThis.Number(object.protocol_minor)
        : 0,
      collectorVersion: isSet(object.collectorVersion)
        ? globalThis.String(object.collectorVersion)
        : isSet(object.collector_version)
        ? globalThis.String(object.collector_version)
        : "",
      hostname: isSet(object.hostname) ? globalThis.String(object.hostname) : "",
      operatingSystem: isSet(object.operatingSystem)
        ? globalThis.String(object.operatingSystem)
        : isSet(object.operating_system)
        ? globalThis.String(object.operating_system)
        : "",
      architecture: isSet(object.architecture) ? globalThis.String(object.architecture) : "",
      startedAt: isSet(object.startedAt)
        ? fromJsonTimestamp(object.startedAt)
        : isSet(object.started_at)
        ? fromJsonTimestamp(object.started_at)
        : undefined,
      capabilities: globalThis.Array.isArray(object?.capabilities)
        ? object.capabilities.map((e: any) => collectorCapabilityFromJSON(e))
        : [],
      inputs: globalThis.Array.isArray(object?.inputs)
        ? object.inputs.map((e: any) => CollectorInputRegistration.fromJSON(e))
        : [],
      lastAcknowledgedBatchSequence: isSet(object.lastAcknowledgedBatchSequence)
        ? BigInt(object.lastAcknowledgedBatchSequence)
        : isSet(object.last_acknowledged_batch_sequence)
        ? BigInt(object.last_acknowledged_batch_sequence)
        : undefined,
    };
  },

  toJSON(message: CollectorHello): unknown {
    const obj: any = {};
    if (message.collectorId !== "") {
      obj.collectorId = message.collectorId;
    }
    if (message.instanceId !== "") {
      obj.instanceId = message.instanceId;
    }
    if (message.protocolMajor !== 0) {
      obj.protocolMajor = Math.round(message.protocolMajor);
    }
    if (message.protocolMinor !== 0) {
      obj.protocolMinor = Math.round(message.protocolMinor);
    }
    if (message.collectorVersion !== "") {
      obj.collectorVersion = message.collectorVersion;
    }
    if (message.hostname !== "") {
      obj.hostname = message.hostname;
    }
    if (message.operatingSystem !== "") {
      obj.operatingSystem = message.operatingSystem;
    }
    if (message.architecture !== "") {
      obj.architecture = message.architecture;
    }
    if (message.startedAt !== undefined) {
      obj.startedAt = message.startedAt.toISOString();
    }
    if (message.capabilities?.length) {
      obj.capabilities = message.capabilities.map((e) => collectorCapabilityToJSON(e));
    }
    if (message.inputs?.length) {
      obj.inputs = message.inputs.map((e) => CollectorInputRegistration.toJSON(e));
    }
    if (message.lastAcknowledgedBatchSequence !== undefined) {
      obj.lastAcknowledgedBatchSequence = message.lastAcknowledgedBatchSequence.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CollectorHello>, I>>(base?: I): CollectorHello {
    return CollectorHello.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CollectorHello>, I>>(object: I): CollectorHello {
    const message = createBaseCollectorHello();
    message.collectorId = object.collectorId ?? "";
    message.instanceId = object.instanceId ?? "";
    message.protocolMajor = object.protocolMajor ?? 0;
    message.protocolMinor = object.protocolMinor ?? 0;
    message.collectorVersion = object.collectorVersion ?? "";
    message.hostname = object.hostname ?? "";
    message.operatingSystem = object.operatingSystem ?? "";
    message.architecture = object.architecture ?? "";
    message.startedAt = object.startedAt ?? undefined;
    message.capabilities = object.capabilities?.map((e) => e) || [];
    message.inputs = object.inputs?.map((e) => CollectorInputRegistration.fromPartial(e)) || [];
    message.lastAcknowledgedBatchSequence =
      (object.lastAcknowledgedBatchSequence !== undefined && object.lastAcknowledgedBatchSequence !== null)
        ? BigInt(object.lastAcknowledgedBatchSequence)
        : undefined;
    return message;
  },
};

function createBaseEventBatch(): EventBatch {
  return {
    collectorId: "",
    batchId: "",
    batchSequence: 0n,
    createdAt: undefined,
    events: [],
    uncompressedSizeBytes: 0n,
    eventIdsSha256: new Uint8Array(0),
    protocolMajor: 0,
    protocolMinor: 0,
  };
}

export const EventBatch: MessageFns<EventBatch> = {
  encode(message: EventBatch, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.collectorId !== "") {
      writer.uint32(10).string(message.collectorId);
    }
    if (message.batchId !== "") {
      writer.uint32(18).string(message.batchId);
    }
    if (message.batchSequence !== 0n) {
      if (BigInt.asUintN(64, message.batchSequence) !== message.batchSequence) {
        throw new globalThis.Error("value provided for field message.batchSequence of type uint64 too large");
      }
      writer.uint32(24).uint64(message.batchSequence);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(34).fork()).join();
    }
    for (const v of message.events) {
      LogEvent.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.uncompressedSizeBytes !== 0n) {
      if (BigInt.asUintN(64, message.uncompressedSizeBytes) !== message.uncompressedSizeBytes) {
        throw new globalThis.Error("value provided for field message.uncompressedSizeBytes of type uint64 too large");
      }
      writer.uint32(48).uint64(message.uncompressedSizeBytes);
    }
    if (message.eventIdsSha256.length !== 0) {
      writer.uint32(58).bytes(message.eventIdsSha256);
    }
    if (message.protocolMajor !== 0) {
      writer.uint32(64).uint32(message.protocolMajor);
    }
    if (message.protocolMinor !== 0) {
      writer.uint32(72).uint32(message.protocolMinor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventBatch {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventBatch();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.collectorId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.batchId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.batchSequence = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.events.push(LogEvent.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.uncompressedSizeBytes = reader.uint64() as bigint;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.eventIdsSha256 = reader.bytes();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.protocolMajor = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.protocolMinor = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventBatch {
    return {
      collectorId: isSet(object.collectorId)
        ? globalThis.String(object.collectorId)
        : isSet(object.collector_id)
        ? globalThis.String(object.collector_id)
        : "",
      batchId: isSet(object.batchId)
        ? globalThis.String(object.batchId)
        : isSet(object.batch_id)
        ? globalThis.String(object.batch_id)
        : "",
      batchSequence: isSet(object.batchSequence)
        ? BigInt(object.batchSequence)
        : isSet(object.batch_sequence)
        ? BigInt(object.batch_sequence)
        : 0n,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      events: globalThis.Array.isArray(object?.events)
        ? object.events.map((e: any) => LogEvent.fromJSON(e))
        : [],
      uncompressedSizeBytes: isSet(object.uncompressedSizeBytes)
        ? BigInt(object.uncompressedSizeBytes)
        : isSet(object.uncompressed_size_bytes)
        ? BigInt(object.uncompressed_size_bytes)
        : 0n,
      eventIdsSha256: isSet(object.eventIdsSha256)
        ? bytesFromBase64(object.eventIdsSha256)
        : isSet(object.event_ids_sha256)
        ? bytesFromBase64(object.event_ids_sha256)
        : new Uint8Array(0),
      protocolMajor: isSet(object.protocolMajor)
        ? globalThis.Number(object.protocolMajor)
        : isSet(object.protocol_major)
        ? globalThis.Number(object.protocol_major)
        : 0,
      protocolMinor: isSet(object.protocolMinor)
        ? globalThis.Number(object.protocolMinor)
        : isSet(object.protocol_minor)
        ? globalThis.Number(object.protocol_minor)
        : 0,
    };
  },

  toJSON(message: EventBatch): unknown {
    const obj: any = {};
    if (message.collectorId !== "") {
      obj.collectorId = message.collectorId;
    }
    if (message.batchId !== "") {
      obj.batchId = message.batchId;
    }
    if (message.batchSequence !== 0n) {
      obj.batchSequence = message.batchSequence.toString();
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.events?.length) {
      obj.events = message.events.map((e) => LogEvent.toJSON(e));
    }
    if (message.uncompressedSizeBytes !== 0n) {
      obj.uncompressedSizeBytes = message.uncompressedSizeBytes.toString();
    }
    if (message.eventIdsSha256.length !== 0) {
      obj.eventIdsSha256 = base64FromBytes(message.eventIdsSha256);
    }
    if (message.protocolMajor !== 0) {
      obj.protocolMajor = Math.round(message.protocolMajor);
    }
    if (message.protocolMinor !== 0) {
      obj.protocolMinor = Math.round(message.protocolMinor);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventBatch>, I>>(base?: I): EventBatch {
    return EventBatch.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventBatch>, I>>(object: I): EventBatch {
    const message = createBaseEventBatch();
    message.collectorId = object.collectorId ?? "";
    message.batchId = object.batchId ?? "";
    message.batchSequence = (object.batchSequence !== undefined && object.batchSequence !== null)
      ? BigInt(object.batchSequence)
      : 0n;
    message.createdAt = object.createdAt ?? undefined;
    message.events = object.events?.map((e) => LogEvent.fromPartial(e)) || [];
    message.uncompressedSizeBytes =
      (object.uncompressedSizeBytes !== undefined && object.uncompressedSizeBytes !== null)
        ? BigInt(object.uncompressedSizeBytes)
        : 0n;
    message.eventIdsSha256 = object.eventIdsSha256 ?? new Uint8Array(0);
    message.protocolMajor = object.protocolMajor ?? 0;
    message.protocolMinor = object.protocolMinor ?? 0;
    return message;
  },
};

function createBaseCollectorQueueStats(): CollectorQueueStats {
  return {
    queuedEvents: 0n,
    queuedBytes: 0n,
    oldestEventAge: undefined,
    sentEventsTotal: 0n,
    acknowledgedEventsTotal: 0n,
    retriedBatchesTotal: 0n,
    rejectedEventsTotal: 0n,
    droppedEventsTotal: 0n,
  };
}

export const CollectorQueueStats: MessageFns<CollectorQueueStats> = {
  encode(message: CollectorQueueStats, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.queuedEvents !== 0n) {
      if (BigInt.asUintN(64, message.queuedEvents) !== message.queuedEvents) {
        throw new globalThis.Error("value provided for field message.queuedEvents of type uint64 too large");
      }
      writer.uint32(8).uint64(message.queuedEvents);
    }
    if (message.queuedBytes !== 0n) {
      if (BigInt.asUintN(64, message.queuedBytes) !== message.queuedBytes) {
        throw new globalThis.Error("value provided for field message.queuedBytes of type uint64 too large");
      }
      writer.uint32(16).uint64(message.queuedBytes);
    }
    if (message.oldestEventAge !== undefined) {
      Duration.encode(message.oldestEventAge, writer.uint32(26).fork()).join();
    }
    if (message.sentEventsTotal !== 0n) {
      if (BigInt.asUintN(64, message.sentEventsTotal) !== message.sentEventsTotal) {
        throw new globalThis.Error("value provided for field message.sentEventsTotal of type uint64 too large");
      }
      writer.uint32(32).uint64(message.sentEventsTotal);
    }
    if (message.acknowledgedEventsTotal !== 0n) {
      if (BigInt.asUintN(64, message.acknowledgedEventsTotal) !== message.acknowledgedEventsTotal) {
        throw new globalThis.Error("value provided for field message.acknowledgedEventsTotal of type uint64 too large");
      }
      writer.uint32(40).uint64(message.acknowledgedEventsTotal);
    }
    if (message.retriedBatchesTotal !== 0n) {
      if (BigInt.asUintN(64, message.retriedBatchesTotal) !== message.retriedBatchesTotal) {
        throw new globalThis.Error("value provided for field message.retriedBatchesTotal of type uint64 too large");
      }
      writer.uint32(48).uint64(message.retriedBatchesTotal);
    }
    if (message.rejectedEventsTotal !== 0n) {
      if (BigInt.asUintN(64, message.rejectedEventsTotal) !== message.rejectedEventsTotal) {
        throw new globalThis.Error("value provided for field message.rejectedEventsTotal of type uint64 too large");
      }
      writer.uint32(56).uint64(message.rejectedEventsTotal);
    }
    if (message.droppedEventsTotal !== 0n) {
      if (BigInt.asUintN(64, message.droppedEventsTotal) !== message.droppedEventsTotal) {
        throw new globalThis.Error("value provided for field message.droppedEventsTotal of type uint64 too large");
      }
      writer.uint32(64).uint64(message.droppedEventsTotal);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CollectorQueueStats {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCollectorQueueStats();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.queuedEvents = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.queuedBytes = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.oldestEventAge = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.sentEventsTotal = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.acknowledgedEventsTotal = reader.uint64() as bigint;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.retriedBatchesTotal = reader.uint64() as bigint;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.rejectedEventsTotal = reader.uint64() as bigint;
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.droppedEventsTotal = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CollectorQueueStats {
    return {
      queuedEvents: isSet(object.queuedEvents)
        ? BigInt(object.queuedEvents)
        : isSet(object.queued_events)
        ? BigInt(object.queued_events)
        : 0n,
      queuedBytes: isSet(object.queuedBytes)
        ? BigInt(object.queuedBytes)
        : isSet(object.queued_bytes)
        ? BigInt(object.queued_bytes)
        : 0n,
      oldestEventAge: isSet(object.oldestEventAge)
        ? Duration.fromJSON(object.oldestEventAge)
        : isSet(object.oldest_event_age)
        ? Duration.fromJSON(object.oldest_event_age)
        : undefined,
      sentEventsTotal: isSet(object.sentEventsTotal)
        ? BigInt(object.sentEventsTotal)
        : isSet(object.sent_events_total)
        ? BigInt(object.sent_events_total)
        : 0n,
      acknowledgedEventsTotal: isSet(object.acknowledgedEventsTotal)
        ? BigInt(object.acknowledgedEventsTotal)
        : isSet(object.acknowledged_events_total)
        ? BigInt(object.acknowledged_events_total)
        : 0n,
      retriedBatchesTotal: isSet(object.retriedBatchesTotal)
        ? BigInt(object.retriedBatchesTotal)
        : isSet(object.retried_batches_total)
        ? BigInt(object.retried_batches_total)
        : 0n,
      rejectedEventsTotal: isSet(object.rejectedEventsTotal)
        ? BigInt(object.rejectedEventsTotal)
        : isSet(object.rejected_events_total)
        ? BigInt(object.rejected_events_total)
        : 0n,
      droppedEventsTotal: isSet(object.droppedEventsTotal)
        ? BigInt(object.droppedEventsTotal)
        : isSet(object.dropped_events_total)
        ? BigInt(object.dropped_events_total)
        : 0n,
    };
  },

  toJSON(message: CollectorQueueStats): unknown {
    const obj: any = {};
    if (message.queuedEvents !== 0n) {
      obj.queuedEvents = message.queuedEvents.toString();
    }
    if (message.queuedBytes !== 0n) {
      obj.queuedBytes = message.queuedBytes.toString();
    }
    if (message.oldestEventAge !== undefined) {
      obj.oldestEventAge = Duration.toJSON(message.oldestEventAge);
    }
    if (message.sentEventsTotal !== 0n) {
      obj.sentEventsTotal = message.sentEventsTotal.toString();
    }
    if (message.acknowledgedEventsTotal !== 0n) {
      obj.acknowledgedEventsTotal = message.acknowledgedEventsTotal.toString();
    }
    if (message.retriedBatchesTotal !== 0n) {
      obj.retriedBatchesTotal = message.retriedBatchesTotal.toString();
    }
    if (message.rejectedEventsTotal !== 0n) {
      obj.rejectedEventsTotal = message.rejectedEventsTotal.toString();
    }
    if (message.droppedEventsTotal !== 0n) {
      obj.droppedEventsTotal = message.droppedEventsTotal.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CollectorQueueStats>, I>>(base?: I): CollectorQueueStats {
    return CollectorQueueStats.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CollectorQueueStats>, I>>(object: I): CollectorQueueStats {
    const message = createBaseCollectorQueueStats();
    message.queuedEvents = (object.queuedEvents !== undefined && object.queuedEvents !== null)
      ? BigInt(object.queuedEvents)
      : 0n;
    message.queuedBytes = (object.queuedBytes !== undefined && object.queuedBytes !== null)
      ? BigInt(object.queuedBytes)
      : 0n;
    message.oldestEventAge = (object.oldestEventAge !== undefined && object.oldestEventAge !== null)
      ? Duration.fromPartial(object.oldestEventAge)
      : undefined;
    message.sentEventsTotal = (object.sentEventsTotal !== undefined && object.sentEventsTotal !== null)
      ? BigInt(object.sentEventsTotal)
      : 0n;
    message.acknowledgedEventsTotal =
      (object.acknowledgedEventsTotal !== undefined && object.acknowledgedEventsTotal !== null)
        ? BigInt(object.acknowledgedEventsTotal)
        : 0n;
    message.retriedBatchesTotal = (object.retriedBatchesTotal !== undefined && object.retriedBatchesTotal !== null)
      ? BigInt(object.retriedBatchesTotal)
      : 0n;
    message.rejectedEventsTotal = (object.rejectedEventsTotal !== undefined && object.rejectedEventsTotal !== null)
      ? BigInt(object.rejectedEventsTotal)
      : 0n;
    message.droppedEventsTotal = (object.droppedEventsTotal !== undefined && object.droppedEventsTotal !== null)
      ? BigInt(object.droppedEventsTotal)
      : 0n;
    return message;
  },
};

function createBaseCollectorInputHealth(): CollectorInputHealth {
  return {
    inputId: "",
    state: 0,
    statusMessage: "",
    discoveredSources: 0n,
    activeSources: 0n,
    eventsReadTotal: 0n,
    bytesReadTotal: 0n,
    lastEventAt: undefined,
    lastErrorAt: undefined,
  };
}

export const CollectorInputHealth: MessageFns<CollectorInputHealth> = {
  encode(message: CollectorInputHealth, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.inputId !== "") {
      writer.uint32(10).string(message.inputId);
    }
    if (message.state !== 0) {
      writer.uint32(16).int32(message.state);
    }
    if (message.statusMessage !== "") {
      writer.uint32(26).string(message.statusMessage);
    }
    if (message.discoveredSources !== 0n) {
      if (BigInt.asUintN(64, message.discoveredSources) !== message.discoveredSources) {
        throw new globalThis.Error("value provided for field message.discoveredSources of type uint64 too large");
      }
      writer.uint32(32).uint64(message.discoveredSources);
    }
    if (message.activeSources !== 0n) {
      if (BigInt.asUintN(64, message.activeSources) !== message.activeSources) {
        throw new globalThis.Error("value provided for field message.activeSources of type uint64 too large");
      }
      writer.uint32(40).uint64(message.activeSources);
    }
    if (message.eventsReadTotal !== 0n) {
      if (BigInt.asUintN(64, message.eventsReadTotal) !== message.eventsReadTotal) {
        throw new globalThis.Error("value provided for field message.eventsReadTotal of type uint64 too large");
      }
      writer.uint32(48).uint64(message.eventsReadTotal);
    }
    if (message.bytesReadTotal !== 0n) {
      if (BigInt.asUintN(64, message.bytesReadTotal) !== message.bytesReadTotal) {
        throw new globalThis.Error("value provided for field message.bytesReadTotal of type uint64 too large");
      }
      writer.uint32(56).uint64(message.bytesReadTotal);
    }
    if (message.lastEventAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastEventAt), writer.uint32(66).fork()).join();
    }
    if (message.lastErrorAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastErrorAt), writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CollectorInputHealth {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCollectorInputHealth();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.inputId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.statusMessage = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.discoveredSources = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.activeSources = reader.uint64() as bigint;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.eventsReadTotal = reader.uint64() as bigint;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.bytesReadTotal = reader.uint64() as bigint;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.lastEventAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.lastErrorAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CollectorInputHealth {
    return {
      inputId: isSet(object.inputId)
        ? globalThis.String(object.inputId)
        : isSet(object.input_id)
        ? globalThis.String(object.input_id)
        : "",
      state: isSet(object.state) ? collectorInputStateFromJSON(object.state) : 0,
      statusMessage: isSet(object.statusMessage)
        ? globalThis.String(object.statusMessage)
        : isSet(object.status_message)
        ? globalThis.String(object.status_message)
        : "",
      discoveredSources: isSet(object.discoveredSources)
        ? BigInt(object.discoveredSources)
        : isSet(object.discovered_sources)
        ? BigInt(object.discovered_sources)
        : 0n,
      activeSources: isSet(object.activeSources)
        ? BigInt(object.activeSources)
        : isSet(object.active_sources)
        ? BigInt(object.active_sources)
        : 0n,
      eventsReadTotal: isSet(object.eventsReadTotal)
        ? BigInt(object.eventsReadTotal)
        : isSet(object.events_read_total)
        ? BigInt(object.events_read_total)
        : 0n,
      bytesReadTotal: isSet(object.bytesReadTotal)
        ? BigInt(object.bytesReadTotal)
        : isSet(object.bytes_read_total)
        ? BigInt(object.bytes_read_total)
        : 0n,
      lastEventAt: isSet(object.lastEventAt)
        ? fromJsonTimestamp(object.lastEventAt)
        : isSet(object.last_event_at)
        ? fromJsonTimestamp(object.last_event_at)
        : undefined,
      lastErrorAt: isSet(object.lastErrorAt)
        ? fromJsonTimestamp(object.lastErrorAt)
        : isSet(object.last_error_at)
        ? fromJsonTimestamp(object.last_error_at)
        : undefined,
    };
  },

  toJSON(message: CollectorInputHealth): unknown {
    const obj: any = {};
    if (message.inputId !== "") {
      obj.inputId = message.inputId;
    }
    if (message.state !== 0) {
      obj.state = collectorInputStateToJSON(message.state);
    }
    if (message.statusMessage !== "") {
      obj.statusMessage = message.statusMessage;
    }
    if (message.discoveredSources !== 0n) {
      obj.discoveredSources = message.discoveredSources.toString();
    }
    if (message.activeSources !== 0n) {
      obj.activeSources = message.activeSources.toString();
    }
    if (message.eventsReadTotal !== 0n) {
      obj.eventsReadTotal = message.eventsReadTotal.toString();
    }
    if (message.bytesReadTotal !== 0n) {
      obj.bytesReadTotal = message.bytesReadTotal.toString();
    }
    if (message.lastEventAt !== undefined) {
      obj.lastEventAt = message.lastEventAt.toISOString();
    }
    if (message.lastErrorAt !== undefined) {
      obj.lastErrorAt = message.lastErrorAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CollectorInputHealth>, I>>(base?: I): CollectorInputHealth {
    return CollectorInputHealth.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CollectorInputHealth>, I>>(object: I): CollectorInputHealth {
    const message = createBaseCollectorInputHealth();
    message.inputId = object.inputId ?? "";
    message.state = object.state ?? 0;
    message.statusMessage = object.statusMessage ?? "";
    message.discoveredSources = (object.discoveredSources !== undefined && object.discoveredSources !== null)
      ? BigInt(object.discoveredSources)
      : 0n;
    message.activeSources = (object.activeSources !== undefined && object.activeSources !== null)
      ? BigInt(object.activeSources)
      : 0n;
    message.eventsReadTotal = (object.eventsReadTotal !== undefined && object.eventsReadTotal !== null)
      ? BigInt(object.eventsReadTotal)
      : 0n;
    message.bytesReadTotal = (object.bytesReadTotal !== undefined && object.bytesReadTotal !== null)
      ? BigInt(object.bytesReadTotal)
      : 0n;
    message.lastEventAt = object.lastEventAt ?? undefined;
    message.lastErrorAt = object.lastErrorAt ?? undefined;
    return message;
  },
};

function createBaseCollectorHeartbeat(): CollectorHeartbeat {
  return {
    collectorId: "",
    instanceId: "",
    observedAt: undefined,
    queue: undefined,
    inputs: [],
    lastSentBatchSequence: undefined,
    lastAcknowledgedBatchSequence: undefined,
    processResidentMemoryBytes: 0n,
    processCpuPercent: 0,
  };
}

export const CollectorHeartbeat: MessageFns<CollectorHeartbeat> = {
  encode(message: CollectorHeartbeat, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.collectorId !== "") {
      writer.uint32(10).string(message.collectorId);
    }
    if (message.instanceId !== "") {
      writer.uint32(18).string(message.instanceId);
    }
    if (message.observedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.observedAt), writer.uint32(26).fork()).join();
    }
    if (message.queue !== undefined) {
      CollectorQueueStats.encode(message.queue, writer.uint32(34).fork()).join();
    }
    for (const v of message.inputs) {
      CollectorInputHealth.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.lastSentBatchSequence !== undefined) {
      if (BigInt.asUintN(64, message.lastSentBatchSequence) !== message.lastSentBatchSequence) {
        throw new globalThis.Error("value provided for field message.lastSentBatchSequence of type uint64 too large");
      }
      writer.uint32(48).uint64(message.lastSentBatchSequence);
    }
    if (message.lastAcknowledgedBatchSequence !== undefined) {
      if (BigInt.asUintN(64, message.lastAcknowledgedBatchSequence) !== message.lastAcknowledgedBatchSequence) {
        throw new globalThis.Error(
          "value provided for field message.lastAcknowledgedBatchSequence of type uint64 too large",
        );
      }
      writer.uint32(56).uint64(message.lastAcknowledgedBatchSequence);
    }
    if (message.processResidentMemoryBytes !== 0n) {
      if (BigInt.asUintN(64, message.processResidentMemoryBytes) !== message.processResidentMemoryBytes) {
        throw new globalThis.Error(
          "value provided for field message.processResidentMemoryBytes of type uint64 too large",
        );
      }
      writer.uint32(64).uint64(message.processResidentMemoryBytes);
    }
    if (message.processCpuPercent !== 0) {
      writer.uint32(73).double(message.processCpuPercent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CollectorHeartbeat {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCollectorHeartbeat();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.collectorId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.instanceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.observedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.queue = CollectorQueueStats.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.inputs.push(CollectorInputHealth.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.lastSentBatchSequence = reader.uint64() as bigint;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.lastAcknowledgedBatchSequence = reader.uint64() as bigint;
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.processResidentMemoryBytes = reader.uint64() as bigint;
          continue;
        }
        case 9: {
          if (tag !== 73) {
            break;
          }

          message.processCpuPercent = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CollectorHeartbeat {
    return {
      collectorId: isSet(object.collectorId)
        ? globalThis.String(object.collectorId)
        : isSet(object.collector_id)
        ? globalThis.String(object.collector_id)
        : "",
      instanceId: isSet(object.instanceId)
        ? globalThis.String(object.instanceId)
        : isSet(object.instance_id)
        ? globalThis.String(object.instance_id)
        : "",
      observedAt: isSet(object.observedAt)
        ? fromJsonTimestamp(object.observedAt)
        : isSet(object.observed_at)
        ? fromJsonTimestamp(object.observed_at)
        : undefined,
      queue: isSet(object.queue) ? CollectorQueueStats.fromJSON(object.queue) : undefined,
      inputs: globalThis.Array.isArray(object?.inputs)
        ? object.inputs.map((e: any) => CollectorInputHealth.fromJSON(e))
        : [],
      lastSentBatchSequence: isSet(object.lastSentBatchSequence)
        ? BigInt(object.lastSentBatchSequence)
        : isSet(object.last_sent_batch_sequence)
        ? BigInt(object.last_sent_batch_sequence)
        : undefined,
      lastAcknowledgedBatchSequence: isSet(object.lastAcknowledgedBatchSequence)
        ? BigInt(object.lastAcknowledgedBatchSequence)
        : isSet(object.last_acknowledged_batch_sequence)
        ? BigInt(object.last_acknowledged_batch_sequence)
        : undefined,
      processResidentMemoryBytes: isSet(object.processResidentMemoryBytes)
        ? BigInt(object.processResidentMemoryBytes)
        : isSet(object.process_resident_memory_bytes)
        ? BigInt(object.process_resident_memory_bytes)
        : 0n,
      processCpuPercent: isSet(object.processCpuPercent)
        ? globalThis.Number(object.processCpuPercent)
        : isSet(object.process_cpu_percent)
        ? globalThis.Number(object.process_cpu_percent)
        : 0,
    };
  },

  toJSON(message: CollectorHeartbeat): unknown {
    const obj: any = {};
    if (message.collectorId !== "") {
      obj.collectorId = message.collectorId;
    }
    if (message.instanceId !== "") {
      obj.instanceId = message.instanceId;
    }
    if (message.observedAt !== undefined) {
      obj.observedAt = message.observedAt.toISOString();
    }
    if (message.queue !== undefined) {
      obj.queue = CollectorQueueStats.toJSON(message.queue);
    }
    if (message.inputs?.length) {
      obj.inputs = message.inputs.map((e) => CollectorInputHealth.toJSON(e));
    }
    if (message.lastSentBatchSequence !== undefined) {
      obj.lastSentBatchSequence = message.lastSentBatchSequence.toString();
    }
    if (message.lastAcknowledgedBatchSequence !== undefined) {
      obj.lastAcknowledgedBatchSequence = message.lastAcknowledgedBatchSequence.toString();
    }
    if (message.processResidentMemoryBytes !== 0n) {
      obj.processResidentMemoryBytes = message.processResidentMemoryBytes.toString();
    }
    if (message.processCpuPercent !== 0) {
      obj.processCpuPercent = message.processCpuPercent;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CollectorHeartbeat>, I>>(base?: I): CollectorHeartbeat {
    return CollectorHeartbeat.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CollectorHeartbeat>, I>>(object: I): CollectorHeartbeat {
    const message = createBaseCollectorHeartbeat();
    message.collectorId = object.collectorId ?? "";
    message.instanceId = object.instanceId ?? "";
    message.observedAt = object.observedAt ?? undefined;
    message.queue = (object.queue !== undefined && object.queue !== null)
      ? CollectorQueueStats.fromPartial(object.queue)
      : undefined;
    message.inputs = object.inputs?.map((e) => CollectorInputHealth.fromPartial(e)) || [];
    message.lastSentBatchSequence =
      (object.lastSentBatchSequence !== undefined && object.lastSentBatchSequence !== null)
        ? BigInt(object.lastSentBatchSequence)
        : undefined;
    message.lastAcknowledgedBatchSequence =
      (object.lastAcknowledgedBatchSequence !== undefined && object.lastAcknowledgedBatchSequence !== null)
        ? BigInt(object.lastAcknowledgedBatchSequence)
        : undefined;
    message.processResidentMemoryBytes =
      (object.processResidentMemoryBytes !== undefined && object.processResidentMemoryBytes !== null)
        ? BigInt(object.processResidentMemoryBytes)
        : 0n;
    message.processCpuPercent = object.processCpuPercent ?? 0;
    return message;
  },
};

function createBaseCollectorGoodbye(): CollectorGoodbye {
  return { reason: 0, message: undefined };
}

export const CollectorGoodbye: MessageFns<CollectorGoodbye> = {
  encode(message: CollectorGoodbye, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reason !== 0) {
      writer.uint32(8).int32(message.reason);
    }
    if (message.message !== undefined) {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CollectorGoodbye {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCollectorGoodbye();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CollectorGoodbye {
    return {
      reason: isSet(object.reason) ? collectorGoodbyeReasonFromJSON(object.reason) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : undefined,
    };
  },

  toJSON(message: CollectorGoodbye): unknown {
    const obj: any = {};
    if (message.reason !== 0) {
      obj.reason = collectorGoodbyeReasonToJSON(message.reason);
    }
    if (message.message !== undefined) {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CollectorGoodbye>, I>>(base?: I): CollectorGoodbye {
    return CollectorGoodbye.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CollectorGoodbye>, I>>(object: I): CollectorGoodbye {
    const message = createBaseCollectorGoodbye();
    message.reason = object.reason ?? 0;
    message.message = object.message ?? undefined;
    return message;
  },
};

function createBaseCollectRequest(): CollectRequest {
  return { streamSequence: 0n, sentAt: undefined, payload: undefined };
}

export const CollectRequest: MessageFns<CollectRequest> = {
  encode(message: CollectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.streamSequence !== 0n) {
      if (BigInt.asUintN(64, message.streamSequence) !== message.streamSequence) {
        throw new globalThis.Error("value provided for field message.streamSequence of type uint64 too large");
      }
      writer.uint32(8).uint64(message.streamSequence);
    }
    if (message.sentAt !== undefined) {
      Timestamp.encode(toTimestamp(message.sentAt), writer.uint32(18).fork()).join();
    }
    switch (message.payload?.$case) {
      case "hello":
        CollectorHello.encode(message.payload.value, writer.uint32(82).fork()).join();
        break;
      case "batch":
        EventBatch.encode(message.payload.value, writer.uint32(90).fork()).join();
        break;
      case "heartbeat":
        CollectorHeartbeat.encode(message.payload.value, writer.uint32(98).fork()).join();
        break;
      case "goodbye":
        CollectorGoodbye.encode(message.payload.value, writer.uint32(106).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CollectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCollectRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.streamSequence = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sentAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.payload = { $case: "hello", value: CollectorHello.decode(reader, reader.uint32()) };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.payload = { $case: "batch", value: EventBatch.decode(reader, reader.uint32()) };
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.payload = { $case: "heartbeat", value: CollectorHeartbeat.decode(reader, reader.uint32()) };
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.payload = { $case: "goodbye", value: CollectorGoodbye.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CollectRequest {
    return {
      streamSequence: isSet(object.streamSequence)
        ? BigInt(object.streamSequence)
        : isSet(object.stream_sequence)
        ? BigInt(object.stream_sequence)
        : 0n,
      sentAt: isSet(object.sentAt)
        ? fromJsonTimestamp(object.sentAt)
        : isSet(object.sent_at)
        ? fromJsonTimestamp(object.sent_at)
        : undefined,
      payload: isSet(object.hello)
        ? { $case: "hello", value: CollectorHello.fromJSON(object.hello) }
        : isSet(object.batch)
        ? { $case: "batch", value: EventBatch.fromJSON(object.batch) }
        : isSet(object.heartbeat)
        ? { $case: "heartbeat", value: CollectorHeartbeat.fromJSON(object.heartbeat) }
        : isSet(object.goodbye)
        ? { $case: "goodbye", value: CollectorGoodbye.fromJSON(object.goodbye) }
        : undefined,
    };
  },

  toJSON(message: CollectRequest): unknown {
    const obj: any = {};
    if (message.streamSequence !== 0n) {
      obj.streamSequence = message.streamSequence.toString();
    }
    if (message.sentAt !== undefined) {
      obj.sentAt = message.sentAt.toISOString();
    }
    if (message.payload?.$case === "hello") {
      obj.hello = CollectorHello.toJSON(message.payload.value);
    } else if (message.payload?.$case === "batch") {
      obj.batch = EventBatch.toJSON(message.payload.value);
    } else if (message.payload?.$case === "heartbeat") {
      obj.heartbeat = CollectorHeartbeat.toJSON(message.payload.value);
    } else if (message.payload?.$case === "goodbye") {
      obj.goodbye = CollectorGoodbye.toJSON(message.payload.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CollectRequest>, I>>(base?: I): CollectRequest {
    return CollectRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CollectRequest>, I>>(object: I): CollectRequest {
    const message = createBaseCollectRequest();
    message.streamSequence = (object.streamSequence !== undefined && object.streamSequence !== null)
      ? BigInt(object.streamSequence)
      : 0n;
    message.sentAt = object.sentAt ?? undefined;
    switch (object.payload?.$case) {
      case "hello": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = { $case: "hello", value: CollectorHello.fromPartial(object.payload.value) };
        }
        break;
      }
      case "batch": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = { $case: "batch", value: EventBatch.fromPartial(object.payload.value) };
        }
        break;
      }
      case "heartbeat": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = { $case: "heartbeat", value: CollectorHeartbeat.fromPartial(object.payload.value) };
        }
        break;
      }
      case "goodbye": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = { $case: "goodbye", value: CollectorGoodbye.fromPartial(object.payload.value) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseCollectorReady(): CollectorReady {
  return {
    streamId: "",
    serverInstanceId: "",
    serverVersion: "",
    protocolMajor: 0,
    protocolMinor: 0,
    serverTime: undefined,
    heartbeatInterval: undefined,
    maxInFlightBatches: 0,
    maxBatchEvents: 0,
    maxBatchBytes: 0n,
    maxEventBytes: 0n,
    authorizedIndexes: [],
    acknowledgmentDurability: 0,
    resumeAfterBatchSequence: undefined,
  };
}

export const CollectorReady: MessageFns<CollectorReady> = {
  encode(message: CollectorReady, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.streamId !== "") {
      writer.uint32(10).string(message.streamId);
    }
    if (message.serverInstanceId !== "") {
      writer.uint32(18).string(message.serverInstanceId);
    }
    if (message.serverVersion !== "") {
      writer.uint32(26).string(message.serverVersion);
    }
    if (message.protocolMajor !== 0) {
      writer.uint32(32).uint32(message.protocolMajor);
    }
    if (message.protocolMinor !== 0) {
      writer.uint32(40).uint32(message.protocolMinor);
    }
    if (message.serverTime !== undefined) {
      Timestamp.encode(toTimestamp(message.serverTime), writer.uint32(50).fork()).join();
    }
    if (message.heartbeatInterval !== undefined) {
      Duration.encode(message.heartbeatInterval, writer.uint32(58).fork()).join();
    }
    if (message.maxInFlightBatches !== 0) {
      writer.uint32(64).uint32(message.maxInFlightBatches);
    }
    if (message.maxBatchEvents !== 0) {
      writer.uint32(72).uint32(message.maxBatchEvents);
    }
    if (message.maxBatchBytes !== 0n) {
      if (BigInt.asUintN(64, message.maxBatchBytes) !== message.maxBatchBytes) {
        throw new globalThis.Error("value provided for field message.maxBatchBytes of type uint64 too large");
      }
      writer.uint32(80).uint64(message.maxBatchBytes);
    }
    if (message.maxEventBytes !== 0n) {
      if (BigInt.asUintN(64, message.maxEventBytes) !== message.maxEventBytes) {
        throw new globalThis.Error("value provided for field message.maxEventBytes of type uint64 too large");
      }
      writer.uint32(88).uint64(message.maxEventBytes);
    }
    for (const v of message.authorizedIndexes) {
      writer.uint32(98).string(v!);
    }
    if (message.acknowledgmentDurability !== 0) {
      writer.uint32(104).int32(message.acknowledgmentDurability);
    }
    if (message.resumeAfterBatchSequence !== undefined) {
      if (BigInt.asUintN(64, message.resumeAfterBatchSequence) !== message.resumeAfterBatchSequence) {
        throw new globalThis.Error(
          "value provided for field message.resumeAfterBatchSequence of type uint64 too large",
        );
      }
      writer.uint32(112).uint64(message.resumeAfterBatchSequence);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CollectorReady {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCollectorReady();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.streamId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.serverInstanceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.serverVersion = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.protocolMajor = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.protocolMinor = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.serverTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.heartbeatInterval = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.maxInFlightBatches = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.maxBatchEvents = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.maxBatchBytes = reader.uint64() as bigint;
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.maxEventBytes = reader.uint64() as bigint;
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.authorizedIndexes.push(reader.string());
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.acknowledgmentDurability = reader.int32() as any;
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.resumeAfterBatchSequence = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CollectorReady {
    return {
      streamId: isSet(object.streamId)
        ? globalThis.String(object.streamId)
        : isSet(object.stream_id)
        ? globalThis.String(object.stream_id)
        : "",
      serverInstanceId: isSet(object.serverInstanceId)
        ? globalThis.String(object.serverInstanceId)
        : isSet(object.server_instance_id)
        ? globalThis.String(object.server_instance_id)
        : "",
      serverVersion: isSet(object.serverVersion)
        ? globalThis.String(object.serverVersion)
        : isSet(object.server_version)
        ? globalThis.String(object.server_version)
        : "",
      protocolMajor: isSet(object.protocolMajor)
        ? globalThis.Number(object.protocolMajor)
        : isSet(object.protocol_major)
        ? globalThis.Number(object.protocol_major)
        : 0,
      protocolMinor: isSet(object.protocolMinor)
        ? globalThis.Number(object.protocolMinor)
        : isSet(object.protocol_minor)
        ? globalThis.Number(object.protocol_minor)
        : 0,
      serverTime: isSet(object.serverTime)
        ? fromJsonTimestamp(object.serverTime)
        : isSet(object.server_time)
        ? fromJsonTimestamp(object.server_time)
        : undefined,
      heartbeatInterval: isSet(object.heartbeatInterval)
        ? Duration.fromJSON(object.heartbeatInterval)
        : isSet(object.heartbeat_interval)
        ? Duration.fromJSON(object.heartbeat_interval)
        : undefined,
      maxInFlightBatches: isSet(object.maxInFlightBatches)
        ? globalThis.Number(object.maxInFlightBatches)
        : isSet(object.max_in_flight_batches)
        ? globalThis.Number(object.max_in_flight_batches)
        : 0,
      maxBatchEvents: isSet(object.maxBatchEvents)
        ? globalThis.Number(object.maxBatchEvents)
        : isSet(object.max_batch_events)
        ? globalThis.Number(object.max_batch_events)
        : 0,
      maxBatchBytes: isSet(object.maxBatchBytes)
        ? BigInt(object.maxBatchBytes)
        : isSet(object.max_batch_bytes)
        ? BigInt(object.max_batch_bytes)
        : 0n,
      maxEventBytes: isSet(object.maxEventBytes)
        ? BigInt(object.maxEventBytes)
        : isSet(object.max_event_bytes)
        ? BigInt(object.max_event_bytes)
        : 0n,
      authorizedIndexes: globalThis.Array.isArray(object?.authorizedIndexes)
        ? object.authorizedIndexes.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.authorized_indexes)
        ? object.authorized_indexes.map((e: any) => globalThis.String(e))
        : [],
      acknowledgmentDurability: isSet(object.acknowledgmentDurability)
        ? ackDurabilityFromJSON(object.acknowledgmentDurability)
        : isSet(object.acknowledgment_durability)
        ? ackDurabilityFromJSON(object.acknowledgment_durability)
        : 0,
      resumeAfterBatchSequence: isSet(object.resumeAfterBatchSequence)
        ? BigInt(object.resumeAfterBatchSequence)
        : isSet(object.resume_after_batch_sequence)
        ? BigInt(object.resume_after_batch_sequence)
        : undefined,
    };
  },

  toJSON(message: CollectorReady): unknown {
    const obj: any = {};
    if (message.streamId !== "") {
      obj.streamId = message.streamId;
    }
    if (message.serverInstanceId !== "") {
      obj.serverInstanceId = message.serverInstanceId;
    }
    if (message.serverVersion !== "") {
      obj.serverVersion = message.serverVersion;
    }
    if (message.protocolMajor !== 0) {
      obj.protocolMajor = Math.round(message.protocolMajor);
    }
    if (message.protocolMinor !== 0) {
      obj.protocolMinor = Math.round(message.protocolMinor);
    }
    if (message.serverTime !== undefined) {
      obj.serverTime = message.serverTime.toISOString();
    }
    if (message.heartbeatInterval !== undefined) {
      obj.heartbeatInterval = Duration.toJSON(message.heartbeatInterval);
    }
    if (message.maxInFlightBatches !== 0) {
      obj.maxInFlightBatches = Math.round(message.maxInFlightBatches);
    }
    if (message.maxBatchEvents !== 0) {
      obj.maxBatchEvents = Math.round(message.maxBatchEvents);
    }
    if (message.maxBatchBytes !== 0n) {
      obj.maxBatchBytes = message.maxBatchBytes.toString();
    }
    if (message.maxEventBytes !== 0n) {
      obj.maxEventBytes = message.maxEventBytes.toString();
    }
    if (message.authorizedIndexes?.length) {
      obj.authorizedIndexes = message.authorizedIndexes;
    }
    if (message.acknowledgmentDurability !== 0) {
      obj.acknowledgmentDurability = ackDurabilityToJSON(message.acknowledgmentDurability);
    }
    if (message.resumeAfterBatchSequence !== undefined) {
      obj.resumeAfterBatchSequence = message.resumeAfterBatchSequence.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CollectorReady>, I>>(base?: I): CollectorReady {
    return CollectorReady.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CollectorReady>, I>>(object: I): CollectorReady {
    const message = createBaseCollectorReady();
    message.streamId = object.streamId ?? "";
    message.serverInstanceId = object.serverInstanceId ?? "";
    message.serverVersion = object.serverVersion ?? "";
    message.protocolMajor = object.protocolMajor ?? 0;
    message.protocolMinor = object.protocolMinor ?? 0;
    message.serverTime = object.serverTime ?? undefined;
    message.heartbeatInterval = (object.heartbeatInterval !== undefined && object.heartbeatInterval !== null)
      ? Duration.fromPartial(object.heartbeatInterval)
      : undefined;
    message.maxInFlightBatches = object.maxInFlightBatches ?? 0;
    message.maxBatchEvents = object.maxBatchEvents ?? 0;
    message.maxBatchBytes = (object.maxBatchBytes !== undefined && object.maxBatchBytes !== null)
      ? BigInt(object.maxBatchBytes)
      : 0n;
    message.maxEventBytes = (object.maxEventBytes !== undefined && object.maxEventBytes !== null)
      ? BigInt(object.maxEventBytes)
      : 0n;
    message.authorizedIndexes = object.authorizedIndexes?.map((e) => e) || [];
    message.acknowledgmentDurability = object.acknowledgmentDurability ?? 0;
    message.resumeAfterBatchSequence =
      (object.resumeAfterBatchSequence !== undefined && object.resumeAfterBatchSequence !== null)
        ? BigInt(object.resumeAfterBatchSequence)
        : undefined;
    return message;
  },
};

function createBaseEventRejection(): EventRejection {
  return { eventIndex: 0, eventId: "", code: 0, message: "", violations: [] };
}

export const EventRejection: MessageFns<EventRejection> = {
  encode(message: EventRejection, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventIndex !== 0) {
      writer.uint32(8).uint32(message.eventIndex);
    }
    if (message.eventId !== "") {
      writer.uint32(18).string(message.eventId);
    }
    if (message.code !== 0) {
      writer.uint32(24).int32(message.code);
    }
    if (message.message !== "") {
      writer.uint32(34).string(message.message);
    }
    for (const v of message.violations) {
      FieldViolation.encode(v!, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventRejection {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventRejection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.eventIndex = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.eventId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.violations.push(FieldViolation.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventRejection {
    return {
      eventIndex: isSet(object.eventIndex)
        ? globalThis.Number(object.eventIndex)
        : isSet(object.event_index)
        ? globalThis.Number(object.event_index)
        : 0,
      eventId: isSet(object.eventId)
        ? globalThis.String(object.eventId)
        : isSet(object.event_id)
        ? globalThis.String(object.event_id)
        : "",
      code: isSet(object.code) ? eventRejectionCodeFromJSON(object.code) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      violations: globalThis.Array.isArray(object?.violations)
        ? object.violations.map((e: any) => FieldViolation.fromJSON(e))
        : [],
    };
  },

  toJSON(message: EventRejection): unknown {
    const obj: any = {};
    if (message.eventIndex !== 0) {
      obj.eventIndex = Math.round(message.eventIndex);
    }
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    if (message.code !== 0) {
      obj.code = eventRejectionCodeToJSON(message.code);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.violations?.length) {
      obj.violations = message.violations.map((e) => FieldViolation.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventRejection>, I>>(base?: I): EventRejection {
    return EventRejection.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventRejection>, I>>(object: I): EventRejection {
    const message = createBaseEventRejection();
    message.eventIndex = object.eventIndex ?? 0;
    message.eventId = object.eventId ?? "";
    message.code = object.code ?? 0;
    message.message = object.message ?? "";
    message.violations = object.violations?.map((e) => FieldViolation.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBatchAck(): BatchAck {
  return {
    batchId: "",
    batchSequence: 0n,
    acknowledgedThroughBatchSequence: undefined,
    durability: 0,
    acceptedEventCount: 0,
    duplicateEventCount: 0,
    rejectedEvents: [],
    committedAt: undefined,
  };
}

export const BatchAck: MessageFns<BatchAck> = {
  encode(message: BatchAck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.batchId !== "") {
      writer.uint32(10).string(message.batchId);
    }
    if (message.batchSequence !== 0n) {
      if (BigInt.asUintN(64, message.batchSequence) !== message.batchSequence) {
        throw new globalThis.Error("value provided for field message.batchSequence of type uint64 too large");
      }
      writer.uint32(16).uint64(message.batchSequence);
    }
    if (message.acknowledgedThroughBatchSequence !== undefined) {
      if (BigInt.asUintN(64, message.acknowledgedThroughBatchSequence) !== message.acknowledgedThroughBatchSequence) {
        throw new globalThis.Error(
          "value provided for field message.acknowledgedThroughBatchSequence of type uint64 too large",
        );
      }
      writer.uint32(24).uint64(message.acknowledgedThroughBatchSequence);
    }
    if (message.durability !== 0) {
      writer.uint32(32).int32(message.durability);
    }
    if (message.acceptedEventCount !== 0) {
      writer.uint32(40).uint32(message.acceptedEventCount);
    }
    if (message.duplicateEventCount !== 0) {
      writer.uint32(48).uint32(message.duplicateEventCount);
    }
    for (const v of message.rejectedEvents) {
      EventRejection.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.committedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.committedAt), writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchAck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.batchId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.batchSequence = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.acknowledgedThroughBatchSequence = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.durability = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.acceptedEventCount = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.duplicateEventCount = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.rejectedEvents.push(EventRejection.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.committedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchAck {
    return {
      batchId: isSet(object.batchId)
        ? globalThis.String(object.batchId)
        : isSet(object.batch_id)
        ? globalThis.String(object.batch_id)
        : "",
      batchSequence: isSet(object.batchSequence)
        ? BigInt(object.batchSequence)
        : isSet(object.batch_sequence)
        ? BigInt(object.batch_sequence)
        : 0n,
      acknowledgedThroughBatchSequence: isSet(object.acknowledgedThroughBatchSequence)
        ? BigInt(object.acknowledgedThroughBatchSequence)
        : isSet(object.acknowledged_through_batch_sequence)
        ? BigInt(object.acknowledged_through_batch_sequence)
        : undefined,
      durability: isSet(object.durability) ? ackDurabilityFromJSON(object.durability) : 0,
      acceptedEventCount: isSet(object.acceptedEventCount)
        ? globalThis.Number(object.acceptedEventCount)
        : isSet(object.accepted_event_count)
        ? globalThis.Number(object.accepted_event_count)
        : 0,
      duplicateEventCount: isSet(object.duplicateEventCount)
        ? globalThis.Number(object.duplicateEventCount)
        : isSet(object.duplicate_event_count)
        ? globalThis.Number(object.duplicate_event_count)
        : 0,
      rejectedEvents: globalThis.Array.isArray(object?.rejectedEvents)
        ? object.rejectedEvents.map((e: any) => EventRejection.fromJSON(e))
        : globalThis.Array.isArray(object?.rejected_events)
        ? object.rejected_events.map((e: any) => EventRejection.fromJSON(e))
        : [],
      committedAt: isSet(object.committedAt)
        ? fromJsonTimestamp(object.committedAt)
        : isSet(object.committed_at)
        ? fromJsonTimestamp(object.committed_at)
        : undefined,
    };
  },

  toJSON(message: BatchAck): unknown {
    const obj: any = {};
    if (message.batchId !== "") {
      obj.batchId = message.batchId;
    }
    if (message.batchSequence !== 0n) {
      obj.batchSequence = message.batchSequence.toString();
    }
    if (message.acknowledgedThroughBatchSequence !== undefined) {
      obj.acknowledgedThroughBatchSequence = message.acknowledgedThroughBatchSequence.toString();
    }
    if (message.durability !== 0) {
      obj.durability = ackDurabilityToJSON(message.durability);
    }
    if (message.acceptedEventCount !== 0) {
      obj.acceptedEventCount = Math.round(message.acceptedEventCount);
    }
    if (message.duplicateEventCount !== 0) {
      obj.duplicateEventCount = Math.round(message.duplicateEventCount);
    }
    if (message.rejectedEvents?.length) {
      obj.rejectedEvents = message.rejectedEvents.map((e) => EventRejection.toJSON(e));
    }
    if (message.committedAt !== undefined) {
      obj.committedAt = message.committedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BatchAck>, I>>(base?: I): BatchAck {
    return BatchAck.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchAck>, I>>(object: I): BatchAck {
    const message = createBaseBatchAck();
    message.batchId = object.batchId ?? "";
    message.batchSequence = (object.batchSequence !== undefined && object.batchSequence !== null)
      ? BigInt(object.batchSequence)
      : 0n;
    message.acknowledgedThroughBatchSequence =
      (object.acknowledgedThroughBatchSequence !== undefined && object.acknowledgedThroughBatchSequence !== null)
        ? BigInt(object.acknowledgedThroughBatchSequence)
        : undefined;
    message.durability = object.durability ?? 0;
    message.acceptedEventCount = object.acceptedEventCount ?? 0;
    message.duplicateEventCount = object.duplicateEventCount ?? 0;
    message.rejectedEvents = object.rejectedEvents?.map((e) => EventRejection.fromPartial(e)) || [];
    message.committedAt = object.committedAt ?? undefined;
    return message;
  },
};

function createBaseBatchReject(): BatchReject {
  return { batchId: "", batchSequence: 0n, code: 0, message: "", violations: [] };
}

export const BatchReject: MessageFns<BatchReject> = {
  encode(message: BatchReject, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.batchId !== "") {
      writer.uint32(10).string(message.batchId);
    }
    if (message.batchSequence !== 0n) {
      if (BigInt.asUintN(64, message.batchSequence) !== message.batchSequence) {
        throw new globalThis.Error("value provided for field message.batchSequence of type uint64 too large");
      }
      writer.uint32(16).uint64(message.batchSequence);
    }
    if (message.code !== 0) {
      writer.uint32(24).int32(message.code);
    }
    if (message.message !== "") {
      writer.uint32(34).string(message.message);
    }
    for (const v of message.violations) {
      FieldViolation.encode(v!, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchReject {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchReject();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.batchId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.batchSequence = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.violations.push(FieldViolation.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchReject {
    return {
      batchId: isSet(object.batchId)
        ? globalThis.String(object.batchId)
        : isSet(object.batch_id)
        ? globalThis.String(object.batch_id)
        : "",
      batchSequence: isSet(object.batchSequence)
        ? BigInt(object.batchSequence)
        : isSet(object.batch_sequence)
        ? BigInt(object.batch_sequence)
        : 0n,
      code: isSet(object.code) ? batchRejectionCodeFromJSON(object.code) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      violations: globalThis.Array.isArray(object?.violations)
        ? object.violations.map((e: any) => FieldViolation.fromJSON(e))
        : [],
    };
  },

  toJSON(message: BatchReject): unknown {
    const obj: any = {};
    if (message.batchId !== "") {
      obj.batchId = message.batchId;
    }
    if (message.batchSequence !== 0n) {
      obj.batchSequence = message.batchSequence.toString();
    }
    if (message.code !== 0) {
      obj.code = batchRejectionCodeToJSON(message.code);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.violations?.length) {
      obj.violations = message.violations.map((e) => FieldViolation.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BatchReject>, I>>(base?: I): BatchReject {
    return BatchReject.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchReject>, I>>(object: I): BatchReject {
    const message = createBaseBatchReject();
    message.batchId = object.batchId ?? "";
    message.batchSequence = (object.batchSequence !== undefined && object.batchSequence !== null)
      ? BigInt(object.batchSequence)
      : 0n;
    message.code = object.code ?? 0;
    message.message = object.message ?? "";
    message.violations = object.violations?.map((e) => FieldViolation.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRetryBatch(): RetryBatch {
  return { batchId: "", batchSequence: 0n, reason: 0, retryAfter: undefined, message: undefined };
}

export const RetryBatch: MessageFns<RetryBatch> = {
  encode(message: RetryBatch, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.batchId !== "") {
      writer.uint32(10).string(message.batchId);
    }
    if (message.batchSequence !== 0n) {
      if (BigInt.asUintN(64, message.batchSequence) !== message.batchSequence) {
        throw new globalThis.Error("value provided for field message.batchSequence of type uint64 too large");
      }
      writer.uint32(16).uint64(message.batchSequence);
    }
    if (message.reason !== 0) {
      writer.uint32(24).int32(message.reason);
    }
    if (message.retryAfter !== undefined) {
      Duration.encode(message.retryAfter, writer.uint32(34).fork()).join();
    }
    if (message.message !== undefined) {
      writer.uint32(42).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RetryBatch {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRetryBatch();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.batchId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.batchSequence = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.retryAfter = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RetryBatch {
    return {
      batchId: isSet(object.batchId)
        ? globalThis.String(object.batchId)
        : isSet(object.batch_id)
        ? globalThis.String(object.batch_id)
        : "",
      batchSequence: isSet(object.batchSequence)
        ? BigInt(object.batchSequence)
        : isSet(object.batch_sequence)
        ? BigInt(object.batch_sequence)
        : 0n,
      reason: isSet(object.reason) ? retryBatchReasonFromJSON(object.reason) : 0,
      retryAfter: isSet(object.retryAfter)
        ? Duration.fromJSON(object.retryAfter)
        : isSet(object.retry_after)
        ? Duration.fromJSON(object.retry_after)
        : undefined,
      message: isSet(object.message) ? globalThis.String(object.message) : undefined,
    };
  },

  toJSON(message: RetryBatch): unknown {
    const obj: any = {};
    if (message.batchId !== "") {
      obj.batchId = message.batchId;
    }
    if (message.batchSequence !== 0n) {
      obj.batchSequence = message.batchSequence.toString();
    }
    if (message.reason !== 0) {
      obj.reason = retryBatchReasonToJSON(message.reason);
    }
    if (message.retryAfter !== undefined) {
      obj.retryAfter = Duration.toJSON(message.retryAfter);
    }
    if (message.message !== undefined) {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RetryBatch>, I>>(base?: I): RetryBatch {
    return RetryBatch.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RetryBatch>, I>>(object: I): RetryBatch {
    const message = createBaseRetryBatch();
    message.batchId = object.batchId ?? "";
    message.batchSequence = (object.batchSequence !== undefined && object.batchSequence !== null)
      ? BigInt(object.batchSequence)
      : 0n;
    message.reason = object.reason ?? 0;
    message.retryAfter = (object.retryAfter !== undefined && object.retryAfter !== null)
      ? Duration.fromPartial(object.retryAfter)
      : undefined;
    message.message = object.message ?? undefined;
    return message;
  },
};

function createBaseThrottle(): Throttle {
  return {
    reason: 0,
    minimumSendDelay: undefined,
    effectiveUntil: undefined,
    maxInFlightBatches: 0,
    maxBatchEvents: 0,
    maxBatchBytes: 0n,
    message: undefined,
  };
}

export const Throttle: MessageFns<Throttle> = {
  encode(message: Throttle, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reason !== 0) {
      writer.uint32(8).int32(message.reason);
    }
    if (message.minimumSendDelay !== undefined) {
      Duration.encode(message.minimumSendDelay, writer.uint32(18).fork()).join();
    }
    if (message.effectiveUntil !== undefined) {
      Timestamp.encode(toTimestamp(message.effectiveUntil), writer.uint32(26).fork()).join();
    }
    if (message.maxInFlightBatches !== 0) {
      writer.uint32(32).uint32(message.maxInFlightBatches);
    }
    if (message.maxBatchEvents !== 0) {
      writer.uint32(40).uint32(message.maxBatchEvents);
    }
    if (message.maxBatchBytes !== 0n) {
      if (BigInt.asUintN(64, message.maxBatchBytes) !== message.maxBatchBytes) {
        throw new globalThis.Error("value provided for field message.maxBatchBytes of type uint64 too large");
      }
      writer.uint32(48).uint64(message.maxBatchBytes);
    }
    if (message.message !== undefined) {
      writer.uint32(58).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Throttle {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseThrottle();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.minimumSendDelay = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.effectiveUntil = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.maxInFlightBatches = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.maxBatchEvents = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.maxBatchBytes = reader.uint64() as bigint;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Throttle {
    return {
      reason: isSet(object.reason) ? throttleReasonFromJSON(object.reason) : 0,
      minimumSendDelay: isSet(object.minimumSendDelay)
        ? Duration.fromJSON(object.minimumSendDelay)
        : isSet(object.minimum_send_delay)
        ? Duration.fromJSON(object.minimum_send_delay)
        : undefined,
      effectiveUntil: isSet(object.effectiveUntil)
        ? fromJsonTimestamp(object.effectiveUntil)
        : isSet(object.effective_until)
        ? fromJsonTimestamp(object.effective_until)
        : undefined,
      maxInFlightBatches: isSet(object.maxInFlightBatches)
        ? globalThis.Number(object.maxInFlightBatches)
        : isSet(object.max_in_flight_batches)
        ? globalThis.Number(object.max_in_flight_batches)
        : 0,
      maxBatchEvents: isSet(object.maxBatchEvents)
        ? globalThis.Number(object.maxBatchEvents)
        : isSet(object.max_batch_events)
        ? globalThis.Number(object.max_batch_events)
        : 0,
      maxBatchBytes: isSet(object.maxBatchBytes)
        ? BigInt(object.maxBatchBytes)
        : isSet(object.max_batch_bytes)
        ? BigInt(object.max_batch_bytes)
        : 0n,
      message: isSet(object.message) ? globalThis.String(object.message) : undefined,
    };
  },

  toJSON(message: Throttle): unknown {
    const obj: any = {};
    if (message.reason !== 0) {
      obj.reason = throttleReasonToJSON(message.reason);
    }
    if (message.minimumSendDelay !== undefined) {
      obj.minimumSendDelay = Duration.toJSON(message.minimumSendDelay);
    }
    if (message.effectiveUntil !== undefined) {
      obj.effectiveUntil = message.effectiveUntil.toISOString();
    }
    if (message.maxInFlightBatches !== 0) {
      obj.maxInFlightBatches = Math.round(message.maxInFlightBatches);
    }
    if (message.maxBatchEvents !== 0) {
      obj.maxBatchEvents = Math.round(message.maxBatchEvents);
    }
    if (message.maxBatchBytes !== 0n) {
      obj.maxBatchBytes = message.maxBatchBytes.toString();
    }
    if (message.message !== undefined) {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Throttle>, I>>(base?: I): Throttle {
    return Throttle.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Throttle>, I>>(object: I): Throttle {
    const message = createBaseThrottle();
    message.reason = object.reason ?? 0;
    message.minimumSendDelay = (object.minimumSendDelay !== undefined && object.minimumSendDelay !== null)
      ? Duration.fromPartial(object.minimumSendDelay)
      : undefined;
    message.effectiveUntil = object.effectiveUntil ?? undefined;
    message.maxInFlightBatches = object.maxInFlightBatches ?? 0;
    message.maxBatchEvents = object.maxBatchEvents ?? 0;
    message.maxBatchBytes = (object.maxBatchBytes !== undefined && object.maxBatchBytes !== null)
      ? BigInt(object.maxBatchBytes)
      : 0n;
    message.message = object.message ?? undefined;
    return message;
  },
};

function createBaseServerNotice(): ServerNotice {
  return { type: 0, code: "", message: "", reconnectAfter: undefined };
}

export const ServerNotice: MessageFns<ServerNotice> = {
  encode(message: ServerNotice, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.code !== "") {
      writer.uint32(18).string(message.code);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    if (message.reconnectAfter !== undefined) {
      Duration.encode(message.reconnectAfter, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerNotice {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerNotice();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.reconnectAfter = Duration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerNotice {
    return {
      type: isSet(object.type) ? serverNoticeTypeFromJSON(object.type) : 0,
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      reconnectAfter: isSet(object.reconnectAfter)
        ? Duration.fromJSON(object.reconnectAfter)
        : isSet(object.reconnect_after)
        ? Duration.fromJSON(object.reconnect_after)
        : undefined,
    };
  },

  toJSON(message: ServerNotice): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = serverNoticeTypeToJSON(message.type);
    }
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.reconnectAfter !== undefined) {
      obj.reconnectAfter = Duration.toJSON(message.reconnectAfter);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerNotice>, I>>(base?: I): ServerNotice {
    return ServerNotice.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerNotice>, I>>(object: I): ServerNotice {
    const message = createBaseServerNotice();
    message.type = object.type ?? 0;
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    message.reconnectAfter = (object.reconnectAfter !== undefined && object.reconnectAfter !== null)
      ? Duration.fromPartial(object.reconnectAfter)
      : undefined;
    return message;
  },
};

function createBaseCollectResponse(): CollectResponse {
  return { streamSequence: 0n, sentAt: undefined, payload: undefined };
}

export const CollectResponse: MessageFns<CollectResponse> = {
  encode(message: CollectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.streamSequence !== 0n) {
      if (BigInt.asUintN(64, message.streamSequence) !== message.streamSequence) {
        throw new globalThis.Error("value provided for field message.streamSequence of type uint64 too large");
      }
      writer.uint32(8).uint64(message.streamSequence);
    }
    if (message.sentAt !== undefined) {
      Timestamp.encode(toTimestamp(message.sentAt), writer.uint32(18).fork()).join();
    }
    switch (message.payload?.$case) {
      case "ready":
        CollectorReady.encode(message.payload.value, writer.uint32(82).fork()).join();
        break;
      case "batchAck":
        BatchAck.encode(message.payload.value, writer.uint32(90).fork()).join();
        break;
      case "batchReject":
        BatchReject.encode(message.payload.value, writer.uint32(98).fork()).join();
        break;
      case "retryBatch":
        RetryBatch.encode(message.payload.value, writer.uint32(106).fork()).join();
        break;
      case "throttle":
        Throttle.encode(message.payload.value, writer.uint32(114).fork()).join();
        break;
      case "notice":
        ServerNotice.encode(message.payload.value, writer.uint32(122).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CollectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCollectResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.streamSequence = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sentAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.payload = { $case: "ready", value: CollectorReady.decode(reader, reader.uint32()) };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.payload = { $case: "batchAck", value: BatchAck.decode(reader, reader.uint32()) };
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.payload = { $case: "batchReject", value: BatchReject.decode(reader, reader.uint32()) };
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.payload = { $case: "retryBatch", value: RetryBatch.decode(reader, reader.uint32()) };
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.payload = { $case: "throttle", value: Throttle.decode(reader, reader.uint32()) };
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.payload = { $case: "notice", value: ServerNotice.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CollectResponse {
    return {
      streamSequence: isSet(object.streamSequence)
        ? BigInt(object.streamSequence)
        : isSet(object.stream_sequence)
        ? BigInt(object.stream_sequence)
        : 0n,
      sentAt: isSet(object.sentAt)
        ? fromJsonTimestamp(object.sentAt)
        : isSet(object.sent_at)
        ? fromJsonTimestamp(object.sent_at)
        : undefined,
      payload: isSet(object.ready)
        ? { $case: "ready", value: CollectorReady.fromJSON(object.ready) }
        : isSet(object.batchAck)
        ? { $case: "batchAck", value: BatchAck.fromJSON(object.batchAck) }
        : isSet(object.batch_ack)
        ? { $case: "batchAck", value: BatchAck.fromJSON(object.batch_ack) }
        : isSet(object.batchReject)
        ? { $case: "batchReject", value: BatchReject.fromJSON(object.batchReject) }
        : isSet(object.batch_reject)
        ? { $case: "batchReject", value: BatchReject.fromJSON(object.batch_reject) }
        : isSet(object.retryBatch)
        ? { $case: "retryBatch", value: RetryBatch.fromJSON(object.retryBatch) }
        : isSet(object.retry_batch)
        ? { $case: "retryBatch", value: RetryBatch.fromJSON(object.retry_batch) }
        : isSet(object.throttle)
        ? { $case: "throttle", value: Throttle.fromJSON(object.throttle) }
        : isSet(object.notice)
        ? { $case: "notice", value: ServerNotice.fromJSON(object.notice) }
        : undefined,
    };
  },

  toJSON(message: CollectResponse): unknown {
    const obj: any = {};
    if (message.streamSequence !== 0n) {
      obj.streamSequence = message.streamSequence.toString();
    }
    if (message.sentAt !== undefined) {
      obj.sentAt = message.sentAt.toISOString();
    }
    if (message.payload?.$case === "ready") {
      obj.ready = CollectorReady.toJSON(message.payload.value);
    } else if (message.payload?.$case === "batchAck") {
      obj.batchAck = BatchAck.toJSON(message.payload.value);
    } else if (message.payload?.$case === "batchReject") {
      obj.batchReject = BatchReject.toJSON(message.payload.value);
    } else if (message.payload?.$case === "retryBatch") {
      obj.retryBatch = RetryBatch.toJSON(message.payload.value);
    } else if (message.payload?.$case === "throttle") {
      obj.throttle = Throttle.toJSON(message.payload.value);
    } else if (message.payload?.$case === "notice") {
      obj.notice = ServerNotice.toJSON(message.payload.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CollectResponse>, I>>(base?: I): CollectResponse {
    return CollectResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CollectResponse>, I>>(object: I): CollectResponse {
    const message = createBaseCollectResponse();
    message.streamSequence = (object.streamSequence !== undefined && object.streamSequence !== null)
      ? BigInt(object.streamSequence)
      : 0n;
    message.sentAt = object.sentAt ?? undefined;
    switch (object.payload?.$case) {
      case "ready": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = { $case: "ready", value: CollectorReady.fromPartial(object.payload.value) };
        }
        break;
      }
      case "batchAck": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = { $case: "batchAck", value: BatchAck.fromPartial(object.payload.value) };
        }
        break;
      }
      case "batchReject": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = { $case: "batchReject", value: BatchReject.fromPartial(object.payload.value) };
        }
        break;
      }
      case "retryBatch": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = { $case: "retryBatch", value: RetryBatch.fromPartial(object.payload.value) };
        }
        break;
      }
      case "throttle": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = { $case: "throttle", value: Throttle.fromPartial(object.payload.value) };
        }
        break;
      }
      case "notice": {
        if (object.payload?.value !== undefined && object.payload?.value !== null) {
          message.payload = { $case: "notice", value: ServerNotice.fromPartial(object.payload.value) };
        }
        break;
      }
    }
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
