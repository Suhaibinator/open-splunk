// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: open_splunk/v1/search_attempt_audit.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import {
  AuditActorKind,
  auditActorKindFromJSON,
  auditActorKindToJSON,
  AuditActorRole,
  auditActorRoleFromJSON,
  auditActorRoleToJSON,
} from "./audit";
import { KnowledgeSnapshotRef } from "./knowledge";

/**
 * SearchAttemptAuditEvent is the immutable, allowlisted projection of one
 * admitted search attempt. It deliberately carries no query, execution, or
 * result content.
 */
export interface SearchAttemptAuditEvent {
  sequence: bigint;
  occurredAt: Date | undefined;
  actorKind: AuditActorKind;
  actorId: string;
  actorRole: AuditActorRole;
  ownerId: string;
  searchJobId: string;
  /**
   * Audit retains only the compact snapshot identity and exact object count;
   * definition and object inventory remain outside this journal.
   */
  knowledgeSnapshot?: KnowledgeSnapshotRef | undefined;
}

function createBaseSearchAttemptAuditEvent(): SearchAttemptAuditEvent {
  return {
    sequence: 0n,
    occurredAt: undefined,
    actorKind: 0,
    actorId: "",
    actorRole: 0,
    ownerId: "",
    searchJobId: "",
    knowledgeSnapshot: undefined,
  };
}

export const SearchAttemptAuditEvent: MessageFns<SearchAttemptAuditEvent> = {
  encode(message: SearchAttemptAuditEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sequence !== 0n) {
      if (BigInt.asUintN(64, message.sequence) !== message.sequence) {
        throw new globalThis.Error("value provided for field message.sequence of type uint64 too large");
      }
      writer.uint32(8).uint64(message.sequence);
    }
    if (message.occurredAt !== undefined) {
      Timestamp.encode(toTimestamp(message.occurredAt), writer.uint32(18).fork()).join();
    }
    if (message.actorKind !== 0) {
      writer.uint32(24).int32(message.actorKind);
    }
    if (message.actorId !== "") {
      writer.uint32(34).string(message.actorId);
    }
    if (message.actorRole !== 0) {
      writer.uint32(40).int32(message.actorRole);
    }
    if (message.ownerId !== "") {
      writer.uint32(50).string(message.ownerId);
    }
    if (message.searchJobId !== "") {
      writer.uint32(58).string(message.searchJobId);
    }
    if (message.knowledgeSnapshot !== undefined) {
      KnowledgeSnapshotRef.encode(message.knowledgeSnapshot, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchAttemptAuditEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchAttemptAuditEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.sequence = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.occurredAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.actorKind = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.actorId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.actorRole = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.ownerId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.searchJobId = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.knowledgeSnapshot = KnowledgeSnapshotRef.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchAttemptAuditEvent {
    return {
      sequence: isSet(object.sequence) ? BigInt(object.sequence) : 0n,
      occurredAt: isSet(object.occurredAt)
        ? fromJsonTimestamp(object.occurredAt)
        : isSet(object.occurred_at)
        ? fromJsonTimestamp(object.occurred_at)
        : undefined,
      actorKind: isSet(object.actorKind)
        ? auditActorKindFromJSON(object.actorKind)
        : isSet(object.actor_kind)
        ? auditActorKindFromJSON(object.actor_kind)
        : 0,
      actorId: isSet(object.actorId)
        ? globalThis.String(object.actorId)
        : isSet(object.actor_id)
        ? globalThis.String(object.actor_id)
        : "",
      actorRole: isSet(object.actorRole)
        ? auditActorRoleFromJSON(object.actorRole)
        : isSet(object.actor_role)
        ? auditActorRoleFromJSON(object.actor_role)
        : 0,
      ownerId: isSet(object.ownerId)
        ? globalThis.String(object.ownerId)
        : isSet(object.owner_id)
        ? globalThis.String(object.owner_id)
        : "",
      searchJobId: isSet(object.searchJobId)
        ? globalThis.String(object.searchJobId)
        : isSet(object.search_job_id)
        ? globalThis.String(object.search_job_id)
        : "",
      knowledgeSnapshot: isSet(object.knowledgeSnapshot)
        ? KnowledgeSnapshotRef.fromJSON(object.knowledgeSnapshot)
        : isSet(object.knowledge_snapshot)
        ? KnowledgeSnapshotRef.fromJSON(object.knowledge_snapshot)
        : undefined,
    };
  },

  toJSON(message: SearchAttemptAuditEvent): unknown {
    const obj: any = {};
    if (message.sequence !== 0n) {
      obj.sequence = message.sequence.toString();
    }
    if (message.occurredAt !== undefined) {
      obj.occurredAt = message.occurredAt.toISOString();
    }
    if (message.actorKind !== 0) {
      obj.actorKind = auditActorKindToJSON(message.actorKind);
    }
    if (message.actorId !== "") {
      obj.actorId = message.actorId;
    }
    if (message.actorRole !== 0) {
      obj.actorRole = auditActorRoleToJSON(message.actorRole);
    }
    if (message.ownerId !== "") {
      obj.ownerId = message.ownerId;
    }
    if (message.searchJobId !== "") {
      obj.searchJobId = message.searchJobId;
    }
    if (message.knowledgeSnapshot !== undefined) {
      obj.knowledgeSnapshot = KnowledgeSnapshotRef.toJSON(message.knowledgeSnapshot);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchAttemptAuditEvent>, I>>(base?: I): SearchAttemptAuditEvent {
    return SearchAttemptAuditEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchAttemptAuditEvent>, I>>(object: I): SearchAttemptAuditEvent {
    const message = createBaseSearchAttemptAuditEvent();
    message.sequence = (object.sequence !== undefined && object.sequence !== null) ? BigInt(object.sequence) : 0n;
    message.occurredAt = object.occurredAt ?? undefined;
    message.actorKind = object.actorKind ?? 0;
    message.actorId = object.actorId ?? "";
    message.actorRole = object.actorRole ?? 0;
    message.ownerId = object.ownerId ?? "";
    message.searchJobId = object.searchJobId ?? "";
    message.knowledgeSnapshot = (object.knowledgeSnapshot !== undefined && object.knowledgeSnapshot !== null)
      ? KnowledgeSnapshotRef.fromPartial(object.knowledgeSnapshot)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
