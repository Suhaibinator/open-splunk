// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.0
// source: open_splunk/v1/history.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../../google/protobuf/duration";
import { Timestamp } from "../../google/protobuf/timestamp";
import { ApiWarning, ResolvedTimeRange } from "./common";
import {
  SearchDefinition,
  SearchFailure,
  SearchJobSource,
  SearchJobState,
  searchJobStateFromJSON,
  searchJobStateToJSON,
} from "./search";

/**
 * SearchHistoryEntry stores execution metadata only; result rows and generated
 * SQL are intentionally excluded. Relative time expressions remain in
 * definition while resolved_time_range records what the original run used.
 */
export interface SearchHistoryEntry {
  searchJobId: string;
  definition: SearchDefinition | undefined;
  source: SearchJobSource | undefined;
  effectiveIndexScope: string[];
  resolvedTimeRange?: ResolvedTimeRange | undefined;
  finalState: SearchJobState;
  matchedEvents: bigint;
  scannedRows: bigint;
  scannedBytes: bigint;
  producedRows: bigint;
  duration: Duration | undefined;
  warnings: ApiWarning[];
  failure?: SearchFailure | undefined;
  compilerVersion: string;
  createdAt: Date | undefined;
  startedAt: Date | undefined;
  finishedAt: Date | undefined;
}

function createBaseSearchHistoryEntry(): SearchHistoryEntry {
  return {
    searchJobId: "",
    definition: undefined,
    source: undefined,
    effectiveIndexScope: [],
    resolvedTimeRange: undefined,
    finalState: 0,
    matchedEvents: 0n,
    scannedRows: 0n,
    scannedBytes: 0n,
    producedRows: 0n,
    duration: undefined,
    warnings: [],
    failure: undefined,
    compilerVersion: "",
    createdAt: undefined,
    startedAt: undefined,
    finishedAt: undefined,
  };
}

export const SearchHistoryEntry: MessageFns<SearchHistoryEntry> = {
  encode(message: SearchHistoryEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.searchJobId !== "") {
      writer.uint32(10).string(message.searchJobId);
    }
    if (message.definition !== undefined) {
      SearchDefinition.encode(message.definition, writer.uint32(18).fork()).join();
    }
    if (message.source !== undefined) {
      SearchJobSource.encode(message.source, writer.uint32(26).fork()).join();
    }
    for (const v of message.effectiveIndexScope) {
      writer.uint32(34).string(v!);
    }
    if (message.resolvedTimeRange !== undefined) {
      ResolvedTimeRange.encode(message.resolvedTimeRange, writer.uint32(42).fork()).join();
    }
    if (message.finalState !== 0) {
      writer.uint32(48).int32(message.finalState);
    }
    if (message.matchedEvents !== 0n) {
      if (BigInt.asUintN(64, message.matchedEvents) !== message.matchedEvents) {
        throw new globalThis.Error("value provided for field message.matchedEvents of type uint64 too large");
      }
      writer.uint32(56).uint64(message.matchedEvents);
    }
    if (message.scannedRows !== 0n) {
      if (BigInt.asUintN(64, message.scannedRows) !== message.scannedRows) {
        throw new globalThis.Error("value provided for field message.scannedRows of type uint64 too large");
      }
      writer.uint32(64).uint64(message.scannedRows);
    }
    if (message.scannedBytes !== 0n) {
      if (BigInt.asUintN(64, message.scannedBytes) !== message.scannedBytes) {
        throw new globalThis.Error("value provided for field message.scannedBytes of type uint64 too large");
      }
      writer.uint32(72).uint64(message.scannedBytes);
    }
    if (message.producedRows !== 0n) {
      if (BigInt.asUintN(64, message.producedRows) !== message.producedRows) {
        throw new globalThis.Error("value provided for field message.producedRows of type uint64 too large");
      }
      writer.uint32(80).uint64(message.producedRows);
    }
    if (message.duration !== undefined) {
      Duration.encode(message.duration, writer.uint32(90).fork()).join();
    }
    for (const v of message.warnings) {
      ApiWarning.encode(v!, writer.uint32(98).fork()).join();
    }
    if (message.failure !== undefined) {
      SearchFailure.encode(message.failure, writer.uint32(106).fork()).join();
    }
    if (message.compilerVersion !== "") {
      writer.uint32(114).string(message.compilerVersion);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(122).fork()).join();
    }
    if (message.startedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startedAt), writer.uint32(130).fork()).join();
    }
    if (message.finishedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.finishedAt), writer.uint32(138).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchHistoryEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchHistoryEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.searchJobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.definition = SearchDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.source = SearchJobSource.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.effectiveIndexScope.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.resolvedTimeRange = ResolvedTimeRange.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.finalState = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.matchedEvents = reader.uint64() as bigint;
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.scannedRows = reader.uint64() as bigint;
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.scannedBytes = reader.uint64() as bigint;
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.producedRows = reader.uint64() as bigint;
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.duration = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.warnings.push(ApiWarning.decode(reader, reader.uint32()));
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.failure = SearchFailure.decode(reader, reader.uint32());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.compilerVersion = reader.string();
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.startedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.finishedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchHistoryEntry {
    return {
      searchJobId: isSet(object.searchJobId)
        ? globalThis.String(object.searchJobId)
        : isSet(object.search_job_id)
        ? globalThis.String(object.search_job_id)
        : "",
      definition: isSet(object.definition) ? SearchDefinition.fromJSON(object.definition) : undefined,
      source: isSet(object.source) ? SearchJobSource.fromJSON(object.source) : undefined,
      effectiveIndexScope: globalThis.Array.isArray(object?.effectiveIndexScope)
        ? object.effectiveIndexScope.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.effective_index_scope)
        ? object.effective_index_scope.map((e: any) => globalThis.String(e))
        : [],
      resolvedTimeRange: isSet(object.resolvedTimeRange)
        ? ResolvedTimeRange.fromJSON(object.resolvedTimeRange)
        : isSet(object.resolved_time_range)
        ? ResolvedTimeRange.fromJSON(object.resolved_time_range)
        : undefined,
      finalState: isSet(object.finalState)
        ? searchJobStateFromJSON(object.finalState)
        : isSet(object.final_state)
        ? searchJobStateFromJSON(object.final_state)
        : 0,
      matchedEvents: isSet(object.matchedEvents)
        ? BigInt(object.matchedEvents)
        : isSet(object.matched_events)
        ? BigInt(object.matched_events)
        : 0n,
      scannedRows: isSet(object.scannedRows)
        ? BigInt(object.scannedRows)
        : isSet(object.scanned_rows)
        ? BigInt(object.scanned_rows)
        : 0n,
      scannedBytes: isSet(object.scannedBytes)
        ? BigInt(object.scannedBytes)
        : isSet(object.scanned_bytes)
        ? BigInt(object.scanned_bytes)
        : 0n,
      producedRows: isSet(object.producedRows)
        ? BigInt(object.producedRows)
        : isSet(object.produced_rows)
        ? BigInt(object.produced_rows)
        : 0n,
      duration: isSet(object.duration) ? Duration.fromJSON(object.duration) : undefined,
      warnings: globalThis.Array.isArray(object?.warnings)
        ? object.warnings.map((e: any) => ApiWarning.fromJSON(e))
        : [],
      failure: isSet(object.failure) ? SearchFailure.fromJSON(object.failure) : undefined,
      compilerVersion: isSet(object.compilerVersion)
        ? globalThis.String(object.compilerVersion)
        : isSet(object.compiler_version)
        ? globalThis.String(object.compiler_version)
        : "",
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      startedAt: isSet(object.startedAt)
        ? fromJsonTimestamp(object.startedAt)
        : isSet(object.started_at)
        ? fromJsonTimestamp(object.started_at)
        : undefined,
      finishedAt: isSet(object.finishedAt)
        ? fromJsonTimestamp(object.finishedAt)
        : isSet(object.finished_at)
        ? fromJsonTimestamp(object.finished_at)
        : undefined,
    };
  },

  toJSON(message: SearchHistoryEntry): unknown {
    const obj: any = {};
    if (message.searchJobId !== "") {
      obj.searchJobId = message.searchJobId;
    }
    if (message.definition !== undefined) {
      obj.definition = SearchDefinition.toJSON(message.definition);
    }
    if (message.source !== undefined) {
      obj.source = SearchJobSource.toJSON(message.source);
    }
    if (message.effectiveIndexScope?.length) {
      obj.effectiveIndexScope = message.effectiveIndexScope;
    }
    if (message.resolvedTimeRange !== undefined) {
      obj.resolvedTimeRange = ResolvedTimeRange.toJSON(message.resolvedTimeRange);
    }
    if (message.finalState !== 0) {
      obj.finalState = searchJobStateToJSON(message.finalState);
    }
    if (message.matchedEvents !== 0n) {
      obj.matchedEvents = message.matchedEvents.toString();
    }
    if (message.scannedRows !== 0n) {
      obj.scannedRows = message.scannedRows.toString();
    }
    if (message.scannedBytes !== 0n) {
      obj.scannedBytes = message.scannedBytes.toString();
    }
    if (message.producedRows !== 0n) {
      obj.producedRows = message.producedRows.toString();
    }
    if (message.duration !== undefined) {
      obj.duration = Duration.toJSON(message.duration);
    }
    if (message.warnings?.length) {
      obj.warnings = message.warnings.map((e) => ApiWarning.toJSON(e));
    }
    if (message.failure !== undefined) {
      obj.failure = SearchFailure.toJSON(message.failure);
    }
    if (message.compilerVersion !== "") {
      obj.compilerVersion = message.compilerVersion;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.startedAt !== undefined) {
      obj.startedAt = message.startedAt.toISOString();
    }
    if (message.finishedAt !== undefined) {
      obj.finishedAt = message.finishedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchHistoryEntry>, I>>(base?: I): SearchHistoryEntry {
    return SearchHistoryEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchHistoryEntry>, I>>(object: I): SearchHistoryEntry {
    const message = createBaseSearchHistoryEntry();
    message.searchJobId = object.searchJobId ?? "";
    message.definition = (object.definition !== undefined && object.definition !== null)
      ? SearchDefinition.fromPartial(object.definition)
      : undefined;
    message.source = (object.source !== undefined && object.source !== null)
      ? SearchJobSource.fromPartial(object.source)
      : undefined;
    message.effectiveIndexScope = object.effectiveIndexScope?.map((e) => e) || [];
    message.resolvedTimeRange = (object.resolvedTimeRange !== undefined && object.resolvedTimeRange !== null)
      ? ResolvedTimeRange.fromPartial(object.resolvedTimeRange)
      : undefined;
    message.finalState = object.finalState ?? 0;
    message.matchedEvents = (object.matchedEvents !== undefined && object.matchedEvents !== null)
      ? BigInt(object.matchedEvents)
      : 0n;
    message.scannedRows = (object.scannedRows !== undefined && object.scannedRows !== null)
      ? BigInt(object.scannedRows)
      : 0n;
    message.scannedBytes = (object.scannedBytes !== undefined && object.scannedBytes !== null)
      ? BigInt(object.scannedBytes)
      : 0n;
    message.producedRows = (object.producedRows !== undefined && object.producedRows !== null)
      ? BigInt(object.producedRows)
      : 0n;
    message.duration = (object.duration !== undefined && object.duration !== null)
      ? Duration.fromPartial(object.duration)
      : undefined;
    message.warnings = object.warnings?.map((e) => ApiWarning.fromPartial(e)) || [];
    message.failure = (object.failure !== undefined && object.failure !== null)
      ? SearchFailure.fromPartial(object.failure)
      : undefined;
    message.compilerVersion = object.compilerVersion ?? "";
    message.createdAt = object.createdAt ?? undefined;
    message.startedAt = object.startedAt ?? undefined;
    message.finishedAt = object.finishedAt ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
