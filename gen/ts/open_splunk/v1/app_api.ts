// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.0
// source: open_splunk/v1/app_api.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { FieldMask } from "../../google/protobuf/field_mask";
import { AppDefinition, AppSelector, AppState, appStateFromJSON, appStateToJSON, AppWorkspace } from "./app";
import { PageRequest, PageResponse, SortDirection, sortDirectionFromJSON, sortDirectionToJSON } from "./common";

export enum AppSortBy {
  APP_SORT_BY_UNSPECIFIED = 0,
  APP_SORT_BY_DISPLAY_NAME = 1,
  APP_SORT_BY_CREATED_AT = 2,
  APP_SORT_BY_UPDATED_AT = 3,
  UNRECOGNIZED = -1,
}

export function appSortByFromJSON(object: any): AppSortBy {
  switch (object) {
    case 0:
    case "APP_SORT_BY_UNSPECIFIED":
      return AppSortBy.APP_SORT_BY_UNSPECIFIED;
    case 1:
    case "APP_SORT_BY_DISPLAY_NAME":
      return AppSortBy.APP_SORT_BY_DISPLAY_NAME;
    case 2:
    case "APP_SORT_BY_CREATED_AT":
      return AppSortBy.APP_SORT_BY_CREATED_AT;
    case 3:
    case "APP_SORT_BY_UPDATED_AT":
      return AppSortBy.APP_SORT_BY_UPDATED_AT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AppSortBy.UNRECOGNIZED;
  }
}

export function appSortByToJSON(object: AppSortBy): string {
  switch (object) {
    case AppSortBy.APP_SORT_BY_UNSPECIFIED:
      return "APP_SORT_BY_UNSPECIFIED";
    case AppSortBy.APP_SORT_BY_DISPLAY_NAME:
      return "APP_SORT_BY_DISPLAY_NAME";
    case AppSortBy.APP_SORT_BY_CREATED_AT:
      return "APP_SORT_BY_CREATED_AT";
    case AppSortBy.APP_SORT_BY_UPDATED_AT:
      return "APP_SORT_BY_UPDATED_AT";
    case AppSortBy.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** POST /api/v1/apps/create */
export interface CreateAppRequest {
  definition: AppDefinition | undefined;
  clientRequestId?: string | undefined;
}

export interface CreateAppResponse {
  app: AppWorkspace | undefined;
}

/** POST /api/v1/apps/get */
export interface GetAppRequest {
  selector: AppSelector | undefined;
}

export interface GetAppResponse {
  app: AppWorkspace | undefined;
}

/** POST /api/v1/apps/list */
export interface ListAppsRequest {
  page: PageRequest | undefined;
  stateFilters: AppState[];
  textFilter?: string | undefined;
  sortBy: AppSortBy;
  sortDirection: SortDirection;
}

export interface ListAppsResponse {
  apps: AppWorkspace[];
  page: PageResponse | undefined;
}

/** POST /api/v1/apps/update */
export interface UpdateAppRequest {
  selector: AppSelector | undefined;
  expectedVersion: bigint;
  definition: AppDefinition | undefined;
  updateMask: string[] | undefined;
}

export interface UpdateAppResponse {
  app: AppWorkspace | undefined;
}

/** POST /api/v1/apps/state/set */
export interface SetAppStateRequest {
  selector: AppSelector | undefined;
  expectedVersion: bigint;
  state: AppState;
}

export interface SetAppStateResponse {
  app: AppWorkspace | undefined;
}

/** POST /api/v1/apps/delete */
export interface DeleteAppRequest {
  selector: AppSelector | undefined;
  expectedVersion: bigint;
  confirmationSlug: string;
}

export interface DeleteAppResponse {
  appId: string;
}

function createBaseCreateAppRequest(): CreateAppRequest {
  return { definition: undefined, clientRequestId: undefined };
}

export const CreateAppRequest: MessageFns<CreateAppRequest> = {
  encode(message: CreateAppRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.definition !== undefined) {
      AppDefinition.encode(message.definition, writer.uint32(10).fork()).join();
    }
    if (message.clientRequestId !== undefined) {
      writer.uint32(18).string(message.clientRequestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateAppRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateAppRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.definition = AppDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.clientRequestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateAppRequest {
    return {
      definition: isSet(object.definition) ? AppDefinition.fromJSON(object.definition) : undefined,
      clientRequestId: isSet(object.clientRequestId)
        ? globalThis.String(object.clientRequestId)
        : isSet(object.client_request_id)
        ? globalThis.String(object.client_request_id)
        : undefined,
    };
  },

  toJSON(message: CreateAppRequest): unknown {
    const obj: any = {};
    if (message.definition !== undefined) {
      obj.definition = AppDefinition.toJSON(message.definition);
    }
    if (message.clientRequestId !== undefined) {
      obj.clientRequestId = message.clientRequestId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateAppRequest>, I>>(base?: I): CreateAppRequest {
    return CreateAppRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateAppRequest>, I>>(object: I): CreateAppRequest {
    const message = createBaseCreateAppRequest();
    message.definition = (object.definition !== undefined && object.definition !== null)
      ? AppDefinition.fromPartial(object.definition)
      : undefined;
    message.clientRequestId = object.clientRequestId ?? undefined;
    return message;
  },
};

function createBaseCreateAppResponse(): CreateAppResponse {
  return { app: undefined };
}

export const CreateAppResponse: MessageFns<CreateAppResponse> = {
  encode(message: CreateAppResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.app !== undefined) {
      AppWorkspace.encode(message.app, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateAppResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateAppResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.app = AppWorkspace.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateAppResponse {
    return { app: isSet(object.app) ? AppWorkspace.fromJSON(object.app) : undefined };
  },

  toJSON(message: CreateAppResponse): unknown {
    const obj: any = {};
    if (message.app !== undefined) {
      obj.app = AppWorkspace.toJSON(message.app);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateAppResponse>, I>>(base?: I): CreateAppResponse {
    return CreateAppResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateAppResponse>, I>>(object: I): CreateAppResponse {
    const message = createBaseCreateAppResponse();
    message.app = (object.app !== undefined && object.app !== null) ? AppWorkspace.fromPartial(object.app) : undefined;
    return message;
  },
};

function createBaseGetAppRequest(): GetAppRequest {
  return { selector: undefined };
}

export const GetAppRequest: MessageFns<GetAppRequest> = {
  encode(message: GetAppRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.selector !== undefined) {
      AppSelector.encode(message.selector, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAppRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAppRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.selector = AppSelector.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetAppRequest {
    return { selector: isSet(object.selector) ? AppSelector.fromJSON(object.selector) : undefined };
  },

  toJSON(message: GetAppRequest): unknown {
    const obj: any = {};
    if (message.selector !== undefined) {
      obj.selector = AppSelector.toJSON(message.selector);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetAppRequest>, I>>(base?: I): GetAppRequest {
    return GetAppRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetAppRequest>, I>>(object: I): GetAppRequest {
    const message = createBaseGetAppRequest();
    message.selector = (object.selector !== undefined && object.selector !== null)
      ? AppSelector.fromPartial(object.selector)
      : undefined;
    return message;
  },
};

function createBaseGetAppResponse(): GetAppResponse {
  return { app: undefined };
}

export const GetAppResponse: MessageFns<GetAppResponse> = {
  encode(message: GetAppResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.app !== undefined) {
      AppWorkspace.encode(message.app, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAppResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAppResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.app = AppWorkspace.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetAppResponse {
    return { app: isSet(object.app) ? AppWorkspace.fromJSON(object.app) : undefined };
  },

  toJSON(message: GetAppResponse): unknown {
    const obj: any = {};
    if (message.app !== undefined) {
      obj.app = AppWorkspace.toJSON(message.app);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetAppResponse>, I>>(base?: I): GetAppResponse {
    return GetAppResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetAppResponse>, I>>(object: I): GetAppResponse {
    const message = createBaseGetAppResponse();
    message.app = (object.app !== undefined && object.app !== null) ? AppWorkspace.fromPartial(object.app) : undefined;
    return message;
  },
};

function createBaseListAppsRequest(): ListAppsRequest {
  return { page: undefined, stateFilters: [], textFilter: undefined, sortBy: 0, sortDirection: 0 };
}

export const ListAppsRequest: MessageFns<ListAppsRequest> = {
  encode(message: ListAppsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.page !== undefined) {
      PageRequest.encode(message.page, writer.uint32(10).fork()).join();
    }
    writer.uint32(18).fork();
    for (const v of message.stateFilters) {
      writer.int32(v);
    }
    writer.join();
    if (message.textFilter !== undefined) {
      writer.uint32(26).string(message.textFilter);
    }
    if (message.sortBy !== 0) {
      writer.uint32(32).int32(message.sortBy);
    }
    if (message.sortDirection !== 0) {
      writer.uint32(40).int32(message.sortDirection);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAppsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAppsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.page = PageRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.stateFilters.push(reader.int32() as any);

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.stateFilters.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.textFilter = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.sortBy = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.sortDirection = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListAppsRequest {
    return {
      page: isSet(object.page) ? PageRequest.fromJSON(object.page) : undefined,
      stateFilters: globalThis.Array.isArray(object?.stateFilters)
        ? object.stateFilters.map((e: any) => appStateFromJSON(e))
        : globalThis.Array.isArray(object?.state_filters)
        ? object.state_filters.map((e: any) => appStateFromJSON(e))
        : [],
      textFilter: isSet(object.textFilter)
        ? globalThis.String(object.textFilter)
        : isSet(object.text_filter)
        ? globalThis.String(object.text_filter)
        : undefined,
      sortBy: isSet(object.sortBy)
        ? appSortByFromJSON(object.sortBy)
        : isSet(object.sort_by)
        ? appSortByFromJSON(object.sort_by)
        : 0,
      sortDirection: isSet(object.sortDirection)
        ? sortDirectionFromJSON(object.sortDirection)
        : isSet(object.sort_direction)
        ? sortDirectionFromJSON(object.sort_direction)
        : 0,
    };
  },

  toJSON(message: ListAppsRequest): unknown {
    const obj: any = {};
    if (message.page !== undefined) {
      obj.page = PageRequest.toJSON(message.page);
    }
    if (message.stateFilters?.length) {
      obj.stateFilters = message.stateFilters.map((e) => appStateToJSON(e));
    }
    if (message.textFilter !== undefined) {
      obj.textFilter = message.textFilter;
    }
    if (message.sortBy !== 0) {
      obj.sortBy = appSortByToJSON(message.sortBy);
    }
    if (message.sortDirection !== 0) {
      obj.sortDirection = sortDirectionToJSON(message.sortDirection);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListAppsRequest>, I>>(base?: I): ListAppsRequest {
    return ListAppsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListAppsRequest>, I>>(object: I): ListAppsRequest {
    const message = createBaseListAppsRequest();
    message.page = (object.page !== undefined && object.page !== null)
      ? PageRequest.fromPartial(object.page)
      : undefined;
    message.stateFilters = object.stateFilters?.map((e) => e) || [];
    message.textFilter = object.textFilter ?? undefined;
    message.sortBy = object.sortBy ?? 0;
    message.sortDirection = object.sortDirection ?? 0;
    return message;
  },
};

function createBaseListAppsResponse(): ListAppsResponse {
  return { apps: [], page: undefined };
}

export const ListAppsResponse: MessageFns<ListAppsResponse> = {
  encode(message: ListAppsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.apps) {
      AppWorkspace.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      PageResponse.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAppsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAppsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.apps.push(AppWorkspace.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListAppsResponse {
    return {
      apps: globalThis.Array.isArray(object?.apps) ? object.apps.map((e: any) => AppWorkspace.fromJSON(e)) : [],
      page: isSet(object.page) ? PageResponse.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListAppsResponse): unknown {
    const obj: any = {};
    if (message.apps?.length) {
      obj.apps = message.apps.map((e) => AppWorkspace.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = PageResponse.toJSON(message.page);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListAppsResponse>, I>>(base?: I): ListAppsResponse {
    return ListAppsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListAppsResponse>, I>>(object: I): ListAppsResponse {
    const message = createBaseListAppsResponse();
    message.apps = object.apps?.map((e) => AppWorkspace.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResponse.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseUpdateAppRequest(): UpdateAppRequest {
  return { selector: undefined, expectedVersion: 0n, definition: undefined, updateMask: undefined };
}

export const UpdateAppRequest: MessageFns<UpdateAppRequest> = {
  encode(message: UpdateAppRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.selector !== undefined) {
      AppSelector.encode(message.selector, writer.uint32(10).fork()).join();
    }
    if (message.expectedVersion !== 0n) {
      if (BigInt.asUintN(64, message.expectedVersion) !== message.expectedVersion) {
        throw new globalThis.Error("value provided for field message.expectedVersion of type uint64 too large");
      }
      writer.uint32(16).uint64(message.expectedVersion);
    }
    if (message.definition !== undefined) {
      AppDefinition.encode(message.definition, writer.uint32(26).fork()).join();
    }
    if (message.updateMask !== undefined) {
      FieldMask.encode(FieldMask.wrap(message.updateMask), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateAppRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateAppRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.selector = AppSelector.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.expectedVersion = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.definition = AppDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.updateMask = FieldMask.unwrap(FieldMask.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateAppRequest {
    return {
      selector: isSet(object.selector) ? AppSelector.fromJSON(object.selector) : undefined,
      expectedVersion: isSet(object.expectedVersion)
        ? BigInt(object.expectedVersion)
        : isSet(object.expected_version)
        ? BigInt(object.expected_version)
        : 0n,
      definition: isSet(object.definition) ? AppDefinition.fromJSON(object.definition) : undefined,
      updateMask: isSet(object.updateMask)
        ? FieldMask.unwrap(FieldMask.fromJSON(object.updateMask))
        : isSet(object.update_mask)
        ? FieldMask.unwrap(FieldMask.fromJSON(object.update_mask))
        : undefined,
    };
  },

  toJSON(message: UpdateAppRequest): unknown {
    const obj: any = {};
    if (message.selector !== undefined) {
      obj.selector = AppSelector.toJSON(message.selector);
    }
    if (message.expectedVersion !== 0n) {
      obj.expectedVersion = message.expectedVersion.toString();
    }
    if (message.definition !== undefined) {
      obj.definition = AppDefinition.toJSON(message.definition);
    }
    if (message.updateMask !== undefined) {
      obj.updateMask = FieldMask.toJSON(FieldMask.wrap(message.updateMask));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateAppRequest>, I>>(base?: I): UpdateAppRequest {
    return UpdateAppRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateAppRequest>, I>>(object: I): UpdateAppRequest {
    const message = createBaseUpdateAppRequest();
    message.selector = (object.selector !== undefined && object.selector !== null)
      ? AppSelector.fromPartial(object.selector)
      : undefined;
    message.expectedVersion = (object.expectedVersion !== undefined && object.expectedVersion !== null)
      ? BigInt(object.expectedVersion)
      : 0n;
    message.definition = (object.definition !== undefined && object.definition !== null)
      ? AppDefinition.fromPartial(object.definition)
      : undefined;
    message.updateMask = object.updateMask ?? undefined;
    return message;
  },
};

function createBaseUpdateAppResponse(): UpdateAppResponse {
  return { app: undefined };
}

export const UpdateAppResponse: MessageFns<UpdateAppResponse> = {
  encode(message: UpdateAppResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.app !== undefined) {
      AppWorkspace.encode(message.app, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateAppResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateAppResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.app = AppWorkspace.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateAppResponse {
    return { app: isSet(object.app) ? AppWorkspace.fromJSON(object.app) : undefined };
  },

  toJSON(message: UpdateAppResponse): unknown {
    const obj: any = {};
    if (message.app !== undefined) {
      obj.app = AppWorkspace.toJSON(message.app);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateAppResponse>, I>>(base?: I): UpdateAppResponse {
    return UpdateAppResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateAppResponse>, I>>(object: I): UpdateAppResponse {
    const message = createBaseUpdateAppResponse();
    message.app = (object.app !== undefined && object.app !== null) ? AppWorkspace.fromPartial(object.app) : undefined;
    return message;
  },
};

function createBaseSetAppStateRequest(): SetAppStateRequest {
  return { selector: undefined, expectedVersion: 0n, state: 0 };
}

export const SetAppStateRequest: MessageFns<SetAppStateRequest> = {
  encode(message: SetAppStateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.selector !== undefined) {
      AppSelector.encode(message.selector, writer.uint32(10).fork()).join();
    }
    if (message.expectedVersion !== 0n) {
      if (BigInt.asUintN(64, message.expectedVersion) !== message.expectedVersion) {
        throw new globalThis.Error("value provided for field message.expectedVersion of type uint64 too large");
      }
      writer.uint32(16).uint64(message.expectedVersion);
    }
    if (message.state !== 0) {
      writer.uint32(24).int32(message.state);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetAppStateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetAppStateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.selector = AppSelector.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.expectedVersion = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetAppStateRequest {
    return {
      selector: isSet(object.selector) ? AppSelector.fromJSON(object.selector) : undefined,
      expectedVersion: isSet(object.expectedVersion)
        ? BigInt(object.expectedVersion)
        : isSet(object.expected_version)
        ? BigInt(object.expected_version)
        : 0n,
      state: isSet(object.state) ? appStateFromJSON(object.state) : 0,
    };
  },

  toJSON(message: SetAppStateRequest): unknown {
    const obj: any = {};
    if (message.selector !== undefined) {
      obj.selector = AppSelector.toJSON(message.selector);
    }
    if (message.expectedVersion !== 0n) {
      obj.expectedVersion = message.expectedVersion.toString();
    }
    if (message.state !== 0) {
      obj.state = appStateToJSON(message.state);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetAppStateRequest>, I>>(base?: I): SetAppStateRequest {
    return SetAppStateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetAppStateRequest>, I>>(object: I): SetAppStateRequest {
    const message = createBaseSetAppStateRequest();
    message.selector = (object.selector !== undefined && object.selector !== null)
      ? AppSelector.fromPartial(object.selector)
      : undefined;
    message.expectedVersion = (object.expectedVersion !== undefined && object.expectedVersion !== null)
      ? BigInt(object.expectedVersion)
      : 0n;
    message.state = object.state ?? 0;
    return message;
  },
};

function createBaseSetAppStateResponse(): SetAppStateResponse {
  return { app: undefined };
}

export const SetAppStateResponse: MessageFns<SetAppStateResponse> = {
  encode(message: SetAppStateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.app !== undefined) {
      AppWorkspace.encode(message.app, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetAppStateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetAppStateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.app = AppWorkspace.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetAppStateResponse {
    return { app: isSet(object.app) ? AppWorkspace.fromJSON(object.app) : undefined };
  },

  toJSON(message: SetAppStateResponse): unknown {
    const obj: any = {};
    if (message.app !== undefined) {
      obj.app = AppWorkspace.toJSON(message.app);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetAppStateResponse>, I>>(base?: I): SetAppStateResponse {
    return SetAppStateResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetAppStateResponse>, I>>(object: I): SetAppStateResponse {
    const message = createBaseSetAppStateResponse();
    message.app = (object.app !== undefined && object.app !== null) ? AppWorkspace.fromPartial(object.app) : undefined;
    return message;
  },
};

function createBaseDeleteAppRequest(): DeleteAppRequest {
  return { selector: undefined, expectedVersion: 0n, confirmationSlug: "" };
}

export const DeleteAppRequest: MessageFns<DeleteAppRequest> = {
  encode(message: DeleteAppRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.selector !== undefined) {
      AppSelector.encode(message.selector, writer.uint32(10).fork()).join();
    }
    if (message.expectedVersion !== 0n) {
      if (BigInt.asUintN(64, message.expectedVersion) !== message.expectedVersion) {
        throw new globalThis.Error("value provided for field message.expectedVersion of type uint64 too large");
      }
      writer.uint32(16).uint64(message.expectedVersion);
    }
    if (message.confirmationSlug !== "") {
      writer.uint32(26).string(message.confirmationSlug);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteAppRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteAppRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.selector = AppSelector.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.expectedVersion = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.confirmationSlug = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteAppRequest {
    return {
      selector: isSet(object.selector) ? AppSelector.fromJSON(object.selector) : undefined,
      expectedVersion: isSet(object.expectedVersion)
        ? BigInt(object.expectedVersion)
        : isSet(object.expected_version)
        ? BigInt(object.expected_version)
        : 0n,
      confirmationSlug: isSet(object.confirmationSlug)
        ? globalThis.String(object.confirmationSlug)
        : isSet(object.confirmation_slug)
        ? globalThis.String(object.confirmation_slug)
        : "",
    };
  },

  toJSON(message: DeleteAppRequest): unknown {
    const obj: any = {};
    if (message.selector !== undefined) {
      obj.selector = AppSelector.toJSON(message.selector);
    }
    if (message.expectedVersion !== 0n) {
      obj.expectedVersion = message.expectedVersion.toString();
    }
    if (message.confirmationSlug !== "") {
      obj.confirmationSlug = message.confirmationSlug;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteAppRequest>, I>>(base?: I): DeleteAppRequest {
    return DeleteAppRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteAppRequest>, I>>(object: I): DeleteAppRequest {
    const message = createBaseDeleteAppRequest();
    message.selector = (object.selector !== undefined && object.selector !== null)
      ? AppSelector.fromPartial(object.selector)
      : undefined;
    message.expectedVersion = (object.expectedVersion !== undefined && object.expectedVersion !== null)
      ? BigInt(object.expectedVersion)
      : 0n;
    message.confirmationSlug = object.confirmationSlug ?? "";
    return message;
  },
};

function createBaseDeleteAppResponse(): DeleteAppResponse {
  return { appId: "" };
}

export const DeleteAppResponse: MessageFns<DeleteAppResponse> = {
  encode(message: DeleteAppResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.appId !== "") {
      writer.uint32(10).string(message.appId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteAppResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteAppResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.appId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteAppResponse {
    return {
      appId: isSet(object.appId)
        ? globalThis.String(object.appId)
        : isSet(object.app_id)
        ? globalThis.String(object.app_id)
        : "",
    };
  },

  toJSON(message: DeleteAppResponse): unknown {
    const obj: any = {};
    if (message.appId !== "") {
      obj.appId = message.appId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteAppResponse>, I>>(base?: I): DeleteAppResponse {
    return DeleteAppResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteAppResponse>, I>>(object: I): DeleteAppResponse {
    const message = createBaseDeleteAppResponse();
    message.appId = object.appId ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
