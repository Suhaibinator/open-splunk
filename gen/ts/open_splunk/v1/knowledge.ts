// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: open_splunk/v1/knowledge.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { SharingScope, sharingScopeFromJSON, sharingScopeToJSON, SourceRange } from "./common";

/**
 * KnowledgeObjectType is closed for mutation. Servers reject unspecified,
 * unknown, and unsupported values rather than treating a future type as a
 * Tier-1 object.
 */
export enum KnowledgeObjectType {
  KNOWLEDGE_OBJECT_TYPE_UNSPECIFIED = 0,
  KNOWLEDGE_OBJECT_TYPE_FIELD_EXTRACTION = 1,
  KNOWLEDGE_OBJECT_TYPE_FIELD_ALIAS = 2,
  KNOWLEDGE_OBJECT_TYPE_CALCULATED_FIELD = 3,
  UNRECOGNIZED = -1,
}

export function knowledgeObjectTypeFromJSON(object: any): KnowledgeObjectType {
  switch (object) {
    case 0:
    case "KNOWLEDGE_OBJECT_TYPE_UNSPECIFIED":
      return KnowledgeObjectType.KNOWLEDGE_OBJECT_TYPE_UNSPECIFIED;
    case 1:
    case "KNOWLEDGE_OBJECT_TYPE_FIELD_EXTRACTION":
      return KnowledgeObjectType.KNOWLEDGE_OBJECT_TYPE_FIELD_EXTRACTION;
    case 2:
    case "KNOWLEDGE_OBJECT_TYPE_FIELD_ALIAS":
      return KnowledgeObjectType.KNOWLEDGE_OBJECT_TYPE_FIELD_ALIAS;
    case 3:
    case "KNOWLEDGE_OBJECT_TYPE_CALCULATED_FIELD":
      return KnowledgeObjectType.KNOWLEDGE_OBJECT_TYPE_CALCULATED_FIELD;
    case -1:
    case "UNRECOGNIZED":
    default:
      return KnowledgeObjectType.UNRECOGNIZED;
  }
}

export function knowledgeObjectTypeToJSON(object: KnowledgeObjectType): string {
  switch (object) {
    case KnowledgeObjectType.KNOWLEDGE_OBJECT_TYPE_UNSPECIFIED:
      return "KNOWLEDGE_OBJECT_TYPE_UNSPECIFIED";
    case KnowledgeObjectType.KNOWLEDGE_OBJECT_TYPE_FIELD_EXTRACTION:
      return "KNOWLEDGE_OBJECT_TYPE_FIELD_EXTRACTION";
    case KnowledgeObjectType.KNOWLEDGE_OBJECT_TYPE_FIELD_ALIAS:
      return "KNOWLEDGE_OBJECT_TYPE_FIELD_ALIAS";
    case KnowledgeObjectType.KNOWLEDGE_OBJECT_TYPE_CALCULATED_FIELD:
      return "KNOWLEDGE_OBJECT_TYPE_CALCULATED_FIELD";
    case KnowledgeObjectType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * KnowledgeObjectState is closed for mutation. DELETED is a retained terminal
 * tombstone created only by the delete operation and is never executable. Its
 * current registry identity remains the authorization authority for an
 * administrator inspecting the tombstone or any retained historical version.
 */
export enum KnowledgeObjectState {
  KNOWLEDGE_OBJECT_STATE_UNSPECIFIED = 0,
  KNOWLEDGE_OBJECT_STATE_DRAFT = 1,
  KNOWLEDGE_OBJECT_STATE_ACTIVE = 2,
  KNOWLEDGE_OBJECT_STATE_DISABLED = 3,
  KNOWLEDGE_OBJECT_STATE_QUARANTINED = 4,
  KNOWLEDGE_OBJECT_STATE_DELETED = 5,
  UNRECOGNIZED = -1,
}

export function knowledgeObjectStateFromJSON(object: any): KnowledgeObjectState {
  switch (object) {
    case 0:
    case "KNOWLEDGE_OBJECT_STATE_UNSPECIFIED":
      return KnowledgeObjectState.KNOWLEDGE_OBJECT_STATE_UNSPECIFIED;
    case 1:
    case "KNOWLEDGE_OBJECT_STATE_DRAFT":
      return KnowledgeObjectState.KNOWLEDGE_OBJECT_STATE_DRAFT;
    case 2:
    case "KNOWLEDGE_OBJECT_STATE_ACTIVE":
      return KnowledgeObjectState.KNOWLEDGE_OBJECT_STATE_ACTIVE;
    case 3:
    case "KNOWLEDGE_OBJECT_STATE_DISABLED":
      return KnowledgeObjectState.KNOWLEDGE_OBJECT_STATE_DISABLED;
    case 4:
    case "KNOWLEDGE_OBJECT_STATE_QUARANTINED":
      return KnowledgeObjectState.KNOWLEDGE_OBJECT_STATE_QUARANTINED;
    case 5:
    case "KNOWLEDGE_OBJECT_STATE_DELETED":
      return KnowledgeObjectState.KNOWLEDGE_OBJECT_STATE_DELETED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return KnowledgeObjectState.UNRECOGNIZED;
  }
}

export function knowledgeObjectStateToJSON(object: KnowledgeObjectState): string {
  switch (object) {
    case KnowledgeObjectState.KNOWLEDGE_OBJECT_STATE_UNSPECIFIED:
      return "KNOWLEDGE_OBJECT_STATE_UNSPECIFIED";
    case KnowledgeObjectState.KNOWLEDGE_OBJECT_STATE_DRAFT:
      return "KNOWLEDGE_OBJECT_STATE_DRAFT";
    case KnowledgeObjectState.KNOWLEDGE_OBJECT_STATE_ACTIVE:
      return "KNOWLEDGE_OBJECT_STATE_ACTIVE";
    case KnowledgeObjectState.KNOWLEDGE_OBJECT_STATE_DISABLED:
      return "KNOWLEDGE_OBJECT_STATE_DISABLED";
    case KnowledgeObjectState.KNOWLEDGE_OBJECT_STATE_QUARANTINED:
      return "KNOWLEDGE_OBJECT_STATE_QUARANTINED";
    case KnowledgeObjectState.KNOWLEDGE_OBJECT_STATE_DELETED:
      return "KNOWLEDGE_OBJECT_STATE_DELETED";
    case KnowledgeObjectState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * KnowledgeSearchStage fixes the initial search-time execution sequence.
 * Numeric order is not an execution authority; planners switch on the enum and
 * reject unsupported values.
 */
export enum KnowledgeSearchStage {
  KNOWLEDGE_SEARCH_STAGE_UNSPECIFIED = 0,
  KNOWLEDGE_SEARCH_STAGE_FIELD_EXTRACTION = 1,
  KNOWLEDGE_SEARCH_STAGE_FIELD_ALIAS = 2,
  KNOWLEDGE_SEARCH_STAGE_CALCULATED_FIELD = 3,
  UNRECOGNIZED = -1,
}

export function knowledgeSearchStageFromJSON(object: any): KnowledgeSearchStage {
  switch (object) {
    case 0:
    case "KNOWLEDGE_SEARCH_STAGE_UNSPECIFIED":
      return KnowledgeSearchStage.KNOWLEDGE_SEARCH_STAGE_UNSPECIFIED;
    case 1:
    case "KNOWLEDGE_SEARCH_STAGE_FIELD_EXTRACTION":
      return KnowledgeSearchStage.KNOWLEDGE_SEARCH_STAGE_FIELD_EXTRACTION;
    case 2:
    case "KNOWLEDGE_SEARCH_STAGE_FIELD_ALIAS":
      return KnowledgeSearchStage.KNOWLEDGE_SEARCH_STAGE_FIELD_ALIAS;
    case 3:
    case "KNOWLEDGE_SEARCH_STAGE_CALCULATED_FIELD":
      return KnowledgeSearchStage.KNOWLEDGE_SEARCH_STAGE_CALCULATED_FIELD;
    case -1:
    case "UNRECOGNIZED":
    default:
      return KnowledgeSearchStage.UNRECOGNIZED;
  }
}

export function knowledgeSearchStageToJSON(object: KnowledgeSearchStage): string {
  switch (object) {
    case KnowledgeSearchStage.KNOWLEDGE_SEARCH_STAGE_UNSPECIFIED:
      return "KNOWLEDGE_SEARCH_STAGE_UNSPECIFIED";
    case KnowledgeSearchStage.KNOWLEDGE_SEARCH_STAGE_FIELD_EXTRACTION:
      return "KNOWLEDGE_SEARCH_STAGE_FIELD_EXTRACTION";
    case KnowledgeSearchStage.KNOWLEDGE_SEARCH_STAGE_FIELD_ALIAS:
      return "KNOWLEDGE_SEARCH_STAGE_FIELD_ALIAS";
    case KnowledgeSearchStage.KNOWLEDGE_SEARCH_STAGE_CALCULATED_FIELD:
      return "KNOWLEDGE_SEARCH_STAGE_CALCULATED_FIELD";
    case KnowledgeSearchStage.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum KnowledgeSelectorMatchKind {
  KNOWLEDGE_SELECTOR_MATCH_KIND_UNSPECIFIED = 0,
  KNOWLEDGE_SELECTOR_MATCH_KIND_EXACT = 1,
  KNOWLEDGE_SELECTOR_MATCH_KIND_WILDCARD = 2,
  UNRECOGNIZED = -1,
}

export function knowledgeSelectorMatchKindFromJSON(object: any): KnowledgeSelectorMatchKind {
  switch (object) {
    case 0:
    case "KNOWLEDGE_SELECTOR_MATCH_KIND_UNSPECIFIED":
      return KnowledgeSelectorMatchKind.KNOWLEDGE_SELECTOR_MATCH_KIND_UNSPECIFIED;
    case 1:
    case "KNOWLEDGE_SELECTOR_MATCH_KIND_EXACT":
      return KnowledgeSelectorMatchKind.KNOWLEDGE_SELECTOR_MATCH_KIND_EXACT;
    case 2:
    case "KNOWLEDGE_SELECTOR_MATCH_KIND_WILDCARD":
      return KnowledgeSelectorMatchKind.KNOWLEDGE_SELECTOR_MATCH_KIND_WILDCARD;
    case -1:
    case "UNRECOGNIZED":
    default:
      return KnowledgeSelectorMatchKind.UNRECOGNIZED;
  }
}

export function knowledgeSelectorMatchKindToJSON(object: KnowledgeSelectorMatchKind): string {
  switch (object) {
    case KnowledgeSelectorMatchKind.KNOWLEDGE_SELECTOR_MATCH_KIND_UNSPECIFIED:
      return "KNOWLEDGE_SELECTOR_MATCH_KIND_UNSPECIFIED";
    case KnowledgeSelectorMatchKind.KNOWLEDGE_SELECTOR_MATCH_KIND_EXACT:
      return "KNOWLEDGE_SELECTOR_MATCH_KIND_EXACT";
    case KnowledgeSelectorMatchKind.KNOWLEDGE_SELECTOR_MATCH_KIND_WILDCARD:
      return "KNOWLEDGE_SELECTOR_MATCH_KIND_WILDCARD";
    case KnowledgeSelectorMatchKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum KnowledgeOverwriteBehavior {
  KNOWLEDGE_OVERWRITE_BEHAVIOR_UNSPECIFIED = 0,
  KNOWLEDGE_OVERWRITE_BEHAVIOR_PRESERVE_EXISTING = 1,
  KNOWLEDGE_OVERWRITE_BEHAVIOR_REPLACE_EXISTING = 2,
  UNRECOGNIZED = -1,
}

export function knowledgeOverwriteBehaviorFromJSON(object: any): KnowledgeOverwriteBehavior {
  switch (object) {
    case 0:
    case "KNOWLEDGE_OVERWRITE_BEHAVIOR_UNSPECIFIED":
      return KnowledgeOverwriteBehavior.KNOWLEDGE_OVERWRITE_BEHAVIOR_UNSPECIFIED;
    case 1:
    case "KNOWLEDGE_OVERWRITE_BEHAVIOR_PRESERVE_EXISTING":
      return KnowledgeOverwriteBehavior.KNOWLEDGE_OVERWRITE_BEHAVIOR_PRESERVE_EXISTING;
    case 2:
    case "KNOWLEDGE_OVERWRITE_BEHAVIOR_REPLACE_EXISTING":
      return KnowledgeOverwriteBehavior.KNOWLEDGE_OVERWRITE_BEHAVIOR_REPLACE_EXISTING;
    case -1:
    case "UNRECOGNIZED":
    default:
      return KnowledgeOverwriteBehavior.UNRECOGNIZED;
  }
}

export function knowledgeOverwriteBehaviorToJSON(object: KnowledgeOverwriteBehavior): string {
  switch (object) {
    case KnowledgeOverwriteBehavior.KNOWLEDGE_OVERWRITE_BEHAVIOR_UNSPECIFIED:
      return "KNOWLEDGE_OVERWRITE_BEHAVIOR_UNSPECIFIED";
    case KnowledgeOverwriteBehavior.KNOWLEDGE_OVERWRITE_BEHAVIOR_PRESERVE_EXISTING:
      return "KNOWLEDGE_OVERWRITE_BEHAVIOR_PRESERVE_EXISTING";
    case KnowledgeOverwriteBehavior.KNOWLEDGE_OVERWRITE_BEHAVIOR_REPLACE_EXISTING:
      return "KNOWLEDGE_OVERWRITE_BEHAVIOR_REPLACE_EXISTING";
    case KnowledgeOverwriteBehavior.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum KnowledgeDependencyRole {
  KNOWLEDGE_DEPENDENCY_ROLE_UNSPECIFIED = 0,
  KNOWLEDGE_DEPENDENCY_ROLE_FIELD_INPUT = 1,
  KNOWLEDGE_DEPENDENCY_ROLE_LOOKUP_ASSET = 2,
  KNOWLEDGE_DEPENDENCY_ROLE_MACRO_REFERENCE = 3,
  KNOWLEDGE_DEPENDENCY_ROLE_EVENT_TYPE_REFERENCE = 4,
  UNRECOGNIZED = -1,
}

export function knowledgeDependencyRoleFromJSON(object: any): KnowledgeDependencyRole {
  switch (object) {
    case 0:
    case "KNOWLEDGE_DEPENDENCY_ROLE_UNSPECIFIED":
      return KnowledgeDependencyRole.KNOWLEDGE_DEPENDENCY_ROLE_UNSPECIFIED;
    case 1:
    case "KNOWLEDGE_DEPENDENCY_ROLE_FIELD_INPUT":
      return KnowledgeDependencyRole.KNOWLEDGE_DEPENDENCY_ROLE_FIELD_INPUT;
    case 2:
    case "KNOWLEDGE_DEPENDENCY_ROLE_LOOKUP_ASSET":
      return KnowledgeDependencyRole.KNOWLEDGE_DEPENDENCY_ROLE_LOOKUP_ASSET;
    case 3:
    case "KNOWLEDGE_DEPENDENCY_ROLE_MACRO_REFERENCE":
      return KnowledgeDependencyRole.KNOWLEDGE_DEPENDENCY_ROLE_MACRO_REFERENCE;
    case 4:
    case "KNOWLEDGE_DEPENDENCY_ROLE_EVENT_TYPE_REFERENCE":
      return KnowledgeDependencyRole.KNOWLEDGE_DEPENDENCY_ROLE_EVENT_TYPE_REFERENCE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return KnowledgeDependencyRole.UNRECOGNIZED;
  }
}

export function knowledgeDependencyRoleToJSON(object: KnowledgeDependencyRole): string {
  switch (object) {
    case KnowledgeDependencyRole.KNOWLEDGE_DEPENDENCY_ROLE_UNSPECIFIED:
      return "KNOWLEDGE_DEPENDENCY_ROLE_UNSPECIFIED";
    case KnowledgeDependencyRole.KNOWLEDGE_DEPENDENCY_ROLE_FIELD_INPUT:
      return "KNOWLEDGE_DEPENDENCY_ROLE_FIELD_INPUT";
    case KnowledgeDependencyRole.KNOWLEDGE_DEPENDENCY_ROLE_LOOKUP_ASSET:
      return "KNOWLEDGE_DEPENDENCY_ROLE_LOOKUP_ASSET";
    case KnowledgeDependencyRole.KNOWLEDGE_DEPENDENCY_ROLE_MACRO_REFERENCE:
      return "KNOWLEDGE_DEPENDENCY_ROLE_MACRO_REFERENCE";
    case KnowledgeDependencyRole.KNOWLEDGE_DEPENDENCY_ROLE_EVENT_TYPE_REFERENCE:
      return "KNOWLEDGE_DEPENDENCY_ROLE_EVENT_TYPE_REFERENCE";
    case KnowledgeDependencyRole.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum KnowledgeSnapshotWarningKind {
  KNOWLEDGE_SNAPSHOT_WARNING_KIND_UNSPECIFIED = 0,
  KNOWLEDGE_SNAPSHOT_WARNING_KIND_SHADOWED_OBJECT = 1,
  KNOWLEDGE_SNAPSHOT_WARNING_KIND_EXPENSIVE_GLOBAL_OBJECT = 2,
  UNRECOGNIZED = -1,
}

export function knowledgeSnapshotWarningKindFromJSON(object: any): KnowledgeSnapshotWarningKind {
  switch (object) {
    case 0:
    case "KNOWLEDGE_SNAPSHOT_WARNING_KIND_UNSPECIFIED":
      return KnowledgeSnapshotWarningKind.KNOWLEDGE_SNAPSHOT_WARNING_KIND_UNSPECIFIED;
    case 1:
    case "KNOWLEDGE_SNAPSHOT_WARNING_KIND_SHADOWED_OBJECT":
      return KnowledgeSnapshotWarningKind.KNOWLEDGE_SNAPSHOT_WARNING_KIND_SHADOWED_OBJECT;
    case 2:
    case "KNOWLEDGE_SNAPSHOT_WARNING_KIND_EXPENSIVE_GLOBAL_OBJECT":
      return KnowledgeSnapshotWarningKind.KNOWLEDGE_SNAPSHOT_WARNING_KIND_EXPENSIVE_GLOBAL_OBJECT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return KnowledgeSnapshotWarningKind.UNRECOGNIZED;
  }
}

export function knowledgeSnapshotWarningKindToJSON(object: KnowledgeSnapshotWarningKind): string {
  switch (object) {
    case KnowledgeSnapshotWarningKind.KNOWLEDGE_SNAPSHOT_WARNING_KIND_UNSPECIFIED:
      return "KNOWLEDGE_SNAPSHOT_WARNING_KIND_UNSPECIFIED";
    case KnowledgeSnapshotWarningKind.KNOWLEDGE_SNAPSHOT_WARNING_KIND_SHADOWED_OBJECT:
      return "KNOWLEDGE_SNAPSHOT_WARNING_KIND_SHADOWED_OBJECT";
    case KnowledgeSnapshotWarningKind.KNOWLEDGE_SNAPSHOT_WARNING_KIND_EXPENSIVE_GLOBAL_OBJECT:
      return "KNOWLEDGE_SNAPSHOT_WARNING_KIND_EXPENSIVE_GLOBAL_OBJECT";
    case KnowledgeSnapshotWarningKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface KnowledgeSelectorPattern {
  /**
   * match_kind is derived from normalized value. A submitted disagreement is
   * invalid rather than alternate matching authority.
   */
  matchKind: KnowledgeSelectorMatchKind;
  value: string;
}

/**
 * Dimensions are ANDed and patterns within one dimension are ORed. An empty
 * dimension is unrestricted. Normalized definitions collapse canonically
 * duplicate globs and order every pattern list by value UTF-8 bytes.
 */
export interface KnowledgeSelector {
  indexPatterns: KnowledgeSelectorPattern[];
  hostPatterns: KnowledgeSelectorPattern[];
  sourcePatterns: KnowledgeSelectorPattern[];
  sourcetypePatterns: KnowledgeSelectorPattern[];
}

export interface RegexFieldExtractionDefinition {
  pattern: string;
  outputFields: string[];
}

export interface JsonFieldExtractionDefinition {
  path: string;
  outputField: string;
}

export interface FieldExtractionDefinition {
  inputField: string;
  extraction: { $case: "regex"; value: RegexFieldExtractionDefinition } | {
    $case: "json";
    value: JsonFieldExtractionDefinition;
  } | undefined;
  overwriteBehavior: KnowledgeOverwriteBehavior;
}

export interface FieldAliasDefinition {
  sourceField: string;
  destinationField: string;
  overwriteBehavior: KnowledgeOverwriteBehavior;
}

export interface CalculatedFieldDefinition {
  destinationField: string;
  expression: string;
  overwriteBehavior: KnowledgeOverwriteBehavior;
}

/**
 * KnowledgeObjectDefinition is the normalized, digestible definition body.
 * app_id, name, and sharing_scope deliberately repeat indexed registry fields;
 * disagreement after decoding is corruption. Executable objects are ordered by
 * stage, normalized binary name, and stable object ID; clients cannot author an
 * execution-order override.
 */
export interface KnowledgeObjectDefinition {
  appId: string;
  name: string;
  description?: string | undefined;
  sharingScope: SharingScope;
  selector: KnowledgeSelector | undefined;
  body:
    | { $case: "fieldExtraction"; value: FieldExtractionDefinition }
    | { $case: "fieldAlias"; value: FieldAliasDefinition }
    | { $case: "calculatedField"; value: CalculatedFieldDefinition }
    | undefined;
}

/**
 * KnowledgeObject is the safe control-plane projection. definition_sha256 is
 * exactly 32 bytes. version is an optimistic token and increases on every
 * successful definition or state mutation. A quarantined projection has no
 * definition because its suspect bytes remain forensic storage only.
 */
export interface KnowledgeObject {
  knowledgeObjectId: string;
  tenantId: string;
  appId: string;
  ownerId: string;
  objectType: KnowledgeObjectType;
  name: string;
  version: bigint;
  sharingScope: SharingScope;
  state: KnowledgeObjectState;
  definition: KnowledgeObjectDefinition | undefined;
  definitionSha256: Uint8Array;
  createdAt: Date | undefined;
  updatedAt: Date | undefined;
  disabledAt?: Date | undefined;
}

export interface KnowledgeObjectVersionReference {
  knowledgeObjectId: string;
  version: bigint;
  definitionSha256: Uint8Array;
}

/**
 * Lookup assets are referenced here so snapshots and dependency messages are
 * forward-compatible with KO-2. Upload, publication, and row contracts are not
 * part of the unadvertised KO-0 surface.
 */
export interface KnowledgeLookupAssetVersionReference {
  lookupAssetId: string;
  version: bigint;
  sizeBytes: bigint;
  contentSha256: Uint8Array;
}

export interface KnowledgeDependencyTarget {
  target: { $case: "object"; value: KnowledgeObjectVersionReference } | {
    $case: "lookupAsset";
    value: KnowledgeLookupAssetVersionReference;
  } | undefined;
}

/**
 * topological_depth is the bounded longest-path depth of the source object.
 * canonical_ordinal is unique and contiguous within a snapshot dependency
 * list, which is encoded in ascending ordinal.
 */
export interface KnowledgeObjectDependency {
  source: KnowledgeObjectVersionReference | undefined;
  target: KnowledgeDependencyTarget | undefined;
  role: KnowledgeDependencyRole;
  sourceStage: KnowledgeSearchStage;
  targetStage: KnowledgeSearchStage;
  topologicalDepth: number;
  canonicalOrdinal: number;
}

export interface KnowledgeAuthoredProvenance {
  sourceRange: SourceRange | undefined;
}

/**
 * definition_location is a stable field path within the normalized object,
 * never the definition text itself. This projection is emitted only after a
 * response-time current-policy authorization check.
 */
export interface KnowledgeAuthorizedObjectProvenance {
  knowledgeObjectId: string;
  knowledgeObjectVersion: bigint;
  objectType: KnowledgeObjectType;
  objectName: string;
  definitionLocation: string;
  stage: KnowledgeSearchStage;
}

/**
 * A currently unauthorized historical reference retains only its bounded
 * transformation kind/stage and a response-local fixed redacted ordinal. It
 * cannot carry an object identity, name, version, or definition location.
 */
export interface KnowledgeRedactedObjectProvenance {
  redactedObjectOrdinal: number;
  objectType: KnowledgeObjectType;
  stage: KnowledgeSearchStage;
}

export interface KnowledgeProvenance {
  source:
    | { $case: "authored"; value: KnowledgeAuthoredProvenance }
    | { $case: "authorizedObject"; value: KnowledgeAuthorizedObjectProvenance }
    | { $case: "redactedObject"; value: KnowledgeRedactedObjectProvenance }
    | undefined;
}

/**
 * KnowledgeSnapshotObject contains detached immutable definition bytes in
 * typed form. resolution_ordinal is unique and contiguous across objects;
 * stage_ordinal is unique and contiguous within one stage. The repeated object
 * list is ordered by resolution_ordinal.
 */
export interface KnowledgeSnapshotObject {
  resolutionOrdinal: number;
  stage: KnowledgeSearchStage;
  stageOrdinal: number;
  knowledgeObjectId: string;
  version: bigint;
  objectType: KnowledgeObjectType;
  name: string;
  appId: string;
  ownerId: string;
  sharingScope: SharingScope;
  definition: KnowledgeObjectDefinition | undefined;
  definitionSha256: Uint8Array;
}

export interface KnowledgeSnapshotLookupAsset {
  assetOrdinal: number;
  asset: KnowledgeLookupAssetVersionReference | undefined;
}

/**
 * KnowledgeSnapshotShadow records a visible lower-precedence object that lost
 * whole-object resolution. Entries are ordered by contiguous shadow_ordinal.
 */
export interface KnowledgeSnapshotShadow {
  shadowOrdinal: number;
  winnerResolutionOrdinal: number;
  knowledgeObjectId: string;
  version: bigint;
  objectType: KnowledgeObjectType;
  name: string;
  appId: string;
  ownerId: string;
  sharingScope: SharingScope;
  definitionSha256: Uint8Array;
}

/**
 * Warnings use closed codes and ordinal references rather than free-form text
 * or definition contents. Entries are ordered by contiguous warning_ordinal.
 */
export interface KnowledgeSnapshotWarning {
  warningOrdinal: number;
  kind: KnowledgeSnapshotWarningKind;
  objectResolutionOrdinal?: number | undefined;
  shadowOrdinal?: number | undefined;
}

/**
 * KnowledgeSnapshotBudgetCharges contains aggregate admitted work, including
 * authored SPL where it shares a query-wide ceiling with knowledge work.
 */
export interface KnowledgeSnapshotBudgetCharges {
  executableObjects: number;
  selectorPatterns: number;
  selectorWildcardWorkUnits: bigint;
  /**
   * The byte charge is the length of deterministic KnowledgeSnapshot protobuf
   * bytes after both snapshot_sha256 and this field are cleared. The charge is
   * then set before the final digest serialization, avoiding a self-reference.
   */
  canonicalSnapshotBytes: bigint;
  dependencyNodes: number;
  dependencyEdges: number;
  dependencyDepth: number;
  generatedOperators: number;
  generatedFields: number;
  regexPrograms: number;
  regexWorkUnits: bigint;
  regexCaptureBytes: bigint;
  scalarExpressions: number;
  scalarExpressionNodes: number;
  generatedSqlBytes: bigint;
}

/**
 * KnowledgeSnapshot is the complete immutable knowledge authority admitted for
 * one search. It contains no maps or floating-point values. objects are ordered
 * by resolution_ordinal, dependencies by canonical_ordinal, and lookup_assets
 * by contiguous asset_ordinal. effective_authorized_indexes are normalized and
 * binary sorted; shadows and warnings use their contiguous ordinals. Unknown
 * fields at any depth are invalid and must be rejected and cleared before
 * hashing. snapshot_sha256 is calculated over deterministic canonical protobuf
 * bytes with that field absent and is exactly 32 bytes. Wall-clock admission
 * time and every other timestamp are deliberately outside this digest message.
 */
export interface KnowledgeSnapshot {
  formatVersion: number;
  tenantId: string;
  principalId: string;
  appId: string;
  tenantCatalogRevision: bigint;
  appCatalogRevision?: bigint | undefined;
  compilerCompatibilityVersion: string;
  objects: KnowledgeSnapshotObject[];
  dependencies: KnowledgeObjectDependency[];
  lookupAssets: KnowledgeSnapshotLookupAsset[];
  snapshotSha256: Uint8Array;
  effectiveAuthorizedIndexes: string[];
  shadows: KnowledgeSnapshotShadow[];
  warnings: KnowledgeSnapshotWarning[];
  budgetCharges: KnowledgeSnapshotBudgetCharges | undefined;
}

function createBaseKnowledgeSelectorPattern(): KnowledgeSelectorPattern {
  return { matchKind: 0, value: "" };
}

export const KnowledgeSelectorPattern: MessageFns<KnowledgeSelectorPattern> = {
  encode(message: KnowledgeSelectorPattern, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.matchKind !== 0) {
      writer.uint32(8).int32(message.matchKind);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeSelectorPattern {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeSelectorPattern();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.matchKind = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeSelectorPattern {
    return {
      matchKind: isSet(object.matchKind)
        ? knowledgeSelectorMatchKindFromJSON(object.matchKind)
        : isSet(object.match_kind)
        ? knowledgeSelectorMatchKindFromJSON(object.match_kind)
        : 0,
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: KnowledgeSelectorPattern): unknown {
    const obj: any = {};
    if (message.matchKind !== 0) {
      obj.matchKind = knowledgeSelectorMatchKindToJSON(message.matchKind);
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeSelectorPattern>, I>>(base?: I): KnowledgeSelectorPattern {
    return KnowledgeSelectorPattern.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeSelectorPattern>, I>>(object: I): KnowledgeSelectorPattern {
    const message = createBaseKnowledgeSelectorPattern();
    message.matchKind = object.matchKind ?? 0;
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseKnowledgeSelector(): KnowledgeSelector {
  return { indexPatterns: [], hostPatterns: [], sourcePatterns: [], sourcetypePatterns: [] };
}

export const KnowledgeSelector: MessageFns<KnowledgeSelector> = {
  encode(message: KnowledgeSelector, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.indexPatterns) {
      KnowledgeSelectorPattern.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.hostPatterns) {
      KnowledgeSelectorPattern.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.sourcePatterns) {
      KnowledgeSelectorPattern.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.sourcetypePatterns) {
      KnowledgeSelectorPattern.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeSelector {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeSelector();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.indexPatterns.push(KnowledgeSelectorPattern.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.hostPatterns.push(KnowledgeSelectorPattern.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sourcePatterns.push(KnowledgeSelectorPattern.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sourcetypePatterns.push(KnowledgeSelectorPattern.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeSelector {
    return {
      indexPatterns: globalThis.Array.isArray(object?.indexPatterns)
        ? object.indexPatterns.map((e: any) => KnowledgeSelectorPattern.fromJSON(e))
        : globalThis.Array.isArray(object?.index_patterns)
        ? object.index_patterns.map((e: any) => KnowledgeSelectorPattern.fromJSON(e))
        : [],
      hostPatterns: globalThis.Array.isArray(object?.hostPatterns)
        ? object.hostPatterns.map((e: any) => KnowledgeSelectorPattern.fromJSON(e))
        : globalThis.Array.isArray(object?.host_patterns)
        ? object.host_patterns.map((e: any) => KnowledgeSelectorPattern.fromJSON(e))
        : [],
      sourcePatterns: globalThis.Array.isArray(object?.sourcePatterns)
        ? object.sourcePatterns.map((e: any) => KnowledgeSelectorPattern.fromJSON(e))
        : globalThis.Array.isArray(object?.source_patterns)
        ? object.source_patterns.map((e: any) => KnowledgeSelectorPattern.fromJSON(e))
        : [],
      sourcetypePatterns: globalThis.Array.isArray(object?.sourcetypePatterns)
        ? object.sourcetypePatterns.map((e: any) => KnowledgeSelectorPattern.fromJSON(e))
        : globalThis.Array.isArray(object?.sourcetype_patterns)
        ? object.sourcetype_patterns.map((e: any) => KnowledgeSelectorPattern.fromJSON(e))
        : [],
    };
  },

  toJSON(message: KnowledgeSelector): unknown {
    const obj: any = {};
    if (message.indexPatterns?.length) {
      obj.indexPatterns = message.indexPatterns.map((e) => KnowledgeSelectorPattern.toJSON(e));
    }
    if (message.hostPatterns?.length) {
      obj.hostPatterns = message.hostPatterns.map((e) => KnowledgeSelectorPattern.toJSON(e));
    }
    if (message.sourcePatterns?.length) {
      obj.sourcePatterns = message.sourcePatterns.map((e) => KnowledgeSelectorPattern.toJSON(e));
    }
    if (message.sourcetypePatterns?.length) {
      obj.sourcetypePatterns = message.sourcetypePatterns.map((e) => KnowledgeSelectorPattern.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeSelector>, I>>(base?: I): KnowledgeSelector {
    return KnowledgeSelector.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeSelector>, I>>(object: I): KnowledgeSelector {
    const message = createBaseKnowledgeSelector();
    message.indexPatterns = object.indexPatterns?.map((e) => KnowledgeSelectorPattern.fromPartial(e)) || [];
    message.hostPatterns = object.hostPatterns?.map((e) => KnowledgeSelectorPattern.fromPartial(e)) || [];
    message.sourcePatterns = object.sourcePatterns?.map((e) => KnowledgeSelectorPattern.fromPartial(e)) || [];
    message.sourcetypePatterns = object.sourcetypePatterns?.map((e) => KnowledgeSelectorPattern.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRegexFieldExtractionDefinition(): RegexFieldExtractionDefinition {
  return { pattern: "", outputFields: [] };
}

export const RegexFieldExtractionDefinition: MessageFns<RegexFieldExtractionDefinition> = {
  encode(message: RegexFieldExtractionDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pattern !== "") {
      writer.uint32(10).string(message.pattern);
    }
    for (const v of message.outputFields) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegexFieldExtractionDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegexFieldExtractionDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pattern = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.outputFields.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegexFieldExtractionDefinition {
    return {
      pattern: isSet(object.pattern) ? globalThis.String(object.pattern) : "",
      outputFields: globalThis.Array.isArray(object?.outputFields)
        ? object.outputFields.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.output_fields)
        ? object.output_fields.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: RegexFieldExtractionDefinition): unknown {
    const obj: any = {};
    if (message.pattern !== "") {
      obj.pattern = message.pattern;
    }
    if (message.outputFields?.length) {
      obj.outputFields = message.outputFields;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegexFieldExtractionDefinition>, I>>(base?: I): RegexFieldExtractionDefinition {
    return RegexFieldExtractionDefinition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegexFieldExtractionDefinition>, I>>(
    object: I,
  ): RegexFieldExtractionDefinition {
    const message = createBaseRegexFieldExtractionDefinition();
    message.pattern = object.pattern ?? "";
    message.outputFields = object.outputFields?.map((e) => e) || [];
    return message;
  },
};

function createBaseJsonFieldExtractionDefinition(): JsonFieldExtractionDefinition {
  return { path: "", outputField: "" };
}

export const JsonFieldExtractionDefinition: MessageFns<JsonFieldExtractionDefinition> = {
  encode(message: JsonFieldExtractionDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.outputField !== "") {
      writer.uint32(18).string(message.outputField);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JsonFieldExtractionDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJsonFieldExtractionDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.outputField = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JsonFieldExtractionDefinition {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      outputField: isSet(object.outputField)
        ? globalThis.String(object.outputField)
        : isSet(object.output_field)
        ? globalThis.String(object.output_field)
        : "",
    };
  },

  toJSON(message: JsonFieldExtractionDefinition): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.outputField !== "") {
      obj.outputField = message.outputField;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JsonFieldExtractionDefinition>, I>>(base?: I): JsonFieldExtractionDefinition {
    return JsonFieldExtractionDefinition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JsonFieldExtractionDefinition>, I>>(
    object: I,
  ): JsonFieldExtractionDefinition {
    const message = createBaseJsonFieldExtractionDefinition();
    message.path = object.path ?? "";
    message.outputField = object.outputField ?? "";
    return message;
  },
};

function createBaseFieldExtractionDefinition(): FieldExtractionDefinition {
  return { inputField: "", extraction: undefined, overwriteBehavior: 0 };
}

export const FieldExtractionDefinition: MessageFns<FieldExtractionDefinition> = {
  encode(message: FieldExtractionDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.inputField !== "") {
      writer.uint32(10).string(message.inputField);
    }
    switch (message.extraction?.$case) {
      case "regex":
        RegexFieldExtractionDefinition.encode(message.extraction.value, writer.uint32(18).fork()).join();
        break;
      case "json":
        JsonFieldExtractionDefinition.encode(message.extraction.value, writer.uint32(26).fork()).join();
        break;
    }
    if (message.overwriteBehavior !== 0) {
      writer.uint32(32).int32(message.overwriteBehavior);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FieldExtractionDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFieldExtractionDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.inputField = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.extraction = {
            $case: "regex",
            value: RegexFieldExtractionDefinition.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.extraction = { $case: "json", value: JsonFieldExtractionDefinition.decode(reader, reader.uint32()) };
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.overwriteBehavior = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FieldExtractionDefinition {
    return {
      inputField: isSet(object.inputField)
        ? globalThis.String(object.inputField)
        : isSet(object.input_field)
        ? globalThis.String(object.input_field)
        : "",
      extraction: isSet(object.regex)
        ? { $case: "regex", value: RegexFieldExtractionDefinition.fromJSON(object.regex) }
        : isSet(object.json)
        ? { $case: "json", value: JsonFieldExtractionDefinition.fromJSON(object.json) }
        : undefined,
      overwriteBehavior: isSet(object.overwriteBehavior)
        ? knowledgeOverwriteBehaviorFromJSON(object.overwriteBehavior)
        : isSet(object.overwrite_behavior)
        ? knowledgeOverwriteBehaviorFromJSON(object.overwrite_behavior)
        : 0,
    };
  },

  toJSON(message: FieldExtractionDefinition): unknown {
    const obj: any = {};
    if (message.inputField !== "") {
      obj.inputField = message.inputField;
    }
    if (message.extraction?.$case === "regex") {
      obj.regex = RegexFieldExtractionDefinition.toJSON(message.extraction.value);
    } else if (message.extraction?.$case === "json") {
      obj.json = JsonFieldExtractionDefinition.toJSON(message.extraction.value);
    }
    if (message.overwriteBehavior !== 0) {
      obj.overwriteBehavior = knowledgeOverwriteBehaviorToJSON(message.overwriteBehavior);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FieldExtractionDefinition>, I>>(base?: I): FieldExtractionDefinition {
    return FieldExtractionDefinition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FieldExtractionDefinition>, I>>(object: I): FieldExtractionDefinition {
    const message = createBaseFieldExtractionDefinition();
    message.inputField = object.inputField ?? "";
    switch (object.extraction?.$case) {
      case "regex": {
        if (object.extraction?.value !== undefined && object.extraction?.value !== null) {
          message.extraction = {
            $case: "regex",
            value: RegexFieldExtractionDefinition.fromPartial(object.extraction.value),
          };
        }
        break;
      }
      case "json": {
        if (object.extraction?.value !== undefined && object.extraction?.value !== null) {
          message.extraction = {
            $case: "json",
            value: JsonFieldExtractionDefinition.fromPartial(object.extraction.value),
          };
        }
        break;
      }
    }
    message.overwriteBehavior = object.overwriteBehavior ?? 0;
    return message;
  },
};

function createBaseFieldAliasDefinition(): FieldAliasDefinition {
  return { sourceField: "", destinationField: "", overwriteBehavior: 0 };
}

export const FieldAliasDefinition: MessageFns<FieldAliasDefinition> = {
  encode(message: FieldAliasDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sourceField !== "") {
      writer.uint32(10).string(message.sourceField);
    }
    if (message.destinationField !== "") {
      writer.uint32(18).string(message.destinationField);
    }
    if (message.overwriteBehavior !== 0) {
      writer.uint32(24).int32(message.overwriteBehavior);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FieldAliasDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFieldAliasDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sourceField = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.destinationField = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.overwriteBehavior = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FieldAliasDefinition {
    return {
      sourceField: isSet(object.sourceField)
        ? globalThis.String(object.sourceField)
        : isSet(object.source_field)
        ? globalThis.String(object.source_field)
        : "",
      destinationField: isSet(object.destinationField)
        ? globalThis.String(object.destinationField)
        : isSet(object.destination_field)
        ? globalThis.String(object.destination_field)
        : "",
      overwriteBehavior: isSet(object.overwriteBehavior)
        ? knowledgeOverwriteBehaviorFromJSON(object.overwriteBehavior)
        : isSet(object.overwrite_behavior)
        ? knowledgeOverwriteBehaviorFromJSON(object.overwrite_behavior)
        : 0,
    };
  },

  toJSON(message: FieldAliasDefinition): unknown {
    const obj: any = {};
    if (message.sourceField !== "") {
      obj.sourceField = message.sourceField;
    }
    if (message.destinationField !== "") {
      obj.destinationField = message.destinationField;
    }
    if (message.overwriteBehavior !== 0) {
      obj.overwriteBehavior = knowledgeOverwriteBehaviorToJSON(message.overwriteBehavior);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FieldAliasDefinition>, I>>(base?: I): FieldAliasDefinition {
    return FieldAliasDefinition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FieldAliasDefinition>, I>>(object: I): FieldAliasDefinition {
    const message = createBaseFieldAliasDefinition();
    message.sourceField = object.sourceField ?? "";
    message.destinationField = object.destinationField ?? "";
    message.overwriteBehavior = object.overwriteBehavior ?? 0;
    return message;
  },
};

function createBaseCalculatedFieldDefinition(): CalculatedFieldDefinition {
  return { destinationField: "", expression: "", overwriteBehavior: 0 };
}

export const CalculatedFieldDefinition: MessageFns<CalculatedFieldDefinition> = {
  encode(message: CalculatedFieldDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.destinationField !== "") {
      writer.uint32(10).string(message.destinationField);
    }
    if (message.expression !== "") {
      writer.uint32(18).string(message.expression);
    }
    if (message.overwriteBehavior !== 0) {
      writer.uint32(24).int32(message.overwriteBehavior);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CalculatedFieldDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCalculatedFieldDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.destinationField = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.expression = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.overwriteBehavior = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CalculatedFieldDefinition {
    return {
      destinationField: isSet(object.destinationField)
        ? globalThis.String(object.destinationField)
        : isSet(object.destination_field)
        ? globalThis.String(object.destination_field)
        : "",
      expression: isSet(object.expression) ? globalThis.String(object.expression) : "",
      overwriteBehavior: isSet(object.overwriteBehavior)
        ? knowledgeOverwriteBehaviorFromJSON(object.overwriteBehavior)
        : isSet(object.overwrite_behavior)
        ? knowledgeOverwriteBehaviorFromJSON(object.overwrite_behavior)
        : 0,
    };
  },

  toJSON(message: CalculatedFieldDefinition): unknown {
    const obj: any = {};
    if (message.destinationField !== "") {
      obj.destinationField = message.destinationField;
    }
    if (message.expression !== "") {
      obj.expression = message.expression;
    }
    if (message.overwriteBehavior !== 0) {
      obj.overwriteBehavior = knowledgeOverwriteBehaviorToJSON(message.overwriteBehavior);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CalculatedFieldDefinition>, I>>(base?: I): CalculatedFieldDefinition {
    return CalculatedFieldDefinition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CalculatedFieldDefinition>, I>>(object: I): CalculatedFieldDefinition {
    const message = createBaseCalculatedFieldDefinition();
    message.destinationField = object.destinationField ?? "";
    message.expression = object.expression ?? "";
    message.overwriteBehavior = object.overwriteBehavior ?? 0;
    return message;
  },
};

function createBaseKnowledgeObjectDefinition(): KnowledgeObjectDefinition {
  return { appId: "", name: "", description: undefined, sharingScope: 0, selector: undefined, body: undefined };
}

export const KnowledgeObjectDefinition: MessageFns<KnowledgeObjectDefinition> = {
  encode(message: KnowledgeObjectDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.appId !== "") {
      writer.uint32(10).string(message.appId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.description !== undefined) {
      writer.uint32(26).string(message.description);
    }
    if (message.sharingScope !== 0) {
      writer.uint32(32).int32(message.sharingScope);
    }
    if (message.selector !== undefined) {
      KnowledgeSelector.encode(message.selector, writer.uint32(42).fork()).join();
    }
    switch (message.body?.$case) {
      case "fieldExtraction":
        FieldExtractionDefinition.encode(message.body.value, writer.uint32(82).fork()).join();
        break;
      case "fieldAlias":
        FieldAliasDefinition.encode(message.body.value, writer.uint32(90).fork()).join();
        break;
      case "calculatedField":
        CalculatedFieldDefinition.encode(message.body.value, writer.uint32(98).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeObjectDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeObjectDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.appId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.sharingScope = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.selector = KnowledgeSelector.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.body = { $case: "fieldExtraction", value: FieldExtractionDefinition.decode(reader, reader.uint32()) };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.body = { $case: "fieldAlias", value: FieldAliasDefinition.decode(reader, reader.uint32()) };
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.body = { $case: "calculatedField", value: CalculatedFieldDefinition.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeObjectDefinition {
    return {
      appId: isSet(object.appId)
        ? globalThis.String(object.appId)
        : isSet(object.app_id)
        ? globalThis.String(object.app_id)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
      sharingScope: isSet(object.sharingScope)
        ? sharingScopeFromJSON(object.sharingScope)
        : isSet(object.sharing_scope)
        ? sharingScopeFromJSON(object.sharing_scope)
        : 0,
      selector: isSet(object.selector) ? KnowledgeSelector.fromJSON(object.selector) : undefined,
      body: isSet(object.fieldExtraction)
        ? { $case: "fieldExtraction", value: FieldExtractionDefinition.fromJSON(object.fieldExtraction) }
        : isSet(object.field_extraction)
        ? { $case: "fieldExtraction", value: FieldExtractionDefinition.fromJSON(object.field_extraction) }
        : isSet(object.fieldAlias)
        ? { $case: "fieldAlias", value: FieldAliasDefinition.fromJSON(object.fieldAlias) }
        : isSet(object.field_alias)
        ? { $case: "fieldAlias", value: FieldAliasDefinition.fromJSON(object.field_alias) }
        : isSet(object.calculatedField)
        ? { $case: "calculatedField", value: CalculatedFieldDefinition.fromJSON(object.calculatedField) }
        : isSet(object.calculated_field)
        ? { $case: "calculatedField", value: CalculatedFieldDefinition.fromJSON(object.calculated_field) }
        : undefined,
    };
  },

  toJSON(message: KnowledgeObjectDefinition): unknown {
    const obj: any = {};
    if (message.appId !== "") {
      obj.appId = message.appId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.sharingScope !== 0) {
      obj.sharingScope = sharingScopeToJSON(message.sharingScope);
    }
    if (message.selector !== undefined) {
      obj.selector = KnowledgeSelector.toJSON(message.selector);
    }
    if (message.body?.$case === "fieldExtraction") {
      obj.fieldExtraction = FieldExtractionDefinition.toJSON(message.body.value);
    } else if (message.body?.$case === "fieldAlias") {
      obj.fieldAlias = FieldAliasDefinition.toJSON(message.body.value);
    } else if (message.body?.$case === "calculatedField") {
      obj.calculatedField = CalculatedFieldDefinition.toJSON(message.body.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeObjectDefinition>, I>>(base?: I): KnowledgeObjectDefinition {
    return KnowledgeObjectDefinition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeObjectDefinition>, I>>(object: I): KnowledgeObjectDefinition {
    const message = createBaseKnowledgeObjectDefinition();
    message.appId = object.appId ?? "";
    message.name = object.name ?? "";
    message.description = object.description ?? undefined;
    message.sharingScope = object.sharingScope ?? 0;
    message.selector = (object.selector !== undefined && object.selector !== null)
      ? KnowledgeSelector.fromPartial(object.selector)
      : undefined;
    switch (object.body?.$case) {
      case "fieldExtraction": {
        if (object.body?.value !== undefined && object.body?.value !== null) {
          message.body = { $case: "fieldExtraction", value: FieldExtractionDefinition.fromPartial(object.body.value) };
        }
        break;
      }
      case "fieldAlias": {
        if (object.body?.value !== undefined && object.body?.value !== null) {
          message.body = { $case: "fieldAlias", value: FieldAliasDefinition.fromPartial(object.body.value) };
        }
        break;
      }
      case "calculatedField": {
        if (object.body?.value !== undefined && object.body?.value !== null) {
          message.body = { $case: "calculatedField", value: CalculatedFieldDefinition.fromPartial(object.body.value) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseKnowledgeObject(): KnowledgeObject {
  return {
    knowledgeObjectId: "",
    tenantId: "",
    appId: "",
    ownerId: "",
    objectType: 0,
    name: "",
    version: 0n,
    sharingScope: 0,
    state: 0,
    definition: undefined,
    definitionSha256: new Uint8Array(0),
    createdAt: undefined,
    updatedAt: undefined,
    disabledAt: undefined,
  };
}

export const KnowledgeObject: MessageFns<KnowledgeObject> = {
  encode(message: KnowledgeObject, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.knowledgeObjectId !== "") {
      writer.uint32(10).string(message.knowledgeObjectId);
    }
    if (message.tenantId !== "") {
      writer.uint32(18).string(message.tenantId);
    }
    if (message.appId !== "") {
      writer.uint32(26).string(message.appId);
    }
    if (message.ownerId !== "") {
      writer.uint32(34).string(message.ownerId);
    }
    if (message.objectType !== 0) {
      writer.uint32(40).int32(message.objectType);
    }
    if (message.name !== "") {
      writer.uint32(50).string(message.name);
    }
    if (message.version !== 0n) {
      if (BigInt.asUintN(64, message.version) !== message.version) {
        throw new globalThis.Error("value provided for field message.version of type uint64 too large");
      }
      writer.uint32(56).uint64(message.version);
    }
    if (message.sharingScope !== 0) {
      writer.uint32(64).int32(message.sharingScope);
    }
    if (message.state !== 0) {
      writer.uint32(72).int32(message.state);
    }
    if (message.definition !== undefined) {
      KnowledgeObjectDefinition.encode(message.definition, writer.uint32(82).fork()).join();
    }
    if (message.definitionSha256.length !== 0) {
      writer.uint32(90).bytes(message.definitionSha256);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(98).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(106).fork()).join();
    }
    if (message.disabledAt !== undefined) {
      Timestamp.encode(toTimestamp(message.disabledAt), writer.uint32(114).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeObject {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeObject();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.knowledgeObjectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tenantId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.appId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.ownerId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.objectType = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.version = reader.uint64() as bigint;
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.sharingScope = reader.int32() as any;
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.definition = KnowledgeObjectDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.definitionSha256 = reader.bytes();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.disabledAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeObject {
    return {
      knowledgeObjectId: isSet(object.knowledgeObjectId)
        ? globalThis.String(object.knowledgeObjectId)
        : isSet(object.knowledge_object_id)
        ? globalThis.String(object.knowledge_object_id)
        : "",
      tenantId: isSet(object.tenantId)
        ? globalThis.String(object.tenantId)
        : isSet(object.tenant_id)
        ? globalThis.String(object.tenant_id)
        : "",
      appId: isSet(object.appId)
        ? globalThis.String(object.appId)
        : isSet(object.app_id)
        ? globalThis.String(object.app_id)
        : "",
      ownerId: isSet(object.ownerId)
        ? globalThis.String(object.ownerId)
        : isSet(object.owner_id)
        ? globalThis.String(object.owner_id)
        : "",
      objectType: isSet(object.objectType)
        ? knowledgeObjectTypeFromJSON(object.objectType)
        : isSet(object.object_type)
        ? knowledgeObjectTypeFromJSON(object.object_type)
        : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      version: isSet(object.version) ? BigInt(object.version) : 0n,
      sharingScope: isSet(object.sharingScope)
        ? sharingScopeFromJSON(object.sharingScope)
        : isSet(object.sharing_scope)
        ? sharingScopeFromJSON(object.sharing_scope)
        : 0,
      state: isSet(object.state) ? knowledgeObjectStateFromJSON(object.state) : 0,
      definition: isSet(object.definition) ? KnowledgeObjectDefinition.fromJSON(object.definition) : undefined,
      definitionSha256: isSet(object.definitionSha256)
        ? bytesFromBase64(object.definitionSha256)
        : isSet(object.definition_sha256)
        ? bytesFromBase64(object.definition_sha256)
        : new Uint8Array(0),
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      updatedAt: isSet(object.updatedAt)
        ? fromJsonTimestamp(object.updatedAt)
        : isSet(object.updated_at)
        ? fromJsonTimestamp(object.updated_at)
        : undefined,
      disabledAt: isSet(object.disabledAt)
        ? fromJsonTimestamp(object.disabledAt)
        : isSet(object.disabled_at)
        ? fromJsonTimestamp(object.disabled_at)
        : undefined,
    };
  },

  toJSON(message: KnowledgeObject): unknown {
    const obj: any = {};
    if (message.knowledgeObjectId !== "") {
      obj.knowledgeObjectId = message.knowledgeObjectId;
    }
    if (message.tenantId !== "") {
      obj.tenantId = message.tenantId;
    }
    if (message.appId !== "") {
      obj.appId = message.appId;
    }
    if (message.ownerId !== "") {
      obj.ownerId = message.ownerId;
    }
    if (message.objectType !== 0) {
      obj.objectType = knowledgeObjectTypeToJSON(message.objectType);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.version !== 0n) {
      obj.version = message.version.toString();
    }
    if (message.sharingScope !== 0) {
      obj.sharingScope = sharingScopeToJSON(message.sharingScope);
    }
    if (message.state !== 0) {
      obj.state = knowledgeObjectStateToJSON(message.state);
    }
    if (message.definition !== undefined) {
      obj.definition = KnowledgeObjectDefinition.toJSON(message.definition);
    }
    if (message.definitionSha256.length !== 0) {
      obj.definitionSha256 = base64FromBytes(message.definitionSha256);
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.disabledAt !== undefined) {
      obj.disabledAt = message.disabledAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeObject>, I>>(base?: I): KnowledgeObject {
    return KnowledgeObject.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeObject>, I>>(object: I): KnowledgeObject {
    const message = createBaseKnowledgeObject();
    message.knowledgeObjectId = object.knowledgeObjectId ?? "";
    message.tenantId = object.tenantId ?? "";
    message.appId = object.appId ?? "";
    message.ownerId = object.ownerId ?? "";
    message.objectType = object.objectType ?? 0;
    message.name = object.name ?? "";
    message.version = (object.version !== undefined && object.version !== null) ? BigInt(object.version) : 0n;
    message.sharingScope = object.sharingScope ?? 0;
    message.state = object.state ?? 0;
    message.definition = (object.definition !== undefined && object.definition !== null)
      ? KnowledgeObjectDefinition.fromPartial(object.definition)
      : undefined;
    message.definitionSha256 = object.definitionSha256 ?? new Uint8Array(0);
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.disabledAt = object.disabledAt ?? undefined;
    return message;
  },
};

function createBaseKnowledgeObjectVersionReference(): KnowledgeObjectVersionReference {
  return { knowledgeObjectId: "", version: 0n, definitionSha256: new Uint8Array(0) };
}

export const KnowledgeObjectVersionReference: MessageFns<KnowledgeObjectVersionReference> = {
  encode(message: KnowledgeObjectVersionReference, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.knowledgeObjectId !== "") {
      writer.uint32(10).string(message.knowledgeObjectId);
    }
    if (message.version !== 0n) {
      if (BigInt.asUintN(64, message.version) !== message.version) {
        throw new globalThis.Error("value provided for field message.version of type uint64 too large");
      }
      writer.uint32(16).uint64(message.version);
    }
    if (message.definitionSha256.length !== 0) {
      writer.uint32(26).bytes(message.definitionSha256);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeObjectVersionReference {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeObjectVersionReference();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.knowledgeObjectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.definitionSha256 = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeObjectVersionReference {
    return {
      knowledgeObjectId: isSet(object.knowledgeObjectId)
        ? globalThis.String(object.knowledgeObjectId)
        : isSet(object.knowledge_object_id)
        ? globalThis.String(object.knowledge_object_id)
        : "",
      version: isSet(object.version) ? BigInt(object.version) : 0n,
      definitionSha256: isSet(object.definitionSha256)
        ? bytesFromBase64(object.definitionSha256)
        : isSet(object.definition_sha256)
        ? bytesFromBase64(object.definition_sha256)
        : new Uint8Array(0),
    };
  },

  toJSON(message: KnowledgeObjectVersionReference): unknown {
    const obj: any = {};
    if (message.knowledgeObjectId !== "") {
      obj.knowledgeObjectId = message.knowledgeObjectId;
    }
    if (message.version !== 0n) {
      obj.version = message.version.toString();
    }
    if (message.definitionSha256.length !== 0) {
      obj.definitionSha256 = base64FromBytes(message.definitionSha256);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeObjectVersionReference>, I>>(base?: I): KnowledgeObjectVersionReference {
    return KnowledgeObjectVersionReference.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeObjectVersionReference>, I>>(
    object: I,
  ): KnowledgeObjectVersionReference {
    const message = createBaseKnowledgeObjectVersionReference();
    message.knowledgeObjectId = object.knowledgeObjectId ?? "";
    message.version = (object.version !== undefined && object.version !== null) ? BigInt(object.version) : 0n;
    message.definitionSha256 = object.definitionSha256 ?? new Uint8Array(0);
    return message;
  },
};

function createBaseKnowledgeLookupAssetVersionReference(): KnowledgeLookupAssetVersionReference {
  return { lookupAssetId: "", version: 0n, sizeBytes: 0n, contentSha256: new Uint8Array(0) };
}

export const KnowledgeLookupAssetVersionReference: MessageFns<KnowledgeLookupAssetVersionReference> = {
  encode(message: KnowledgeLookupAssetVersionReference, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lookupAssetId !== "") {
      writer.uint32(10).string(message.lookupAssetId);
    }
    if (message.version !== 0n) {
      if (BigInt.asUintN(64, message.version) !== message.version) {
        throw new globalThis.Error("value provided for field message.version of type uint64 too large");
      }
      writer.uint32(16).uint64(message.version);
    }
    if (message.sizeBytes !== 0n) {
      if (BigInt.asUintN(64, message.sizeBytes) !== message.sizeBytes) {
        throw new globalThis.Error("value provided for field message.sizeBytes of type uint64 too large");
      }
      writer.uint32(24).uint64(message.sizeBytes);
    }
    if (message.contentSha256.length !== 0) {
      writer.uint32(34).bytes(message.contentSha256);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeLookupAssetVersionReference {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeLookupAssetVersionReference();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.lookupAssetId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sizeBytes = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.contentSha256 = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeLookupAssetVersionReference {
    return {
      lookupAssetId: isSet(object.lookupAssetId)
        ? globalThis.String(object.lookupAssetId)
        : isSet(object.lookup_asset_id)
        ? globalThis.String(object.lookup_asset_id)
        : "",
      version: isSet(object.version) ? BigInt(object.version) : 0n,
      sizeBytes: isSet(object.sizeBytes)
        ? BigInt(object.sizeBytes)
        : isSet(object.size_bytes)
        ? BigInt(object.size_bytes)
        : 0n,
      contentSha256: isSet(object.contentSha256)
        ? bytesFromBase64(object.contentSha256)
        : isSet(object.content_sha256)
        ? bytesFromBase64(object.content_sha256)
        : new Uint8Array(0),
    };
  },

  toJSON(message: KnowledgeLookupAssetVersionReference): unknown {
    const obj: any = {};
    if (message.lookupAssetId !== "") {
      obj.lookupAssetId = message.lookupAssetId;
    }
    if (message.version !== 0n) {
      obj.version = message.version.toString();
    }
    if (message.sizeBytes !== 0n) {
      obj.sizeBytes = message.sizeBytes.toString();
    }
    if (message.contentSha256.length !== 0) {
      obj.contentSha256 = base64FromBytes(message.contentSha256);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeLookupAssetVersionReference>, I>>(
    base?: I,
  ): KnowledgeLookupAssetVersionReference {
    return KnowledgeLookupAssetVersionReference.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeLookupAssetVersionReference>, I>>(
    object: I,
  ): KnowledgeLookupAssetVersionReference {
    const message = createBaseKnowledgeLookupAssetVersionReference();
    message.lookupAssetId = object.lookupAssetId ?? "";
    message.version = (object.version !== undefined && object.version !== null) ? BigInt(object.version) : 0n;
    message.sizeBytes = (object.sizeBytes !== undefined && object.sizeBytes !== null) ? BigInt(object.sizeBytes) : 0n;
    message.contentSha256 = object.contentSha256 ?? new Uint8Array(0);
    return message;
  },
};

function createBaseKnowledgeDependencyTarget(): KnowledgeDependencyTarget {
  return { target: undefined };
}

export const KnowledgeDependencyTarget: MessageFns<KnowledgeDependencyTarget> = {
  encode(message: KnowledgeDependencyTarget, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.target?.$case) {
      case "object":
        KnowledgeObjectVersionReference.encode(message.target.value, writer.uint32(10).fork()).join();
        break;
      case "lookupAsset":
        KnowledgeLookupAssetVersionReference.encode(message.target.value, writer.uint32(18).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeDependencyTarget {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeDependencyTarget();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.target = { $case: "object", value: KnowledgeObjectVersionReference.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.target = {
            $case: "lookupAsset",
            value: KnowledgeLookupAssetVersionReference.decode(reader, reader.uint32()),
          };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeDependencyTarget {
    return {
      target: isSet(object.object)
        ? { $case: "object", value: KnowledgeObjectVersionReference.fromJSON(object.object) }
        : isSet(object.lookupAsset)
        ? { $case: "lookupAsset", value: KnowledgeLookupAssetVersionReference.fromJSON(object.lookupAsset) }
        : isSet(object.lookup_asset)
        ? { $case: "lookupAsset", value: KnowledgeLookupAssetVersionReference.fromJSON(object.lookup_asset) }
        : undefined,
    };
  },

  toJSON(message: KnowledgeDependencyTarget): unknown {
    const obj: any = {};
    if (message.target?.$case === "object") {
      obj.object = KnowledgeObjectVersionReference.toJSON(message.target.value);
    } else if (message.target?.$case === "lookupAsset") {
      obj.lookupAsset = KnowledgeLookupAssetVersionReference.toJSON(message.target.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeDependencyTarget>, I>>(base?: I): KnowledgeDependencyTarget {
    return KnowledgeDependencyTarget.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeDependencyTarget>, I>>(object: I): KnowledgeDependencyTarget {
    const message = createBaseKnowledgeDependencyTarget();
    switch (object.target?.$case) {
      case "object": {
        if (object.target?.value !== undefined && object.target?.value !== null) {
          message.target = { $case: "object", value: KnowledgeObjectVersionReference.fromPartial(object.target.value) };
        }
        break;
      }
      case "lookupAsset": {
        if (object.target?.value !== undefined && object.target?.value !== null) {
          message.target = {
            $case: "lookupAsset",
            value: KnowledgeLookupAssetVersionReference.fromPartial(object.target.value),
          };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseKnowledgeObjectDependency(): KnowledgeObjectDependency {
  return {
    source: undefined,
    target: undefined,
    role: 0,
    sourceStage: 0,
    targetStage: 0,
    topologicalDepth: 0,
    canonicalOrdinal: 0,
  };
}

export const KnowledgeObjectDependency: MessageFns<KnowledgeObjectDependency> = {
  encode(message: KnowledgeObjectDependency, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.source !== undefined) {
      KnowledgeObjectVersionReference.encode(message.source, writer.uint32(10).fork()).join();
    }
    if (message.target !== undefined) {
      KnowledgeDependencyTarget.encode(message.target, writer.uint32(18).fork()).join();
    }
    if (message.role !== 0) {
      writer.uint32(24).int32(message.role);
    }
    if (message.sourceStage !== 0) {
      writer.uint32(32).int32(message.sourceStage);
    }
    if (message.targetStage !== 0) {
      writer.uint32(40).int32(message.targetStage);
    }
    if (message.topologicalDepth !== 0) {
      writer.uint32(48).uint32(message.topologicalDepth);
    }
    if (message.canonicalOrdinal !== 0) {
      writer.uint32(56).uint32(message.canonicalOrdinal);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeObjectDependency {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeObjectDependency();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.source = KnowledgeObjectVersionReference.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.target = KnowledgeDependencyTarget.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.role = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.sourceStage = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.targetStage = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.topologicalDepth = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.canonicalOrdinal = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeObjectDependency {
    return {
      source: isSet(object.source) ? KnowledgeObjectVersionReference.fromJSON(object.source) : undefined,
      target: isSet(object.target) ? KnowledgeDependencyTarget.fromJSON(object.target) : undefined,
      role: isSet(object.role) ? knowledgeDependencyRoleFromJSON(object.role) : 0,
      sourceStage: isSet(object.sourceStage)
        ? knowledgeSearchStageFromJSON(object.sourceStage)
        : isSet(object.source_stage)
        ? knowledgeSearchStageFromJSON(object.source_stage)
        : 0,
      targetStage: isSet(object.targetStage)
        ? knowledgeSearchStageFromJSON(object.targetStage)
        : isSet(object.target_stage)
        ? knowledgeSearchStageFromJSON(object.target_stage)
        : 0,
      topologicalDepth: isSet(object.topologicalDepth)
        ? globalThis.Number(object.topologicalDepth)
        : isSet(object.topological_depth)
        ? globalThis.Number(object.topological_depth)
        : 0,
      canonicalOrdinal: isSet(object.canonicalOrdinal)
        ? globalThis.Number(object.canonicalOrdinal)
        : isSet(object.canonical_ordinal)
        ? globalThis.Number(object.canonical_ordinal)
        : 0,
    };
  },

  toJSON(message: KnowledgeObjectDependency): unknown {
    const obj: any = {};
    if (message.source !== undefined) {
      obj.source = KnowledgeObjectVersionReference.toJSON(message.source);
    }
    if (message.target !== undefined) {
      obj.target = KnowledgeDependencyTarget.toJSON(message.target);
    }
    if (message.role !== 0) {
      obj.role = knowledgeDependencyRoleToJSON(message.role);
    }
    if (message.sourceStage !== 0) {
      obj.sourceStage = knowledgeSearchStageToJSON(message.sourceStage);
    }
    if (message.targetStage !== 0) {
      obj.targetStage = knowledgeSearchStageToJSON(message.targetStage);
    }
    if (message.topologicalDepth !== 0) {
      obj.topologicalDepth = Math.round(message.topologicalDepth);
    }
    if (message.canonicalOrdinal !== 0) {
      obj.canonicalOrdinal = Math.round(message.canonicalOrdinal);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeObjectDependency>, I>>(base?: I): KnowledgeObjectDependency {
    return KnowledgeObjectDependency.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeObjectDependency>, I>>(object: I): KnowledgeObjectDependency {
    const message = createBaseKnowledgeObjectDependency();
    message.source = (object.source !== undefined && object.source !== null)
      ? KnowledgeObjectVersionReference.fromPartial(object.source)
      : undefined;
    message.target = (object.target !== undefined && object.target !== null)
      ? KnowledgeDependencyTarget.fromPartial(object.target)
      : undefined;
    message.role = object.role ?? 0;
    message.sourceStage = object.sourceStage ?? 0;
    message.targetStage = object.targetStage ?? 0;
    message.topologicalDepth = object.topologicalDepth ?? 0;
    message.canonicalOrdinal = object.canonicalOrdinal ?? 0;
    return message;
  },
};

function createBaseKnowledgeAuthoredProvenance(): KnowledgeAuthoredProvenance {
  return { sourceRange: undefined };
}

export const KnowledgeAuthoredProvenance: MessageFns<KnowledgeAuthoredProvenance> = {
  encode(message: KnowledgeAuthoredProvenance, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sourceRange !== undefined) {
      SourceRange.encode(message.sourceRange, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeAuthoredProvenance {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeAuthoredProvenance();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sourceRange = SourceRange.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeAuthoredProvenance {
    return {
      sourceRange: isSet(object.sourceRange)
        ? SourceRange.fromJSON(object.sourceRange)
        : isSet(object.source_range)
        ? SourceRange.fromJSON(object.source_range)
        : undefined,
    };
  },

  toJSON(message: KnowledgeAuthoredProvenance): unknown {
    const obj: any = {};
    if (message.sourceRange !== undefined) {
      obj.sourceRange = SourceRange.toJSON(message.sourceRange);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeAuthoredProvenance>, I>>(base?: I): KnowledgeAuthoredProvenance {
    return KnowledgeAuthoredProvenance.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeAuthoredProvenance>, I>>(object: I): KnowledgeAuthoredProvenance {
    const message = createBaseKnowledgeAuthoredProvenance();
    message.sourceRange = (object.sourceRange !== undefined && object.sourceRange !== null)
      ? SourceRange.fromPartial(object.sourceRange)
      : undefined;
    return message;
  },
};

function createBaseKnowledgeAuthorizedObjectProvenance(): KnowledgeAuthorizedObjectProvenance {
  return {
    knowledgeObjectId: "",
    knowledgeObjectVersion: 0n,
    objectType: 0,
    objectName: "",
    definitionLocation: "",
    stage: 0,
  };
}

export const KnowledgeAuthorizedObjectProvenance: MessageFns<KnowledgeAuthorizedObjectProvenance> = {
  encode(message: KnowledgeAuthorizedObjectProvenance, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.knowledgeObjectId !== "") {
      writer.uint32(10).string(message.knowledgeObjectId);
    }
    if (message.knowledgeObjectVersion !== 0n) {
      if (BigInt.asUintN(64, message.knowledgeObjectVersion) !== message.knowledgeObjectVersion) {
        throw new globalThis.Error("value provided for field message.knowledgeObjectVersion of type uint64 too large");
      }
      writer.uint32(16).uint64(message.knowledgeObjectVersion);
    }
    if (message.objectType !== 0) {
      writer.uint32(24).int32(message.objectType);
    }
    if (message.objectName !== "") {
      writer.uint32(34).string(message.objectName);
    }
    if (message.definitionLocation !== "") {
      writer.uint32(42).string(message.definitionLocation);
    }
    if (message.stage !== 0) {
      writer.uint32(48).int32(message.stage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeAuthorizedObjectProvenance {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeAuthorizedObjectProvenance();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.knowledgeObjectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.knowledgeObjectVersion = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.objectType = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.objectName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.definitionLocation = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.stage = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeAuthorizedObjectProvenance {
    return {
      knowledgeObjectId: isSet(object.knowledgeObjectId)
        ? globalThis.String(object.knowledgeObjectId)
        : isSet(object.knowledge_object_id)
        ? globalThis.String(object.knowledge_object_id)
        : "",
      knowledgeObjectVersion: isSet(object.knowledgeObjectVersion)
        ? BigInt(object.knowledgeObjectVersion)
        : isSet(object.knowledge_object_version)
        ? BigInt(object.knowledge_object_version)
        : 0n,
      objectType: isSet(object.objectType)
        ? knowledgeObjectTypeFromJSON(object.objectType)
        : isSet(object.object_type)
        ? knowledgeObjectTypeFromJSON(object.object_type)
        : 0,
      objectName: isSet(object.objectName)
        ? globalThis.String(object.objectName)
        : isSet(object.object_name)
        ? globalThis.String(object.object_name)
        : "",
      definitionLocation: isSet(object.definitionLocation)
        ? globalThis.String(object.definitionLocation)
        : isSet(object.definition_location)
        ? globalThis.String(object.definition_location)
        : "",
      stage: isSet(object.stage) ? knowledgeSearchStageFromJSON(object.stage) : 0,
    };
  },

  toJSON(message: KnowledgeAuthorizedObjectProvenance): unknown {
    const obj: any = {};
    if (message.knowledgeObjectId !== "") {
      obj.knowledgeObjectId = message.knowledgeObjectId;
    }
    if (message.knowledgeObjectVersion !== 0n) {
      obj.knowledgeObjectVersion = message.knowledgeObjectVersion.toString();
    }
    if (message.objectType !== 0) {
      obj.objectType = knowledgeObjectTypeToJSON(message.objectType);
    }
    if (message.objectName !== "") {
      obj.objectName = message.objectName;
    }
    if (message.definitionLocation !== "") {
      obj.definitionLocation = message.definitionLocation;
    }
    if (message.stage !== 0) {
      obj.stage = knowledgeSearchStageToJSON(message.stage);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeAuthorizedObjectProvenance>, I>>(
    base?: I,
  ): KnowledgeAuthorizedObjectProvenance {
    return KnowledgeAuthorizedObjectProvenance.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeAuthorizedObjectProvenance>, I>>(
    object: I,
  ): KnowledgeAuthorizedObjectProvenance {
    const message = createBaseKnowledgeAuthorizedObjectProvenance();
    message.knowledgeObjectId = object.knowledgeObjectId ?? "";
    message.knowledgeObjectVersion =
      (object.knowledgeObjectVersion !== undefined && object.knowledgeObjectVersion !== null)
        ? BigInt(object.knowledgeObjectVersion)
        : 0n;
    message.objectType = object.objectType ?? 0;
    message.objectName = object.objectName ?? "";
    message.definitionLocation = object.definitionLocation ?? "";
    message.stage = object.stage ?? 0;
    return message;
  },
};

function createBaseKnowledgeRedactedObjectProvenance(): KnowledgeRedactedObjectProvenance {
  return { redactedObjectOrdinal: 0, objectType: 0, stage: 0 };
}

export const KnowledgeRedactedObjectProvenance: MessageFns<KnowledgeRedactedObjectProvenance> = {
  encode(message: KnowledgeRedactedObjectProvenance, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.redactedObjectOrdinal !== 0) {
      writer.uint32(8).uint32(message.redactedObjectOrdinal);
    }
    if (message.objectType !== 0) {
      writer.uint32(16).int32(message.objectType);
    }
    if (message.stage !== 0) {
      writer.uint32(24).int32(message.stage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeRedactedObjectProvenance {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeRedactedObjectProvenance();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.redactedObjectOrdinal = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.objectType = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.stage = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeRedactedObjectProvenance {
    return {
      redactedObjectOrdinal: isSet(object.redactedObjectOrdinal)
        ? globalThis.Number(object.redactedObjectOrdinal)
        : isSet(object.redacted_object_ordinal)
        ? globalThis.Number(object.redacted_object_ordinal)
        : 0,
      objectType: isSet(object.objectType)
        ? knowledgeObjectTypeFromJSON(object.objectType)
        : isSet(object.object_type)
        ? knowledgeObjectTypeFromJSON(object.object_type)
        : 0,
      stage: isSet(object.stage) ? knowledgeSearchStageFromJSON(object.stage) : 0,
    };
  },

  toJSON(message: KnowledgeRedactedObjectProvenance): unknown {
    const obj: any = {};
    if (message.redactedObjectOrdinal !== 0) {
      obj.redactedObjectOrdinal = Math.round(message.redactedObjectOrdinal);
    }
    if (message.objectType !== 0) {
      obj.objectType = knowledgeObjectTypeToJSON(message.objectType);
    }
    if (message.stage !== 0) {
      obj.stage = knowledgeSearchStageToJSON(message.stage);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeRedactedObjectProvenance>, I>>(
    base?: I,
  ): KnowledgeRedactedObjectProvenance {
    return KnowledgeRedactedObjectProvenance.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeRedactedObjectProvenance>, I>>(
    object: I,
  ): KnowledgeRedactedObjectProvenance {
    const message = createBaseKnowledgeRedactedObjectProvenance();
    message.redactedObjectOrdinal = object.redactedObjectOrdinal ?? 0;
    message.objectType = object.objectType ?? 0;
    message.stage = object.stage ?? 0;
    return message;
  },
};

function createBaseKnowledgeProvenance(): KnowledgeProvenance {
  return { source: undefined };
}

export const KnowledgeProvenance: MessageFns<KnowledgeProvenance> = {
  encode(message: KnowledgeProvenance, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.source?.$case) {
      case "authored":
        KnowledgeAuthoredProvenance.encode(message.source.value, writer.uint32(10).fork()).join();
        break;
      case "authorizedObject":
        KnowledgeAuthorizedObjectProvenance.encode(message.source.value, writer.uint32(18).fork()).join();
        break;
      case "redactedObject":
        KnowledgeRedactedObjectProvenance.encode(message.source.value, writer.uint32(26).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeProvenance {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeProvenance();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.source = { $case: "authored", value: KnowledgeAuthoredProvenance.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.source = {
            $case: "authorizedObject",
            value: KnowledgeAuthorizedObjectProvenance.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.source = {
            $case: "redactedObject",
            value: KnowledgeRedactedObjectProvenance.decode(reader, reader.uint32()),
          };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeProvenance {
    return {
      source: isSet(object.authored)
        ? { $case: "authored", value: KnowledgeAuthoredProvenance.fromJSON(object.authored) }
        : isSet(object.authorizedObject)
        ? { $case: "authorizedObject", value: KnowledgeAuthorizedObjectProvenance.fromJSON(object.authorizedObject) }
        : isSet(object.authorized_object)
        ? { $case: "authorizedObject", value: KnowledgeAuthorizedObjectProvenance.fromJSON(object.authorized_object) }
        : isSet(object.redactedObject)
        ? { $case: "redactedObject", value: KnowledgeRedactedObjectProvenance.fromJSON(object.redactedObject) }
        : isSet(object.redacted_object)
        ? { $case: "redactedObject", value: KnowledgeRedactedObjectProvenance.fromJSON(object.redacted_object) }
        : undefined,
    };
  },

  toJSON(message: KnowledgeProvenance): unknown {
    const obj: any = {};
    if (message.source?.$case === "authored") {
      obj.authored = KnowledgeAuthoredProvenance.toJSON(message.source.value);
    } else if (message.source?.$case === "authorizedObject") {
      obj.authorizedObject = KnowledgeAuthorizedObjectProvenance.toJSON(message.source.value);
    } else if (message.source?.$case === "redactedObject") {
      obj.redactedObject = KnowledgeRedactedObjectProvenance.toJSON(message.source.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeProvenance>, I>>(base?: I): KnowledgeProvenance {
    return KnowledgeProvenance.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeProvenance>, I>>(object: I): KnowledgeProvenance {
    const message = createBaseKnowledgeProvenance();
    switch (object.source?.$case) {
      case "authored": {
        if (object.source?.value !== undefined && object.source?.value !== null) {
          message.source = { $case: "authored", value: KnowledgeAuthoredProvenance.fromPartial(object.source.value) };
        }
        break;
      }
      case "authorizedObject": {
        if (object.source?.value !== undefined && object.source?.value !== null) {
          message.source = {
            $case: "authorizedObject",
            value: KnowledgeAuthorizedObjectProvenance.fromPartial(object.source.value),
          };
        }
        break;
      }
      case "redactedObject": {
        if (object.source?.value !== undefined && object.source?.value !== null) {
          message.source = {
            $case: "redactedObject",
            value: KnowledgeRedactedObjectProvenance.fromPartial(object.source.value),
          };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseKnowledgeSnapshotObject(): KnowledgeSnapshotObject {
  return {
    resolutionOrdinal: 0,
    stage: 0,
    stageOrdinal: 0,
    knowledgeObjectId: "",
    version: 0n,
    objectType: 0,
    name: "",
    appId: "",
    ownerId: "",
    sharingScope: 0,
    definition: undefined,
    definitionSha256: new Uint8Array(0),
  };
}

export const KnowledgeSnapshotObject: MessageFns<KnowledgeSnapshotObject> = {
  encode(message: KnowledgeSnapshotObject, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.resolutionOrdinal !== 0) {
      writer.uint32(8).uint32(message.resolutionOrdinal);
    }
    if (message.stage !== 0) {
      writer.uint32(16).int32(message.stage);
    }
    if (message.stageOrdinal !== 0) {
      writer.uint32(24).uint32(message.stageOrdinal);
    }
    if (message.knowledgeObjectId !== "") {
      writer.uint32(34).string(message.knowledgeObjectId);
    }
    if (message.version !== 0n) {
      if (BigInt.asUintN(64, message.version) !== message.version) {
        throw new globalThis.Error("value provided for field message.version of type uint64 too large");
      }
      writer.uint32(40).uint64(message.version);
    }
    if (message.objectType !== 0) {
      writer.uint32(48).int32(message.objectType);
    }
    if (message.name !== "") {
      writer.uint32(58).string(message.name);
    }
    if (message.appId !== "") {
      writer.uint32(66).string(message.appId);
    }
    if (message.ownerId !== "") {
      writer.uint32(74).string(message.ownerId);
    }
    if (message.sharingScope !== 0) {
      writer.uint32(80).int32(message.sharingScope);
    }
    if (message.definition !== undefined) {
      KnowledgeObjectDefinition.encode(message.definition, writer.uint32(90).fork()).join();
    }
    if (message.definitionSha256.length !== 0) {
      writer.uint32(98).bytes(message.definitionSha256);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeSnapshotObject {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeSnapshotObject();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.resolutionOrdinal = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.stage = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.stageOrdinal = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.knowledgeObjectId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.version = reader.uint64() as bigint;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.objectType = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.appId = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.ownerId = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.sharingScope = reader.int32() as any;
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.definition = KnowledgeObjectDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.definitionSha256 = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeSnapshotObject {
    return {
      resolutionOrdinal: isSet(object.resolutionOrdinal)
        ? globalThis.Number(object.resolutionOrdinal)
        : isSet(object.resolution_ordinal)
        ? globalThis.Number(object.resolution_ordinal)
        : 0,
      stage: isSet(object.stage) ? knowledgeSearchStageFromJSON(object.stage) : 0,
      stageOrdinal: isSet(object.stageOrdinal)
        ? globalThis.Number(object.stageOrdinal)
        : isSet(object.stage_ordinal)
        ? globalThis.Number(object.stage_ordinal)
        : 0,
      knowledgeObjectId: isSet(object.knowledgeObjectId)
        ? globalThis.String(object.knowledgeObjectId)
        : isSet(object.knowledge_object_id)
        ? globalThis.String(object.knowledge_object_id)
        : "",
      version: isSet(object.version) ? BigInt(object.version) : 0n,
      objectType: isSet(object.objectType)
        ? knowledgeObjectTypeFromJSON(object.objectType)
        : isSet(object.object_type)
        ? knowledgeObjectTypeFromJSON(object.object_type)
        : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      appId: isSet(object.appId)
        ? globalThis.String(object.appId)
        : isSet(object.app_id)
        ? globalThis.String(object.app_id)
        : "",
      ownerId: isSet(object.ownerId)
        ? globalThis.String(object.ownerId)
        : isSet(object.owner_id)
        ? globalThis.String(object.owner_id)
        : "",
      sharingScope: isSet(object.sharingScope)
        ? sharingScopeFromJSON(object.sharingScope)
        : isSet(object.sharing_scope)
        ? sharingScopeFromJSON(object.sharing_scope)
        : 0,
      definition: isSet(object.definition) ? KnowledgeObjectDefinition.fromJSON(object.definition) : undefined,
      definitionSha256: isSet(object.definitionSha256)
        ? bytesFromBase64(object.definitionSha256)
        : isSet(object.definition_sha256)
        ? bytesFromBase64(object.definition_sha256)
        : new Uint8Array(0),
    };
  },

  toJSON(message: KnowledgeSnapshotObject): unknown {
    const obj: any = {};
    if (message.resolutionOrdinal !== 0) {
      obj.resolutionOrdinal = Math.round(message.resolutionOrdinal);
    }
    if (message.stage !== 0) {
      obj.stage = knowledgeSearchStageToJSON(message.stage);
    }
    if (message.stageOrdinal !== 0) {
      obj.stageOrdinal = Math.round(message.stageOrdinal);
    }
    if (message.knowledgeObjectId !== "") {
      obj.knowledgeObjectId = message.knowledgeObjectId;
    }
    if (message.version !== 0n) {
      obj.version = message.version.toString();
    }
    if (message.objectType !== 0) {
      obj.objectType = knowledgeObjectTypeToJSON(message.objectType);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.appId !== "") {
      obj.appId = message.appId;
    }
    if (message.ownerId !== "") {
      obj.ownerId = message.ownerId;
    }
    if (message.sharingScope !== 0) {
      obj.sharingScope = sharingScopeToJSON(message.sharingScope);
    }
    if (message.definition !== undefined) {
      obj.definition = KnowledgeObjectDefinition.toJSON(message.definition);
    }
    if (message.definitionSha256.length !== 0) {
      obj.definitionSha256 = base64FromBytes(message.definitionSha256);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeSnapshotObject>, I>>(base?: I): KnowledgeSnapshotObject {
    return KnowledgeSnapshotObject.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeSnapshotObject>, I>>(object: I): KnowledgeSnapshotObject {
    const message = createBaseKnowledgeSnapshotObject();
    message.resolutionOrdinal = object.resolutionOrdinal ?? 0;
    message.stage = object.stage ?? 0;
    message.stageOrdinal = object.stageOrdinal ?? 0;
    message.knowledgeObjectId = object.knowledgeObjectId ?? "";
    message.version = (object.version !== undefined && object.version !== null) ? BigInt(object.version) : 0n;
    message.objectType = object.objectType ?? 0;
    message.name = object.name ?? "";
    message.appId = object.appId ?? "";
    message.ownerId = object.ownerId ?? "";
    message.sharingScope = object.sharingScope ?? 0;
    message.definition = (object.definition !== undefined && object.definition !== null)
      ? KnowledgeObjectDefinition.fromPartial(object.definition)
      : undefined;
    message.definitionSha256 = object.definitionSha256 ?? new Uint8Array(0);
    return message;
  },
};

function createBaseKnowledgeSnapshotLookupAsset(): KnowledgeSnapshotLookupAsset {
  return { assetOrdinal: 0, asset: undefined };
}

export const KnowledgeSnapshotLookupAsset: MessageFns<KnowledgeSnapshotLookupAsset> = {
  encode(message: KnowledgeSnapshotLookupAsset, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.assetOrdinal !== 0) {
      writer.uint32(8).uint32(message.assetOrdinal);
    }
    if (message.asset !== undefined) {
      KnowledgeLookupAssetVersionReference.encode(message.asset, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeSnapshotLookupAsset {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeSnapshotLookupAsset();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.assetOrdinal = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.asset = KnowledgeLookupAssetVersionReference.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeSnapshotLookupAsset {
    return {
      assetOrdinal: isSet(object.assetOrdinal)
        ? globalThis.Number(object.assetOrdinal)
        : isSet(object.asset_ordinal)
        ? globalThis.Number(object.asset_ordinal)
        : 0,
      asset: isSet(object.asset) ? KnowledgeLookupAssetVersionReference.fromJSON(object.asset) : undefined,
    };
  },

  toJSON(message: KnowledgeSnapshotLookupAsset): unknown {
    const obj: any = {};
    if (message.assetOrdinal !== 0) {
      obj.assetOrdinal = Math.round(message.assetOrdinal);
    }
    if (message.asset !== undefined) {
      obj.asset = KnowledgeLookupAssetVersionReference.toJSON(message.asset);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeSnapshotLookupAsset>, I>>(base?: I): KnowledgeSnapshotLookupAsset {
    return KnowledgeSnapshotLookupAsset.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeSnapshotLookupAsset>, I>>(object: I): KnowledgeSnapshotLookupAsset {
    const message = createBaseKnowledgeSnapshotLookupAsset();
    message.assetOrdinal = object.assetOrdinal ?? 0;
    message.asset = (object.asset !== undefined && object.asset !== null)
      ? KnowledgeLookupAssetVersionReference.fromPartial(object.asset)
      : undefined;
    return message;
  },
};

function createBaseKnowledgeSnapshotShadow(): KnowledgeSnapshotShadow {
  return {
    shadowOrdinal: 0,
    winnerResolutionOrdinal: 0,
    knowledgeObjectId: "",
    version: 0n,
    objectType: 0,
    name: "",
    appId: "",
    ownerId: "",
    sharingScope: 0,
    definitionSha256: new Uint8Array(0),
  };
}

export const KnowledgeSnapshotShadow: MessageFns<KnowledgeSnapshotShadow> = {
  encode(message: KnowledgeSnapshotShadow, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.shadowOrdinal !== 0) {
      writer.uint32(8).uint32(message.shadowOrdinal);
    }
    if (message.winnerResolutionOrdinal !== 0) {
      writer.uint32(16).uint32(message.winnerResolutionOrdinal);
    }
    if (message.knowledgeObjectId !== "") {
      writer.uint32(26).string(message.knowledgeObjectId);
    }
    if (message.version !== 0n) {
      if (BigInt.asUintN(64, message.version) !== message.version) {
        throw new globalThis.Error("value provided for field message.version of type uint64 too large");
      }
      writer.uint32(32).uint64(message.version);
    }
    if (message.objectType !== 0) {
      writer.uint32(40).int32(message.objectType);
    }
    if (message.name !== "") {
      writer.uint32(50).string(message.name);
    }
    if (message.appId !== "") {
      writer.uint32(58).string(message.appId);
    }
    if (message.ownerId !== "") {
      writer.uint32(66).string(message.ownerId);
    }
    if (message.sharingScope !== 0) {
      writer.uint32(72).int32(message.sharingScope);
    }
    if (message.definitionSha256.length !== 0) {
      writer.uint32(82).bytes(message.definitionSha256);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeSnapshotShadow {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeSnapshotShadow();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.shadowOrdinal = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.winnerResolutionOrdinal = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.knowledgeObjectId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.version = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.objectType = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.appId = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.ownerId = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.sharingScope = reader.int32() as any;
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.definitionSha256 = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeSnapshotShadow {
    return {
      shadowOrdinal: isSet(object.shadowOrdinal)
        ? globalThis.Number(object.shadowOrdinal)
        : isSet(object.shadow_ordinal)
        ? globalThis.Number(object.shadow_ordinal)
        : 0,
      winnerResolutionOrdinal: isSet(object.winnerResolutionOrdinal)
        ? globalThis.Number(object.winnerResolutionOrdinal)
        : isSet(object.winner_resolution_ordinal)
        ? globalThis.Number(object.winner_resolution_ordinal)
        : 0,
      knowledgeObjectId: isSet(object.knowledgeObjectId)
        ? globalThis.String(object.knowledgeObjectId)
        : isSet(object.knowledge_object_id)
        ? globalThis.String(object.knowledge_object_id)
        : "",
      version: isSet(object.version) ? BigInt(object.version) : 0n,
      objectType: isSet(object.objectType)
        ? knowledgeObjectTypeFromJSON(object.objectType)
        : isSet(object.object_type)
        ? knowledgeObjectTypeFromJSON(object.object_type)
        : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      appId: isSet(object.appId)
        ? globalThis.String(object.appId)
        : isSet(object.app_id)
        ? globalThis.String(object.app_id)
        : "",
      ownerId: isSet(object.ownerId)
        ? globalThis.String(object.ownerId)
        : isSet(object.owner_id)
        ? globalThis.String(object.owner_id)
        : "",
      sharingScope: isSet(object.sharingScope)
        ? sharingScopeFromJSON(object.sharingScope)
        : isSet(object.sharing_scope)
        ? sharingScopeFromJSON(object.sharing_scope)
        : 0,
      definitionSha256: isSet(object.definitionSha256)
        ? bytesFromBase64(object.definitionSha256)
        : isSet(object.definition_sha256)
        ? bytesFromBase64(object.definition_sha256)
        : new Uint8Array(0),
    };
  },

  toJSON(message: KnowledgeSnapshotShadow): unknown {
    const obj: any = {};
    if (message.shadowOrdinal !== 0) {
      obj.shadowOrdinal = Math.round(message.shadowOrdinal);
    }
    if (message.winnerResolutionOrdinal !== 0) {
      obj.winnerResolutionOrdinal = Math.round(message.winnerResolutionOrdinal);
    }
    if (message.knowledgeObjectId !== "") {
      obj.knowledgeObjectId = message.knowledgeObjectId;
    }
    if (message.version !== 0n) {
      obj.version = message.version.toString();
    }
    if (message.objectType !== 0) {
      obj.objectType = knowledgeObjectTypeToJSON(message.objectType);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.appId !== "") {
      obj.appId = message.appId;
    }
    if (message.ownerId !== "") {
      obj.ownerId = message.ownerId;
    }
    if (message.sharingScope !== 0) {
      obj.sharingScope = sharingScopeToJSON(message.sharingScope);
    }
    if (message.definitionSha256.length !== 0) {
      obj.definitionSha256 = base64FromBytes(message.definitionSha256);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeSnapshotShadow>, I>>(base?: I): KnowledgeSnapshotShadow {
    return KnowledgeSnapshotShadow.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeSnapshotShadow>, I>>(object: I): KnowledgeSnapshotShadow {
    const message = createBaseKnowledgeSnapshotShadow();
    message.shadowOrdinal = object.shadowOrdinal ?? 0;
    message.winnerResolutionOrdinal = object.winnerResolutionOrdinal ?? 0;
    message.knowledgeObjectId = object.knowledgeObjectId ?? "";
    message.version = (object.version !== undefined && object.version !== null) ? BigInt(object.version) : 0n;
    message.objectType = object.objectType ?? 0;
    message.name = object.name ?? "";
    message.appId = object.appId ?? "";
    message.ownerId = object.ownerId ?? "";
    message.sharingScope = object.sharingScope ?? 0;
    message.definitionSha256 = object.definitionSha256 ?? new Uint8Array(0);
    return message;
  },
};

function createBaseKnowledgeSnapshotWarning(): KnowledgeSnapshotWarning {
  return { warningOrdinal: 0, kind: 0, objectResolutionOrdinal: undefined, shadowOrdinal: undefined };
}

export const KnowledgeSnapshotWarning: MessageFns<KnowledgeSnapshotWarning> = {
  encode(message: KnowledgeSnapshotWarning, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.warningOrdinal !== 0) {
      writer.uint32(8).uint32(message.warningOrdinal);
    }
    if (message.kind !== 0) {
      writer.uint32(16).int32(message.kind);
    }
    if (message.objectResolutionOrdinal !== undefined) {
      writer.uint32(24).uint32(message.objectResolutionOrdinal);
    }
    if (message.shadowOrdinal !== undefined) {
      writer.uint32(32).uint32(message.shadowOrdinal);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeSnapshotWarning {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeSnapshotWarning();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.warningOrdinal = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.objectResolutionOrdinal = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.shadowOrdinal = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeSnapshotWarning {
    return {
      warningOrdinal: isSet(object.warningOrdinal)
        ? globalThis.Number(object.warningOrdinal)
        : isSet(object.warning_ordinal)
        ? globalThis.Number(object.warning_ordinal)
        : 0,
      kind: isSet(object.kind) ? knowledgeSnapshotWarningKindFromJSON(object.kind) : 0,
      objectResolutionOrdinal: isSet(object.objectResolutionOrdinal)
        ? globalThis.Number(object.objectResolutionOrdinal)
        : isSet(object.object_resolution_ordinal)
        ? globalThis.Number(object.object_resolution_ordinal)
        : undefined,
      shadowOrdinal: isSet(object.shadowOrdinal)
        ? globalThis.Number(object.shadowOrdinal)
        : isSet(object.shadow_ordinal)
        ? globalThis.Number(object.shadow_ordinal)
        : undefined,
    };
  },

  toJSON(message: KnowledgeSnapshotWarning): unknown {
    const obj: any = {};
    if (message.warningOrdinal !== 0) {
      obj.warningOrdinal = Math.round(message.warningOrdinal);
    }
    if (message.kind !== 0) {
      obj.kind = knowledgeSnapshotWarningKindToJSON(message.kind);
    }
    if (message.objectResolutionOrdinal !== undefined) {
      obj.objectResolutionOrdinal = Math.round(message.objectResolutionOrdinal);
    }
    if (message.shadowOrdinal !== undefined) {
      obj.shadowOrdinal = Math.round(message.shadowOrdinal);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeSnapshotWarning>, I>>(base?: I): KnowledgeSnapshotWarning {
    return KnowledgeSnapshotWarning.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeSnapshotWarning>, I>>(object: I): KnowledgeSnapshotWarning {
    const message = createBaseKnowledgeSnapshotWarning();
    message.warningOrdinal = object.warningOrdinal ?? 0;
    message.kind = object.kind ?? 0;
    message.objectResolutionOrdinal = object.objectResolutionOrdinal ?? undefined;
    message.shadowOrdinal = object.shadowOrdinal ?? undefined;
    return message;
  },
};

function createBaseKnowledgeSnapshotBudgetCharges(): KnowledgeSnapshotBudgetCharges {
  return {
    executableObjects: 0,
    selectorPatterns: 0,
    selectorWildcardWorkUnits: 0n,
    canonicalSnapshotBytes: 0n,
    dependencyNodes: 0,
    dependencyEdges: 0,
    dependencyDepth: 0,
    generatedOperators: 0,
    generatedFields: 0,
    regexPrograms: 0,
    regexWorkUnits: 0n,
    regexCaptureBytes: 0n,
    scalarExpressions: 0,
    scalarExpressionNodes: 0,
    generatedSqlBytes: 0n,
  };
}

export const KnowledgeSnapshotBudgetCharges: MessageFns<KnowledgeSnapshotBudgetCharges> = {
  encode(message: KnowledgeSnapshotBudgetCharges, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.executableObjects !== 0) {
      writer.uint32(8).uint32(message.executableObjects);
    }
    if (message.selectorPatterns !== 0) {
      writer.uint32(16).uint32(message.selectorPatterns);
    }
    if (message.selectorWildcardWorkUnits !== 0n) {
      if (BigInt.asUintN(64, message.selectorWildcardWorkUnits) !== message.selectorWildcardWorkUnits) {
        throw new globalThis.Error(
          "value provided for field message.selectorWildcardWorkUnits of type uint64 too large",
        );
      }
      writer.uint32(24).uint64(message.selectorWildcardWorkUnits);
    }
    if (message.canonicalSnapshotBytes !== 0n) {
      if (BigInt.asUintN(64, message.canonicalSnapshotBytes) !== message.canonicalSnapshotBytes) {
        throw new globalThis.Error("value provided for field message.canonicalSnapshotBytes of type uint64 too large");
      }
      writer.uint32(32).uint64(message.canonicalSnapshotBytes);
    }
    if (message.dependencyNodes !== 0) {
      writer.uint32(40).uint32(message.dependencyNodes);
    }
    if (message.dependencyEdges !== 0) {
      writer.uint32(48).uint32(message.dependencyEdges);
    }
    if (message.dependencyDepth !== 0) {
      writer.uint32(56).uint32(message.dependencyDepth);
    }
    if (message.generatedOperators !== 0) {
      writer.uint32(64).uint32(message.generatedOperators);
    }
    if (message.generatedFields !== 0) {
      writer.uint32(72).uint32(message.generatedFields);
    }
    if (message.regexPrograms !== 0) {
      writer.uint32(80).uint32(message.regexPrograms);
    }
    if (message.regexWorkUnits !== 0n) {
      if (BigInt.asUintN(64, message.regexWorkUnits) !== message.regexWorkUnits) {
        throw new globalThis.Error("value provided for field message.regexWorkUnits of type uint64 too large");
      }
      writer.uint32(88).uint64(message.regexWorkUnits);
    }
    if (message.regexCaptureBytes !== 0n) {
      if (BigInt.asUintN(64, message.regexCaptureBytes) !== message.regexCaptureBytes) {
        throw new globalThis.Error("value provided for field message.regexCaptureBytes of type uint64 too large");
      }
      writer.uint32(96).uint64(message.regexCaptureBytes);
    }
    if (message.scalarExpressions !== 0) {
      writer.uint32(104).uint32(message.scalarExpressions);
    }
    if (message.scalarExpressionNodes !== 0) {
      writer.uint32(112).uint32(message.scalarExpressionNodes);
    }
    if (message.generatedSqlBytes !== 0n) {
      if (BigInt.asUintN(64, message.generatedSqlBytes) !== message.generatedSqlBytes) {
        throw new globalThis.Error("value provided for field message.generatedSqlBytes of type uint64 too large");
      }
      writer.uint32(120).uint64(message.generatedSqlBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeSnapshotBudgetCharges {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeSnapshotBudgetCharges();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.executableObjects = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.selectorPatterns = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.selectorWildcardWorkUnits = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.canonicalSnapshotBytes = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.dependencyNodes = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.dependencyEdges = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.dependencyDepth = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.generatedOperators = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.generatedFields = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.regexPrograms = reader.uint32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.regexWorkUnits = reader.uint64() as bigint;
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.regexCaptureBytes = reader.uint64() as bigint;
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.scalarExpressions = reader.uint32();
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.scalarExpressionNodes = reader.uint32();
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.generatedSqlBytes = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeSnapshotBudgetCharges {
    return {
      executableObjects: isSet(object.executableObjects)
        ? globalThis.Number(object.executableObjects)
        : isSet(object.executable_objects)
        ? globalThis.Number(object.executable_objects)
        : 0,
      selectorPatterns: isSet(object.selectorPatterns)
        ? globalThis.Number(object.selectorPatterns)
        : isSet(object.selector_patterns)
        ? globalThis.Number(object.selector_patterns)
        : 0,
      selectorWildcardWorkUnits: isSet(object.selectorWildcardWorkUnits)
        ? BigInt(object.selectorWildcardWorkUnits)
        : isSet(object.selector_wildcard_work_units)
        ? BigInt(object.selector_wildcard_work_units)
        : 0n,
      canonicalSnapshotBytes: isSet(object.canonicalSnapshotBytes)
        ? BigInt(object.canonicalSnapshotBytes)
        : isSet(object.canonical_snapshot_bytes)
        ? BigInt(object.canonical_snapshot_bytes)
        : 0n,
      dependencyNodes: isSet(object.dependencyNodes)
        ? globalThis.Number(object.dependencyNodes)
        : isSet(object.dependency_nodes)
        ? globalThis.Number(object.dependency_nodes)
        : 0,
      dependencyEdges: isSet(object.dependencyEdges)
        ? globalThis.Number(object.dependencyEdges)
        : isSet(object.dependency_edges)
        ? globalThis.Number(object.dependency_edges)
        : 0,
      dependencyDepth: isSet(object.dependencyDepth)
        ? globalThis.Number(object.dependencyDepth)
        : isSet(object.dependency_depth)
        ? globalThis.Number(object.dependency_depth)
        : 0,
      generatedOperators: isSet(object.generatedOperators)
        ? globalThis.Number(object.generatedOperators)
        : isSet(object.generated_operators)
        ? globalThis.Number(object.generated_operators)
        : 0,
      generatedFields: isSet(object.generatedFields)
        ? globalThis.Number(object.generatedFields)
        : isSet(object.generated_fields)
        ? globalThis.Number(object.generated_fields)
        : 0,
      regexPrograms: isSet(object.regexPrograms)
        ? globalThis.Number(object.regexPrograms)
        : isSet(object.regex_programs)
        ? globalThis.Number(object.regex_programs)
        : 0,
      regexWorkUnits: isSet(object.regexWorkUnits)
        ? BigInt(object.regexWorkUnits)
        : isSet(object.regex_work_units)
        ? BigInt(object.regex_work_units)
        : 0n,
      regexCaptureBytes: isSet(object.regexCaptureBytes)
        ? BigInt(object.regexCaptureBytes)
        : isSet(object.regex_capture_bytes)
        ? BigInt(object.regex_capture_bytes)
        : 0n,
      scalarExpressions: isSet(object.scalarExpressions)
        ? globalThis.Number(object.scalarExpressions)
        : isSet(object.scalar_expressions)
        ? globalThis.Number(object.scalar_expressions)
        : 0,
      scalarExpressionNodes: isSet(object.scalarExpressionNodes)
        ? globalThis.Number(object.scalarExpressionNodes)
        : isSet(object.scalar_expression_nodes)
        ? globalThis.Number(object.scalar_expression_nodes)
        : 0,
      generatedSqlBytes: isSet(object.generatedSqlBytes)
        ? BigInt(object.generatedSqlBytes)
        : isSet(object.generated_sql_bytes)
        ? BigInt(object.generated_sql_bytes)
        : 0n,
    };
  },

  toJSON(message: KnowledgeSnapshotBudgetCharges): unknown {
    const obj: any = {};
    if (message.executableObjects !== 0) {
      obj.executableObjects = Math.round(message.executableObjects);
    }
    if (message.selectorPatterns !== 0) {
      obj.selectorPatterns = Math.round(message.selectorPatterns);
    }
    if (message.selectorWildcardWorkUnits !== 0n) {
      obj.selectorWildcardWorkUnits = message.selectorWildcardWorkUnits.toString();
    }
    if (message.canonicalSnapshotBytes !== 0n) {
      obj.canonicalSnapshotBytes = message.canonicalSnapshotBytes.toString();
    }
    if (message.dependencyNodes !== 0) {
      obj.dependencyNodes = Math.round(message.dependencyNodes);
    }
    if (message.dependencyEdges !== 0) {
      obj.dependencyEdges = Math.round(message.dependencyEdges);
    }
    if (message.dependencyDepth !== 0) {
      obj.dependencyDepth = Math.round(message.dependencyDepth);
    }
    if (message.generatedOperators !== 0) {
      obj.generatedOperators = Math.round(message.generatedOperators);
    }
    if (message.generatedFields !== 0) {
      obj.generatedFields = Math.round(message.generatedFields);
    }
    if (message.regexPrograms !== 0) {
      obj.regexPrograms = Math.round(message.regexPrograms);
    }
    if (message.regexWorkUnits !== 0n) {
      obj.regexWorkUnits = message.regexWorkUnits.toString();
    }
    if (message.regexCaptureBytes !== 0n) {
      obj.regexCaptureBytes = message.regexCaptureBytes.toString();
    }
    if (message.scalarExpressions !== 0) {
      obj.scalarExpressions = Math.round(message.scalarExpressions);
    }
    if (message.scalarExpressionNodes !== 0) {
      obj.scalarExpressionNodes = Math.round(message.scalarExpressionNodes);
    }
    if (message.generatedSqlBytes !== 0n) {
      obj.generatedSqlBytes = message.generatedSqlBytes.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeSnapshotBudgetCharges>, I>>(base?: I): KnowledgeSnapshotBudgetCharges {
    return KnowledgeSnapshotBudgetCharges.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeSnapshotBudgetCharges>, I>>(
    object: I,
  ): KnowledgeSnapshotBudgetCharges {
    const message = createBaseKnowledgeSnapshotBudgetCharges();
    message.executableObjects = object.executableObjects ?? 0;
    message.selectorPatterns = object.selectorPatterns ?? 0;
    message.selectorWildcardWorkUnits =
      (object.selectorWildcardWorkUnits !== undefined && object.selectorWildcardWorkUnits !== null)
        ? BigInt(object.selectorWildcardWorkUnits)
        : 0n;
    message.canonicalSnapshotBytes =
      (object.canonicalSnapshotBytes !== undefined && object.canonicalSnapshotBytes !== null)
        ? BigInt(object.canonicalSnapshotBytes)
        : 0n;
    message.dependencyNodes = object.dependencyNodes ?? 0;
    message.dependencyEdges = object.dependencyEdges ?? 0;
    message.dependencyDepth = object.dependencyDepth ?? 0;
    message.generatedOperators = object.generatedOperators ?? 0;
    message.generatedFields = object.generatedFields ?? 0;
    message.regexPrograms = object.regexPrograms ?? 0;
    message.regexWorkUnits = (object.regexWorkUnits !== undefined && object.regexWorkUnits !== null)
      ? BigInt(object.regexWorkUnits)
      : 0n;
    message.regexCaptureBytes = (object.regexCaptureBytes !== undefined && object.regexCaptureBytes !== null)
      ? BigInt(object.regexCaptureBytes)
      : 0n;
    message.scalarExpressions = object.scalarExpressions ?? 0;
    message.scalarExpressionNodes = object.scalarExpressionNodes ?? 0;
    message.generatedSqlBytes = (object.generatedSqlBytes !== undefined && object.generatedSqlBytes !== null)
      ? BigInt(object.generatedSqlBytes)
      : 0n;
    return message;
  },
};

function createBaseKnowledgeSnapshot(): KnowledgeSnapshot {
  return {
    formatVersion: 0,
    tenantId: "",
    principalId: "",
    appId: "",
    tenantCatalogRevision: 0n,
    appCatalogRevision: undefined,
    compilerCompatibilityVersion: "",
    objects: [],
    dependencies: [],
    lookupAssets: [],
    snapshotSha256: new Uint8Array(0),
    effectiveAuthorizedIndexes: [],
    shadows: [],
    warnings: [],
    budgetCharges: undefined,
  };
}

export const KnowledgeSnapshot: MessageFns<KnowledgeSnapshot> = {
  encode(message: KnowledgeSnapshot, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.formatVersion !== 0) {
      writer.uint32(8).uint32(message.formatVersion);
    }
    if (message.tenantId !== "") {
      writer.uint32(18).string(message.tenantId);
    }
    if (message.principalId !== "") {
      writer.uint32(26).string(message.principalId);
    }
    if (message.appId !== "") {
      writer.uint32(34).string(message.appId);
    }
    if (message.tenantCatalogRevision !== 0n) {
      if (BigInt.asUintN(64, message.tenantCatalogRevision) !== message.tenantCatalogRevision) {
        throw new globalThis.Error("value provided for field message.tenantCatalogRevision of type uint64 too large");
      }
      writer.uint32(40).uint64(message.tenantCatalogRevision);
    }
    if (message.appCatalogRevision !== undefined) {
      if (BigInt.asUintN(64, message.appCatalogRevision) !== message.appCatalogRevision) {
        throw new globalThis.Error("value provided for field message.appCatalogRevision of type uint64 too large");
      }
      writer.uint32(48).uint64(message.appCatalogRevision);
    }
    if (message.compilerCompatibilityVersion !== "") {
      writer.uint32(58).string(message.compilerCompatibilityVersion);
    }
    for (const v of message.objects) {
      KnowledgeSnapshotObject.encode(v!, writer.uint32(66).fork()).join();
    }
    for (const v of message.dependencies) {
      KnowledgeObjectDependency.encode(v!, writer.uint32(74).fork()).join();
    }
    for (const v of message.lookupAssets) {
      KnowledgeSnapshotLookupAsset.encode(v!, writer.uint32(82).fork()).join();
    }
    if (message.snapshotSha256.length !== 0) {
      writer.uint32(90).bytes(message.snapshotSha256);
    }
    for (const v of message.effectiveAuthorizedIndexes) {
      writer.uint32(98).string(v!);
    }
    for (const v of message.shadows) {
      KnowledgeSnapshotShadow.encode(v!, writer.uint32(106).fork()).join();
    }
    for (const v of message.warnings) {
      KnowledgeSnapshotWarning.encode(v!, writer.uint32(114).fork()).join();
    }
    if (message.budgetCharges !== undefined) {
      KnowledgeSnapshotBudgetCharges.encode(message.budgetCharges, writer.uint32(122).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeSnapshot {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeSnapshot();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.formatVersion = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tenantId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.principalId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.appId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.tenantCatalogRevision = reader.uint64() as bigint;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.appCatalogRevision = reader.uint64() as bigint;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.compilerCompatibilityVersion = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.objects.push(KnowledgeSnapshotObject.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.dependencies.push(KnowledgeObjectDependency.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.lookupAssets.push(KnowledgeSnapshotLookupAsset.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.snapshotSha256 = reader.bytes();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.effectiveAuthorizedIndexes.push(reader.string());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.shadows.push(KnowledgeSnapshotShadow.decode(reader, reader.uint32()));
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.warnings.push(KnowledgeSnapshotWarning.decode(reader, reader.uint32()));
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.budgetCharges = KnowledgeSnapshotBudgetCharges.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeSnapshot {
    return {
      formatVersion: isSet(object.formatVersion)
        ? globalThis.Number(object.formatVersion)
        : isSet(object.format_version)
        ? globalThis.Number(object.format_version)
        : 0,
      tenantId: isSet(object.tenantId)
        ? globalThis.String(object.tenantId)
        : isSet(object.tenant_id)
        ? globalThis.String(object.tenant_id)
        : "",
      principalId: isSet(object.principalId)
        ? globalThis.String(object.principalId)
        : isSet(object.principal_id)
        ? globalThis.String(object.principal_id)
        : "",
      appId: isSet(object.appId)
        ? globalThis.String(object.appId)
        : isSet(object.app_id)
        ? globalThis.String(object.app_id)
        : "",
      tenantCatalogRevision: isSet(object.tenantCatalogRevision)
        ? BigInt(object.tenantCatalogRevision)
        : isSet(object.tenant_catalog_revision)
        ? BigInt(object.tenant_catalog_revision)
        : 0n,
      appCatalogRevision: isSet(object.appCatalogRevision)
        ? BigInt(object.appCatalogRevision)
        : isSet(object.app_catalog_revision)
        ? BigInt(object.app_catalog_revision)
        : undefined,
      compilerCompatibilityVersion: isSet(object.compilerCompatibilityVersion)
        ? globalThis.String(object.compilerCompatibilityVersion)
        : isSet(object.compiler_compatibility_version)
        ? globalThis.String(object.compiler_compatibility_version)
        : "",
      objects: globalThis.Array.isArray(object?.objects)
        ? object.objects.map((e: any) => KnowledgeSnapshotObject.fromJSON(e))
        : [],
      dependencies: globalThis.Array.isArray(object?.dependencies)
        ? object.dependencies.map((e: any) => KnowledgeObjectDependency.fromJSON(e))
        : [],
      lookupAssets: globalThis.Array.isArray(object?.lookupAssets)
        ? object.lookupAssets.map((e: any) => KnowledgeSnapshotLookupAsset.fromJSON(e))
        : globalThis.Array.isArray(object?.lookup_assets)
        ? object.lookup_assets.map((e: any) => KnowledgeSnapshotLookupAsset.fromJSON(e))
        : [],
      snapshotSha256: isSet(object.snapshotSha256)
        ? bytesFromBase64(object.snapshotSha256)
        : isSet(object.snapshot_sha256)
        ? bytesFromBase64(object.snapshot_sha256)
        : new Uint8Array(0),
      effectiveAuthorizedIndexes: globalThis.Array.isArray(object?.effectiveAuthorizedIndexes)
        ? object.effectiveAuthorizedIndexes.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.effective_authorized_indexes)
        ? object.effective_authorized_indexes.map((e: any) => globalThis.String(e))
        : [],
      shadows: globalThis.Array.isArray(object?.shadows)
        ? object.shadows.map((e: any) => KnowledgeSnapshotShadow.fromJSON(e))
        : [],
      warnings: globalThis.Array.isArray(object?.warnings)
        ? object.warnings.map((e: any) => KnowledgeSnapshotWarning.fromJSON(e))
        : [],
      budgetCharges: isSet(object.budgetCharges)
        ? KnowledgeSnapshotBudgetCharges.fromJSON(object.budgetCharges)
        : isSet(object.budget_charges)
        ? KnowledgeSnapshotBudgetCharges.fromJSON(object.budget_charges)
        : undefined,
    };
  },

  toJSON(message: KnowledgeSnapshot): unknown {
    const obj: any = {};
    if (message.formatVersion !== 0) {
      obj.formatVersion = Math.round(message.formatVersion);
    }
    if (message.tenantId !== "") {
      obj.tenantId = message.tenantId;
    }
    if (message.principalId !== "") {
      obj.principalId = message.principalId;
    }
    if (message.appId !== "") {
      obj.appId = message.appId;
    }
    if (message.tenantCatalogRevision !== 0n) {
      obj.tenantCatalogRevision = message.tenantCatalogRevision.toString();
    }
    if (message.appCatalogRevision !== undefined) {
      obj.appCatalogRevision = message.appCatalogRevision.toString();
    }
    if (message.compilerCompatibilityVersion !== "") {
      obj.compilerCompatibilityVersion = message.compilerCompatibilityVersion;
    }
    if (message.objects?.length) {
      obj.objects = message.objects.map((e) => KnowledgeSnapshotObject.toJSON(e));
    }
    if (message.dependencies?.length) {
      obj.dependencies = message.dependencies.map((e) => KnowledgeObjectDependency.toJSON(e));
    }
    if (message.lookupAssets?.length) {
      obj.lookupAssets = message.lookupAssets.map((e) => KnowledgeSnapshotLookupAsset.toJSON(e));
    }
    if (message.snapshotSha256.length !== 0) {
      obj.snapshotSha256 = base64FromBytes(message.snapshotSha256);
    }
    if (message.effectiveAuthorizedIndexes?.length) {
      obj.effectiveAuthorizedIndexes = message.effectiveAuthorizedIndexes;
    }
    if (message.shadows?.length) {
      obj.shadows = message.shadows.map((e) => KnowledgeSnapshotShadow.toJSON(e));
    }
    if (message.warnings?.length) {
      obj.warnings = message.warnings.map((e) => KnowledgeSnapshotWarning.toJSON(e));
    }
    if (message.budgetCharges !== undefined) {
      obj.budgetCharges = KnowledgeSnapshotBudgetCharges.toJSON(message.budgetCharges);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeSnapshot>, I>>(base?: I): KnowledgeSnapshot {
    return KnowledgeSnapshot.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeSnapshot>, I>>(object: I): KnowledgeSnapshot {
    const message = createBaseKnowledgeSnapshot();
    message.formatVersion = object.formatVersion ?? 0;
    message.tenantId = object.tenantId ?? "";
    message.principalId = object.principalId ?? "";
    message.appId = object.appId ?? "";
    message.tenantCatalogRevision =
      (object.tenantCatalogRevision !== undefined && object.tenantCatalogRevision !== null)
        ? BigInt(object.tenantCatalogRevision)
        : 0n;
    message.appCatalogRevision = (object.appCatalogRevision !== undefined && object.appCatalogRevision !== null)
      ? BigInt(object.appCatalogRevision)
      : undefined;
    message.compilerCompatibilityVersion = object.compilerCompatibilityVersion ?? "";
    message.objects = object.objects?.map((e) => KnowledgeSnapshotObject.fromPartial(e)) || [];
    message.dependencies = object.dependencies?.map((e) => KnowledgeObjectDependency.fromPartial(e)) || [];
    message.lookupAssets = object.lookupAssets?.map((e) => KnowledgeSnapshotLookupAsset.fromPartial(e)) || [];
    message.snapshotSha256 = object.snapshotSha256 ?? new Uint8Array(0);
    message.effectiveAuthorizedIndexes = object.effectiveAuthorizedIndexes?.map((e) => e) || [];
    message.shadows = object.shadows?.map((e) => KnowledgeSnapshotShadow.fromPartial(e)) || [];
    message.warnings = object.warnings?.map((e) => KnowledgeSnapshotWarning.fromPartial(e)) || [];
    message.budgetCharges = (object.budgetCharges !== undefined && object.budgetCharges !== null)
      ? KnowledgeSnapshotBudgetCharges.fromPartial(object.budgetCharges)
      : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
