// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.0
// source: open_splunk/v1/system_api.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../../google/protobuf/duration";
import { Timestamp } from "../../google/protobuf/timestamp";
import { AppSummary } from "./app";
import { IndexSummary } from "./index";

export enum ServerFeature {
  SERVER_FEATURE_UNSPECIFIED = 0,
  SERVER_FEATURE_SEARCH = 1,
  SERVER_FEATURE_SEARCH_PREVIEW = 2,
  SERVER_FEATURE_FIELD_DISCOVERY = 3,
  SERVER_FEATURE_TIMELINE = 4,
  SERVER_FEATURE_SAVED_SEARCHES = 5,
  SERVER_FEATURE_SEARCH_HISTORY = 6,
  SERVER_FEATURE_EXPORT_CSV = 7,
  SERVER_FEATURE_EXPORT_JSON_LINES = 8,
  SERVER_FEATURE_COLLECTOR_ADMIN = 9,
  SERVER_FEATURE_INDEX_ADMIN = 10,
  SERVER_FEATURE_APP_ADMIN = 11,
  SERVER_FEATURE_PLAN_INSPECTION = 12,
  UNRECOGNIZED = -1,
}

export function serverFeatureFromJSON(object: any): ServerFeature {
  switch (object) {
    case 0:
    case "SERVER_FEATURE_UNSPECIFIED":
      return ServerFeature.SERVER_FEATURE_UNSPECIFIED;
    case 1:
    case "SERVER_FEATURE_SEARCH":
      return ServerFeature.SERVER_FEATURE_SEARCH;
    case 2:
    case "SERVER_FEATURE_SEARCH_PREVIEW":
      return ServerFeature.SERVER_FEATURE_SEARCH_PREVIEW;
    case 3:
    case "SERVER_FEATURE_FIELD_DISCOVERY":
      return ServerFeature.SERVER_FEATURE_FIELD_DISCOVERY;
    case 4:
    case "SERVER_FEATURE_TIMELINE":
      return ServerFeature.SERVER_FEATURE_TIMELINE;
    case 5:
    case "SERVER_FEATURE_SAVED_SEARCHES":
      return ServerFeature.SERVER_FEATURE_SAVED_SEARCHES;
    case 6:
    case "SERVER_FEATURE_SEARCH_HISTORY":
      return ServerFeature.SERVER_FEATURE_SEARCH_HISTORY;
    case 7:
    case "SERVER_FEATURE_EXPORT_CSV":
      return ServerFeature.SERVER_FEATURE_EXPORT_CSV;
    case 8:
    case "SERVER_FEATURE_EXPORT_JSON_LINES":
      return ServerFeature.SERVER_FEATURE_EXPORT_JSON_LINES;
    case 9:
    case "SERVER_FEATURE_COLLECTOR_ADMIN":
      return ServerFeature.SERVER_FEATURE_COLLECTOR_ADMIN;
    case 10:
    case "SERVER_FEATURE_INDEX_ADMIN":
      return ServerFeature.SERVER_FEATURE_INDEX_ADMIN;
    case 11:
    case "SERVER_FEATURE_APP_ADMIN":
      return ServerFeature.SERVER_FEATURE_APP_ADMIN;
    case 12:
    case "SERVER_FEATURE_PLAN_INSPECTION":
      return ServerFeature.SERVER_FEATURE_PLAN_INSPECTION;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ServerFeature.UNRECOGNIZED;
  }
}

export function serverFeatureToJSON(object: ServerFeature): string {
  switch (object) {
    case ServerFeature.SERVER_FEATURE_UNSPECIFIED:
      return "SERVER_FEATURE_UNSPECIFIED";
    case ServerFeature.SERVER_FEATURE_SEARCH:
      return "SERVER_FEATURE_SEARCH";
    case ServerFeature.SERVER_FEATURE_SEARCH_PREVIEW:
      return "SERVER_FEATURE_SEARCH_PREVIEW";
    case ServerFeature.SERVER_FEATURE_FIELD_DISCOVERY:
      return "SERVER_FEATURE_FIELD_DISCOVERY";
    case ServerFeature.SERVER_FEATURE_TIMELINE:
      return "SERVER_FEATURE_TIMELINE";
    case ServerFeature.SERVER_FEATURE_SAVED_SEARCHES:
      return "SERVER_FEATURE_SAVED_SEARCHES";
    case ServerFeature.SERVER_FEATURE_SEARCH_HISTORY:
      return "SERVER_FEATURE_SEARCH_HISTORY";
    case ServerFeature.SERVER_FEATURE_EXPORT_CSV:
      return "SERVER_FEATURE_EXPORT_CSV";
    case ServerFeature.SERVER_FEATURE_EXPORT_JSON_LINES:
      return "SERVER_FEATURE_EXPORT_JSON_LINES";
    case ServerFeature.SERVER_FEATURE_COLLECTOR_ADMIN:
      return "SERVER_FEATURE_COLLECTOR_ADMIN";
    case ServerFeature.SERVER_FEATURE_INDEX_ADMIN:
      return "SERVER_FEATURE_INDEX_ADMIN";
    case ServerFeature.SERVER_FEATURE_APP_ADMIN:
      return "SERVER_FEATURE_APP_ADMIN";
    case ServerFeature.SERVER_FEATURE_PLAN_INSPECTION:
      return "SERVER_FEATURE_PLAN_INSPECTION";
    case ServerFeature.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface BrowserApiLimits {
  maximumPageSize: number;
  maximumPreviewRows: number;
  maximumWebsocketSubscriptions: number;
  maximumWebsocketFrameBytes: bigint;
  maximumExportRows: bigint;
  maximumExportBytes: bigint;
  defaultSearchTimeout: Duration | undefined;
  searchResultRetention: Duration | undefined;
}

/** POST /api/v1/system/bootstrap */
export interface GetSystemBootstrapRequest {
  preferredAppId?: string | undefined;
}

export interface GetSystemBootstrapResponse {
  serverVersion: string;
  apiVersion: string;
  splCompatibilityVersion: string;
  searchWebsocketPath: string;
  features: ServerFeature[];
  limits: BrowserApiLimits | undefined;
  apps: AppSummary[];
  indexes: IndexSummary[];
  selectedAppId?: string | undefined;
  serverTime: Date | undefined;
}

function createBaseBrowserApiLimits(): BrowserApiLimits {
  return {
    maximumPageSize: 0,
    maximumPreviewRows: 0,
    maximumWebsocketSubscriptions: 0,
    maximumWebsocketFrameBytes: 0n,
    maximumExportRows: 0n,
    maximumExportBytes: 0n,
    defaultSearchTimeout: undefined,
    searchResultRetention: undefined,
  };
}

export const BrowserApiLimits: MessageFns<BrowserApiLimits> = {
  encode(message: BrowserApiLimits, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maximumPageSize !== 0) {
      writer.uint32(8).uint32(message.maximumPageSize);
    }
    if (message.maximumPreviewRows !== 0) {
      writer.uint32(16).uint32(message.maximumPreviewRows);
    }
    if (message.maximumWebsocketSubscriptions !== 0) {
      writer.uint32(24).uint32(message.maximumWebsocketSubscriptions);
    }
    if (message.maximumWebsocketFrameBytes !== 0n) {
      if (BigInt.asUintN(64, message.maximumWebsocketFrameBytes) !== message.maximumWebsocketFrameBytes) {
        throw new globalThis.Error(
          "value provided for field message.maximumWebsocketFrameBytes of type uint64 too large",
        );
      }
      writer.uint32(32).uint64(message.maximumWebsocketFrameBytes);
    }
    if (message.maximumExportRows !== 0n) {
      if (BigInt.asUintN(64, message.maximumExportRows) !== message.maximumExportRows) {
        throw new globalThis.Error("value provided for field message.maximumExportRows of type uint64 too large");
      }
      writer.uint32(40).uint64(message.maximumExportRows);
    }
    if (message.maximumExportBytes !== 0n) {
      if (BigInt.asUintN(64, message.maximumExportBytes) !== message.maximumExportBytes) {
        throw new globalThis.Error("value provided for field message.maximumExportBytes of type uint64 too large");
      }
      writer.uint32(48).uint64(message.maximumExportBytes);
    }
    if (message.defaultSearchTimeout !== undefined) {
      Duration.encode(message.defaultSearchTimeout, writer.uint32(58).fork()).join();
    }
    if (message.searchResultRetention !== undefined) {
      Duration.encode(message.searchResultRetention, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BrowserApiLimits {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBrowserApiLimits();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.maximumPageSize = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.maximumPreviewRows = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maximumWebsocketSubscriptions = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.maximumWebsocketFrameBytes = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.maximumExportRows = reader.uint64() as bigint;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.maximumExportBytes = reader.uint64() as bigint;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.defaultSearchTimeout = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.searchResultRetention = Duration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BrowserApiLimits {
    return {
      maximumPageSize: isSet(object.maximumPageSize)
        ? globalThis.Number(object.maximumPageSize)
        : isSet(object.maximum_page_size)
        ? globalThis.Number(object.maximum_page_size)
        : 0,
      maximumPreviewRows: isSet(object.maximumPreviewRows)
        ? globalThis.Number(object.maximumPreviewRows)
        : isSet(object.maximum_preview_rows)
        ? globalThis.Number(object.maximum_preview_rows)
        : 0,
      maximumWebsocketSubscriptions: isSet(object.maximumWebsocketSubscriptions)
        ? globalThis.Number(object.maximumWebsocketSubscriptions)
        : isSet(object.maximum_websocket_subscriptions)
        ? globalThis.Number(object.maximum_websocket_subscriptions)
        : 0,
      maximumWebsocketFrameBytes: isSet(object.maximumWebsocketFrameBytes)
        ? BigInt(object.maximumWebsocketFrameBytes)
        : isSet(object.maximum_websocket_frame_bytes)
        ? BigInt(object.maximum_websocket_frame_bytes)
        : 0n,
      maximumExportRows: isSet(object.maximumExportRows)
        ? BigInt(object.maximumExportRows)
        : isSet(object.maximum_export_rows)
        ? BigInt(object.maximum_export_rows)
        : 0n,
      maximumExportBytes: isSet(object.maximumExportBytes)
        ? BigInt(object.maximumExportBytes)
        : isSet(object.maximum_export_bytes)
        ? BigInt(object.maximum_export_bytes)
        : 0n,
      defaultSearchTimeout: isSet(object.defaultSearchTimeout)
        ? Duration.fromJSON(object.defaultSearchTimeout)
        : isSet(object.default_search_timeout)
        ? Duration.fromJSON(object.default_search_timeout)
        : undefined,
      searchResultRetention: isSet(object.searchResultRetention)
        ? Duration.fromJSON(object.searchResultRetention)
        : isSet(object.search_result_retention)
        ? Duration.fromJSON(object.search_result_retention)
        : undefined,
    };
  },

  toJSON(message: BrowserApiLimits): unknown {
    const obj: any = {};
    if (message.maximumPageSize !== 0) {
      obj.maximumPageSize = Math.round(message.maximumPageSize);
    }
    if (message.maximumPreviewRows !== 0) {
      obj.maximumPreviewRows = Math.round(message.maximumPreviewRows);
    }
    if (message.maximumWebsocketSubscriptions !== 0) {
      obj.maximumWebsocketSubscriptions = Math.round(message.maximumWebsocketSubscriptions);
    }
    if (message.maximumWebsocketFrameBytes !== 0n) {
      obj.maximumWebsocketFrameBytes = message.maximumWebsocketFrameBytes.toString();
    }
    if (message.maximumExportRows !== 0n) {
      obj.maximumExportRows = message.maximumExportRows.toString();
    }
    if (message.maximumExportBytes !== 0n) {
      obj.maximumExportBytes = message.maximumExportBytes.toString();
    }
    if (message.defaultSearchTimeout !== undefined) {
      obj.defaultSearchTimeout = Duration.toJSON(message.defaultSearchTimeout);
    }
    if (message.searchResultRetention !== undefined) {
      obj.searchResultRetention = Duration.toJSON(message.searchResultRetention);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BrowserApiLimits>, I>>(base?: I): BrowserApiLimits {
    return BrowserApiLimits.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BrowserApiLimits>, I>>(object: I): BrowserApiLimits {
    const message = createBaseBrowserApiLimits();
    message.maximumPageSize = object.maximumPageSize ?? 0;
    message.maximumPreviewRows = object.maximumPreviewRows ?? 0;
    message.maximumWebsocketSubscriptions = object.maximumWebsocketSubscriptions ?? 0;
    message.maximumWebsocketFrameBytes =
      (object.maximumWebsocketFrameBytes !== undefined && object.maximumWebsocketFrameBytes !== null)
        ? BigInt(object.maximumWebsocketFrameBytes)
        : 0n;
    message.maximumExportRows = (object.maximumExportRows !== undefined && object.maximumExportRows !== null)
      ? BigInt(object.maximumExportRows)
      : 0n;
    message.maximumExportBytes = (object.maximumExportBytes !== undefined && object.maximumExportBytes !== null)
      ? BigInt(object.maximumExportBytes)
      : 0n;
    message.defaultSearchTimeout = (object.defaultSearchTimeout !== undefined && object.defaultSearchTimeout !== null)
      ? Duration.fromPartial(object.defaultSearchTimeout)
      : undefined;
    message.searchResultRetention =
      (object.searchResultRetention !== undefined && object.searchResultRetention !== null)
        ? Duration.fromPartial(object.searchResultRetention)
        : undefined;
    return message;
  },
};

function createBaseGetSystemBootstrapRequest(): GetSystemBootstrapRequest {
  return { preferredAppId: undefined };
}

export const GetSystemBootstrapRequest: MessageFns<GetSystemBootstrapRequest> = {
  encode(message: GetSystemBootstrapRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.preferredAppId !== undefined) {
      writer.uint32(10).string(message.preferredAppId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSystemBootstrapRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSystemBootstrapRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.preferredAppId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSystemBootstrapRequest {
    return {
      preferredAppId: isSet(object.preferredAppId)
        ? globalThis.String(object.preferredAppId)
        : isSet(object.preferred_app_id)
        ? globalThis.String(object.preferred_app_id)
        : undefined,
    };
  },

  toJSON(message: GetSystemBootstrapRequest): unknown {
    const obj: any = {};
    if (message.preferredAppId !== undefined) {
      obj.preferredAppId = message.preferredAppId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSystemBootstrapRequest>, I>>(base?: I): GetSystemBootstrapRequest {
    return GetSystemBootstrapRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSystemBootstrapRequest>, I>>(object: I): GetSystemBootstrapRequest {
    const message = createBaseGetSystemBootstrapRequest();
    message.preferredAppId = object.preferredAppId ?? undefined;
    return message;
  },
};

function createBaseGetSystemBootstrapResponse(): GetSystemBootstrapResponse {
  return {
    serverVersion: "",
    apiVersion: "",
    splCompatibilityVersion: "",
    searchWebsocketPath: "",
    features: [],
    limits: undefined,
    apps: [],
    indexes: [],
    selectedAppId: undefined,
    serverTime: undefined,
  };
}

export const GetSystemBootstrapResponse: MessageFns<GetSystemBootstrapResponse> = {
  encode(message: GetSystemBootstrapResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverVersion !== "") {
      writer.uint32(10).string(message.serverVersion);
    }
    if (message.apiVersion !== "") {
      writer.uint32(18).string(message.apiVersion);
    }
    if (message.splCompatibilityVersion !== "") {
      writer.uint32(26).string(message.splCompatibilityVersion);
    }
    if (message.searchWebsocketPath !== "") {
      writer.uint32(34).string(message.searchWebsocketPath);
    }
    writer.uint32(42).fork();
    for (const v of message.features) {
      writer.int32(v);
    }
    writer.join();
    if (message.limits !== undefined) {
      BrowserApiLimits.encode(message.limits, writer.uint32(50).fork()).join();
    }
    for (const v of message.apps) {
      AppSummary.encode(v!, writer.uint32(58).fork()).join();
    }
    for (const v of message.indexes) {
      IndexSummary.encode(v!, writer.uint32(66).fork()).join();
    }
    if (message.selectedAppId !== undefined) {
      writer.uint32(74).string(message.selectedAppId);
    }
    if (message.serverTime !== undefined) {
      Timestamp.encode(toTimestamp(message.serverTime), writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSystemBootstrapResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSystemBootstrapResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.serverVersion = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.apiVersion = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.splCompatibilityVersion = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.searchWebsocketPath = reader.string();
          continue;
        }
        case 5: {
          if (tag === 40) {
            message.features.push(reader.int32() as any);

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.features.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.limits = BrowserApiLimits.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.apps.push(AppSummary.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.indexes.push(IndexSummary.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.selectedAppId = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.serverTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSystemBootstrapResponse {
    return {
      serverVersion: isSet(object.serverVersion)
        ? globalThis.String(object.serverVersion)
        : isSet(object.server_version)
        ? globalThis.String(object.server_version)
        : "",
      apiVersion: isSet(object.apiVersion)
        ? globalThis.String(object.apiVersion)
        : isSet(object.api_version)
        ? globalThis.String(object.api_version)
        : "",
      splCompatibilityVersion: isSet(object.splCompatibilityVersion)
        ? globalThis.String(object.splCompatibilityVersion)
        : isSet(object.spl_compatibility_version)
        ? globalThis.String(object.spl_compatibility_version)
        : "",
      searchWebsocketPath: isSet(object.searchWebsocketPath)
        ? globalThis.String(object.searchWebsocketPath)
        : isSet(object.search_websocket_path)
        ? globalThis.String(object.search_websocket_path)
        : "",
      features: globalThis.Array.isArray(object?.features)
        ? object.features.map((e: any) => serverFeatureFromJSON(e))
        : [],
      limits: isSet(object.limits) ? BrowserApiLimits.fromJSON(object.limits) : undefined,
      apps: globalThis.Array.isArray(object?.apps) ? object.apps.map((e: any) => AppSummary.fromJSON(e)) : [],
      indexes: globalThis.Array.isArray(object?.indexes)
        ? object.indexes.map((e: any) => IndexSummary.fromJSON(e))
        : [],
      selectedAppId: isSet(object.selectedAppId)
        ? globalThis.String(object.selectedAppId)
        : isSet(object.selected_app_id)
        ? globalThis.String(object.selected_app_id)
        : undefined,
      serverTime: isSet(object.serverTime)
        ? fromJsonTimestamp(object.serverTime)
        : isSet(object.server_time)
        ? fromJsonTimestamp(object.server_time)
        : undefined,
    };
  },

  toJSON(message: GetSystemBootstrapResponse): unknown {
    const obj: any = {};
    if (message.serverVersion !== "") {
      obj.serverVersion = message.serverVersion;
    }
    if (message.apiVersion !== "") {
      obj.apiVersion = message.apiVersion;
    }
    if (message.splCompatibilityVersion !== "") {
      obj.splCompatibilityVersion = message.splCompatibilityVersion;
    }
    if (message.searchWebsocketPath !== "") {
      obj.searchWebsocketPath = message.searchWebsocketPath;
    }
    if (message.features?.length) {
      obj.features = message.features.map((e) => serverFeatureToJSON(e));
    }
    if (message.limits !== undefined) {
      obj.limits = BrowserApiLimits.toJSON(message.limits);
    }
    if (message.apps?.length) {
      obj.apps = message.apps.map((e) => AppSummary.toJSON(e));
    }
    if (message.indexes?.length) {
      obj.indexes = message.indexes.map((e) => IndexSummary.toJSON(e));
    }
    if (message.selectedAppId !== undefined) {
      obj.selectedAppId = message.selectedAppId;
    }
    if (message.serverTime !== undefined) {
      obj.serverTime = message.serverTime.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSystemBootstrapResponse>, I>>(base?: I): GetSystemBootstrapResponse {
    return GetSystemBootstrapResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSystemBootstrapResponse>, I>>(object: I): GetSystemBootstrapResponse {
    const message = createBaseGetSystemBootstrapResponse();
    message.serverVersion = object.serverVersion ?? "";
    message.apiVersion = object.apiVersion ?? "";
    message.splCompatibilityVersion = object.splCompatibilityVersion ?? "";
    message.searchWebsocketPath = object.searchWebsocketPath ?? "";
    message.features = object.features?.map((e) => e) || [];
    message.limits = (object.limits !== undefined && object.limits !== null)
      ? BrowserApiLimits.fromPartial(object.limits)
      : undefined;
    message.apps = object.apps?.map((e) => AppSummary.fromPartial(e)) || [];
    message.indexes = object.indexes?.map((e) => IndexSummary.fromPartial(e)) || [];
    message.selectedAppId = object.selectedAppId ?? undefined;
    message.serverTime = object.serverTime ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
