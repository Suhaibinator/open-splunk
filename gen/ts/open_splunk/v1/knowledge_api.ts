// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: open_splunk/v1/knowledge_api.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { FieldMask } from "../../google/protobuf/field_mask";
import { Timestamp } from "../../google/protobuf/timestamp";
import {
  Diagnostic,
  FieldViolation,
  PageRequest,
  PageResponse,
  SharingScope,
  sharingScopeFromJSON,
  sharingScopeToJSON,
  SortDirection,
  sortDirectionFromJSON,
  sortDirectionToJSON,
} from "./common";
import {
  KnowledgeDependencyRole,
  knowledgeDependencyRoleFromJSON,
  knowledgeDependencyRoleToJSON,
  KnowledgeObject,
  KnowledgeObjectDefinition,
  KnowledgeObjectState,
  knowledgeObjectStateFromJSON,
  knowledgeObjectStateToJSON,
  KnowledgeObjectType,
  knowledgeObjectTypeFromJSON,
  knowledgeObjectTypeToJSON,
  KnowledgeObjectVersionReference,
} from "./knowledge";
import { ResultRow, ResultSchema } from "./result";

export enum KnowledgeObjectSortBy {
  KNOWLEDGE_OBJECT_SORT_BY_UNSPECIFIED = 0,
  KNOWLEDGE_OBJECT_SORT_BY_NAME = 1,
  KNOWLEDGE_OBJECT_SORT_BY_CREATED_AT = 2,
  KNOWLEDGE_OBJECT_SORT_BY_UPDATED_AT = 3,
  KNOWLEDGE_OBJECT_SORT_BY_OBJECT_TYPE = 4,
  UNRECOGNIZED = -1,
}

export function knowledgeObjectSortByFromJSON(object: any): KnowledgeObjectSortBy {
  switch (object) {
    case 0:
    case "KNOWLEDGE_OBJECT_SORT_BY_UNSPECIFIED":
      return KnowledgeObjectSortBy.KNOWLEDGE_OBJECT_SORT_BY_UNSPECIFIED;
    case 1:
    case "KNOWLEDGE_OBJECT_SORT_BY_NAME":
      return KnowledgeObjectSortBy.KNOWLEDGE_OBJECT_SORT_BY_NAME;
    case 2:
    case "KNOWLEDGE_OBJECT_SORT_BY_CREATED_AT":
      return KnowledgeObjectSortBy.KNOWLEDGE_OBJECT_SORT_BY_CREATED_AT;
    case 3:
    case "KNOWLEDGE_OBJECT_SORT_BY_UPDATED_AT":
      return KnowledgeObjectSortBy.KNOWLEDGE_OBJECT_SORT_BY_UPDATED_AT;
    case 4:
    case "KNOWLEDGE_OBJECT_SORT_BY_OBJECT_TYPE":
      return KnowledgeObjectSortBy.KNOWLEDGE_OBJECT_SORT_BY_OBJECT_TYPE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return KnowledgeObjectSortBy.UNRECOGNIZED;
  }
}

export function knowledgeObjectSortByToJSON(object: KnowledgeObjectSortBy): string {
  switch (object) {
    case KnowledgeObjectSortBy.KNOWLEDGE_OBJECT_SORT_BY_UNSPECIFIED:
      return "KNOWLEDGE_OBJECT_SORT_BY_UNSPECIFIED";
    case KnowledgeObjectSortBy.KNOWLEDGE_OBJECT_SORT_BY_NAME:
      return "KNOWLEDGE_OBJECT_SORT_BY_NAME";
    case KnowledgeObjectSortBy.KNOWLEDGE_OBJECT_SORT_BY_CREATED_AT:
      return "KNOWLEDGE_OBJECT_SORT_BY_CREATED_AT";
    case KnowledgeObjectSortBy.KNOWLEDGE_OBJECT_SORT_BY_UPDATED_AT:
      return "KNOWLEDGE_OBJECT_SORT_BY_UPDATED_AT";
    case KnowledgeObjectSortBy.KNOWLEDGE_OBJECT_SORT_BY_OBJECT_TYPE:
      return "KNOWLEDGE_OBJECT_SORT_BY_OBJECT_TYPE";
    case KnowledgeObjectSortBy.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum KnowledgeQuarantineReason {
  KNOWLEDGE_QUARANTINE_REASON_UNSPECIFIED = 0,
  KNOWLEDGE_QUARANTINE_REASON_ROOT_CORRUPTION = 1,
  KNOWLEDGE_QUARANTINE_REASON_DEPENDENCY_RECOVERY = 2,
  UNRECOGNIZED = -1,
}

export function knowledgeQuarantineReasonFromJSON(object: any): KnowledgeQuarantineReason {
  switch (object) {
    case 0:
    case "KNOWLEDGE_QUARANTINE_REASON_UNSPECIFIED":
      return KnowledgeQuarantineReason.KNOWLEDGE_QUARANTINE_REASON_UNSPECIFIED;
    case 1:
    case "KNOWLEDGE_QUARANTINE_REASON_ROOT_CORRUPTION":
      return KnowledgeQuarantineReason.KNOWLEDGE_QUARANTINE_REASON_ROOT_CORRUPTION;
    case 2:
    case "KNOWLEDGE_QUARANTINE_REASON_DEPENDENCY_RECOVERY":
      return KnowledgeQuarantineReason.KNOWLEDGE_QUARANTINE_REASON_DEPENDENCY_RECOVERY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return KnowledgeQuarantineReason.UNRECOGNIZED;
  }
}

export function knowledgeQuarantineReasonToJSON(object: KnowledgeQuarantineReason): string {
  switch (object) {
    case KnowledgeQuarantineReason.KNOWLEDGE_QUARANTINE_REASON_UNSPECIFIED:
      return "KNOWLEDGE_QUARANTINE_REASON_UNSPECIFIED";
    case KnowledgeQuarantineReason.KNOWLEDGE_QUARANTINE_REASON_ROOT_CORRUPTION:
      return "KNOWLEDGE_QUARANTINE_REASON_ROOT_CORRUPTION";
    case KnowledgeQuarantineReason.KNOWLEDGE_QUARANTINE_REASON_DEPENDENCY_RECOVERY:
      return "KNOWLEDGE_QUARANTINE_REASON_DEPENDENCY_RECOVERY";
    case KnowledgeQuarantineReason.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * KnowledgeValidationIntent selects the lifecycle authority which validation
 * proves. The request must contain exactly one of the two defined nonzero
 * values. UNSPECIFIED and every unknown numeric enum value are request-envelope
 * errors rather than in-band candidate validation results.
 */
export enum KnowledgeValidationIntent {
  KNOWLEDGE_VALIDATION_INTENT_UNSPECIFIED = 0,
  /**
   * KNOWLEDGE_VALIDATION_INTENT_INACTIVE_STORAGE - INACTIVE_STORAGE proves that the candidate is canonical and bounded for
   * persistence in a non-ACTIVE lifecycle state. It performs no publication-
   * readiness derivation, does not claim that the candidate is currently
   * publishable, and returns no derived dependencies.
   */
  KNOWLEDGE_VALIDATION_INTENT_INACTIVE_STORAGE = 1,
  /**
   * KNOWLEDGE_VALIDATION_INTENT_ACTIVE_PUBLICATION - ACTIVE_PUBLICATION evaluates the candidate as the proposed ACTIVE version
   * in one fixed knowledge, app, and index catalog transaction. The response
   * revision identifies only the knowledge-ledger component of that advisory
   * evaluation; it is not the complete transaction authority.
   */
  KNOWLEDGE_VALIDATION_INTENT_ACTIVE_PUBLICATION = 2,
  UNRECOGNIZED = -1,
}

export function knowledgeValidationIntentFromJSON(object: any): KnowledgeValidationIntent {
  switch (object) {
    case 0:
    case "KNOWLEDGE_VALIDATION_INTENT_UNSPECIFIED":
      return KnowledgeValidationIntent.KNOWLEDGE_VALIDATION_INTENT_UNSPECIFIED;
    case 1:
    case "KNOWLEDGE_VALIDATION_INTENT_INACTIVE_STORAGE":
      return KnowledgeValidationIntent.KNOWLEDGE_VALIDATION_INTENT_INACTIVE_STORAGE;
    case 2:
    case "KNOWLEDGE_VALIDATION_INTENT_ACTIVE_PUBLICATION":
      return KnowledgeValidationIntent.KNOWLEDGE_VALIDATION_INTENT_ACTIVE_PUBLICATION;
    case -1:
    case "UNRECOGNIZED":
    default:
      return KnowledgeValidationIntent.UNRECOGNIZED;
  }
}

export function knowledgeValidationIntentToJSON(object: KnowledgeValidationIntent): string {
  switch (object) {
    case KnowledgeValidationIntent.KNOWLEDGE_VALIDATION_INTENT_UNSPECIFIED:
      return "KNOWLEDGE_VALIDATION_INTENT_UNSPECIFIED";
    case KnowledgeValidationIntent.KNOWLEDGE_VALIDATION_INTENT_INACTIVE_STORAGE:
      return "KNOWLEDGE_VALIDATION_INTENT_INACTIVE_STORAGE";
    case KnowledgeValidationIntent.KNOWLEDGE_VALIDATION_INTENT_ACTIVE_PUBLICATION:
      return "KNOWLEDGE_VALIDATION_INTENT_ACTIVE_PUBLICATION";
    case KnowledgeValidationIntent.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** POST /api/v1/knowledge/objects/create */
export interface CreateKnowledgeObjectRequest {
  definition:
    | KnowledgeObjectDefinition
    | undefined;
  /** initial_state accepts only DRAFT or ACTIVE. */
  initialState: KnowledgeObjectState;
  clientRequestId: string;
}

export interface CreateKnowledgeObjectResponse {
  knowledgeObject:
    | KnowledgeObject
    | undefined;
  /**
   * The revision and state token form one catalog snapshot identity. The token
   * is exactly 32 bytes and must never be interpreted without this revision.
   */
  tenantCatalogRevision: bigint;
  tenantCatalogStateToken: Uint8Array;
}

/**
 * POST /api/v1/knowledge/objects/get
 * Historical reads are authorized from the current registry identity. A
 * currently quarantined identity never returns current or historical
 * definition-derived bytes.
 */
export interface GetKnowledgeObjectRequest {
  knowledgeObjectId: string;
  version?: bigint | undefined;
}

export interface GetKnowledgeObjectResponse {
  knowledgeObject: KnowledgeObject | undefined;
}

/**
 * POST /api/v1/knowledge/objects/list
 * The signed continuation binds every normalized filter, ordering choice,
 * caller scope, page bound, and first-page catalog revision plus its exact
 * 32-byte restore-fork-safe catalog-state commitment.
 * text_filter is a binary substring of the current name or description;
 * selector_text_filter is a binary substring of one individual current
 * selector pattern. Every predicate is applied before keyset LIMIT.
 */
export interface ListKnowledgeObjectsRequest {
  page: PageRequest | undefined;
  appIdFilter?: string | undefined;
  ownerIdFilter?: string | undefined;
  textFilter?: string | undefined;
  objectTypeFilters: KnowledgeObjectType[];
  stateFilters: KnowledgeObjectState[];
  sharingScopeFilters: SharingScope[];
  selectorTextFilter?: string | undefined;
  sortBy: KnowledgeObjectSortBy;
  sortDirection: SortDirection;
}

export interface ListKnowledgeObjectsResponse {
  knowledgeObjects: KnowledgeObject[];
  page: PageResponse | undefined;
  tenantCatalogRevision: bigint;
}

/** POST /api/v1/knowledge/objects/update */
export interface UpdateKnowledgeObjectRequest {
  knowledgeObjectId: string;
  expectedVersion: bigint;
  definition:
    | KnowledgeObjectDefinition
    | undefined;
  /** Paths are relative to KnowledgeObjectDefinition, never this request. */
  updateMask: string[] | undefined;
  clientRequestId: string;
}

export interface UpdateKnowledgeObjectResponse {
  knowledgeObject:
    | KnowledgeObject
    | undefined;
  /**
   * The revision and state token form one catalog snapshot identity. The token
   * is exactly 32 bytes and must never be interpreted without this revision.
   */
  tenantCatalogRevision: bigint;
  tenantCatalogStateToken: Uint8Array;
}

/**
 * POST /api/v1/knowledge/objects/set-state
 * state must be ACTIVE or DISABLED. DRAFT is an initial create state rather
 * than a reversible transition. QUARANTINED and DELETED are terminal and can
 * be entered only through their dedicated recovery-token and delete routes.
 */
export interface SetKnowledgeObjectStateRequest {
  knowledgeObjectId: string;
  expectedVersion: bigint;
  state: KnowledgeObjectState;
  clientRequestId: string;
}

export interface SetKnowledgeObjectStateResponse {
  knowledgeObject:
    | KnowledgeObject
    | undefined;
  /**
   * The revision and state token form one catalog snapshot identity. The token
   * is exactly 32 bytes and must never be interpreted without this revision.
   */
  tenantCatalogRevision: bigint;
  tenantCatalogStateToken: Uint8Array;
}

/**
 * POST /api/v1/knowledge/objects/delete
 * Delete appends an immutable version and leaves a terminal DELETED registry
 * tombstone; it does not physically erase retained definition or version data.
 */
export interface DeleteKnowledgeObjectRequest {
  knowledgeObjectId: string;
  expectedVersion: bigint;
  clientRequestId: string;
}

export interface DeleteKnowledgeObjectResponse {
  knowledgeObjectId: string;
  deletedVersion: bigint;
  /**
   * The revision and state token form one catalog snapshot identity. The token
   * is exactly 32 bytes and must never be interpreted without this revision.
   */
  tenantCatalogRevision: bigint;
  tenantCatalogStateToken: Uint8Array;
}

/**
 * Internal, strictly canonical replay authority persisted in
 * knowledge_mutation_idempotency.outcome_proto. It deliberately duplicates
 * the bounded row scalars so same-width storage corruption cannot forge a
 * response snapshot identity. Ordinary mutations use successful_audit_sequence;
 * protective quarantine uses recovery_audit_sequence.
 */
export interface KnowledgeMutationOutcomeRecord {
  route: string;
  mutationKind: string;
  object: KnowledgeObjectVersionReference | undefined;
  tenantCatalogRevision: bigint;
  tenantCatalogStateToken: Uint8Array;
  /**
   * Immutable mutation occurrence and retention authorities. Reclamation
   * validates these canonical microsecond values against the receipt row so a
   * damaged or shortened fence cannot make an idempotent outcome disappear.
   */
  occurredAtUnixMicro: bigint;
  retentionAnchorUnixMicro: bigint;
  retainUntilUnixMicro: bigint;
  /**
   * Keep the oneof declaration after the ordinary scalar fields. The Go
   * runtime emits oneofs after ordinary fields, while the TypeScript runtime
   * follows declaration order; this ordering makes their canonical v1 wire
   * bytes identical without changing any established field number.
   */
  auditAuthority: { $case: "successfulAuditSequence"; value: bigint } | {
    $case: "recoveryAuditSequence";
    value: bigint;
  } | undefined;
}

export interface KnowledgeQuarantineTransition {
  cascadeOrdinal: number;
  knowledgeObjectId: string;
  previousVersion: bigint;
  quarantinedVersion: bigint;
  reason: KnowledgeQuarantineReason;
}

/**
 * POST /api/v1/knowledge/objects/quarantine/prepare
 * This administrator-only integrity scan returns no definition bytes. Its token
 * expires after ten minutes and binds the raw root row and ordered active-
 * dependent closure observed at tenant_catalog_revision.
 */
export interface PrepareKnowledgeObjectQuarantineRequest {
  knowledgeObjectId: string;
}

export interface PrepareKnowledgeObjectQuarantineResponse {
  rootKnowledgeObjectId: string;
  recoveryToken: string;
  expiresAt: Date | undefined;
  dependentCount: number;
  tenantCatalogRevision: bigint;
}

/**
 * POST /api/v1/knowledge/objects/quarantine
 * recovery_token is a single-use, short-lived HMAC token issued by a privileged
 * integrity scan. It binds the tenant revision, raw root-row digest, and bounded
 * ordered dependent closure. The server re-reads and re-digests that authority
 * transactionally without decoding suspect definitions.
 */
export interface QuarantineKnowledgeObjectRequest {
  recoveryToken: string;
  clientRequestId: string;
}

export interface QuarantineKnowledgeObjectResponse {
  rootKnowledgeObjectId: string;
  transitions: KnowledgeQuarantineTransition[];
  tenantCatalogRevision: bigint;
}

/**
 * KnowledgeValidationDependency is one direct dependency of only the applied
 * candidate. It deliberately excludes a source identity, definition digests,
 * snapshot stages, global depth, and canonical snapshot ordinals. target is an
 * exact currently authorized object identity. Only ACTIVE_PUBLICATION may
 * return these entries, and KO-1 emits only FIELD_INPUT. Missing and
 * unauthorized dependency targets are indistinguishable and never produce an
 * entry: candidate invalidity uses only the static diagnostic code
 * KNOWLEDGE_DEPENDENCY_UNAVAILABLE, while boundary failures use a uniform
 * non-2xx response.
 */
export interface KnowledgeValidationDependency {
  target: KnowledgeManagementObjectVersionIdentity | undefined;
  role: KnowledgeDependencyRole;
}

/**
 * KnowledgeValidationDiagnostic locates a compiler diagnostic within the
 * applied candidate. field_path is a path in KnowledgeObjectDefinition and is
 * at most 1 KiB of UTF-8. When diagnostic.source_range is present, its byte
 * offsets are relative to the UTF-8 scalar value at field_path, never to the
 * encoded request or the complete definition. A present source_range requires
 * nonnil start and end positions and denotes a half-open range with start byte
 * offset less than or equal to end. Both offsets are at most the exact scalar
 * UTF-8 byte length and fall on code-point boundaries. Source line and column
 * values must equal the uniquely derived one-based coordinates: offset zero is
 * line 1, column 1; LF increments line and resets column to 1; every other
 * Unicode scalar, including CR, increments column. UNSPECIFIED and unknown
 * diagnostic severities are invalid service output.
 * diagnostic.code is at most 128 UTF-8 bytes, diagnostic.message is at most
 * 4 KiB, and diagnostic.suggestions contains at most 32 unique values of at
 * most 1 KiB each in ascending binary UTF-8 order. field_path, code, message,
 * and suggestions may contain only stable static templates plus exact source
 * text already present in the applied candidate scalar. They must never expose
 * any other catalog object, app, owner, name, ID, version, digest, definition,
 * index inventory beyond candidate-authored text, cohort or global count,
 * generated SQL, or hidden authority.
 */
export interface KnowledgeValidationDiagnostic {
  fieldPath: string;
  diagnostic: Diagnostic | undefined;
}

/**
 * Every charge is attributable only to the applied candidate, never to the
 * complete tenant catalog or an affected publication cohort. A present report
 * is complete; partial resource estimates are forbidden. selector_patterns is
 * the exact number of normalized selector patterns in the applied candidate,
 * and normalized_definition_bytes is its exact deterministic protobuf size.
 *
 * INACTIVE_STORAGE sets dependency_nodes, dependency_edges, and every compile-
 * derived field -- generated_operators, generated_fields, regex_programs,
 * estimated_regex_work_units, scalar_expressions, scalar_expression_nodes,
 * extraction_outputs, json_evaluation_work_units, and scalar_predicates -- to
 * exactly zero because publication compilation does not occur.
 *
 * ACTIVE_PUBLICATION obtains compile-derived fields by compiling a canonical
 * knowledge program input whose only object is the applied normalized
 * candidate and whose dependency list is empty. The fields equal, respectively,
 * that singleton program's intrinsic Charges.GeneratedOperators,
 * Charges.GeneratedFields, Charges.RegexPrograms, Charges.RegexWorkUnits,
 * Charges.ScalarExpressions, Charges.ScalarExpressionNodes,
 * Charges.ExtractionOutputs, Charges.JSONEvaluationWork, and
 * Charges.ScalarPredicates. They are neither affected-cohort totals nor
 * marginal deltas after cohort operator fusion. dependency_nodes and
 * dependency_edges remain derived from the full ACTIVE transition and the
 * returned authorized result.dependencies, not from the singleton program.
 */
export interface KnowledgeResourceEstimate {
  /** Exact number of normalized selector patterns in the applied candidate. */
  selectorPatterns: number;
  /** Exact deterministic protobuf size of the normalized candidate definition. */
  normalizedDefinitionBytes: bigint;
  /**
   * Distinct exact direct targets in result.dependencies, excluding the
   * candidate itself. This is zero for INACTIVE_STORAGE; for a valid
   * ACTIVE_PUBLICATION it derives only from returned authorized dependencies.
   */
  dependencyNodes: number;
  /**
   * Direct candidate dependency edges; this equals result.dependencies size
   * and is zero for INACTIVE_STORAGE. For a valid ACTIVE_PUBLICATION it derives
   * only from returned authorized dependencies.
   */
  dependencyEdges: number;
  generatedOperators: number;
  generatedFields: number;
  regexPrograms: number;
  estimatedRegexWorkUnits: bigint;
  scalarExpressions: number;
  scalarExpressionNodes: number;
  extractionOutputs: number;
  jsonEvaluationWorkUnits: number;
  scalarPredicates: number;
}

/**
 * KnowledgeValidationResult reports candidate-authored definition validity
 * under the selected intent in-band with HTTP 200. valid is not mutation
 * acceptability, a reservation, or a promise that a later Writer operation
 * will succeed. An applied masked update that is identical to the current
 * definition may be valid. INACTIVE_STORAGE against a currently ACTIVE object
 * proves only hypothetical non-ACTIVE storage validity and never ACTIVE Update
 * admissibility. Every later Writer operation independently revalidates its
 * then-current authorization, version, lifecycle, capacity, app, index, and
 * publication authority. Request-envelope, authentication, authorization-to-
 * the-requested object, catalog-integrity, and service failures remain ordinary
 * non-2xx API errors and never appear as valid=false.
 *
 * valid=false requires at least one retained field violation or ERROR
 * diagnostic even when either issue list is truncated, and requires
 * normalized_definition, definition_sha256, dependencies, and resources to be
 * absent. valid=true requires a normalized_definition, an exact 32-byte SHA-256
 * digest of its deterministic protobuf encoding, a complete resources report, no
 * field violations, no ERROR diagnostics, and field_violations_truncated=false.
 * object_type is UNSPECIFIED only when an invalid candidate's body cannot be
 * identified; otherwise it is the applied candidate's exact body type.
 * Persisted corruption and hidden inventory failures are always uniform non-2xx
 * service failures and must not be converted into candidate issues.
 * Before issue canonicalization or serialization, the response boundary
 * recursively rejects unknown protobuf fields in this result and every nested
 * message. The violation and diagnostic keys cover every currently recognized
 * nested field; any future appended issue field must extend per-entry
 * validation, deduplication, and comparison before it may be emitted.
 */
export interface KnowledgeValidationResult {
  valid: boolean;
  objectType: KnowledgeObjectType;
  normalizedDefinition?: KnowledgeObjectDefinition | undefined;
  definitionSha256?:
    | Uint8Array
    | undefined;
  /**
   * Validate every candidate-authored value against its per-entry limits,
   * deduplicate exact full values, sort by ascending binary UTF-8 field_path,
   * code, then message, and retain the longest prefix bounded by both 256
   * values and a 256 KiB (262144-byte)
   * aggregate UTF-8 text charge. One value's charge is the sum of the UTF-8
   * byte lengths of field_path, code, and message, without separators or wire
   * framing. Each field_path is at most 1 KiB, code at most 128 bytes, and
   * message at most 4 KiB. Once the next sorted value would exceed either
   * bound, it and every later value are omitted. field_path, code, and message
   * obey the same static-template, candidate-source-only nondisclosure rule as
   * KnowledgeValidationDiagnostic. field_violations_truncated records omission
   * by either the field_violations count or aggregate-text bound.
   */
  fieldViolations: FieldViolation[];
  resources:
    | KnowledgeResourceEstimate
    | undefined;
  /**
   * ACTIVE_PUBLICATION returns at most 1024 unique values, sorted by ascending
   * binary UTF-8 target object ID, target version, then numeric role. role must
   * be FIELD_INPUT. INACTIVE_STORAGE always returns an empty list.
   */
  dependencies: KnowledgeValidationDependency[];
  /**
   * Validate every candidate-authored value against its per-entry limits, then
   * deduplicate exact full values and sort first by explicit severity rank
   * ERROR, WARNING, INFO. UNSPECIFIED and unknown severities are invalid. The
   * remaining keys are field_path, absent source range before a present range,
   * start byte offset, end byte offset, code, message, canonical derived start
   * and end line/column coordinates, then the complete suggestions sequence
   * lexicographically. Sequence comparison is elementwise ascending binary
   * UTF-8 with a shorter equal-prefix sequence first. This is a total key.
   * Retain the longest prefix bounded by both 256 values and a 768 KiB
   * (786432-byte) aggregate UTF-8 text charge. One value's charge is the sum of
   * the UTF-8 byte lengths of its field_path, diagnostic code, diagnostic
   * message, and every suggestion, without separators or wire framing. Once
   * the next sorted value would exceed either bound, it and every later value
   * are omitted. ERROR-first ordering and the per-entry limit guarantee that
   * valid=false retains at least one ERROR diagnostic even when warnings alone
   * would otherwise fill the aggregate budget. diagnostics_truncated records
   * omission by either the diagnostics count or aggregate-text bound.
   */
  diagnostics: KnowledgeValidationDiagnostic[];
  /**
   * True exactly when one or more otherwise-valid sorted field violations were
   * omitted by the field_violations count or aggregate-text bound; false means
   * the returned list is complete. Per-entry validation failures are service
   * failures, never truncation. This must be false when valid=true.
   */
  fieldViolationsTruncated: boolean;
  /**
   * True exactly when one or more otherwise-valid sorted diagnostics were
   * omitted by the diagnostics count or aggregate-text bound; false means the
   * returned list is complete. Per-entry validation failures are service
   * failures, never truncation.
   */
  diagnosticsTruncated: boolean;
}

/**
 * POST /api/v1/knowledge/objects/validate
 * This message defines a future route contract; declaring it does not register
 * the route or advertise a knowledge-management capability.
 *
 * definition message presence is required. A missing top-level definition is
 * a request-envelope error; a present definition whose body is missing or
 * unknown is candidate-authored invalidity reported in-band. intent must be
 * exactly INACTIVE_STORAGE or ACTIVE_PUBLICATION; UNSPECIFIED and unknown
 * numeric values are request-envelope errors. Create mode is selected only when
 * knowledge_object_id is absent and requires expected_version and update_mask
 * message to both be absent, not merely zero or empty. ACTIVE_PUBLICATION
 * create validation evaluates the candidate under a deterministic,
 * non-persisted object ID proven fresh in the same catalog transaction. Its
 * validity, diagnostics, resources, and target-only dependency projection must
 * be invariant under alpha-renaming that ID to any other fresh valid ID. The
 * result neither reserves that ID nor authorizes a later Create: Create
 * generates its own ID and revalidates the then-current catalog, app, and index
 * authority, so intervening changes may alter the outcome. Update mode is
 * selected only when knowledge_object_id is present; that ID must be nonempty,
 * expected_version must be present in the inclusive range 1 through MaxInt64
 * (9223372036854775807), and update_mask must be a present message with at
 * least one canonical path. Update validation applies the masked candidate to
 * that exact current version.
 * Violations of these rules are request-envelope errors, never an in-band
 * valid=false candidate result.
 */
export interface ValidateKnowledgeObjectRequest {
  definition: KnowledgeObjectDefinition | undefined;
  knowledgeObjectId?: string | undefined;
  expectedVersion?:
    | bigint
    | undefined;
  /** Paths are relative to KnowledgeObjectDefinition, never this request. */
  updateMask: string[] | undefined;
  intent: KnowledgeValidationIntent;
}

/**
 * Validation observes the knowledge, app, and index catalogs in one fixed
 * transaction. The deterministic protobuf encoding of this complete response
 * is at most 8 MiB (8388608 bytes), including result framing and
 * tenant_catalog_revision. The boundary recursively rejects unknown protobuf
 * fields in the response, result, and every nested message before deterministic
 * serialization.
 */
export interface ValidateKnowledgeObjectResponse {
  /** Required by the response boundary; an absent result is invalid. */
  result:
    | KnowledgeValidationResult
    | undefined;
  /**
   * Exact knowledge-ledger revision observed in the fixed transaction; zero
   * denotes a proven empty knowledge ledger. It identifies only the knowledge-
   * ledger component, not the app or index authority which also affected the
   * result. It is advisory correlation metadata, not a reusable full authority,
   * reservation, mutation proof, or promise that later validation will agree.
   */
  tenantCatalogRevision: bigint;
}

/**
 * KnowledgeManagementObjectVersionIdentity identifies one exact immutable
 * object version without exposing its definition digest. Definition digests
 * remain subject to the containing object's current disclosure policy.
 */
export interface KnowledgeManagementObjectVersionIdentity {
  knowledgeObjectId: string;
  version: bigint;
}

/**
 * KnowledgeManagementDependencyEdge is one direct persisted object-to-object
 * edge. Snapshot-global stage, topological-depth, and canonical-order metadata
 * deliberately do not belong to this management projection.
 */
export interface KnowledgeManagementDependencyEdge {
  source: KnowledgeManagementObjectVersionIdentity | undefined;
  target: KnowledgeManagementObjectVersionIdentity | undefined;
  role: KnowledgeDependencyRole;
}

/**
 * POST /api/v1/knowledge/objects/dependencies
 * The current registry identity authorizes the requested source; selecting a
 * historical version never grants authority. The response lists the exact
 * direct outgoing edges persisted for that selected source version. An edge
 * whose target is not currently disclosable is omitted before pagination and
 * total counting, without a redacted placeholder or hidden count. A currently
 * quarantined requested source is reported through the uniform not-found-or-
 * forbidden response and exposes no current or historical dependency data.
 * Disclosed edges use fixed ascending binary target ID, target version, then
 * role order. The signed continuation binds this route, caller scope, request
 * version presence and value, resolved source, page bound and total-count
 * choice, and the first page's catalog revision plus exact state commitment.
 * Any later catalog identity invalidates the continuation.
 */
export interface ListKnowledgeObjectDependenciesRequest {
  knowledgeObjectId: string;
  version?: bigint | undefined;
  page: PageRequest | undefined;
}

export interface ListKnowledgeObjectDependenciesResponse {
  dependencies: KnowledgeManagementDependencyEdge[];
  page: PageResponse | undefined;
  tenantCatalogRevision: bigint;
  /**
   * resolved_object is the exact source selected by the request, including on
   * an empty page when request.version was absent.
   */
  resolvedObject: KnowledgeManagementObjectVersionIdentity | undefined;
}

/**
 * POST /api/v1/knowledge/objects/dependents
 * The current registry identity authorizes the requested target; selecting a
 * historical version never grants authority. The response lists exact incoming
 * edges to that target version only from source versions which are their
 * objects' current registry versions. Every source lifecycle state is eligible,
 * but a source which is not currently disclosable is omitted before pagination
 * and total counting, without a redacted placeholder or hidden count. A
 * currently quarantined requested target is reported through the uniform not-
 * found-or-forbidden response and exposes no current or historical dependency
 * data.
 * Disclosed edges use fixed ascending binary source ID, source version, then
 * role order. The signed continuation binds this route, caller scope, request
 * version presence and value, resolved target, page bound and total-count
 * choice, and the first page's catalog revision plus exact state commitment.
 * Any later catalog identity invalidates the continuation.
 */
export interface ListKnowledgeObjectDependentsRequest {
  knowledgeObjectId: string;
  version?: bigint | undefined;
  page: PageRequest | undefined;
}

export interface ListKnowledgeObjectDependentsResponse {
  dependents: KnowledgeManagementDependencyEdge[];
  page: PageResponse | undefined;
  tenantCatalogRevision: bigint;
  /**
   * resolved_object is the exact target selected by the request, including on
   * an empty page when request.version was absent.
   */
  resolvedObject: KnowledgeManagementObjectVersionIdentity | undefined;
}

/**
 * POST /api/v1/knowledge/objects/preview
 * retained_search_job_id selects a server-authorized immutable event snapshot.
 * A browser cannot submit events, index scope, a physical table, or SQL.
 */
export interface PreviewKnowledgeObjectRequest {
  retainedSearchJobId: string;
  definition: KnowledgeObjectDefinition | undefined;
  knowledgeObjectId?: string | undefined;
  expectedVersion?:
    | bigint
    | undefined;
  /** Paths are relative to KnowledgeObjectDefinition, never this request. */
  updateMask: string[] | undefined;
  maximumRows?: number | undefined;
}

/**
 * This future unregistered route has no independent validation intent.
 * validation always uses ACTIVE_PUBLICATION and applies the same create/update
 * candidate-envelope semantics as ValidateKnowledgeObjectRequest. It evaluates
 * definition validity in one fixed knowledge, app, and index catalog
 * transaction and obeys every candidate dependency, resource, truncation, and
 * nondisclosure invariant of KnowledgeValidationResult before preview
 * execution. Its tenant_catalog_revision identifies only the advisory
 * knowledge-ledger component; it is not mutation acceptability, a reservation,
 * or a reusable publication proof, and every later Writer operation revalidates
 * its then-current authority.
 */
export interface PreviewKnowledgeObjectResponse {
  validation: KnowledgeValidationResult | undefined;
  beforeSchema: ResultSchema | undefined;
  afterSchema: ResultSchema | undefined;
  beforeRows: ResultRow[];
  afterRows: ResultRow[];
  truncated: boolean;
  tenantCatalogRevision: bigint;
}

function createBaseCreateKnowledgeObjectRequest(): CreateKnowledgeObjectRequest {
  return { definition: undefined, initialState: 0, clientRequestId: "" };
}

export const CreateKnowledgeObjectRequest: MessageFns<CreateKnowledgeObjectRequest> = {
  encode(message: CreateKnowledgeObjectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.definition !== undefined) {
      KnowledgeObjectDefinition.encode(message.definition, writer.uint32(10).fork()).join();
    }
    if (message.initialState !== 0) {
      writer.uint32(16).int32(message.initialState);
    }
    if (message.clientRequestId !== "") {
      writer.uint32(26).string(message.clientRequestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateKnowledgeObjectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateKnowledgeObjectRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.definition = KnowledgeObjectDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.initialState = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.clientRequestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateKnowledgeObjectRequest {
    return {
      definition: isSet(object.definition) ? KnowledgeObjectDefinition.fromJSON(object.definition) : undefined,
      initialState: isSet(object.initialState)
        ? knowledgeObjectStateFromJSON(object.initialState)
        : isSet(object.initial_state)
        ? knowledgeObjectStateFromJSON(object.initial_state)
        : 0,
      clientRequestId: isSet(object.clientRequestId)
        ? globalThis.String(object.clientRequestId)
        : isSet(object.client_request_id)
        ? globalThis.String(object.client_request_id)
        : "",
    };
  },

  toJSON(message: CreateKnowledgeObjectRequest): unknown {
    const obj: any = {};
    if (message.definition !== undefined) {
      obj.definition = KnowledgeObjectDefinition.toJSON(message.definition);
    }
    if (message.initialState !== 0) {
      obj.initialState = knowledgeObjectStateToJSON(message.initialState);
    }
    if (message.clientRequestId !== "") {
      obj.clientRequestId = message.clientRequestId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateKnowledgeObjectRequest>, I>>(base?: I): CreateKnowledgeObjectRequest {
    return CreateKnowledgeObjectRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateKnowledgeObjectRequest>, I>>(object: I): CreateKnowledgeObjectRequest {
    const message = createBaseCreateKnowledgeObjectRequest();
    message.definition = (object.definition !== undefined && object.definition !== null)
      ? KnowledgeObjectDefinition.fromPartial(object.definition)
      : undefined;
    message.initialState = object.initialState ?? 0;
    message.clientRequestId = object.clientRequestId ?? "";
    return message;
  },
};

function createBaseCreateKnowledgeObjectResponse(): CreateKnowledgeObjectResponse {
  return { knowledgeObject: undefined, tenantCatalogRevision: 0n, tenantCatalogStateToken: new Uint8Array(0) };
}

export const CreateKnowledgeObjectResponse: MessageFns<CreateKnowledgeObjectResponse> = {
  encode(message: CreateKnowledgeObjectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.knowledgeObject !== undefined) {
      KnowledgeObject.encode(message.knowledgeObject, writer.uint32(10).fork()).join();
    }
    if (message.tenantCatalogRevision !== 0n) {
      if (BigInt.asUintN(64, message.tenantCatalogRevision) !== message.tenantCatalogRevision) {
        throw new globalThis.Error("value provided for field message.tenantCatalogRevision of type uint64 too large");
      }
      writer.uint32(16).uint64(message.tenantCatalogRevision);
    }
    if (message.tenantCatalogStateToken.length !== 0) {
      writer.uint32(26).bytes(message.tenantCatalogStateToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateKnowledgeObjectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateKnowledgeObjectResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.knowledgeObject = KnowledgeObject.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.tenantCatalogRevision = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tenantCatalogStateToken = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateKnowledgeObjectResponse {
    return {
      knowledgeObject: isSet(object.knowledgeObject)
        ? KnowledgeObject.fromJSON(object.knowledgeObject)
        : isSet(object.knowledge_object)
        ? KnowledgeObject.fromJSON(object.knowledge_object)
        : undefined,
      tenantCatalogRevision: isSet(object.tenantCatalogRevision)
        ? BigInt(object.tenantCatalogRevision)
        : isSet(object.tenant_catalog_revision)
        ? BigInt(object.tenant_catalog_revision)
        : 0n,
      tenantCatalogStateToken: isSet(object.tenantCatalogStateToken)
        ? bytesFromBase64(object.tenantCatalogStateToken)
        : isSet(object.tenant_catalog_state_token)
        ? bytesFromBase64(object.tenant_catalog_state_token)
        : new Uint8Array(0),
    };
  },

  toJSON(message: CreateKnowledgeObjectResponse): unknown {
    const obj: any = {};
    if (message.knowledgeObject !== undefined) {
      obj.knowledgeObject = KnowledgeObject.toJSON(message.knowledgeObject);
    }
    if (message.tenantCatalogRevision !== 0n) {
      obj.tenantCatalogRevision = message.tenantCatalogRevision.toString();
    }
    if (message.tenantCatalogStateToken.length !== 0) {
      obj.tenantCatalogStateToken = base64FromBytes(message.tenantCatalogStateToken);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateKnowledgeObjectResponse>, I>>(base?: I): CreateKnowledgeObjectResponse {
    return CreateKnowledgeObjectResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateKnowledgeObjectResponse>, I>>(
    object: I,
  ): CreateKnowledgeObjectResponse {
    const message = createBaseCreateKnowledgeObjectResponse();
    message.knowledgeObject = (object.knowledgeObject !== undefined && object.knowledgeObject !== null)
      ? KnowledgeObject.fromPartial(object.knowledgeObject)
      : undefined;
    message.tenantCatalogRevision =
      (object.tenantCatalogRevision !== undefined && object.tenantCatalogRevision !== null)
        ? BigInt(object.tenantCatalogRevision)
        : 0n;
    message.tenantCatalogStateToken = object.tenantCatalogStateToken ?? new Uint8Array(0);
    return message;
  },
};

function createBaseGetKnowledgeObjectRequest(): GetKnowledgeObjectRequest {
  return { knowledgeObjectId: "", version: undefined };
}

export const GetKnowledgeObjectRequest: MessageFns<GetKnowledgeObjectRequest> = {
  encode(message: GetKnowledgeObjectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.knowledgeObjectId !== "") {
      writer.uint32(10).string(message.knowledgeObjectId);
    }
    if (message.version !== undefined) {
      if (BigInt.asUintN(64, message.version) !== message.version) {
        throw new globalThis.Error("value provided for field message.version of type uint64 too large");
      }
      writer.uint32(16).uint64(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetKnowledgeObjectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetKnowledgeObjectRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.knowledgeObjectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetKnowledgeObjectRequest {
    return {
      knowledgeObjectId: isSet(object.knowledgeObjectId)
        ? globalThis.String(object.knowledgeObjectId)
        : isSet(object.knowledge_object_id)
        ? globalThis.String(object.knowledge_object_id)
        : "",
      version: isSet(object.version) ? BigInt(object.version) : undefined,
    };
  },

  toJSON(message: GetKnowledgeObjectRequest): unknown {
    const obj: any = {};
    if (message.knowledgeObjectId !== "") {
      obj.knowledgeObjectId = message.knowledgeObjectId;
    }
    if (message.version !== undefined) {
      obj.version = message.version.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetKnowledgeObjectRequest>, I>>(base?: I): GetKnowledgeObjectRequest {
    return GetKnowledgeObjectRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetKnowledgeObjectRequest>, I>>(object: I): GetKnowledgeObjectRequest {
    const message = createBaseGetKnowledgeObjectRequest();
    message.knowledgeObjectId = object.knowledgeObjectId ?? "";
    message.version = (object.version !== undefined && object.version !== null) ? BigInt(object.version) : undefined;
    return message;
  },
};

function createBaseGetKnowledgeObjectResponse(): GetKnowledgeObjectResponse {
  return { knowledgeObject: undefined };
}

export const GetKnowledgeObjectResponse: MessageFns<GetKnowledgeObjectResponse> = {
  encode(message: GetKnowledgeObjectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.knowledgeObject !== undefined) {
      KnowledgeObject.encode(message.knowledgeObject, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetKnowledgeObjectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetKnowledgeObjectResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.knowledgeObject = KnowledgeObject.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetKnowledgeObjectResponse {
    return {
      knowledgeObject: isSet(object.knowledgeObject)
        ? KnowledgeObject.fromJSON(object.knowledgeObject)
        : isSet(object.knowledge_object)
        ? KnowledgeObject.fromJSON(object.knowledge_object)
        : undefined,
    };
  },

  toJSON(message: GetKnowledgeObjectResponse): unknown {
    const obj: any = {};
    if (message.knowledgeObject !== undefined) {
      obj.knowledgeObject = KnowledgeObject.toJSON(message.knowledgeObject);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetKnowledgeObjectResponse>, I>>(base?: I): GetKnowledgeObjectResponse {
    return GetKnowledgeObjectResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetKnowledgeObjectResponse>, I>>(object: I): GetKnowledgeObjectResponse {
    const message = createBaseGetKnowledgeObjectResponse();
    message.knowledgeObject = (object.knowledgeObject !== undefined && object.knowledgeObject !== null)
      ? KnowledgeObject.fromPartial(object.knowledgeObject)
      : undefined;
    return message;
  },
};

function createBaseListKnowledgeObjectsRequest(): ListKnowledgeObjectsRequest {
  return {
    page: undefined,
    appIdFilter: undefined,
    ownerIdFilter: undefined,
    textFilter: undefined,
    objectTypeFilters: [],
    stateFilters: [],
    sharingScopeFilters: [],
    selectorTextFilter: undefined,
    sortBy: 0,
    sortDirection: 0,
  };
}

export const ListKnowledgeObjectsRequest: MessageFns<ListKnowledgeObjectsRequest> = {
  encode(message: ListKnowledgeObjectsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.page !== undefined) {
      PageRequest.encode(message.page, writer.uint32(10).fork()).join();
    }
    if (message.appIdFilter !== undefined) {
      writer.uint32(18).string(message.appIdFilter);
    }
    if (message.ownerIdFilter !== undefined) {
      writer.uint32(26).string(message.ownerIdFilter);
    }
    if (message.textFilter !== undefined) {
      writer.uint32(34).string(message.textFilter);
    }
    writer.uint32(42).fork();
    for (const v of message.objectTypeFilters) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(50).fork();
    for (const v of message.stateFilters) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(58).fork();
    for (const v of message.sharingScopeFilters) {
      writer.int32(v);
    }
    writer.join();
    if (message.selectorTextFilter !== undefined) {
      writer.uint32(66).string(message.selectorTextFilter);
    }
    if (message.sortBy !== 0) {
      writer.uint32(72).int32(message.sortBy);
    }
    if (message.sortDirection !== 0) {
      writer.uint32(80).int32(message.sortDirection);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListKnowledgeObjectsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListKnowledgeObjectsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.page = PageRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.appIdFilter = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ownerIdFilter = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.textFilter = reader.string();
          continue;
        }
        case 5: {
          if (tag === 40) {
            message.objectTypeFilters.push(reader.int32() as any);

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.objectTypeFilters.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 6: {
          if (tag === 48) {
            message.stateFilters.push(reader.int32() as any);

            continue;
          }

          if (tag === 50) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.stateFilters.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 7: {
          if (tag === 56) {
            message.sharingScopeFilters.push(reader.int32() as any);

            continue;
          }

          if (tag === 58) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.sharingScopeFilters.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.selectorTextFilter = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.sortBy = reader.int32() as any;
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.sortDirection = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListKnowledgeObjectsRequest {
    return {
      page: isSet(object.page) ? PageRequest.fromJSON(object.page) : undefined,
      appIdFilter: isSet(object.appIdFilter)
        ? globalThis.String(object.appIdFilter)
        : isSet(object.app_id_filter)
        ? globalThis.String(object.app_id_filter)
        : undefined,
      ownerIdFilter: isSet(object.ownerIdFilter)
        ? globalThis.String(object.ownerIdFilter)
        : isSet(object.owner_id_filter)
        ? globalThis.String(object.owner_id_filter)
        : undefined,
      textFilter: isSet(object.textFilter)
        ? globalThis.String(object.textFilter)
        : isSet(object.text_filter)
        ? globalThis.String(object.text_filter)
        : undefined,
      objectTypeFilters: globalThis.Array.isArray(object?.objectTypeFilters)
        ? object.objectTypeFilters.map((e: any) => knowledgeObjectTypeFromJSON(e))
        : globalThis.Array.isArray(object?.object_type_filters)
        ? object.object_type_filters.map((e: any) => knowledgeObjectTypeFromJSON(e))
        : [],
      stateFilters: globalThis.Array.isArray(object?.stateFilters)
        ? object.stateFilters.map((e: any) => knowledgeObjectStateFromJSON(e))
        : globalThis.Array.isArray(object?.state_filters)
        ? object.state_filters.map((e: any) => knowledgeObjectStateFromJSON(e))
        : [],
      sharingScopeFilters: globalThis.Array.isArray(object?.sharingScopeFilters)
        ? object.sharingScopeFilters.map((e: any) => sharingScopeFromJSON(e))
        : globalThis.Array.isArray(object?.sharing_scope_filters)
        ? object.sharing_scope_filters.map((e: any) => sharingScopeFromJSON(e))
        : [],
      selectorTextFilter: isSet(object.selectorTextFilter)
        ? globalThis.String(object.selectorTextFilter)
        : isSet(object.selector_text_filter)
        ? globalThis.String(object.selector_text_filter)
        : undefined,
      sortBy: isSet(object.sortBy)
        ? knowledgeObjectSortByFromJSON(object.sortBy)
        : isSet(object.sort_by)
        ? knowledgeObjectSortByFromJSON(object.sort_by)
        : 0,
      sortDirection: isSet(object.sortDirection)
        ? sortDirectionFromJSON(object.sortDirection)
        : isSet(object.sort_direction)
        ? sortDirectionFromJSON(object.sort_direction)
        : 0,
    };
  },

  toJSON(message: ListKnowledgeObjectsRequest): unknown {
    const obj: any = {};
    if (message.page !== undefined) {
      obj.page = PageRequest.toJSON(message.page);
    }
    if (message.appIdFilter !== undefined) {
      obj.appIdFilter = message.appIdFilter;
    }
    if (message.ownerIdFilter !== undefined) {
      obj.ownerIdFilter = message.ownerIdFilter;
    }
    if (message.textFilter !== undefined) {
      obj.textFilter = message.textFilter;
    }
    if (message.objectTypeFilters?.length) {
      obj.objectTypeFilters = message.objectTypeFilters.map((e) => knowledgeObjectTypeToJSON(e));
    }
    if (message.stateFilters?.length) {
      obj.stateFilters = message.stateFilters.map((e) => knowledgeObjectStateToJSON(e));
    }
    if (message.sharingScopeFilters?.length) {
      obj.sharingScopeFilters = message.sharingScopeFilters.map((e) => sharingScopeToJSON(e));
    }
    if (message.selectorTextFilter !== undefined) {
      obj.selectorTextFilter = message.selectorTextFilter;
    }
    if (message.sortBy !== 0) {
      obj.sortBy = knowledgeObjectSortByToJSON(message.sortBy);
    }
    if (message.sortDirection !== 0) {
      obj.sortDirection = sortDirectionToJSON(message.sortDirection);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListKnowledgeObjectsRequest>, I>>(base?: I): ListKnowledgeObjectsRequest {
    return ListKnowledgeObjectsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListKnowledgeObjectsRequest>, I>>(object: I): ListKnowledgeObjectsRequest {
    const message = createBaseListKnowledgeObjectsRequest();
    message.page = (object.page !== undefined && object.page !== null)
      ? PageRequest.fromPartial(object.page)
      : undefined;
    message.appIdFilter = object.appIdFilter ?? undefined;
    message.ownerIdFilter = object.ownerIdFilter ?? undefined;
    message.textFilter = object.textFilter ?? undefined;
    message.objectTypeFilters = object.objectTypeFilters?.map((e) => e) || [];
    message.stateFilters = object.stateFilters?.map((e) => e) || [];
    message.sharingScopeFilters = object.sharingScopeFilters?.map((e) => e) || [];
    message.selectorTextFilter = object.selectorTextFilter ?? undefined;
    message.sortBy = object.sortBy ?? 0;
    message.sortDirection = object.sortDirection ?? 0;
    return message;
  },
};

function createBaseListKnowledgeObjectsResponse(): ListKnowledgeObjectsResponse {
  return { knowledgeObjects: [], page: undefined, tenantCatalogRevision: 0n };
}

export const ListKnowledgeObjectsResponse: MessageFns<ListKnowledgeObjectsResponse> = {
  encode(message: ListKnowledgeObjectsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.knowledgeObjects) {
      KnowledgeObject.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      PageResponse.encode(message.page, writer.uint32(18).fork()).join();
    }
    if (message.tenantCatalogRevision !== 0n) {
      if (BigInt.asUintN(64, message.tenantCatalogRevision) !== message.tenantCatalogRevision) {
        throw new globalThis.Error("value provided for field message.tenantCatalogRevision of type uint64 too large");
      }
      writer.uint32(24).uint64(message.tenantCatalogRevision);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListKnowledgeObjectsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListKnowledgeObjectsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.knowledgeObjects.push(KnowledgeObject.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageResponse.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.tenantCatalogRevision = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListKnowledgeObjectsResponse {
    return {
      knowledgeObjects: globalThis.Array.isArray(object?.knowledgeObjects)
        ? object.knowledgeObjects.map((e: any) => KnowledgeObject.fromJSON(e))
        : globalThis.Array.isArray(object?.knowledge_objects)
        ? object.knowledge_objects.map((e: any) => KnowledgeObject.fromJSON(e))
        : [],
      page: isSet(object.page) ? PageResponse.fromJSON(object.page) : undefined,
      tenantCatalogRevision: isSet(object.tenantCatalogRevision)
        ? BigInt(object.tenantCatalogRevision)
        : isSet(object.tenant_catalog_revision)
        ? BigInt(object.tenant_catalog_revision)
        : 0n,
    };
  },

  toJSON(message: ListKnowledgeObjectsResponse): unknown {
    const obj: any = {};
    if (message.knowledgeObjects?.length) {
      obj.knowledgeObjects = message.knowledgeObjects.map((e) => KnowledgeObject.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = PageResponse.toJSON(message.page);
    }
    if (message.tenantCatalogRevision !== 0n) {
      obj.tenantCatalogRevision = message.tenantCatalogRevision.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListKnowledgeObjectsResponse>, I>>(base?: I): ListKnowledgeObjectsResponse {
    return ListKnowledgeObjectsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListKnowledgeObjectsResponse>, I>>(object: I): ListKnowledgeObjectsResponse {
    const message = createBaseListKnowledgeObjectsResponse();
    message.knowledgeObjects = object.knowledgeObjects?.map((e) => KnowledgeObject.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResponse.fromPartial(object.page)
      : undefined;
    message.tenantCatalogRevision =
      (object.tenantCatalogRevision !== undefined && object.tenantCatalogRevision !== null)
        ? BigInt(object.tenantCatalogRevision)
        : 0n;
    return message;
  },
};

function createBaseUpdateKnowledgeObjectRequest(): UpdateKnowledgeObjectRequest {
  return {
    knowledgeObjectId: "",
    expectedVersion: 0n,
    definition: undefined,
    updateMask: undefined,
    clientRequestId: "",
  };
}

export const UpdateKnowledgeObjectRequest: MessageFns<UpdateKnowledgeObjectRequest> = {
  encode(message: UpdateKnowledgeObjectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.knowledgeObjectId !== "") {
      writer.uint32(10).string(message.knowledgeObjectId);
    }
    if (message.expectedVersion !== 0n) {
      if (BigInt.asUintN(64, message.expectedVersion) !== message.expectedVersion) {
        throw new globalThis.Error("value provided for field message.expectedVersion of type uint64 too large");
      }
      writer.uint32(16).uint64(message.expectedVersion);
    }
    if (message.definition !== undefined) {
      KnowledgeObjectDefinition.encode(message.definition, writer.uint32(26).fork()).join();
    }
    if (message.updateMask !== undefined) {
      FieldMask.encode(FieldMask.wrap(message.updateMask), writer.uint32(34).fork()).join();
    }
    if (message.clientRequestId !== "") {
      writer.uint32(42).string(message.clientRequestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateKnowledgeObjectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateKnowledgeObjectRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.knowledgeObjectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.expectedVersion = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.definition = KnowledgeObjectDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.updateMask = FieldMask.unwrap(FieldMask.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.clientRequestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateKnowledgeObjectRequest {
    return {
      knowledgeObjectId: isSet(object.knowledgeObjectId)
        ? globalThis.String(object.knowledgeObjectId)
        : isSet(object.knowledge_object_id)
        ? globalThis.String(object.knowledge_object_id)
        : "",
      expectedVersion: isSet(object.expectedVersion)
        ? BigInt(object.expectedVersion)
        : isSet(object.expected_version)
        ? BigInt(object.expected_version)
        : 0n,
      definition: isSet(object.definition) ? KnowledgeObjectDefinition.fromJSON(object.definition) : undefined,
      updateMask: isSet(object.updateMask)
        ? FieldMask.unwrap(FieldMask.fromJSON(object.updateMask))
        : isSet(object.update_mask)
        ? FieldMask.unwrap(FieldMask.fromJSON(object.update_mask))
        : undefined,
      clientRequestId: isSet(object.clientRequestId)
        ? globalThis.String(object.clientRequestId)
        : isSet(object.client_request_id)
        ? globalThis.String(object.client_request_id)
        : "",
    };
  },

  toJSON(message: UpdateKnowledgeObjectRequest): unknown {
    const obj: any = {};
    if (message.knowledgeObjectId !== "") {
      obj.knowledgeObjectId = message.knowledgeObjectId;
    }
    if (message.expectedVersion !== 0n) {
      obj.expectedVersion = message.expectedVersion.toString();
    }
    if (message.definition !== undefined) {
      obj.definition = KnowledgeObjectDefinition.toJSON(message.definition);
    }
    if (message.updateMask !== undefined) {
      obj.updateMask = FieldMask.toJSON(FieldMask.wrap(message.updateMask));
    }
    if (message.clientRequestId !== "") {
      obj.clientRequestId = message.clientRequestId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateKnowledgeObjectRequest>, I>>(base?: I): UpdateKnowledgeObjectRequest {
    return UpdateKnowledgeObjectRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateKnowledgeObjectRequest>, I>>(object: I): UpdateKnowledgeObjectRequest {
    const message = createBaseUpdateKnowledgeObjectRequest();
    message.knowledgeObjectId = object.knowledgeObjectId ?? "";
    message.expectedVersion = (object.expectedVersion !== undefined && object.expectedVersion !== null)
      ? BigInt(object.expectedVersion)
      : 0n;
    message.definition = (object.definition !== undefined && object.definition !== null)
      ? KnowledgeObjectDefinition.fromPartial(object.definition)
      : undefined;
    message.updateMask = object.updateMask ?? undefined;
    message.clientRequestId = object.clientRequestId ?? "";
    return message;
  },
};

function createBaseUpdateKnowledgeObjectResponse(): UpdateKnowledgeObjectResponse {
  return { knowledgeObject: undefined, tenantCatalogRevision: 0n, tenantCatalogStateToken: new Uint8Array(0) };
}

export const UpdateKnowledgeObjectResponse: MessageFns<UpdateKnowledgeObjectResponse> = {
  encode(message: UpdateKnowledgeObjectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.knowledgeObject !== undefined) {
      KnowledgeObject.encode(message.knowledgeObject, writer.uint32(10).fork()).join();
    }
    if (message.tenantCatalogRevision !== 0n) {
      if (BigInt.asUintN(64, message.tenantCatalogRevision) !== message.tenantCatalogRevision) {
        throw new globalThis.Error("value provided for field message.tenantCatalogRevision of type uint64 too large");
      }
      writer.uint32(16).uint64(message.tenantCatalogRevision);
    }
    if (message.tenantCatalogStateToken.length !== 0) {
      writer.uint32(26).bytes(message.tenantCatalogStateToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateKnowledgeObjectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateKnowledgeObjectResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.knowledgeObject = KnowledgeObject.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.tenantCatalogRevision = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tenantCatalogStateToken = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateKnowledgeObjectResponse {
    return {
      knowledgeObject: isSet(object.knowledgeObject)
        ? KnowledgeObject.fromJSON(object.knowledgeObject)
        : isSet(object.knowledge_object)
        ? KnowledgeObject.fromJSON(object.knowledge_object)
        : undefined,
      tenantCatalogRevision: isSet(object.tenantCatalogRevision)
        ? BigInt(object.tenantCatalogRevision)
        : isSet(object.tenant_catalog_revision)
        ? BigInt(object.tenant_catalog_revision)
        : 0n,
      tenantCatalogStateToken: isSet(object.tenantCatalogStateToken)
        ? bytesFromBase64(object.tenantCatalogStateToken)
        : isSet(object.tenant_catalog_state_token)
        ? bytesFromBase64(object.tenant_catalog_state_token)
        : new Uint8Array(0),
    };
  },

  toJSON(message: UpdateKnowledgeObjectResponse): unknown {
    const obj: any = {};
    if (message.knowledgeObject !== undefined) {
      obj.knowledgeObject = KnowledgeObject.toJSON(message.knowledgeObject);
    }
    if (message.tenantCatalogRevision !== 0n) {
      obj.tenantCatalogRevision = message.tenantCatalogRevision.toString();
    }
    if (message.tenantCatalogStateToken.length !== 0) {
      obj.tenantCatalogStateToken = base64FromBytes(message.tenantCatalogStateToken);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateKnowledgeObjectResponse>, I>>(base?: I): UpdateKnowledgeObjectResponse {
    return UpdateKnowledgeObjectResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateKnowledgeObjectResponse>, I>>(
    object: I,
  ): UpdateKnowledgeObjectResponse {
    const message = createBaseUpdateKnowledgeObjectResponse();
    message.knowledgeObject = (object.knowledgeObject !== undefined && object.knowledgeObject !== null)
      ? KnowledgeObject.fromPartial(object.knowledgeObject)
      : undefined;
    message.tenantCatalogRevision =
      (object.tenantCatalogRevision !== undefined && object.tenantCatalogRevision !== null)
        ? BigInt(object.tenantCatalogRevision)
        : 0n;
    message.tenantCatalogStateToken = object.tenantCatalogStateToken ?? new Uint8Array(0);
    return message;
  },
};

function createBaseSetKnowledgeObjectStateRequest(): SetKnowledgeObjectStateRequest {
  return { knowledgeObjectId: "", expectedVersion: 0n, state: 0, clientRequestId: "" };
}

export const SetKnowledgeObjectStateRequest: MessageFns<SetKnowledgeObjectStateRequest> = {
  encode(message: SetKnowledgeObjectStateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.knowledgeObjectId !== "") {
      writer.uint32(10).string(message.knowledgeObjectId);
    }
    if (message.expectedVersion !== 0n) {
      if (BigInt.asUintN(64, message.expectedVersion) !== message.expectedVersion) {
        throw new globalThis.Error("value provided for field message.expectedVersion of type uint64 too large");
      }
      writer.uint32(16).uint64(message.expectedVersion);
    }
    if (message.state !== 0) {
      writer.uint32(24).int32(message.state);
    }
    if (message.clientRequestId !== "") {
      writer.uint32(34).string(message.clientRequestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetKnowledgeObjectStateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetKnowledgeObjectStateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.knowledgeObjectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.expectedVersion = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.clientRequestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetKnowledgeObjectStateRequest {
    return {
      knowledgeObjectId: isSet(object.knowledgeObjectId)
        ? globalThis.String(object.knowledgeObjectId)
        : isSet(object.knowledge_object_id)
        ? globalThis.String(object.knowledge_object_id)
        : "",
      expectedVersion: isSet(object.expectedVersion)
        ? BigInt(object.expectedVersion)
        : isSet(object.expected_version)
        ? BigInt(object.expected_version)
        : 0n,
      state: isSet(object.state) ? knowledgeObjectStateFromJSON(object.state) : 0,
      clientRequestId: isSet(object.clientRequestId)
        ? globalThis.String(object.clientRequestId)
        : isSet(object.client_request_id)
        ? globalThis.String(object.client_request_id)
        : "",
    };
  },

  toJSON(message: SetKnowledgeObjectStateRequest): unknown {
    const obj: any = {};
    if (message.knowledgeObjectId !== "") {
      obj.knowledgeObjectId = message.knowledgeObjectId;
    }
    if (message.expectedVersion !== 0n) {
      obj.expectedVersion = message.expectedVersion.toString();
    }
    if (message.state !== 0) {
      obj.state = knowledgeObjectStateToJSON(message.state);
    }
    if (message.clientRequestId !== "") {
      obj.clientRequestId = message.clientRequestId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetKnowledgeObjectStateRequest>, I>>(base?: I): SetKnowledgeObjectStateRequest {
    return SetKnowledgeObjectStateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetKnowledgeObjectStateRequest>, I>>(
    object: I,
  ): SetKnowledgeObjectStateRequest {
    const message = createBaseSetKnowledgeObjectStateRequest();
    message.knowledgeObjectId = object.knowledgeObjectId ?? "";
    message.expectedVersion = (object.expectedVersion !== undefined && object.expectedVersion !== null)
      ? BigInt(object.expectedVersion)
      : 0n;
    message.state = object.state ?? 0;
    message.clientRequestId = object.clientRequestId ?? "";
    return message;
  },
};

function createBaseSetKnowledgeObjectStateResponse(): SetKnowledgeObjectStateResponse {
  return { knowledgeObject: undefined, tenantCatalogRevision: 0n, tenantCatalogStateToken: new Uint8Array(0) };
}

export const SetKnowledgeObjectStateResponse: MessageFns<SetKnowledgeObjectStateResponse> = {
  encode(message: SetKnowledgeObjectStateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.knowledgeObject !== undefined) {
      KnowledgeObject.encode(message.knowledgeObject, writer.uint32(10).fork()).join();
    }
    if (message.tenantCatalogRevision !== 0n) {
      if (BigInt.asUintN(64, message.tenantCatalogRevision) !== message.tenantCatalogRevision) {
        throw new globalThis.Error("value provided for field message.tenantCatalogRevision of type uint64 too large");
      }
      writer.uint32(16).uint64(message.tenantCatalogRevision);
    }
    if (message.tenantCatalogStateToken.length !== 0) {
      writer.uint32(26).bytes(message.tenantCatalogStateToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetKnowledgeObjectStateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetKnowledgeObjectStateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.knowledgeObject = KnowledgeObject.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.tenantCatalogRevision = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tenantCatalogStateToken = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetKnowledgeObjectStateResponse {
    return {
      knowledgeObject: isSet(object.knowledgeObject)
        ? KnowledgeObject.fromJSON(object.knowledgeObject)
        : isSet(object.knowledge_object)
        ? KnowledgeObject.fromJSON(object.knowledge_object)
        : undefined,
      tenantCatalogRevision: isSet(object.tenantCatalogRevision)
        ? BigInt(object.tenantCatalogRevision)
        : isSet(object.tenant_catalog_revision)
        ? BigInt(object.tenant_catalog_revision)
        : 0n,
      tenantCatalogStateToken: isSet(object.tenantCatalogStateToken)
        ? bytesFromBase64(object.tenantCatalogStateToken)
        : isSet(object.tenant_catalog_state_token)
        ? bytesFromBase64(object.tenant_catalog_state_token)
        : new Uint8Array(0),
    };
  },

  toJSON(message: SetKnowledgeObjectStateResponse): unknown {
    const obj: any = {};
    if (message.knowledgeObject !== undefined) {
      obj.knowledgeObject = KnowledgeObject.toJSON(message.knowledgeObject);
    }
    if (message.tenantCatalogRevision !== 0n) {
      obj.tenantCatalogRevision = message.tenantCatalogRevision.toString();
    }
    if (message.tenantCatalogStateToken.length !== 0) {
      obj.tenantCatalogStateToken = base64FromBytes(message.tenantCatalogStateToken);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetKnowledgeObjectStateResponse>, I>>(base?: I): SetKnowledgeObjectStateResponse {
    return SetKnowledgeObjectStateResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetKnowledgeObjectStateResponse>, I>>(
    object: I,
  ): SetKnowledgeObjectStateResponse {
    const message = createBaseSetKnowledgeObjectStateResponse();
    message.knowledgeObject = (object.knowledgeObject !== undefined && object.knowledgeObject !== null)
      ? KnowledgeObject.fromPartial(object.knowledgeObject)
      : undefined;
    message.tenantCatalogRevision =
      (object.tenantCatalogRevision !== undefined && object.tenantCatalogRevision !== null)
        ? BigInt(object.tenantCatalogRevision)
        : 0n;
    message.tenantCatalogStateToken = object.tenantCatalogStateToken ?? new Uint8Array(0);
    return message;
  },
};

function createBaseDeleteKnowledgeObjectRequest(): DeleteKnowledgeObjectRequest {
  return { knowledgeObjectId: "", expectedVersion: 0n, clientRequestId: "" };
}

export const DeleteKnowledgeObjectRequest: MessageFns<DeleteKnowledgeObjectRequest> = {
  encode(message: DeleteKnowledgeObjectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.knowledgeObjectId !== "") {
      writer.uint32(10).string(message.knowledgeObjectId);
    }
    if (message.expectedVersion !== 0n) {
      if (BigInt.asUintN(64, message.expectedVersion) !== message.expectedVersion) {
        throw new globalThis.Error("value provided for field message.expectedVersion of type uint64 too large");
      }
      writer.uint32(16).uint64(message.expectedVersion);
    }
    if (message.clientRequestId !== "") {
      writer.uint32(26).string(message.clientRequestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteKnowledgeObjectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteKnowledgeObjectRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.knowledgeObjectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.expectedVersion = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.clientRequestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteKnowledgeObjectRequest {
    return {
      knowledgeObjectId: isSet(object.knowledgeObjectId)
        ? globalThis.String(object.knowledgeObjectId)
        : isSet(object.knowledge_object_id)
        ? globalThis.String(object.knowledge_object_id)
        : "",
      expectedVersion: isSet(object.expectedVersion)
        ? BigInt(object.expectedVersion)
        : isSet(object.expected_version)
        ? BigInt(object.expected_version)
        : 0n,
      clientRequestId: isSet(object.clientRequestId)
        ? globalThis.String(object.clientRequestId)
        : isSet(object.client_request_id)
        ? globalThis.String(object.client_request_id)
        : "",
    };
  },

  toJSON(message: DeleteKnowledgeObjectRequest): unknown {
    const obj: any = {};
    if (message.knowledgeObjectId !== "") {
      obj.knowledgeObjectId = message.knowledgeObjectId;
    }
    if (message.expectedVersion !== 0n) {
      obj.expectedVersion = message.expectedVersion.toString();
    }
    if (message.clientRequestId !== "") {
      obj.clientRequestId = message.clientRequestId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteKnowledgeObjectRequest>, I>>(base?: I): DeleteKnowledgeObjectRequest {
    return DeleteKnowledgeObjectRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteKnowledgeObjectRequest>, I>>(object: I): DeleteKnowledgeObjectRequest {
    const message = createBaseDeleteKnowledgeObjectRequest();
    message.knowledgeObjectId = object.knowledgeObjectId ?? "";
    message.expectedVersion = (object.expectedVersion !== undefined && object.expectedVersion !== null)
      ? BigInt(object.expectedVersion)
      : 0n;
    message.clientRequestId = object.clientRequestId ?? "";
    return message;
  },
};

function createBaseDeleteKnowledgeObjectResponse(): DeleteKnowledgeObjectResponse {
  return {
    knowledgeObjectId: "",
    deletedVersion: 0n,
    tenantCatalogRevision: 0n,
    tenantCatalogStateToken: new Uint8Array(0),
  };
}

export const DeleteKnowledgeObjectResponse: MessageFns<DeleteKnowledgeObjectResponse> = {
  encode(message: DeleteKnowledgeObjectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.knowledgeObjectId !== "") {
      writer.uint32(10).string(message.knowledgeObjectId);
    }
    if (message.deletedVersion !== 0n) {
      if (BigInt.asUintN(64, message.deletedVersion) !== message.deletedVersion) {
        throw new globalThis.Error("value provided for field message.deletedVersion of type uint64 too large");
      }
      writer.uint32(16).uint64(message.deletedVersion);
    }
    if (message.tenantCatalogRevision !== 0n) {
      if (BigInt.asUintN(64, message.tenantCatalogRevision) !== message.tenantCatalogRevision) {
        throw new globalThis.Error("value provided for field message.tenantCatalogRevision of type uint64 too large");
      }
      writer.uint32(24).uint64(message.tenantCatalogRevision);
    }
    if (message.tenantCatalogStateToken.length !== 0) {
      writer.uint32(34).bytes(message.tenantCatalogStateToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteKnowledgeObjectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteKnowledgeObjectResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.knowledgeObjectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.deletedVersion = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.tenantCatalogRevision = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.tenantCatalogStateToken = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteKnowledgeObjectResponse {
    return {
      knowledgeObjectId: isSet(object.knowledgeObjectId)
        ? globalThis.String(object.knowledgeObjectId)
        : isSet(object.knowledge_object_id)
        ? globalThis.String(object.knowledge_object_id)
        : "",
      deletedVersion: isSet(object.deletedVersion)
        ? BigInt(object.deletedVersion)
        : isSet(object.deleted_version)
        ? BigInt(object.deleted_version)
        : 0n,
      tenantCatalogRevision: isSet(object.tenantCatalogRevision)
        ? BigInt(object.tenantCatalogRevision)
        : isSet(object.tenant_catalog_revision)
        ? BigInt(object.tenant_catalog_revision)
        : 0n,
      tenantCatalogStateToken: isSet(object.tenantCatalogStateToken)
        ? bytesFromBase64(object.tenantCatalogStateToken)
        : isSet(object.tenant_catalog_state_token)
        ? bytesFromBase64(object.tenant_catalog_state_token)
        : new Uint8Array(0),
    };
  },

  toJSON(message: DeleteKnowledgeObjectResponse): unknown {
    const obj: any = {};
    if (message.knowledgeObjectId !== "") {
      obj.knowledgeObjectId = message.knowledgeObjectId;
    }
    if (message.deletedVersion !== 0n) {
      obj.deletedVersion = message.deletedVersion.toString();
    }
    if (message.tenantCatalogRevision !== 0n) {
      obj.tenantCatalogRevision = message.tenantCatalogRevision.toString();
    }
    if (message.tenantCatalogStateToken.length !== 0) {
      obj.tenantCatalogStateToken = base64FromBytes(message.tenantCatalogStateToken);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteKnowledgeObjectResponse>, I>>(base?: I): DeleteKnowledgeObjectResponse {
    return DeleteKnowledgeObjectResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteKnowledgeObjectResponse>, I>>(
    object: I,
  ): DeleteKnowledgeObjectResponse {
    const message = createBaseDeleteKnowledgeObjectResponse();
    message.knowledgeObjectId = object.knowledgeObjectId ?? "";
    message.deletedVersion = (object.deletedVersion !== undefined && object.deletedVersion !== null)
      ? BigInt(object.deletedVersion)
      : 0n;
    message.tenantCatalogRevision =
      (object.tenantCatalogRevision !== undefined && object.tenantCatalogRevision !== null)
        ? BigInt(object.tenantCatalogRevision)
        : 0n;
    message.tenantCatalogStateToken = object.tenantCatalogStateToken ?? new Uint8Array(0);
    return message;
  },
};

function createBaseKnowledgeMutationOutcomeRecord(): KnowledgeMutationOutcomeRecord {
  return {
    route: "",
    mutationKind: "",
    object: undefined,
    tenantCatalogRevision: 0n,
    tenantCatalogStateToken: new Uint8Array(0),
    occurredAtUnixMicro: 0n,
    retentionAnchorUnixMicro: 0n,
    retainUntilUnixMicro: 0n,
    auditAuthority: undefined,
  };
}

export const KnowledgeMutationOutcomeRecord: MessageFns<KnowledgeMutationOutcomeRecord> = {
  encode(message: KnowledgeMutationOutcomeRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.route !== "") {
      writer.uint32(10).string(message.route);
    }
    if (message.mutationKind !== "") {
      writer.uint32(18).string(message.mutationKind);
    }
    if (message.object !== undefined) {
      KnowledgeObjectVersionReference.encode(message.object, writer.uint32(26).fork()).join();
    }
    if (message.tenantCatalogRevision !== 0n) {
      if (BigInt.asUintN(64, message.tenantCatalogRevision) !== message.tenantCatalogRevision) {
        throw new globalThis.Error("value provided for field message.tenantCatalogRevision of type uint64 too large");
      }
      writer.uint32(32).uint64(message.tenantCatalogRevision);
    }
    if (message.tenantCatalogStateToken.length !== 0) {
      writer.uint32(42).bytes(message.tenantCatalogStateToken);
    }
    if (message.occurredAtUnixMicro !== 0n) {
      if (BigInt.asIntN(64, message.occurredAtUnixMicro) !== message.occurredAtUnixMicro) {
        throw new globalThis.Error("value provided for field message.occurredAtUnixMicro of type int64 too large");
      }
      writer.uint32(64).int64(message.occurredAtUnixMicro);
    }
    if (message.retentionAnchorUnixMicro !== 0n) {
      if (BigInt.asIntN(64, message.retentionAnchorUnixMicro) !== message.retentionAnchorUnixMicro) {
        throw new globalThis.Error("value provided for field message.retentionAnchorUnixMicro of type int64 too large");
      }
      writer.uint32(72).int64(message.retentionAnchorUnixMicro);
    }
    if (message.retainUntilUnixMicro !== 0n) {
      if (BigInt.asIntN(64, message.retainUntilUnixMicro) !== message.retainUntilUnixMicro) {
        throw new globalThis.Error("value provided for field message.retainUntilUnixMicro of type int64 too large");
      }
      writer.uint32(80).int64(message.retainUntilUnixMicro);
    }
    switch (message.auditAuthority?.$case) {
      case "successfulAuditSequence":
        if (BigInt.asUintN(64, message.auditAuthority.value) !== message.auditAuthority.value) {
          throw new globalThis.Error("value provided for field message.auditAuthority.value of type uint64 too large");
        }
        writer.uint32(48).uint64(message.auditAuthority.value);
        break;
      case "recoveryAuditSequence":
        if (BigInt.asUintN(64, message.auditAuthority.value) !== message.auditAuthority.value) {
          throw new globalThis.Error("value provided for field message.auditAuthority.value of type uint64 too large");
        }
        writer.uint32(56).uint64(message.auditAuthority.value);
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeMutationOutcomeRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeMutationOutcomeRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.route = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.mutationKind = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.object = KnowledgeObjectVersionReference.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.tenantCatalogRevision = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.tenantCatalogStateToken = reader.bytes();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.occurredAtUnixMicro = reader.int64() as bigint;
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.retentionAnchorUnixMicro = reader.int64() as bigint;
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.retainUntilUnixMicro = reader.int64() as bigint;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.auditAuthority = { $case: "successfulAuditSequence", value: reader.uint64() as bigint };
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.auditAuthority = { $case: "recoveryAuditSequence", value: reader.uint64() as bigint };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeMutationOutcomeRecord {
    return {
      route: isSet(object.route) ? globalThis.String(object.route) : "",
      mutationKind: isSet(object.mutationKind)
        ? globalThis.String(object.mutationKind)
        : isSet(object.mutation_kind)
        ? globalThis.String(object.mutation_kind)
        : "",
      object: isSet(object.object) ? KnowledgeObjectVersionReference.fromJSON(object.object) : undefined,
      tenantCatalogRevision: isSet(object.tenantCatalogRevision)
        ? BigInt(object.tenantCatalogRevision)
        : isSet(object.tenant_catalog_revision)
        ? BigInt(object.tenant_catalog_revision)
        : 0n,
      tenantCatalogStateToken: isSet(object.tenantCatalogStateToken)
        ? bytesFromBase64(object.tenantCatalogStateToken)
        : isSet(object.tenant_catalog_state_token)
        ? bytesFromBase64(object.tenant_catalog_state_token)
        : new Uint8Array(0),
      occurredAtUnixMicro: isSet(object.occurredAtUnixMicro)
        ? BigInt(object.occurredAtUnixMicro)
        : isSet(object.occurred_at_unix_micro)
        ? BigInt(object.occurred_at_unix_micro)
        : 0n,
      retentionAnchorUnixMicro: isSet(object.retentionAnchorUnixMicro)
        ? BigInt(object.retentionAnchorUnixMicro)
        : isSet(object.retention_anchor_unix_micro)
        ? BigInt(object.retention_anchor_unix_micro)
        : 0n,
      retainUntilUnixMicro: isSet(object.retainUntilUnixMicro)
        ? BigInt(object.retainUntilUnixMicro)
        : isSet(object.retain_until_unix_micro)
        ? BigInt(object.retain_until_unix_micro)
        : 0n,
      auditAuthority: isSet(object.successfulAuditSequence)
        ? { $case: "successfulAuditSequence", value: BigInt(object.successfulAuditSequence) }
        : isSet(object.successful_audit_sequence)
        ? { $case: "successfulAuditSequence", value: BigInt(object.successful_audit_sequence) }
        : isSet(object.recoveryAuditSequence)
        ? { $case: "recoveryAuditSequence", value: BigInt(object.recoveryAuditSequence) }
        : isSet(object.recovery_audit_sequence)
        ? { $case: "recoveryAuditSequence", value: BigInt(object.recovery_audit_sequence) }
        : undefined,
    };
  },

  toJSON(message: KnowledgeMutationOutcomeRecord): unknown {
    const obj: any = {};
    if (message.route !== "") {
      obj.route = message.route;
    }
    if (message.mutationKind !== "") {
      obj.mutationKind = message.mutationKind;
    }
    if (message.object !== undefined) {
      obj.object = KnowledgeObjectVersionReference.toJSON(message.object);
    }
    if (message.tenantCatalogRevision !== 0n) {
      obj.tenantCatalogRevision = message.tenantCatalogRevision.toString();
    }
    if (message.tenantCatalogStateToken.length !== 0) {
      obj.tenantCatalogStateToken = base64FromBytes(message.tenantCatalogStateToken);
    }
    if (message.occurredAtUnixMicro !== 0n) {
      obj.occurredAtUnixMicro = message.occurredAtUnixMicro.toString();
    }
    if (message.retentionAnchorUnixMicro !== 0n) {
      obj.retentionAnchorUnixMicro = message.retentionAnchorUnixMicro.toString();
    }
    if (message.retainUntilUnixMicro !== 0n) {
      obj.retainUntilUnixMicro = message.retainUntilUnixMicro.toString();
    }
    if (message.auditAuthority?.$case === "successfulAuditSequence") {
      obj.successfulAuditSequence = message.auditAuthority.value.toString();
    } else if (message.auditAuthority?.$case === "recoveryAuditSequence") {
      obj.recoveryAuditSequence = message.auditAuthority.value.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeMutationOutcomeRecord>, I>>(base?: I): KnowledgeMutationOutcomeRecord {
    return KnowledgeMutationOutcomeRecord.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeMutationOutcomeRecord>, I>>(
    object: I,
  ): KnowledgeMutationOutcomeRecord {
    const message = createBaseKnowledgeMutationOutcomeRecord();
    message.route = object.route ?? "";
    message.mutationKind = object.mutationKind ?? "";
    message.object = (object.object !== undefined && object.object !== null)
      ? KnowledgeObjectVersionReference.fromPartial(object.object)
      : undefined;
    message.tenantCatalogRevision =
      (object.tenantCatalogRevision !== undefined && object.tenantCatalogRevision !== null)
        ? BigInt(object.tenantCatalogRevision)
        : 0n;
    message.tenantCatalogStateToken = object.tenantCatalogStateToken ?? new Uint8Array(0);
    message.occurredAtUnixMicro = (object.occurredAtUnixMicro !== undefined && object.occurredAtUnixMicro !== null)
      ? BigInt(object.occurredAtUnixMicro)
      : 0n;
    message.retentionAnchorUnixMicro =
      (object.retentionAnchorUnixMicro !== undefined && object.retentionAnchorUnixMicro !== null)
        ? BigInt(object.retentionAnchorUnixMicro)
        : 0n;
    message.retainUntilUnixMicro = (object.retainUntilUnixMicro !== undefined && object.retainUntilUnixMicro !== null)
      ? BigInt(object.retainUntilUnixMicro)
      : 0n;
    switch (object.auditAuthority?.$case) {
      case "successfulAuditSequence": {
        if (object.auditAuthority?.value !== undefined && object.auditAuthority?.value !== null) {
          message.auditAuthority = { $case: "successfulAuditSequence", value: BigInt(object.auditAuthority.value) };
        }
        break;
      }
      case "recoveryAuditSequence": {
        if (object.auditAuthority?.value !== undefined && object.auditAuthority?.value !== null) {
          message.auditAuthority = { $case: "recoveryAuditSequence", value: BigInt(object.auditAuthority.value) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseKnowledgeQuarantineTransition(): KnowledgeQuarantineTransition {
  return { cascadeOrdinal: 0, knowledgeObjectId: "", previousVersion: 0n, quarantinedVersion: 0n, reason: 0 };
}

export const KnowledgeQuarantineTransition: MessageFns<KnowledgeQuarantineTransition> = {
  encode(message: KnowledgeQuarantineTransition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cascadeOrdinal !== 0) {
      writer.uint32(8).uint32(message.cascadeOrdinal);
    }
    if (message.knowledgeObjectId !== "") {
      writer.uint32(18).string(message.knowledgeObjectId);
    }
    if (message.previousVersion !== 0n) {
      if (BigInt.asUintN(64, message.previousVersion) !== message.previousVersion) {
        throw new globalThis.Error("value provided for field message.previousVersion of type uint64 too large");
      }
      writer.uint32(24).uint64(message.previousVersion);
    }
    if (message.quarantinedVersion !== 0n) {
      if (BigInt.asUintN(64, message.quarantinedVersion) !== message.quarantinedVersion) {
        throw new globalThis.Error("value provided for field message.quarantinedVersion of type uint64 too large");
      }
      writer.uint32(32).uint64(message.quarantinedVersion);
    }
    if (message.reason !== 0) {
      writer.uint32(40).int32(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeQuarantineTransition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeQuarantineTransition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.cascadeOrdinal = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.knowledgeObjectId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.previousVersion = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.quarantinedVersion = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeQuarantineTransition {
    return {
      cascadeOrdinal: isSet(object.cascadeOrdinal)
        ? globalThis.Number(object.cascadeOrdinal)
        : isSet(object.cascade_ordinal)
        ? globalThis.Number(object.cascade_ordinal)
        : 0,
      knowledgeObjectId: isSet(object.knowledgeObjectId)
        ? globalThis.String(object.knowledgeObjectId)
        : isSet(object.knowledge_object_id)
        ? globalThis.String(object.knowledge_object_id)
        : "",
      previousVersion: isSet(object.previousVersion)
        ? BigInt(object.previousVersion)
        : isSet(object.previous_version)
        ? BigInt(object.previous_version)
        : 0n,
      quarantinedVersion: isSet(object.quarantinedVersion)
        ? BigInt(object.quarantinedVersion)
        : isSet(object.quarantined_version)
        ? BigInt(object.quarantined_version)
        : 0n,
      reason: isSet(object.reason) ? knowledgeQuarantineReasonFromJSON(object.reason) : 0,
    };
  },

  toJSON(message: KnowledgeQuarantineTransition): unknown {
    const obj: any = {};
    if (message.cascadeOrdinal !== 0) {
      obj.cascadeOrdinal = Math.round(message.cascadeOrdinal);
    }
    if (message.knowledgeObjectId !== "") {
      obj.knowledgeObjectId = message.knowledgeObjectId;
    }
    if (message.previousVersion !== 0n) {
      obj.previousVersion = message.previousVersion.toString();
    }
    if (message.quarantinedVersion !== 0n) {
      obj.quarantinedVersion = message.quarantinedVersion.toString();
    }
    if (message.reason !== 0) {
      obj.reason = knowledgeQuarantineReasonToJSON(message.reason);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeQuarantineTransition>, I>>(base?: I): KnowledgeQuarantineTransition {
    return KnowledgeQuarantineTransition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeQuarantineTransition>, I>>(
    object: I,
  ): KnowledgeQuarantineTransition {
    const message = createBaseKnowledgeQuarantineTransition();
    message.cascadeOrdinal = object.cascadeOrdinal ?? 0;
    message.knowledgeObjectId = object.knowledgeObjectId ?? "";
    message.previousVersion = (object.previousVersion !== undefined && object.previousVersion !== null)
      ? BigInt(object.previousVersion)
      : 0n;
    message.quarantinedVersion = (object.quarantinedVersion !== undefined && object.quarantinedVersion !== null)
      ? BigInt(object.quarantinedVersion)
      : 0n;
    message.reason = object.reason ?? 0;
    return message;
  },
};

function createBasePrepareKnowledgeObjectQuarantineRequest(): PrepareKnowledgeObjectQuarantineRequest {
  return { knowledgeObjectId: "" };
}

export const PrepareKnowledgeObjectQuarantineRequest: MessageFns<PrepareKnowledgeObjectQuarantineRequest> = {
  encode(message: PrepareKnowledgeObjectQuarantineRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.knowledgeObjectId !== "") {
      writer.uint32(10).string(message.knowledgeObjectId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PrepareKnowledgeObjectQuarantineRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePrepareKnowledgeObjectQuarantineRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.knowledgeObjectId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PrepareKnowledgeObjectQuarantineRequest {
    return {
      knowledgeObjectId: isSet(object.knowledgeObjectId)
        ? globalThis.String(object.knowledgeObjectId)
        : isSet(object.knowledge_object_id)
        ? globalThis.String(object.knowledge_object_id)
        : "",
    };
  },

  toJSON(message: PrepareKnowledgeObjectQuarantineRequest): unknown {
    const obj: any = {};
    if (message.knowledgeObjectId !== "") {
      obj.knowledgeObjectId = message.knowledgeObjectId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PrepareKnowledgeObjectQuarantineRequest>, I>>(
    base?: I,
  ): PrepareKnowledgeObjectQuarantineRequest {
    return PrepareKnowledgeObjectQuarantineRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PrepareKnowledgeObjectQuarantineRequest>, I>>(
    object: I,
  ): PrepareKnowledgeObjectQuarantineRequest {
    const message = createBasePrepareKnowledgeObjectQuarantineRequest();
    message.knowledgeObjectId = object.knowledgeObjectId ?? "";
    return message;
  },
};

function createBasePrepareKnowledgeObjectQuarantineResponse(): PrepareKnowledgeObjectQuarantineResponse {
  return {
    rootKnowledgeObjectId: "",
    recoveryToken: "",
    expiresAt: undefined,
    dependentCount: 0,
    tenantCatalogRevision: 0n,
  };
}

export const PrepareKnowledgeObjectQuarantineResponse: MessageFns<PrepareKnowledgeObjectQuarantineResponse> = {
  encode(message: PrepareKnowledgeObjectQuarantineResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rootKnowledgeObjectId !== "") {
      writer.uint32(10).string(message.rootKnowledgeObjectId);
    }
    if (message.recoveryToken !== "") {
      writer.uint32(18).string(message.recoveryToken);
    }
    if (message.expiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expiresAt), writer.uint32(26).fork()).join();
    }
    if (message.dependentCount !== 0) {
      writer.uint32(32).uint32(message.dependentCount);
    }
    if (message.tenantCatalogRevision !== 0n) {
      if (BigInt.asUintN(64, message.tenantCatalogRevision) !== message.tenantCatalogRevision) {
        throw new globalThis.Error("value provided for field message.tenantCatalogRevision of type uint64 too large");
      }
      writer.uint32(40).uint64(message.tenantCatalogRevision);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PrepareKnowledgeObjectQuarantineResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePrepareKnowledgeObjectQuarantineResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rootKnowledgeObjectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.recoveryToken = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.expiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.dependentCount = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.tenantCatalogRevision = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PrepareKnowledgeObjectQuarantineResponse {
    return {
      rootKnowledgeObjectId: isSet(object.rootKnowledgeObjectId)
        ? globalThis.String(object.rootKnowledgeObjectId)
        : isSet(object.root_knowledge_object_id)
        ? globalThis.String(object.root_knowledge_object_id)
        : "",
      recoveryToken: isSet(object.recoveryToken)
        ? globalThis.String(object.recoveryToken)
        : isSet(object.recovery_token)
        ? globalThis.String(object.recovery_token)
        : "",
      expiresAt: isSet(object.expiresAt)
        ? fromJsonTimestamp(object.expiresAt)
        : isSet(object.expires_at)
        ? fromJsonTimestamp(object.expires_at)
        : undefined,
      dependentCount: isSet(object.dependentCount)
        ? globalThis.Number(object.dependentCount)
        : isSet(object.dependent_count)
        ? globalThis.Number(object.dependent_count)
        : 0,
      tenantCatalogRevision: isSet(object.tenantCatalogRevision)
        ? BigInt(object.tenantCatalogRevision)
        : isSet(object.tenant_catalog_revision)
        ? BigInt(object.tenant_catalog_revision)
        : 0n,
    };
  },

  toJSON(message: PrepareKnowledgeObjectQuarantineResponse): unknown {
    const obj: any = {};
    if (message.rootKnowledgeObjectId !== "") {
      obj.rootKnowledgeObjectId = message.rootKnowledgeObjectId;
    }
    if (message.recoveryToken !== "") {
      obj.recoveryToken = message.recoveryToken;
    }
    if (message.expiresAt !== undefined) {
      obj.expiresAt = message.expiresAt.toISOString();
    }
    if (message.dependentCount !== 0) {
      obj.dependentCount = Math.round(message.dependentCount);
    }
    if (message.tenantCatalogRevision !== 0n) {
      obj.tenantCatalogRevision = message.tenantCatalogRevision.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PrepareKnowledgeObjectQuarantineResponse>, I>>(
    base?: I,
  ): PrepareKnowledgeObjectQuarantineResponse {
    return PrepareKnowledgeObjectQuarantineResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PrepareKnowledgeObjectQuarantineResponse>, I>>(
    object: I,
  ): PrepareKnowledgeObjectQuarantineResponse {
    const message = createBasePrepareKnowledgeObjectQuarantineResponse();
    message.rootKnowledgeObjectId = object.rootKnowledgeObjectId ?? "";
    message.recoveryToken = object.recoveryToken ?? "";
    message.expiresAt = object.expiresAt ?? undefined;
    message.dependentCount = object.dependentCount ?? 0;
    message.tenantCatalogRevision =
      (object.tenantCatalogRevision !== undefined && object.tenantCatalogRevision !== null)
        ? BigInt(object.tenantCatalogRevision)
        : 0n;
    return message;
  },
};

function createBaseQuarantineKnowledgeObjectRequest(): QuarantineKnowledgeObjectRequest {
  return { recoveryToken: "", clientRequestId: "" };
}

export const QuarantineKnowledgeObjectRequest: MessageFns<QuarantineKnowledgeObjectRequest> = {
  encode(message: QuarantineKnowledgeObjectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.recoveryToken !== "") {
      writer.uint32(10).string(message.recoveryToken);
    }
    if (message.clientRequestId !== "") {
      writer.uint32(18).string(message.clientRequestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuarantineKnowledgeObjectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuarantineKnowledgeObjectRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.recoveryToken = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.clientRequestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuarantineKnowledgeObjectRequest {
    return {
      recoveryToken: isSet(object.recoveryToken)
        ? globalThis.String(object.recoveryToken)
        : isSet(object.recovery_token)
        ? globalThis.String(object.recovery_token)
        : "",
      clientRequestId: isSet(object.clientRequestId)
        ? globalThis.String(object.clientRequestId)
        : isSet(object.client_request_id)
        ? globalThis.String(object.client_request_id)
        : "",
    };
  },

  toJSON(message: QuarantineKnowledgeObjectRequest): unknown {
    const obj: any = {};
    if (message.recoveryToken !== "") {
      obj.recoveryToken = message.recoveryToken;
    }
    if (message.clientRequestId !== "") {
      obj.clientRequestId = message.clientRequestId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuarantineKnowledgeObjectRequest>, I>>(
    base?: I,
  ): QuarantineKnowledgeObjectRequest {
    return QuarantineKnowledgeObjectRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuarantineKnowledgeObjectRequest>, I>>(
    object: I,
  ): QuarantineKnowledgeObjectRequest {
    const message = createBaseQuarantineKnowledgeObjectRequest();
    message.recoveryToken = object.recoveryToken ?? "";
    message.clientRequestId = object.clientRequestId ?? "";
    return message;
  },
};

function createBaseQuarantineKnowledgeObjectResponse(): QuarantineKnowledgeObjectResponse {
  return { rootKnowledgeObjectId: "", transitions: [], tenantCatalogRevision: 0n };
}

export const QuarantineKnowledgeObjectResponse: MessageFns<QuarantineKnowledgeObjectResponse> = {
  encode(message: QuarantineKnowledgeObjectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rootKnowledgeObjectId !== "") {
      writer.uint32(10).string(message.rootKnowledgeObjectId);
    }
    for (const v of message.transitions) {
      KnowledgeQuarantineTransition.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.tenantCatalogRevision !== 0n) {
      if (BigInt.asUintN(64, message.tenantCatalogRevision) !== message.tenantCatalogRevision) {
        throw new globalThis.Error("value provided for field message.tenantCatalogRevision of type uint64 too large");
      }
      writer.uint32(24).uint64(message.tenantCatalogRevision);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuarantineKnowledgeObjectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuarantineKnowledgeObjectResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rootKnowledgeObjectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.transitions.push(KnowledgeQuarantineTransition.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.tenantCatalogRevision = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuarantineKnowledgeObjectResponse {
    return {
      rootKnowledgeObjectId: isSet(object.rootKnowledgeObjectId)
        ? globalThis.String(object.rootKnowledgeObjectId)
        : isSet(object.root_knowledge_object_id)
        ? globalThis.String(object.root_knowledge_object_id)
        : "",
      transitions: globalThis.Array.isArray(object?.transitions)
        ? object.transitions.map((e: any) => KnowledgeQuarantineTransition.fromJSON(e))
        : [],
      tenantCatalogRevision: isSet(object.tenantCatalogRevision)
        ? BigInt(object.tenantCatalogRevision)
        : isSet(object.tenant_catalog_revision)
        ? BigInt(object.tenant_catalog_revision)
        : 0n,
    };
  },

  toJSON(message: QuarantineKnowledgeObjectResponse): unknown {
    const obj: any = {};
    if (message.rootKnowledgeObjectId !== "") {
      obj.rootKnowledgeObjectId = message.rootKnowledgeObjectId;
    }
    if (message.transitions?.length) {
      obj.transitions = message.transitions.map((e) => KnowledgeQuarantineTransition.toJSON(e));
    }
    if (message.tenantCatalogRevision !== 0n) {
      obj.tenantCatalogRevision = message.tenantCatalogRevision.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuarantineKnowledgeObjectResponse>, I>>(
    base?: I,
  ): QuarantineKnowledgeObjectResponse {
    return QuarantineKnowledgeObjectResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuarantineKnowledgeObjectResponse>, I>>(
    object: I,
  ): QuarantineKnowledgeObjectResponse {
    const message = createBaseQuarantineKnowledgeObjectResponse();
    message.rootKnowledgeObjectId = object.rootKnowledgeObjectId ?? "";
    message.transitions = object.transitions?.map((e) => KnowledgeQuarantineTransition.fromPartial(e)) || [];
    message.tenantCatalogRevision =
      (object.tenantCatalogRevision !== undefined && object.tenantCatalogRevision !== null)
        ? BigInt(object.tenantCatalogRevision)
        : 0n;
    return message;
  },
};

function createBaseKnowledgeValidationDependency(): KnowledgeValidationDependency {
  return { target: undefined, role: 0 };
}

export const KnowledgeValidationDependency: MessageFns<KnowledgeValidationDependency> = {
  encode(message: KnowledgeValidationDependency, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.target !== undefined) {
      KnowledgeManagementObjectVersionIdentity.encode(message.target, writer.uint32(10).fork()).join();
    }
    if (message.role !== 0) {
      writer.uint32(16).int32(message.role);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeValidationDependency {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeValidationDependency();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.target = KnowledgeManagementObjectVersionIdentity.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.role = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeValidationDependency {
    return {
      target: isSet(object.target) ? KnowledgeManagementObjectVersionIdentity.fromJSON(object.target) : undefined,
      role: isSet(object.role) ? knowledgeDependencyRoleFromJSON(object.role) : 0,
    };
  },

  toJSON(message: KnowledgeValidationDependency): unknown {
    const obj: any = {};
    if (message.target !== undefined) {
      obj.target = KnowledgeManagementObjectVersionIdentity.toJSON(message.target);
    }
    if (message.role !== 0) {
      obj.role = knowledgeDependencyRoleToJSON(message.role);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeValidationDependency>, I>>(base?: I): KnowledgeValidationDependency {
    return KnowledgeValidationDependency.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeValidationDependency>, I>>(
    object: I,
  ): KnowledgeValidationDependency {
    const message = createBaseKnowledgeValidationDependency();
    message.target = (object.target !== undefined && object.target !== null)
      ? KnowledgeManagementObjectVersionIdentity.fromPartial(object.target)
      : undefined;
    message.role = object.role ?? 0;
    return message;
  },
};

function createBaseKnowledgeValidationDiagnostic(): KnowledgeValidationDiagnostic {
  return { fieldPath: "", diagnostic: undefined };
}

export const KnowledgeValidationDiagnostic: MessageFns<KnowledgeValidationDiagnostic> = {
  encode(message: KnowledgeValidationDiagnostic, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fieldPath !== "") {
      writer.uint32(10).string(message.fieldPath);
    }
    if (message.diagnostic !== undefined) {
      Diagnostic.encode(message.diagnostic, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeValidationDiagnostic {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeValidationDiagnostic();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.fieldPath = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.diagnostic = Diagnostic.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeValidationDiagnostic {
    return {
      fieldPath: isSet(object.fieldPath)
        ? globalThis.String(object.fieldPath)
        : isSet(object.field_path)
        ? globalThis.String(object.field_path)
        : "",
      diagnostic: isSet(object.diagnostic) ? Diagnostic.fromJSON(object.diagnostic) : undefined,
    };
  },

  toJSON(message: KnowledgeValidationDiagnostic): unknown {
    const obj: any = {};
    if (message.fieldPath !== "") {
      obj.fieldPath = message.fieldPath;
    }
    if (message.diagnostic !== undefined) {
      obj.diagnostic = Diagnostic.toJSON(message.diagnostic);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeValidationDiagnostic>, I>>(base?: I): KnowledgeValidationDiagnostic {
    return KnowledgeValidationDiagnostic.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeValidationDiagnostic>, I>>(
    object: I,
  ): KnowledgeValidationDiagnostic {
    const message = createBaseKnowledgeValidationDiagnostic();
    message.fieldPath = object.fieldPath ?? "";
    message.diagnostic = (object.diagnostic !== undefined && object.diagnostic !== null)
      ? Diagnostic.fromPartial(object.diagnostic)
      : undefined;
    return message;
  },
};

function createBaseKnowledgeResourceEstimate(): KnowledgeResourceEstimate {
  return {
    selectorPatterns: 0,
    normalizedDefinitionBytes: 0n,
    dependencyNodes: 0,
    dependencyEdges: 0,
    generatedOperators: 0,
    generatedFields: 0,
    regexPrograms: 0,
    estimatedRegexWorkUnits: 0n,
    scalarExpressions: 0,
    scalarExpressionNodes: 0,
    extractionOutputs: 0,
    jsonEvaluationWorkUnits: 0,
    scalarPredicates: 0,
  };
}

export const KnowledgeResourceEstimate: MessageFns<KnowledgeResourceEstimate> = {
  encode(message: KnowledgeResourceEstimate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.selectorPatterns !== 0) {
      writer.uint32(8).uint32(message.selectorPatterns);
    }
    if (message.normalizedDefinitionBytes !== 0n) {
      if (BigInt.asUintN(64, message.normalizedDefinitionBytes) !== message.normalizedDefinitionBytes) {
        throw new globalThis.Error(
          "value provided for field message.normalizedDefinitionBytes of type uint64 too large",
        );
      }
      writer.uint32(16).uint64(message.normalizedDefinitionBytes);
    }
    if (message.dependencyNodes !== 0) {
      writer.uint32(24).uint32(message.dependencyNodes);
    }
    if (message.dependencyEdges !== 0) {
      writer.uint32(32).uint32(message.dependencyEdges);
    }
    if (message.generatedOperators !== 0) {
      writer.uint32(40).uint32(message.generatedOperators);
    }
    if (message.generatedFields !== 0) {
      writer.uint32(48).uint32(message.generatedFields);
    }
    if (message.regexPrograms !== 0) {
      writer.uint32(56).uint32(message.regexPrograms);
    }
    if (message.estimatedRegexWorkUnits !== 0n) {
      if (BigInt.asUintN(64, message.estimatedRegexWorkUnits) !== message.estimatedRegexWorkUnits) {
        throw new globalThis.Error("value provided for field message.estimatedRegexWorkUnits of type uint64 too large");
      }
      writer.uint32(64).uint64(message.estimatedRegexWorkUnits);
    }
    if (message.scalarExpressions !== 0) {
      writer.uint32(72).uint32(message.scalarExpressions);
    }
    if (message.scalarExpressionNodes !== 0) {
      writer.uint32(80).uint32(message.scalarExpressionNodes);
    }
    if (message.extractionOutputs !== 0) {
      writer.uint32(96).uint32(message.extractionOutputs);
    }
    if (message.jsonEvaluationWorkUnits !== 0) {
      writer.uint32(104).uint32(message.jsonEvaluationWorkUnits);
    }
    if (message.scalarPredicates !== 0) {
      writer.uint32(112).uint32(message.scalarPredicates);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeResourceEstimate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeResourceEstimate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.selectorPatterns = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.normalizedDefinitionBytes = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.dependencyNodes = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.dependencyEdges = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.generatedOperators = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.generatedFields = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.regexPrograms = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.estimatedRegexWorkUnits = reader.uint64() as bigint;
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.scalarExpressions = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.scalarExpressionNodes = reader.uint32();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.extractionOutputs = reader.uint32();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.jsonEvaluationWorkUnits = reader.uint32();
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.scalarPredicates = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeResourceEstimate {
    return {
      selectorPatterns: isSet(object.selectorPatterns)
        ? globalThis.Number(object.selectorPatterns)
        : isSet(object.selector_patterns)
        ? globalThis.Number(object.selector_patterns)
        : 0,
      normalizedDefinitionBytes: isSet(object.normalizedDefinitionBytes)
        ? BigInt(object.normalizedDefinitionBytes)
        : isSet(object.normalized_definition_bytes)
        ? BigInt(object.normalized_definition_bytes)
        : 0n,
      dependencyNodes: isSet(object.dependencyNodes)
        ? globalThis.Number(object.dependencyNodes)
        : isSet(object.dependency_nodes)
        ? globalThis.Number(object.dependency_nodes)
        : 0,
      dependencyEdges: isSet(object.dependencyEdges)
        ? globalThis.Number(object.dependencyEdges)
        : isSet(object.dependency_edges)
        ? globalThis.Number(object.dependency_edges)
        : 0,
      generatedOperators: isSet(object.generatedOperators)
        ? globalThis.Number(object.generatedOperators)
        : isSet(object.generated_operators)
        ? globalThis.Number(object.generated_operators)
        : 0,
      generatedFields: isSet(object.generatedFields)
        ? globalThis.Number(object.generatedFields)
        : isSet(object.generated_fields)
        ? globalThis.Number(object.generated_fields)
        : 0,
      regexPrograms: isSet(object.regexPrograms)
        ? globalThis.Number(object.regexPrograms)
        : isSet(object.regex_programs)
        ? globalThis.Number(object.regex_programs)
        : 0,
      estimatedRegexWorkUnits: isSet(object.estimatedRegexWorkUnits)
        ? BigInt(object.estimatedRegexWorkUnits)
        : isSet(object.estimated_regex_work_units)
        ? BigInt(object.estimated_regex_work_units)
        : 0n,
      scalarExpressions: isSet(object.scalarExpressions)
        ? globalThis.Number(object.scalarExpressions)
        : isSet(object.scalar_expressions)
        ? globalThis.Number(object.scalar_expressions)
        : 0,
      scalarExpressionNodes: isSet(object.scalarExpressionNodes)
        ? globalThis.Number(object.scalarExpressionNodes)
        : isSet(object.scalar_expression_nodes)
        ? globalThis.Number(object.scalar_expression_nodes)
        : 0,
      extractionOutputs: isSet(object.extractionOutputs)
        ? globalThis.Number(object.extractionOutputs)
        : isSet(object.extraction_outputs)
        ? globalThis.Number(object.extraction_outputs)
        : 0,
      jsonEvaluationWorkUnits: isSet(object.jsonEvaluationWorkUnits)
        ? globalThis.Number(object.jsonEvaluationWorkUnits)
        : isSet(object.json_evaluation_work_units)
        ? globalThis.Number(object.json_evaluation_work_units)
        : 0,
      scalarPredicates: isSet(object.scalarPredicates)
        ? globalThis.Number(object.scalarPredicates)
        : isSet(object.scalar_predicates)
        ? globalThis.Number(object.scalar_predicates)
        : 0,
    };
  },

  toJSON(message: KnowledgeResourceEstimate): unknown {
    const obj: any = {};
    if (message.selectorPatterns !== 0) {
      obj.selectorPatterns = Math.round(message.selectorPatterns);
    }
    if (message.normalizedDefinitionBytes !== 0n) {
      obj.normalizedDefinitionBytes = message.normalizedDefinitionBytes.toString();
    }
    if (message.dependencyNodes !== 0) {
      obj.dependencyNodes = Math.round(message.dependencyNodes);
    }
    if (message.dependencyEdges !== 0) {
      obj.dependencyEdges = Math.round(message.dependencyEdges);
    }
    if (message.generatedOperators !== 0) {
      obj.generatedOperators = Math.round(message.generatedOperators);
    }
    if (message.generatedFields !== 0) {
      obj.generatedFields = Math.round(message.generatedFields);
    }
    if (message.regexPrograms !== 0) {
      obj.regexPrograms = Math.round(message.regexPrograms);
    }
    if (message.estimatedRegexWorkUnits !== 0n) {
      obj.estimatedRegexWorkUnits = message.estimatedRegexWorkUnits.toString();
    }
    if (message.scalarExpressions !== 0) {
      obj.scalarExpressions = Math.round(message.scalarExpressions);
    }
    if (message.scalarExpressionNodes !== 0) {
      obj.scalarExpressionNodes = Math.round(message.scalarExpressionNodes);
    }
    if (message.extractionOutputs !== 0) {
      obj.extractionOutputs = Math.round(message.extractionOutputs);
    }
    if (message.jsonEvaluationWorkUnits !== 0) {
      obj.jsonEvaluationWorkUnits = Math.round(message.jsonEvaluationWorkUnits);
    }
    if (message.scalarPredicates !== 0) {
      obj.scalarPredicates = Math.round(message.scalarPredicates);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeResourceEstimate>, I>>(base?: I): KnowledgeResourceEstimate {
    return KnowledgeResourceEstimate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeResourceEstimate>, I>>(object: I): KnowledgeResourceEstimate {
    const message = createBaseKnowledgeResourceEstimate();
    message.selectorPatterns = object.selectorPatterns ?? 0;
    message.normalizedDefinitionBytes =
      (object.normalizedDefinitionBytes !== undefined && object.normalizedDefinitionBytes !== null)
        ? BigInt(object.normalizedDefinitionBytes)
        : 0n;
    message.dependencyNodes = object.dependencyNodes ?? 0;
    message.dependencyEdges = object.dependencyEdges ?? 0;
    message.generatedOperators = object.generatedOperators ?? 0;
    message.generatedFields = object.generatedFields ?? 0;
    message.regexPrograms = object.regexPrograms ?? 0;
    message.estimatedRegexWorkUnits =
      (object.estimatedRegexWorkUnits !== undefined && object.estimatedRegexWorkUnits !== null)
        ? BigInt(object.estimatedRegexWorkUnits)
        : 0n;
    message.scalarExpressions = object.scalarExpressions ?? 0;
    message.scalarExpressionNodes = object.scalarExpressionNodes ?? 0;
    message.extractionOutputs = object.extractionOutputs ?? 0;
    message.jsonEvaluationWorkUnits = object.jsonEvaluationWorkUnits ?? 0;
    message.scalarPredicates = object.scalarPredicates ?? 0;
    return message;
  },
};

function createBaseKnowledgeValidationResult(): KnowledgeValidationResult {
  return {
    valid: false,
    objectType: 0,
    normalizedDefinition: undefined,
    definitionSha256: undefined,
    fieldViolations: [],
    resources: undefined,
    dependencies: [],
    diagnostics: [],
    fieldViolationsTruncated: false,
    diagnosticsTruncated: false,
  };
}

export const KnowledgeValidationResult: MessageFns<KnowledgeValidationResult> = {
  encode(message: KnowledgeValidationResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.valid !== false) {
      writer.uint32(8).bool(message.valid);
    }
    if (message.objectType !== 0) {
      writer.uint32(16).int32(message.objectType);
    }
    if (message.normalizedDefinition !== undefined) {
      KnowledgeObjectDefinition.encode(message.normalizedDefinition, writer.uint32(26).fork()).join();
    }
    if (message.definitionSha256 !== undefined) {
      writer.uint32(34).bytes(message.definitionSha256);
    }
    for (const v of message.fieldViolations) {
      FieldViolation.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.resources !== undefined) {
      KnowledgeResourceEstimate.encode(message.resources, writer.uint32(66).fork()).join();
    }
    for (const v of message.dependencies) {
      KnowledgeValidationDependency.encode(v!, writer.uint32(74).fork()).join();
    }
    for (const v of message.diagnostics) {
      KnowledgeValidationDiagnostic.encode(v!, writer.uint32(82).fork()).join();
    }
    if (message.fieldViolationsTruncated !== false) {
      writer.uint32(88).bool(message.fieldViolationsTruncated);
    }
    if (message.diagnosticsTruncated !== false) {
      writer.uint32(96).bool(message.diagnosticsTruncated);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeValidationResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeValidationResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.valid = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.objectType = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.normalizedDefinition = KnowledgeObjectDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.definitionSha256 = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.fieldViolations.push(FieldViolation.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.resources = KnowledgeResourceEstimate.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.dependencies.push(KnowledgeValidationDependency.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.diagnostics.push(KnowledgeValidationDiagnostic.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.fieldViolationsTruncated = reader.bool();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.diagnosticsTruncated = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeValidationResult {
    return {
      valid: isSet(object.valid) ? globalThis.Boolean(object.valid) : false,
      objectType: isSet(object.objectType)
        ? knowledgeObjectTypeFromJSON(object.objectType)
        : isSet(object.object_type)
        ? knowledgeObjectTypeFromJSON(object.object_type)
        : 0,
      normalizedDefinition: isSet(object.normalizedDefinition)
        ? KnowledgeObjectDefinition.fromJSON(object.normalizedDefinition)
        : isSet(object.normalized_definition)
        ? KnowledgeObjectDefinition.fromJSON(object.normalized_definition)
        : undefined,
      definitionSha256: isSet(object.definitionSha256)
        ? bytesFromBase64(object.definitionSha256)
        : isSet(object.definition_sha256)
        ? bytesFromBase64(object.definition_sha256)
        : undefined,
      fieldViolations: globalThis.Array.isArray(object?.fieldViolations)
        ? object.fieldViolations.map((e: any) => FieldViolation.fromJSON(e))
        : globalThis.Array.isArray(object?.field_violations)
        ? object.field_violations.map((e: any) => FieldViolation.fromJSON(e))
        : [],
      resources: isSet(object.resources) ? KnowledgeResourceEstimate.fromJSON(object.resources) : undefined,
      dependencies: globalThis.Array.isArray(object?.dependencies)
        ? object.dependencies.map((e: any) => KnowledgeValidationDependency.fromJSON(e))
        : [],
      diagnostics: globalThis.Array.isArray(object?.diagnostics)
        ? object.diagnostics.map((e: any) => KnowledgeValidationDiagnostic.fromJSON(e))
        : [],
      fieldViolationsTruncated: isSet(object.fieldViolationsTruncated)
        ? globalThis.Boolean(object.fieldViolationsTruncated)
        : isSet(object.field_violations_truncated)
        ? globalThis.Boolean(object.field_violations_truncated)
        : false,
      diagnosticsTruncated: isSet(object.diagnosticsTruncated)
        ? globalThis.Boolean(object.diagnosticsTruncated)
        : isSet(object.diagnostics_truncated)
        ? globalThis.Boolean(object.diagnostics_truncated)
        : false,
    };
  },

  toJSON(message: KnowledgeValidationResult): unknown {
    const obj: any = {};
    if (message.valid !== false) {
      obj.valid = message.valid;
    }
    if (message.objectType !== 0) {
      obj.objectType = knowledgeObjectTypeToJSON(message.objectType);
    }
    if (message.normalizedDefinition !== undefined) {
      obj.normalizedDefinition = KnowledgeObjectDefinition.toJSON(message.normalizedDefinition);
    }
    if (message.definitionSha256 !== undefined) {
      obj.definitionSha256 = base64FromBytes(message.definitionSha256);
    }
    if (message.fieldViolations?.length) {
      obj.fieldViolations = message.fieldViolations.map((e) => FieldViolation.toJSON(e));
    }
    if (message.resources !== undefined) {
      obj.resources = KnowledgeResourceEstimate.toJSON(message.resources);
    }
    if (message.dependencies?.length) {
      obj.dependencies = message.dependencies.map((e) => KnowledgeValidationDependency.toJSON(e));
    }
    if (message.diagnostics?.length) {
      obj.diagnostics = message.diagnostics.map((e) => KnowledgeValidationDiagnostic.toJSON(e));
    }
    if (message.fieldViolationsTruncated !== false) {
      obj.fieldViolationsTruncated = message.fieldViolationsTruncated;
    }
    if (message.diagnosticsTruncated !== false) {
      obj.diagnosticsTruncated = message.diagnosticsTruncated;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeValidationResult>, I>>(base?: I): KnowledgeValidationResult {
    return KnowledgeValidationResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeValidationResult>, I>>(object: I): KnowledgeValidationResult {
    const message = createBaseKnowledgeValidationResult();
    message.valid = object.valid ?? false;
    message.objectType = object.objectType ?? 0;
    message.normalizedDefinition = (object.normalizedDefinition !== undefined && object.normalizedDefinition !== null)
      ? KnowledgeObjectDefinition.fromPartial(object.normalizedDefinition)
      : undefined;
    message.definitionSha256 = object.definitionSha256 ?? undefined;
    message.fieldViolations = object.fieldViolations?.map((e) => FieldViolation.fromPartial(e)) || [];
    message.resources = (object.resources !== undefined && object.resources !== null)
      ? KnowledgeResourceEstimate.fromPartial(object.resources)
      : undefined;
    message.dependencies = object.dependencies?.map((e) => KnowledgeValidationDependency.fromPartial(e)) || [];
    message.diagnostics = object.diagnostics?.map((e) => KnowledgeValidationDiagnostic.fromPartial(e)) || [];
    message.fieldViolationsTruncated = object.fieldViolationsTruncated ?? false;
    message.diagnosticsTruncated = object.diagnosticsTruncated ?? false;
    return message;
  },
};

function createBaseValidateKnowledgeObjectRequest(): ValidateKnowledgeObjectRequest {
  return {
    definition: undefined,
    knowledgeObjectId: undefined,
    expectedVersion: undefined,
    updateMask: undefined,
    intent: 0,
  };
}

export const ValidateKnowledgeObjectRequest: MessageFns<ValidateKnowledgeObjectRequest> = {
  encode(message: ValidateKnowledgeObjectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.definition !== undefined) {
      KnowledgeObjectDefinition.encode(message.definition, writer.uint32(10).fork()).join();
    }
    if (message.knowledgeObjectId !== undefined) {
      writer.uint32(18).string(message.knowledgeObjectId);
    }
    if (message.expectedVersion !== undefined) {
      if (BigInt.asUintN(64, message.expectedVersion) !== message.expectedVersion) {
        throw new globalThis.Error("value provided for field message.expectedVersion of type uint64 too large");
      }
      writer.uint32(24).uint64(message.expectedVersion);
    }
    if (message.updateMask !== undefined) {
      FieldMask.encode(FieldMask.wrap(message.updateMask), writer.uint32(34).fork()).join();
    }
    if (message.intent !== 0) {
      writer.uint32(40).int32(message.intent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValidateKnowledgeObjectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValidateKnowledgeObjectRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.definition = KnowledgeObjectDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.knowledgeObjectId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.expectedVersion = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.updateMask = FieldMask.unwrap(FieldMask.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.intent = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ValidateKnowledgeObjectRequest {
    return {
      definition: isSet(object.definition) ? KnowledgeObjectDefinition.fromJSON(object.definition) : undefined,
      knowledgeObjectId: isSet(object.knowledgeObjectId)
        ? globalThis.String(object.knowledgeObjectId)
        : isSet(object.knowledge_object_id)
        ? globalThis.String(object.knowledge_object_id)
        : undefined,
      expectedVersion: isSet(object.expectedVersion)
        ? BigInt(object.expectedVersion)
        : isSet(object.expected_version)
        ? BigInt(object.expected_version)
        : undefined,
      updateMask: isSet(object.updateMask)
        ? FieldMask.unwrap(FieldMask.fromJSON(object.updateMask))
        : isSet(object.update_mask)
        ? FieldMask.unwrap(FieldMask.fromJSON(object.update_mask))
        : undefined,
      intent: isSet(object.intent) ? knowledgeValidationIntentFromJSON(object.intent) : 0,
    };
  },

  toJSON(message: ValidateKnowledgeObjectRequest): unknown {
    const obj: any = {};
    if (message.definition !== undefined) {
      obj.definition = KnowledgeObjectDefinition.toJSON(message.definition);
    }
    if (message.knowledgeObjectId !== undefined) {
      obj.knowledgeObjectId = message.knowledgeObjectId;
    }
    if (message.expectedVersion !== undefined) {
      obj.expectedVersion = message.expectedVersion.toString();
    }
    if (message.updateMask !== undefined) {
      obj.updateMask = FieldMask.toJSON(FieldMask.wrap(message.updateMask));
    }
    if (message.intent !== 0) {
      obj.intent = knowledgeValidationIntentToJSON(message.intent);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ValidateKnowledgeObjectRequest>, I>>(base?: I): ValidateKnowledgeObjectRequest {
    return ValidateKnowledgeObjectRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ValidateKnowledgeObjectRequest>, I>>(
    object: I,
  ): ValidateKnowledgeObjectRequest {
    const message = createBaseValidateKnowledgeObjectRequest();
    message.definition = (object.definition !== undefined && object.definition !== null)
      ? KnowledgeObjectDefinition.fromPartial(object.definition)
      : undefined;
    message.knowledgeObjectId = object.knowledgeObjectId ?? undefined;
    message.expectedVersion = (object.expectedVersion !== undefined && object.expectedVersion !== null)
      ? BigInt(object.expectedVersion)
      : undefined;
    message.updateMask = object.updateMask ?? undefined;
    message.intent = object.intent ?? 0;
    return message;
  },
};

function createBaseValidateKnowledgeObjectResponse(): ValidateKnowledgeObjectResponse {
  return { result: undefined, tenantCatalogRevision: 0n };
}

export const ValidateKnowledgeObjectResponse: MessageFns<ValidateKnowledgeObjectResponse> = {
  encode(message: ValidateKnowledgeObjectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.result !== undefined) {
      KnowledgeValidationResult.encode(message.result, writer.uint32(10).fork()).join();
    }
    if (message.tenantCatalogRevision !== 0n) {
      if (BigInt.asUintN(64, message.tenantCatalogRevision) !== message.tenantCatalogRevision) {
        throw new globalThis.Error("value provided for field message.tenantCatalogRevision of type uint64 too large");
      }
      writer.uint32(16).uint64(message.tenantCatalogRevision);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValidateKnowledgeObjectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValidateKnowledgeObjectResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.result = KnowledgeValidationResult.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.tenantCatalogRevision = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ValidateKnowledgeObjectResponse {
    return {
      result: isSet(object.result) ? KnowledgeValidationResult.fromJSON(object.result) : undefined,
      tenantCatalogRevision: isSet(object.tenantCatalogRevision)
        ? BigInt(object.tenantCatalogRevision)
        : isSet(object.tenant_catalog_revision)
        ? BigInt(object.tenant_catalog_revision)
        : 0n,
    };
  },

  toJSON(message: ValidateKnowledgeObjectResponse): unknown {
    const obj: any = {};
    if (message.result !== undefined) {
      obj.result = KnowledgeValidationResult.toJSON(message.result);
    }
    if (message.tenantCatalogRevision !== 0n) {
      obj.tenantCatalogRevision = message.tenantCatalogRevision.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ValidateKnowledgeObjectResponse>, I>>(base?: I): ValidateKnowledgeObjectResponse {
    return ValidateKnowledgeObjectResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ValidateKnowledgeObjectResponse>, I>>(
    object: I,
  ): ValidateKnowledgeObjectResponse {
    const message = createBaseValidateKnowledgeObjectResponse();
    message.result = (object.result !== undefined && object.result !== null)
      ? KnowledgeValidationResult.fromPartial(object.result)
      : undefined;
    message.tenantCatalogRevision =
      (object.tenantCatalogRevision !== undefined && object.tenantCatalogRevision !== null)
        ? BigInt(object.tenantCatalogRevision)
        : 0n;
    return message;
  },
};

function createBaseKnowledgeManagementObjectVersionIdentity(): KnowledgeManagementObjectVersionIdentity {
  return { knowledgeObjectId: "", version: 0n };
}

export const KnowledgeManagementObjectVersionIdentity: MessageFns<KnowledgeManagementObjectVersionIdentity> = {
  encode(message: KnowledgeManagementObjectVersionIdentity, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.knowledgeObjectId !== "") {
      writer.uint32(10).string(message.knowledgeObjectId);
    }
    if (message.version !== 0n) {
      if (BigInt.asUintN(64, message.version) !== message.version) {
        throw new globalThis.Error("value provided for field message.version of type uint64 too large");
      }
      writer.uint32(16).uint64(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeManagementObjectVersionIdentity {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeManagementObjectVersionIdentity();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.knowledgeObjectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeManagementObjectVersionIdentity {
    return {
      knowledgeObjectId: isSet(object.knowledgeObjectId)
        ? globalThis.String(object.knowledgeObjectId)
        : isSet(object.knowledge_object_id)
        ? globalThis.String(object.knowledge_object_id)
        : "",
      version: isSet(object.version) ? BigInt(object.version) : 0n,
    };
  },

  toJSON(message: KnowledgeManagementObjectVersionIdentity): unknown {
    const obj: any = {};
    if (message.knowledgeObjectId !== "") {
      obj.knowledgeObjectId = message.knowledgeObjectId;
    }
    if (message.version !== 0n) {
      obj.version = message.version.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeManagementObjectVersionIdentity>, I>>(
    base?: I,
  ): KnowledgeManagementObjectVersionIdentity {
    return KnowledgeManagementObjectVersionIdentity.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeManagementObjectVersionIdentity>, I>>(
    object: I,
  ): KnowledgeManagementObjectVersionIdentity {
    const message = createBaseKnowledgeManagementObjectVersionIdentity();
    message.knowledgeObjectId = object.knowledgeObjectId ?? "";
    message.version = (object.version !== undefined && object.version !== null) ? BigInt(object.version) : 0n;
    return message;
  },
};

function createBaseKnowledgeManagementDependencyEdge(): KnowledgeManagementDependencyEdge {
  return { source: undefined, target: undefined, role: 0 };
}

export const KnowledgeManagementDependencyEdge: MessageFns<KnowledgeManagementDependencyEdge> = {
  encode(message: KnowledgeManagementDependencyEdge, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.source !== undefined) {
      KnowledgeManagementObjectVersionIdentity.encode(message.source, writer.uint32(10).fork()).join();
    }
    if (message.target !== undefined) {
      KnowledgeManagementObjectVersionIdentity.encode(message.target, writer.uint32(18).fork()).join();
    }
    if (message.role !== 0) {
      writer.uint32(24).int32(message.role);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KnowledgeManagementDependencyEdge {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKnowledgeManagementDependencyEdge();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.source = KnowledgeManagementObjectVersionIdentity.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.target = KnowledgeManagementObjectVersionIdentity.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.role = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KnowledgeManagementDependencyEdge {
    return {
      source: isSet(object.source) ? KnowledgeManagementObjectVersionIdentity.fromJSON(object.source) : undefined,
      target: isSet(object.target) ? KnowledgeManagementObjectVersionIdentity.fromJSON(object.target) : undefined,
      role: isSet(object.role) ? knowledgeDependencyRoleFromJSON(object.role) : 0,
    };
  },

  toJSON(message: KnowledgeManagementDependencyEdge): unknown {
    const obj: any = {};
    if (message.source !== undefined) {
      obj.source = KnowledgeManagementObjectVersionIdentity.toJSON(message.source);
    }
    if (message.target !== undefined) {
      obj.target = KnowledgeManagementObjectVersionIdentity.toJSON(message.target);
    }
    if (message.role !== 0) {
      obj.role = knowledgeDependencyRoleToJSON(message.role);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KnowledgeManagementDependencyEdge>, I>>(
    base?: I,
  ): KnowledgeManagementDependencyEdge {
    return KnowledgeManagementDependencyEdge.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KnowledgeManagementDependencyEdge>, I>>(
    object: I,
  ): KnowledgeManagementDependencyEdge {
    const message = createBaseKnowledgeManagementDependencyEdge();
    message.source = (object.source !== undefined && object.source !== null)
      ? KnowledgeManagementObjectVersionIdentity.fromPartial(object.source)
      : undefined;
    message.target = (object.target !== undefined && object.target !== null)
      ? KnowledgeManagementObjectVersionIdentity.fromPartial(object.target)
      : undefined;
    message.role = object.role ?? 0;
    return message;
  },
};

function createBaseListKnowledgeObjectDependenciesRequest(): ListKnowledgeObjectDependenciesRequest {
  return { knowledgeObjectId: "", version: undefined, page: undefined };
}

export const ListKnowledgeObjectDependenciesRequest: MessageFns<ListKnowledgeObjectDependenciesRequest> = {
  encode(message: ListKnowledgeObjectDependenciesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.knowledgeObjectId !== "") {
      writer.uint32(10).string(message.knowledgeObjectId);
    }
    if (message.version !== undefined) {
      if (BigInt.asUintN(64, message.version) !== message.version) {
        throw new globalThis.Error("value provided for field message.version of type uint64 too large");
      }
      writer.uint32(16).uint64(message.version);
    }
    if (message.page !== undefined) {
      PageRequest.encode(message.page, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListKnowledgeObjectDependenciesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListKnowledgeObjectDependenciesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.knowledgeObjectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.page = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListKnowledgeObjectDependenciesRequest {
    return {
      knowledgeObjectId: isSet(object.knowledgeObjectId)
        ? globalThis.String(object.knowledgeObjectId)
        : isSet(object.knowledge_object_id)
        ? globalThis.String(object.knowledge_object_id)
        : "",
      version: isSet(object.version) ? BigInt(object.version) : undefined,
      page: isSet(object.page) ? PageRequest.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListKnowledgeObjectDependenciesRequest): unknown {
    const obj: any = {};
    if (message.knowledgeObjectId !== "") {
      obj.knowledgeObjectId = message.knowledgeObjectId;
    }
    if (message.version !== undefined) {
      obj.version = message.version.toString();
    }
    if (message.page !== undefined) {
      obj.page = PageRequest.toJSON(message.page);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListKnowledgeObjectDependenciesRequest>, I>>(
    base?: I,
  ): ListKnowledgeObjectDependenciesRequest {
    return ListKnowledgeObjectDependenciesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListKnowledgeObjectDependenciesRequest>, I>>(
    object: I,
  ): ListKnowledgeObjectDependenciesRequest {
    const message = createBaseListKnowledgeObjectDependenciesRequest();
    message.knowledgeObjectId = object.knowledgeObjectId ?? "";
    message.version = (object.version !== undefined && object.version !== null) ? BigInt(object.version) : undefined;
    message.page = (object.page !== undefined && object.page !== null)
      ? PageRequest.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseListKnowledgeObjectDependenciesResponse(): ListKnowledgeObjectDependenciesResponse {
  return { dependencies: [], page: undefined, tenantCatalogRevision: 0n, resolvedObject: undefined };
}

export const ListKnowledgeObjectDependenciesResponse: MessageFns<ListKnowledgeObjectDependenciesResponse> = {
  encode(message: ListKnowledgeObjectDependenciesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.dependencies) {
      KnowledgeManagementDependencyEdge.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      PageResponse.encode(message.page, writer.uint32(18).fork()).join();
    }
    if (message.tenantCatalogRevision !== 0n) {
      if (BigInt.asUintN(64, message.tenantCatalogRevision) !== message.tenantCatalogRevision) {
        throw new globalThis.Error("value provided for field message.tenantCatalogRevision of type uint64 too large");
      }
      writer.uint32(24).uint64(message.tenantCatalogRevision);
    }
    if (message.resolvedObject !== undefined) {
      KnowledgeManagementObjectVersionIdentity.encode(message.resolvedObject, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListKnowledgeObjectDependenciesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListKnowledgeObjectDependenciesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dependencies.push(KnowledgeManagementDependencyEdge.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageResponse.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.tenantCatalogRevision = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.resolvedObject = KnowledgeManagementObjectVersionIdentity.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListKnowledgeObjectDependenciesResponse {
    return {
      dependencies: globalThis.Array.isArray(object?.dependencies)
        ? object.dependencies.map((e: any) => KnowledgeManagementDependencyEdge.fromJSON(e))
        : [],
      page: isSet(object.page) ? PageResponse.fromJSON(object.page) : undefined,
      tenantCatalogRevision: isSet(object.tenantCatalogRevision)
        ? BigInt(object.tenantCatalogRevision)
        : isSet(object.tenant_catalog_revision)
        ? BigInt(object.tenant_catalog_revision)
        : 0n,
      resolvedObject: isSet(object.resolvedObject)
        ? KnowledgeManagementObjectVersionIdentity.fromJSON(object.resolvedObject)
        : isSet(object.resolved_object)
        ? KnowledgeManagementObjectVersionIdentity.fromJSON(object.resolved_object)
        : undefined,
    };
  },

  toJSON(message: ListKnowledgeObjectDependenciesResponse): unknown {
    const obj: any = {};
    if (message.dependencies?.length) {
      obj.dependencies = message.dependencies.map((e) => KnowledgeManagementDependencyEdge.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = PageResponse.toJSON(message.page);
    }
    if (message.tenantCatalogRevision !== 0n) {
      obj.tenantCatalogRevision = message.tenantCatalogRevision.toString();
    }
    if (message.resolvedObject !== undefined) {
      obj.resolvedObject = KnowledgeManagementObjectVersionIdentity.toJSON(message.resolvedObject);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListKnowledgeObjectDependenciesResponse>, I>>(
    base?: I,
  ): ListKnowledgeObjectDependenciesResponse {
    return ListKnowledgeObjectDependenciesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListKnowledgeObjectDependenciesResponse>, I>>(
    object: I,
  ): ListKnowledgeObjectDependenciesResponse {
    const message = createBaseListKnowledgeObjectDependenciesResponse();
    message.dependencies = object.dependencies?.map((e) => KnowledgeManagementDependencyEdge.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResponse.fromPartial(object.page)
      : undefined;
    message.tenantCatalogRevision =
      (object.tenantCatalogRevision !== undefined && object.tenantCatalogRevision !== null)
        ? BigInt(object.tenantCatalogRevision)
        : 0n;
    message.resolvedObject = (object.resolvedObject !== undefined && object.resolvedObject !== null)
      ? KnowledgeManagementObjectVersionIdentity.fromPartial(object.resolvedObject)
      : undefined;
    return message;
  },
};

function createBaseListKnowledgeObjectDependentsRequest(): ListKnowledgeObjectDependentsRequest {
  return { knowledgeObjectId: "", version: undefined, page: undefined };
}

export const ListKnowledgeObjectDependentsRequest: MessageFns<ListKnowledgeObjectDependentsRequest> = {
  encode(message: ListKnowledgeObjectDependentsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.knowledgeObjectId !== "") {
      writer.uint32(10).string(message.knowledgeObjectId);
    }
    if (message.version !== undefined) {
      if (BigInt.asUintN(64, message.version) !== message.version) {
        throw new globalThis.Error("value provided for field message.version of type uint64 too large");
      }
      writer.uint32(16).uint64(message.version);
    }
    if (message.page !== undefined) {
      PageRequest.encode(message.page, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListKnowledgeObjectDependentsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListKnowledgeObjectDependentsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.knowledgeObjectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.page = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListKnowledgeObjectDependentsRequest {
    return {
      knowledgeObjectId: isSet(object.knowledgeObjectId)
        ? globalThis.String(object.knowledgeObjectId)
        : isSet(object.knowledge_object_id)
        ? globalThis.String(object.knowledge_object_id)
        : "",
      version: isSet(object.version) ? BigInt(object.version) : undefined,
      page: isSet(object.page) ? PageRequest.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListKnowledgeObjectDependentsRequest): unknown {
    const obj: any = {};
    if (message.knowledgeObjectId !== "") {
      obj.knowledgeObjectId = message.knowledgeObjectId;
    }
    if (message.version !== undefined) {
      obj.version = message.version.toString();
    }
    if (message.page !== undefined) {
      obj.page = PageRequest.toJSON(message.page);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListKnowledgeObjectDependentsRequest>, I>>(
    base?: I,
  ): ListKnowledgeObjectDependentsRequest {
    return ListKnowledgeObjectDependentsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListKnowledgeObjectDependentsRequest>, I>>(
    object: I,
  ): ListKnowledgeObjectDependentsRequest {
    const message = createBaseListKnowledgeObjectDependentsRequest();
    message.knowledgeObjectId = object.knowledgeObjectId ?? "";
    message.version = (object.version !== undefined && object.version !== null) ? BigInt(object.version) : undefined;
    message.page = (object.page !== undefined && object.page !== null)
      ? PageRequest.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseListKnowledgeObjectDependentsResponse(): ListKnowledgeObjectDependentsResponse {
  return { dependents: [], page: undefined, tenantCatalogRevision: 0n, resolvedObject: undefined };
}

export const ListKnowledgeObjectDependentsResponse: MessageFns<ListKnowledgeObjectDependentsResponse> = {
  encode(message: ListKnowledgeObjectDependentsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.dependents) {
      KnowledgeManagementDependencyEdge.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      PageResponse.encode(message.page, writer.uint32(18).fork()).join();
    }
    if (message.tenantCatalogRevision !== 0n) {
      if (BigInt.asUintN(64, message.tenantCatalogRevision) !== message.tenantCatalogRevision) {
        throw new globalThis.Error("value provided for field message.tenantCatalogRevision of type uint64 too large");
      }
      writer.uint32(24).uint64(message.tenantCatalogRevision);
    }
    if (message.resolvedObject !== undefined) {
      KnowledgeManagementObjectVersionIdentity.encode(message.resolvedObject, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListKnowledgeObjectDependentsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListKnowledgeObjectDependentsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dependents.push(KnowledgeManagementDependencyEdge.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageResponse.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.tenantCatalogRevision = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.resolvedObject = KnowledgeManagementObjectVersionIdentity.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListKnowledgeObjectDependentsResponse {
    return {
      dependents: globalThis.Array.isArray(object?.dependents)
        ? object.dependents.map((e: any) => KnowledgeManagementDependencyEdge.fromJSON(e))
        : [],
      page: isSet(object.page) ? PageResponse.fromJSON(object.page) : undefined,
      tenantCatalogRevision: isSet(object.tenantCatalogRevision)
        ? BigInt(object.tenantCatalogRevision)
        : isSet(object.tenant_catalog_revision)
        ? BigInt(object.tenant_catalog_revision)
        : 0n,
      resolvedObject: isSet(object.resolvedObject)
        ? KnowledgeManagementObjectVersionIdentity.fromJSON(object.resolvedObject)
        : isSet(object.resolved_object)
        ? KnowledgeManagementObjectVersionIdentity.fromJSON(object.resolved_object)
        : undefined,
    };
  },

  toJSON(message: ListKnowledgeObjectDependentsResponse): unknown {
    const obj: any = {};
    if (message.dependents?.length) {
      obj.dependents = message.dependents.map((e) => KnowledgeManagementDependencyEdge.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = PageResponse.toJSON(message.page);
    }
    if (message.tenantCatalogRevision !== 0n) {
      obj.tenantCatalogRevision = message.tenantCatalogRevision.toString();
    }
    if (message.resolvedObject !== undefined) {
      obj.resolvedObject = KnowledgeManagementObjectVersionIdentity.toJSON(message.resolvedObject);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListKnowledgeObjectDependentsResponse>, I>>(
    base?: I,
  ): ListKnowledgeObjectDependentsResponse {
    return ListKnowledgeObjectDependentsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListKnowledgeObjectDependentsResponse>, I>>(
    object: I,
  ): ListKnowledgeObjectDependentsResponse {
    const message = createBaseListKnowledgeObjectDependentsResponse();
    message.dependents = object.dependents?.map((e) => KnowledgeManagementDependencyEdge.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResponse.fromPartial(object.page)
      : undefined;
    message.tenantCatalogRevision =
      (object.tenantCatalogRevision !== undefined && object.tenantCatalogRevision !== null)
        ? BigInt(object.tenantCatalogRevision)
        : 0n;
    message.resolvedObject = (object.resolvedObject !== undefined && object.resolvedObject !== null)
      ? KnowledgeManagementObjectVersionIdentity.fromPartial(object.resolvedObject)
      : undefined;
    return message;
  },
};

function createBasePreviewKnowledgeObjectRequest(): PreviewKnowledgeObjectRequest {
  return {
    retainedSearchJobId: "",
    definition: undefined,
    knowledgeObjectId: undefined,
    expectedVersion: undefined,
    updateMask: undefined,
    maximumRows: undefined,
  };
}

export const PreviewKnowledgeObjectRequest: MessageFns<PreviewKnowledgeObjectRequest> = {
  encode(message: PreviewKnowledgeObjectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.retainedSearchJobId !== "") {
      writer.uint32(10).string(message.retainedSearchJobId);
    }
    if (message.definition !== undefined) {
      KnowledgeObjectDefinition.encode(message.definition, writer.uint32(18).fork()).join();
    }
    if (message.knowledgeObjectId !== undefined) {
      writer.uint32(26).string(message.knowledgeObjectId);
    }
    if (message.expectedVersion !== undefined) {
      if (BigInt.asUintN(64, message.expectedVersion) !== message.expectedVersion) {
        throw new globalThis.Error("value provided for field message.expectedVersion of type uint64 too large");
      }
      writer.uint32(32).uint64(message.expectedVersion);
    }
    if (message.updateMask !== undefined) {
      FieldMask.encode(FieldMask.wrap(message.updateMask), writer.uint32(42).fork()).join();
    }
    if (message.maximumRows !== undefined) {
      writer.uint32(48).uint32(message.maximumRows);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PreviewKnowledgeObjectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePreviewKnowledgeObjectRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.retainedSearchJobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.definition = KnowledgeObjectDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.knowledgeObjectId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.expectedVersion = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.updateMask = FieldMask.unwrap(FieldMask.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.maximumRows = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PreviewKnowledgeObjectRequest {
    return {
      retainedSearchJobId: isSet(object.retainedSearchJobId)
        ? globalThis.String(object.retainedSearchJobId)
        : isSet(object.retained_search_job_id)
        ? globalThis.String(object.retained_search_job_id)
        : "",
      definition: isSet(object.definition) ? KnowledgeObjectDefinition.fromJSON(object.definition) : undefined,
      knowledgeObjectId: isSet(object.knowledgeObjectId)
        ? globalThis.String(object.knowledgeObjectId)
        : isSet(object.knowledge_object_id)
        ? globalThis.String(object.knowledge_object_id)
        : undefined,
      expectedVersion: isSet(object.expectedVersion)
        ? BigInt(object.expectedVersion)
        : isSet(object.expected_version)
        ? BigInt(object.expected_version)
        : undefined,
      updateMask: isSet(object.updateMask)
        ? FieldMask.unwrap(FieldMask.fromJSON(object.updateMask))
        : isSet(object.update_mask)
        ? FieldMask.unwrap(FieldMask.fromJSON(object.update_mask))
        : undefined,
      maximumRows: isSet(object.maximumRows)
        ? globalThis.Number(object.maximumRows)
        : isSet(object.maximum_rows)
        ? globalThis.Number(object.maximum_rows)
        : undefined,
    };
  },

  toJSON(message: PreviewKnowledgeObjectRequest): unknown {
    const obj: any = {};
    if (message.retainedSearchJobId !== "") {
      obj.retainedSearchJobId = message.retainedSearchJobId;
    }
    if (message.definition !== undefined) {
      obj.definition = KnowledgeObjectDefinition.toJSON(message.definition);
    }
    if (message.knowledgeObjectId !== undefined) {
      obj.knowledgeObjectId = message.knowledgeObjectId;
    }
    if (message.expectedVersion !== undefined) {
      obj.expectedVersion = message.expectedVersion.toString();
    }
    if (message.updateMask !== undefined) {
      obj.updateMask = FieldMask.toJSON(FieldMask.wrap(message.updateMask));
    }
    if (message.maximumRows !== undefined) {
      obj.maximumRows = Math.round(message.maximumRows);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PreviewKnowledgeObjectRequest>, I>>(base?: I): PreviewKnowledgeObjectRequest {
    return PreviewKnowledgeObjectRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PreviewKnowledgeObjectRequest>, I>>(
    object: I,
  ): PreviewKnowledgeObjectRequest {
    const message = createBasePreviewKnowledgeObjectRequest();
    message.retainedSearchJobId = object.retainedSearchJobId ?? "";
    message.definition = (object.definition !== undefined && object.definition !== null)
      ? KnowledgeObjectDefinition.fromPartial(object.definition)
      : undefined;
    message.knowledgeObjectId = object.knowledgeObjectId ?? undefined;
    message.expectedVersion = (object.expectedVersion !== undefined && object.expectedVersion !== null)
      ? BigInt(object.expectedVersion)
      : undefined;
    message.updateMask = object.updateMask ?? undefined;
    message.maximumRows = object.maximumRows ?? undefined;
    return message;
  },
};

function createBasePreviewKnowledgeObjectResponse(): PreviewKnowledgeObjectResponse {
  return {
    validation: undefined,
    beforeSchema: undefined,
    afterSchema: undefined,
    beforeRows: [],
    afterRows: [],
    truncated: false,
    tenantCatalogRevision: 0n,
  };
}

export const PreviewKnowledgeObjectResponse: MessageFns<PreviewKnowledgeObjectResponse> = {
  encode(message: PreviewKnowledgeObjectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.validation !== undefined) {
      KnowledgeValidationResult.encode(message.validation, writer.uint32(10).fork()).join();
    }
    if (message.beforeSchema !== undefined) {
      ResultSchema.encode(message.beforeSchema, writer.uint32(18).fork()).join();
    }
    if (message.afterSchema !== undefined) {
      ResultSchema.encode(message.afterSchema, writer.uint32(26).fork()).join();
    }
    for (const v of message.beforeRows) {
      ResultRow.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.afterRows) {
      ResultRow.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.truncated !== false) {
      writer.uint32(48).bool(message.truncated);
    }
    if (message.tenantCatalogRevision !== 0n) {
      if (BigInt.asUintN(64, message.tenantCatalogRevision) !== message.tenantCatalogRevision) {
        throw new globalThis.Error("value provided for field message.tenantCatalogRevision of type uint64 too large");
      }
      writer.uint32(56).uint64(message.tenantCatalogRevision);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PreviewKnowledgeObjectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePreviewKnowledgeObjectResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.validation = KnowledgeValidationResult.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.beforeSchema = ResultSchema.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.afterSchema = ResultSchema.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.beforeRows.push(ResultRow.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.afterRows.push(ResultRow.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.truncated = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.tenantCatalogRevision = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PreviewKnowledgeObjectResponse {
    return {
      validation: isSet(object.validation) ? KnowledgeValidationResult.fromJSON(object.validation) : undefined,
      beforeSchema: isSet(object.beforeSchema)
        ? ResultSchema.fromJSON(object.beforeSchema)
        : isSet(object.before_schema)
        ? ResultSchema.fromJSON(object.before_schema)
        : undefined,
      afterSchema: isSet(object.afterSchema)
        ? ResultSchema.fromJSON(object.afterSchema)
        : isSet(object.after_schema)
        ? ResultSchema.fromJSON(object.after_schema)
        : undefined,
      beforeRows: globalThis.Array.isArray(object?.beforeRows)
        ? object.beforeRows.map((e: any) => ResultRow.fromJSON(e))
        : globalThis.Array.isArray(object?.before_rows)
        ? object.before_rows.map((e: any) => ResultRow.fromJSON(e))
        : [],
      afterRows: globalThis.Array.isArray(object?.afterRows)
        ? object.afterRows.map((e: any) => ResultRow.fromJSON(e))
        : globalThis.Array.isArray(object?.after_rows)
        ? object.after_rows.map((e: any) => ResultRow.fromJSON(e))
        : [],
      truncated: isSet(object.truncated) ? globalThis.Boolean(object.truncated) : false,
      tenantCatalogRevision: isSet(object.tenantCatalogRevision)
        ? BigInt(object.tenantCatalogRevision)
        : isSet(object.tenant_catalog_revision)
        ? BigInt(object.tenant_catalog_revision)
        : 0n,
    };
  },

  toJSON(message: PreviewKnowledgeObjectResponse): unknown {
    const obj: any = {};
    if (message.validation !== undefined) {
      obj.validation = KnowledgeValidationResult.toJSON(message.validation);
    }
    if (message.beforeSchema !== undefined) {
      obj.beforeSchema = ResultSchema.toJSON(message.beforeSchema);
    }
    if (message.afterSchema !== undefined) {
      obj.afterSchema = ResultSchema.toJSON(message.afterSchema);
    }
    if (message.beforeRows?.length) {
      obj.beforeRows = message.beforeRows.map((e) => ResultRow.toJSON(e));
    }
    if (message.afterRows?.length) {
      obj.afterRows = message.afterRows.map((e) => ResultRow.toJSON(e));
    }
    if (message.truncated !== false) {
      obj.truncated = message.truncated;
    }
    if (message.tenantCatalogRevision !== 0n) {
      obj.tenantCatalogRevision = message.tenantCatalogRevision.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PreviewKnowledgeObjectResponse>, I>>(base?: I): PreviewKnowledgeObjectResponse {
    return PreviewKnowledgeObjectResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PreviewKnowledgeObjectResponse>, I>>(
    object: I,
  ): PreviewKnowledgeObjectResponse {
    const message = createBasePreviewKnowledgeObjectResponse();
    message.validation = (object.validation !== undefined && object.validation !== null)
      ? KnowledgeValidationResult.fromPartial(object.validation)
      : undefined;
    message.beforeSchema = (object.beforeSchema !== undefined && object.beforeSchema !== null)
      ? ResultSchema.fromPartial(object.beforeSchema)
      : undefined;
    message.afterSchema = (object.afterSchema !== undefined && object.afterSchema !== null)
      ? ResultSchema.fromPartial(object.afterSchema)
      : undefined;
    message.beforeRows = object.beforeRows?.map((e) => ResultRow.fromPartial(e)) || [];
    message.afterRows = object.afterRows?.map((e) => ResultRow.fromPartial(e)) || [];
    message.truncated = object.truncated ?? false;
    message.tenantCatalogRevision =
      (object.tenantCatalogRevision !== undefined && object.tenantCatalogRevision !== null)
        ? BigInt(object.tenantCatalogRevision)
        : 0n;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
