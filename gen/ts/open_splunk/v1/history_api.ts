// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.0
// source: open_splunk/v1/history_api.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { PageRequest, PageResponse, SortDirection, sortDirectionFromJSON, sortDirectionToJSON } from "./common";
import { SearchHistoryEntry } from "./history";
import { SearchJobState, searchJobStateFromJSON, searchJobStateToJSON } from "./search";

export enum SearchHistorySortBy {
  SEARCH_HISTORY_SORT_BY_UNSPECIFIED = 0,
  SEARCH_HISTORY_SORT_BY_CREATED_AT = 1,
  SEARCH_HISTORY_SORT_BY_FINISHED_AT = 2,
  SEARCH_HISTORY_SORT_BY_DURATION = 3,
  SEARCH_HISTORY_SORT_BY_MATCHED_EVENTS = 4,
  UNRECOGNIZED = -1,
}

export function searchHistorySortByFromJSON(object: any): SearchHistorySortBy {
  switch (object) {
    case 0:
    case "SEARCH_HISTORY_SORT_BY_UNSPECIFIED":
      return SearchHistorySortBy.SEARCH_HISTORY_SORT_BY_UNSPECIFIED;
    case 1:
    case "SEARCH_HISTORY_SORT_BY_CREATED_AT":
      return SearchHistorySortBy.SEARCH_HISTORY_SORT_BY_CREATED_AT;
    case 2:
    case "SEARCH_HISTORY_SORT_BY_FINISHED_AT":
      return SearchHistorySortBy.SEARCH_HISTORY_SORT_BY_FINISHED_AT;
    case 3:
    case "SEARCH_HISTORY_SORT_BY_DURATION":
      return SearchHistorySortBy.SEARCH_HISTORY_SORT_BY_DURATION;
    case 4:
    case "SEARCH_HISTORY_SORT_BY_MATCHED_EVENTS":
      return SearchHistorySortBy.SEARCH_HISTORY_SORT_BY_MATCHED_EVENTS;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SearchHistorySortBy.UNRECOGNIZED;
  }
}

export function searchHistorySortByToJSON(object: SearchHistorySortBy): string {
  switch (object) {
    case SearchHistorySortBy.SEARCH_HISTORY_SORT_BY_UNSPECIFIED:
      return "SEARCH_HISTORY_SORT_BY_UNSPECIFIED";
    case SearchHistorySortBy.SEARCH_HISTORY_SORT_BY_CREATED_AT:
      return "SEARCH_HISTORY_SORT_BY_CREATED_AT";
    case SearchHistorySortBy.SEARCH_HISTORY_SORT_BY_FINISHED_AT:
      return "SEARCH_HISTORY_SORT_BY_FINISHED_AT";
    case SearchHistorySortBy.SEARCH_HISTORY_SORT_BY_DURATION:
      return "SEARCH_HISTORY_SORT_BY_DURATION";
    case SearchHistorySortBy.SEARCH_HISTORY_SORT_BY_MATCHED_EVENTS:
      return "SEARCH_HISTORY_SORT_BY_MATCHED_EVENTS";
    case SearchHistorySortBy.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** POST /api/v1/search/history/get */
export interface GetSearchHistoryEntryRequest {
  searchJobId: string;
}

export interface GetSearchHistoryEntryResponse {
  historyEntry: SearchHistoryEntry | undefined;
}

export interface SearchHistoryFilter {
  appId?: string | undefined;
  stateFilters: SearchJobState[];
  text?: string | undefined;
  savedSearchId?: string | undefined;
  createdAfter?: Date | undefined;
  createdBefore?: Date | undefined;
}

/** POST /api/v1/search/history/list */
export interface ListSearchHistoryRequest {
  page: PageRequest | undefined;
  filter: SearchHistoryFilter | undefined;
  sortBy: SearchHistorySortBy;
  sortDirection: SortDirection;
}

export interface ListSearchHistoryResponse {
  historyEntries: SearchHistoryEntry[];
  page: PageResponse | undefined;
}

/** POST /api/v1/search/history/delete */
export interface DeleteSearchHistoryEntryRequest {
  searchJobId: string;
}

export interface DeleteSearchHistoryEntryResponse {
  searchJobId: string;
}

/** POST /api/v1/search/history/clear */
export interface ClearSearchHistoryRequest {
  filter: SearchHistoryFilter | undefined;
  confirmation: string;
}

export interface ClearSearchHistoryResponse {
  deletedCount: bigint;
}

function createBaseGetSearchHistoryEntryRequest(): GetSearchHistoryEntryRequest {
  return { searchJobId: "" };
}

export const GetSearchHistoryEntryRequest: MessageFns<GetSearchHistoryEntryRequest> = {
  encode(message: GetSearchHistoryEntryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.searchJobId !== "") {
      writer.uint32(10).string(message.searchJobId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSearchHistoryEntryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSearchHistoryEntryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.searchJobId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSearchHistoryEntryRequest {
    return {
      searchJobId: isSet(object.searchJobId)
        ? globalThis.String(object.searchJobId)
        : isSet(object.search_job_id)
        ? globalThis.String(object.search_job_id)
        : "",
    };
  },

  toJSON(message: GetSearchHistoryEntryRequest): unknown {
    const obj: any = {};
    if (message.searchJobId !== "") {
      obj.searchJobId = message.searchJobId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSearchHistoryEntryRequest>, I>>(base?: I): GetSearchHistoryEntryRequest {
    return GetSearchHistoryEntryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSearchHistoryEntryRequest>, I>>(object: I): GetSearchHistoryEntryRequest {
    const message = createBaseGetSearchHistoryEntryRequest();
    message.searchJobId = object.searchJobId ?? "";
    return message;
  },
};

function createBaseGetSearchHistoryEntryResponse(): GetSearchHistoryEntryResponse {
  return { historyEntry: undefined };
}

export const GetSearchHistoryEntryResponse: MessageFns<GetSearchHistoryEntryResponse> = {
  encode(message: GetSearchHistoryEntryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.historyEntry !== undefined) {
      SearchHistoryEntry.encode(message.historyEntry, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSearchHistoryEntryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSearchHistoryEntryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.historyEntry = SearchHistoryEntry.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSearchHistoryEntryResponse {
    return {
      historyEntry: isSet(object.historyEntry)
        ? SearchHistoryEntry.fromJSON(object.historyEntry)
        : isSet(object.history_entry)
        ? SearchHistoryEntry.fromJSON(object.history_entry)
        : undefined,
    };
  },

  toJSON(message: GetSearchHistoryEntryResponse): unknown {
    const obj: any = {};
    if (message.historyEntry !== undefined) {
      obj.historyEntry = SearchHistoryEntry.toJSON(message.historyEntry);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSearchHistoryEntryResponse>, I>>(base?: I): GetSearchHistoryEntryResponse {
    return GetSearchHistoryEntryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSearchHistoryEntryResponse>, I>>(
    object: I,
  ): GetSearchHistoryEntryResponse {
    const message = createBaseGetSearchHistoryEntryResponse();
    message.historyEntry = (object.historyEntry !== undefined && object.historyEntry !== null)
      ? SearchHistoryEntry.fromPartial(object.historyEntry)
      : undefined;
    return message;
  },
};

function createBaseSearchHistoryFilter(): SearchHistoryFilter {
  return {
    appId: undefined,
    stateFilters: [],
    text: undefined,
    savedSearchId: undefined,
    createdAfter: undefined,
    createdBefore: undefined,
  };
}

export const SearchHistoryFilter: MessageFns<SearchHistoryFilter> = {
  encode(message: SearchHistoryFilter, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.appId !== undefined) {
      writer.uint32(10).string(message.appId);
    }
    writer.uint32(18).fork();
    for (const v of message.stateFilters) {
      writer.int32(v);
    }
    writer.join();
    if (message.text !== undefined) {
      writer.uint32(26).string(message.text);
    }
    if (message.savedSearchId !== undefined) {
      writer.uint32(34).string(message.savedSearchId);
    }
    if (message.createdAfter !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAfter), writer.uint32(42).fork()).join();
    }
    if (message.createdBefore !== undefined) {
      Timestamp.encode(toTimestamp(message.createdBefore), writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchHistoryFilter {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchHistoryFilter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.appId = reader.string();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.stateFilters.push(reader.int32() as any);

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.stateFilters.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.savedSearchId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.createdAfter = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.createdBefore = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchHistoryFilter {
    return {
      appId: isSet(object.appId)
        ? globalThis.String(object.appId)
        : isSet(object.app_id)
        ? globalThis.String(object.app_id)
        : undefined,
      stateFilters: globalThis.Array.isArray(object?.stateFilters)
        ? object.stateFilters.map((e: any) => searchJobStateFromJSON(e))
        : globalThis.Array.isArray(object?.state_filters)
        ? object.state_filters.map((e: any) => searchJobStateFromJSON(e))
        : [],
      text: isSet(object.text) ? globalThis.String(object.text) : undefined,
      savedSearchId: isSet(object.savedSearchId)
        ? globalThis.String(object.savedSearchId)
        : isSet(object.saved_search_id)
        ? globalThis.String(object.saved_search_id)
        : undefined,
      createdAfter: isSet(object.createdAfter)
        ? fromJsonTimestamp(object.createdAfter)
        : isSet(object.created_after)
        ? fromJsonTimestamp(object.created_after)
        : undefined,
      createdBefore: isSet(object.createdBefore)
        ? fromJsonTimestamp(object.createdBefore)
        : isSet(object.created_before)
        ? fromJsonTimestamp(object.created_before)
        : undefined,
    };
  },

  toJSON(message: SearchHistoryFilter): unknown {
    const obj: any = {};
    if (message.appId !== undefined) {
      obj.appId = message.appId;
    }
    if (message.stateFilters?.length) {
      obj.stateFilters = message.stateFilters.map((e) => searchJobStateToJSON(e));
    }
    if (message.text !== undefined) {
      obj.text = message.text;
    }
    if (message.savedSearchId !== undefined) {
      obj.savedSearchId = message.savedSearchId;
    }
    if (message.createdAfter !== undefined) {
      obj.createdAfter = message.createdAfter.toISOString();
    }
    if (message.createdBefore !== undefined) {
      obj.createdBefore = message.createdBefore.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchHistoryFilter>, I>>(base?: I): SearchHistoryFilter {
    return SearchHistoryFilter.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchHistoryFilter>, I>>(object: I): SearchHistoryFilter {
    const message = createBaseSearchHistoryFilter();
    message.appId = object.appId ?? undefined;
    message.stateFilters = object.stateFilters?.map((e) => e) || [];
    message.text = object.text ?? undefined;
    message.savedSearchId = object.savedSearchId ?? undefined;
    message.createdAfter = object.createdAfter ?? undefined;
    message.createdBefore = object.createdBefore ?? undefined;
    return message;
  },
};

function createBaseListSearchHistoryRequest(): ListSearchHistoryRequest {
  return { page: undefined, filter: undefined, sortBy: 0, sortDirection: 0 };
}

export const ListSearchHistoryRequest: MessageFns<ListSearchHistoryRequest> = {
  encode(message: ListSearchHistoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.page !== undefined) {
      PageRequest.encode(message.page, writer.uint32(10).fork()).join();
    }
    if (message.filter !== undefined) {
      SearchHistoryFilter.encode(message.filter, writer.uint32(18).fork()).join();
    }
    if (message.sortBy !== 0) {
      writer.uint32(24).int32(message.sortBy);
    }
    if (message.sortDirection !== 0) {
      writer.uint32(32).int32(message.sortDirection);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSearchHistoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSearchHistoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.page = PageRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.filter = SearchHistoryFilter.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sortBy = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.sortDirection = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListSearchHistoryRequest {
    return {
      page: isSet(object.page) ? PageRequest.fromJSON(object.page) : undefined,
      filter: isSet(object.filter) ? SearchHistoryFilter.fromJSON(object.filter) : undefined,
      sortBy: isSet(object.sortBy)
        ? searchHistorySortByFromJSON(object.sortBy)
        : isSet(object.sort_by)
        ? searchHistorySortByFromJSON(object.sort_by)
        : 0,
      sortDirection: isSet(object.sortDirection)
        ? sortDirectionFromJSON(object.sortDirection)
        : isSet(object.sort_direction)
        ? sortDirectionFromJSON(object.sort_direction)
        : 0,
    };
  },

  toJSON(message: ListSearchHistoryRequest): unknown {
    const obj: any = {};
    if (message.page !== undefined) {
      obj.page = PageRequest.toJSON(message.page);
    }
    if (message.filter !== undefined) {
      obj.filter = SearchHistoryFilter.toJSON(message.filter);
    }
    if (message.sortBy !== 0) {
      obj.sortBy = searchHistorySortByToJSON(message.sortBy);
    }
    if (message.sortDirection !== 0) {
      obj.sortDirection = sortDirectionToJSON(message.sortDirection);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListSearchHistoryRequest>, I>>(base?: I): ListSearchHistoryRequest {
    return ListSearchHistoryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListSearchHistoryRequest>, I>>(object: I): ListSearchHistoryRequest {
    const message = createBaseListSearchHistoryRequest();
    message.page = (object.page !== undefined && object.page !== null)
      ? PageRequest.fromPartial(object.page)
      : undefined;
    message.filter = (object.filter !== undefined && object.filter !== null)
      ? SearchHistoryFilter.fromPartial(object.filter)
      : undefined;
    message.sortBy = object.sortBy ?? 0;
    message.sortDirection = object.sortDirection ?? 0;
    return message;
  },
};

function createBaseListSearchHistoryResponse(): ListSearchHistoryResponse {
  return { historyEntries: [], page: undefined };
}

export const ListSearchHistoryResponse: MessageFns<ListSearchHistoryResponse> = {
  encode(message: ListSearchHistoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.historyEntries) {
      SearchHistoryEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      PageResponse.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSearchHistoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSearchHistoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.historyEntries.push(SearchHistoryEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListSearchHistoryResponse {
    return {
      historyEntries: globalThis.Array.isArray(object?.historyEntries)
        ? object.historyEntries.map((e: any) => SearchHistoryEntry.fromJSON(e))
        : globalThis.Array.isArray(object?.history_entries)
        ? object.history_entries.map((e: any) => SearchHistoryEntry.fromJSON(e))
        : [],
      page: isSet(object.page) ? PageResponse.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListSearchHistoryResponse): unknown {
    const obj: any = {};
    if (message.historyEntries?.length) {
      obj.historyEntries = message.historyEntries.map((e) => SearchHistoryEntry.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = PageResponse.toJSON(message.page);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListSearchHistoryResponse>, I>>(base?: I): ListSearchHistoryResponse {
    return ListSearchHistoryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListSearchHistoryResponse>, I>>(object: I): ListSearchHistoryResponse {
    const message = createBaseListSearchHistoryResponse();
    message.historyEntries = object.historyEntries?.map((e) => SearchHistoryEntry.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResponse.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseDeleteSearchHistoryEntryRequest(): DeleteSearchHistoryEntryRequest {
  return { searchJobId: "" };
}

export const DeleteSearchHistoryEntryRequest: MessageFns<DeleteSearchHistoryEntryRequest> = {
  encode(message: DeleteSearchHistoryEntryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.searchJobId !== "") {
      writer.uint32(10).string(message.searchJobId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteSearchHistoryEntryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteSearchHistoryEntryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.searchJobId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteSearchHistoryEntryRequest {
    return {
      searchJobId: isSet(object.searchJobId)
        ? globalThis.String(object.searchJobId)
        : isSet(object.search_job_id)
        ? globalThis.String(object.search_job_id)
        : "",
    };
  },

  toJSON(message: DeleteSearchHistoryEntryRequest): unknown {
    const obj: any = {};
    if (message.searchJobId !== "") {
      obj.searchJobId = message.searchJobId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteSearchHistoryEntryRequest>, I>>(base?: I): DeleteSearchHistoryEntryRequest {
    return DeleteSearchHistoryEntryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteSearchHistoryEntryRequest>, I>>(
    object: I,
  ): DeleteSearchHistoryEntryRequest {
    const message = createBaseDeleteSearchHistoryEntryRequest();
    message.searchJobId = object.searchJobId ?? "";
    return message;
  },
};

function createBaseDeleteSearchHistoryEntryResponse(): DeleteSearchHistoryEntryResponse {
  return { searchJobId: "" };
}

export const DeleteSearchHistoryEntryResponse: MessageFns<DeleteSearchHistoryEntryResponse> = {
  encode(message: DeleteSearchHistoryEntryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.searchJobId !== "") {
      writer.uint32(10).string(message.searchJobId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteSearchHistoryEntryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteSearchHistoryEntryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.searchJobId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteSearchHistoryEntryResponse {
    return {
      searchJobId: isSet(object.searchJobId)
        ? globalThis.String(object.searchJobId)
        : isSet(object.search_job_id)
        ? globalThis.String(object.search_job_id)
        : "",
    };
  },

  toJSON(message: DeleteSearchHistoryEntryResponse): unknown {
    const obj: any = {};
    if (message.searchJobId !== "") {
      obj.searchJobId = message.searchJobId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteSearchHistoryEntryResponse>, I>>(
    base?: I,
  ): DeleteSearchHistoryEntryResponse {
    return DeleteSearchHistoryEntryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteSearchHistoryEntryResponse>, I>>(
    object: I,
  ): DeleteSearchHistoryEntryResponse {
    const message = createBaseDeleteSearchHistoryEntryResponse();
    message.searchJobId = object.searchJobId ?? "";
    return message;
  },
};

function createBaseClearSearchHistoryRequest(): ClearSearchHistoryRequest {
  return { filter: undefined, confirmation: "" };
}

export const ClearSearchHistoryRequest: MessageFns<ClearSearchHistoryRequest> = {
  encode(message: ClearSearchHistoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.filter !== undefined) {
      SearchHistoryFilter.encode(message.filter, writer.uint32(10).fork()).join();
    }
    if (message.confirmation !== "") {
      writer.uint32(18).string(message.confirmation);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClearSearchHistoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClearSearchHistoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.filter = SearchHistoryFilter.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.confirmation = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClearSearchHistoryRequest {
    return {
      filter: isSet(object.filter) ? SearchHistoryFilter.fromJSON(object.filter) : undefined,
      confirmation: isSet(object.confirmation) ? globalThis.String(object.confirmation) : "",
    };
  },

  toJSON(message: ClearSearchHistoryRequest): unknown {
    const obj: any = {};
    if (message.filter !== undefined) {
      obj.filter = SearchHistoryFilter.toJSON(message.filter);
    }
    if (message.confirmation !== "") {
      obj.confirmation = message.confirmation;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ClearSearchHistoryRequest>, I>>(base?: I): ClearSearchHistoryRequest {
    return ClearSearchHistoryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ClearSearchHistoryRequest>, I>>(object: I): ClearSearchHistoryRequest {
    const message = createBaseClearSearchHistoryRequest();
    message.filter = (object.filter !== undefined && object.filter !== null)
      ? SearchHistoryFilter.fromPartial(object.filter)
      : undefined;
    message.confirmation = object.confirmation ?? "";
    return message;
  },
};

function createBaseClearSearchHistoryResponse(): ClearSearchHistoryResponse {
  return { deletedCount: 0n };
}

export const ClearSearchHistoryResponse: MessageFns<ClearSearchHistoryResponse> = {
  encode(message: ClearSearchHistoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deletedCount !== 0n) {
      if (BigInt.asUintN(64, message.deletedCount) !== message.deletedCount) {
        throw new globalThis.Error("value provided for field message.deletedCount of type uint64 too large");
      }
      writer.uint32(8).uint64(message.deletedCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClearSearchHistoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClearSearchHistoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.deletedCount = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClearSearchHistoryResponse {
    return {
      deletedCount: isSet(object.deletedCount)
        ? BigInt(object.deletedCount)
        : isSet(object.deleted_count)
        ? BigInt(object.deleted_count)
        : 0n,
    };
  },

  toJSON(message: ClearSearchHistoryResponse): unknown {
    const obj: any = {};
    if (message.deletedCount !== 0n) {
      obj.deletedCount = message.deletedCount.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ClearSearchHistoryResponse>, I>>(base?: I): ClearSearchHistoryResponse {
    return ClearSearchHistoryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ClearSearchHistoryResponse>, I>>(object: I): ClearSearchHistoryResponse {
    const message = createBaseClearSearchHistoryResponse();
    message.deletedCount = (object.deletedCount !== undefined && object.deletedCount !== null)
      ? BigInt(object.deletedCount)
      : 0n;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
