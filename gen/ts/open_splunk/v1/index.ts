// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.0
// source: open_splunk/v1/index.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../../google/protobuf/duration";
import { Timestamp } from "../../google/protobuf/timestamp";

export enum IndexState {
  INDEX_STATE_UNSPECIFIED = 0,
  INDEX_STATE_ACTIVE = 1,
  INDEX_STATE_ARCHIVED = 2,
  INDEX_STATE_DELETING = 3,
  UNRECOGNIZED = -1,
}

export function indexStateFromJSON(object: any): IndexState {
  switch (object) {
    case 0:
    case "INDEX_STATE_UNSPECIFIED":
      return IndexState.INDEX_STATE_UNSPECIFIED;
    case 1:
    case "INDEX_STATE_ACTIVE":
      return IndexState.INDEX_STATE_ACTIVE;
    case 2:
    case "INDEX_STATE_ARCHIVED":
      return IndexState.INDEX_STATE_ARCHIVED;
    case 3:
    case "INDEX_STATE_DELETING":
      return IndexState.INDEX_STATE_DELETING;
    case -1:
    case "UNRECOGNIZED":
    default:
      return IndexState.UNRECOGNIZED;
  }
}

export function indexStateToJSON(object: IndexState): string {
  switch (object) {
    case IndexState.INDEX_STATE_UNSPECIFIED:
      return "INDEX_STATE_UNSPECIFIED";
    case IndexState.INDEX_STATE_ACTIVE:
      return "INDEX_STATE_ACTIVE";
    case IndexState.INDEX_STATE_ARCHIVED:
      return "INDEX_STATE_ARCHIVED";
    case IndexState.INDEX_STATE_DELETING:
      return "INDEX_STATE_DELETING";
    case IndexState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum IndexAccessState {
  INDEX_ACCESS_STATE_UNSPECIFIED = 0,
  INDEX_ACCESS_STATE_ENABLED = 1,
  INDEX_ACCESS_STATE_DISABLED = 2,
  UNRECOGNIZED = -1,
}

export function indexAccessStateFromJSON(object: any): IndexAccessState {
  switch (object) {
    case 0:
    case "INDEX_ACCESS_STATE_UNSPECIFIED":
      return IndexAccessState.INDEX_ACCESS_STATE_UNSPECIFIED;
    case 1:
    case "INDEX_ACCESS_STATE_ENABLED":
      return IndexAccessState.INDEX_ACCESS_STATE_ENABLED;
    case 2:
    case "INDEX_ACCESS_STATE_DISABLED":
      return IndexAccessState.INDEX_ACCESS_STATE_DISABLED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return IndexAccessState.UNRECOGNIZED;
  }
}

export function indexAccessStateToJSON(object: IndexAccessState): string {
  switch (object) {
    case IndexAccessState.INDEX_ACCESS_STATE_UNSPECIFIED:
      return "INDEX_ACCESS_STATE_UNSPECIFIED";
    case IndexAccessState.INDEX_ACCESS_STATE_ENABLED:
      return "INDEX_ACCESS_STATE_ENABLED";
    case IndexAccessState.INDEX_ACCESS_STATE_DISABLED:
      return "INDEX_ACCESS_STATE_DISABLED";
    case IndexAccessState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface IndexSelector {
  selector: { $case: "indexId"; value: string } | { $case: "indexName"; value: string } | undefined;
}

export interface IndexLimits {
  maxEventBytes?: bigint | undefined;
  maxFieldCount?: number | undefined;
  maxNestingDepth?: number | undefined;
  maximumFutureSkew?: Duration | undefined;
  maximumEventAge?: Duration | undefined;
}

export interface IndexDefinition {
  /**
   * name is immutable after creation because it is part of SPL and collector
   * configuration. Use display_name for operator-facing renames.
   */
  name: string;
  displayName: string;
  description?: string | undefined;
  retentionPeriod?: Duration | undefined;
  ingestionAccess: IndexAccessState;
  searchAccess: IndexAccessState;
  defaultSourcetype?: string | undefined;
  limits: IndexLimits | undefined;
}

export interface Index {
  indexId: string;
  version: bigint;
  definition: IndexDefinition | undefined;
  state: IndexState;
  createdAt: Date | undefined;
  updatedAt: Date | undefined;
}

export interface IndexStats {
  indexId: string;
  eventCount: bigint;
  storageBytes: bigint;
  earliestEventTime: Date | undefined;
  latestEventTime: Date | undefined;
  measuredAt: Date | undefined;
  estimates: boolean;
}

export interface IndexSummary {
  indexId: string;
  name: string;
  displayName: string;
  state: IndexState;
  ingestionAccess: IndexAccessState;
  searchAccess: IndexAccessState;
}

function createBaseIndexSelector(): IndexSelector {
  return { selector: undefined };
}

export const IndexSelector: MessageFns<IndexSelector> = {
  encode(message: IndexSelector, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.selector?.$case) {
      case "indexId":
        writer.uint32(10).string(message.selector.value);
        break;
      case "indexName":
        writer.uint32(18).string(message.selector.value);
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IndexSelector {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIndexSelector();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.selector = { $case: "indexId", value: reader.string() };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.selector = { $case: "indexName", value: reader.string() };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IndexSelector {
    return {
      selector: isSet(object.indexId)
        ? { $case: "indexId", value: globalThis.String(object.indexId) }
        : isSet(object.index_id)
        ? { $case: "indexId", value: globalThis.String(object.index_id) }
        : isSet(object.indexName)
        ? { $case: "indexName", value: globalThis.String(object.indexName) }
        : isSet(object.index_name)
        ? { $case: "indexName", value: globalThis.String(object.index_name) }
        : undefined,
    };
  },

  toJSON(message: IndexSelector): unknown {
    const obj: any = {};
    if (message.selector?.$case === "indexId") {
      obj.indexId = message.selector.value;
    } else if (message.selector?.$case === "indexName") {
      obj.indexName = message.selector.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IndexSelector>, I>>(base?: I): IndexSelector {
    return IndexSelector.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IndexSelector>, I>>(object: I): IndexSelector {
    const message = createBaseIndexSelector();
    switch (object.selector?.$case) {
      case "indexId": {
        if (object.selector?.value !== undefined && object.selector?.value !== null) {
          message.selector = { $case: "indexId", value: object.selector.value };
        }
        break;
      }
      case "indexName": {
        if (object.selector?.value !== undefined && object.selector?.value !== null) {
          message.selector = { $case: "indexName", value: object.selector.value };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseIndexLimits(): IndexLimits {
  return {
    maxEventBytes: undefined,
    maxFieldCount: undefined,
    maxNestingDepth: undefined,
    maximumFutureSkew: undefined,
    maximumEventAge: undefined,
  };
}

export const IndexLimits: MessageFns<IndexLimits> = {
  encode(message: IndexLimits, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maxEventBytes !== undefined) {
      if (BigInt.asUintN(64, message.maxEventBytes) !== message.maxEventBytes) {
        throw new globalThis.Error("value provided for field message.maxEventBytes of type uint64 too large");
      }
      writer.uint32(8).uint64(message.maxEventBytes);
    }
    if (message.maxFieldCount !== undefined) {
      writer.uint32(16).uint32(message.maxFieldCount);
    }
    if (message.maxNestingDepth !== undefined) {
      writer.uint32(24).uint32(message.maxNestingDepth);
    }
    if (message.maximumFutureSkew !== undefined) {
      Duration.encode(message.maximumFutureSkew, writer.uint32(34).fork()).join();
    }
    if (message.maximumEventAge !== undefined) {
      Duration.encode(message.maximumEventAge, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IndexLimits {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIndexLimits();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.maxEventBytes = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.maxFieldCount = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maxNestingDepth = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.maximumFutureSkew = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.maximumEventAge = Duration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IndexLimits {
    return {
      maxEventBytes: isSet(object.maxEventBytes)
        ? BigInt(object.maxEventBytes)
        : isSet(object.max_event_bytes)
        ? BigInt(object.max_event_bytes)
        : undefined,
      maxFieldCount: isSet(object.maxFieldCount)
        ? globalThis.Number(object.maxFieldCount)
        : isSet(object.max_field_count)
        ? globalThis.Number(object.max_field_count)
        : undefined,
      maxNestingDepth: isSet(object.maxNestingDepth)
        ? globalThis.Number(object.maxNestingDepth)
        : isSet(object.max_nesting_depth)
        ? globalThis.Number(object.max_nesting_depth)
        : undefined,
      maximumFutureSkew: isSet(object.maximumFutureSkew)
        ? Duration.fromJSON(object.maximumFutureSkew)
        : isSet(object.maximum_future_skew)
        ? Duration.fromJSON(object.maximum_future_skew)
        : undefined,
      maximumEventAge: isSet(object.maximumEventAge)
        ? Duration.fromJSON(object.maximumEventAge)
        : isSet(object.maximum_event_age)
        ? Duration.fromJSON(object.maximum_event_age)
        : undefined,
    };
  },

  toJSON(message: IndexLimits): unknown {
    const obj: any = {};
    if (message.maxEventBytes !== undefined) {
      obj.maxEventBytes = message.maxEventBytes.toString();
    }
    if (message.maxFieldCount !== undefined) {
      obj.maxFieldCount = Math.round(message.maxFieldCount);
    }
    if (message.maxNestingDepth !== undefined) {
      obj.maxNestingDepth = Math.round(message.maxNestingDepth);
    }
    if (message.maximumFutureSkew !== undefined) {
      obj.maximumFutureSkew = Duration.toJSON(message.maximumFutureSkew);
    }
    if (message.maximumEventAge !== undefined) {
      obj.maximumEventAge = Duration.toJSON(message.maximumEventAge);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IndexLimits>, I>>(base?: I): IndexLimits {
    return IndexLimits.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IndexLimits>, I>>(object: I): IndexLimits {
    const message = createBaseIndexLimits();
    message.maxEventBytes = (object.maxEventBytes !== undefined && object.maxEventBytes !== null)
      ? BigInt(object.maxEventBytes)
      : undefined;
    message.maxFieldCount = object.maxFieldCount ?? undefined;
    message.maxNestingDepth = object.maxNestingDepth ?? undefined;
    message.maximumFutureSkew = (object.maximumFutureSkew !== undefined && object.maximumFutureSkew !== null)
      ? Duration.fromPartial(object.maximumFutureSkew)
      : undefined;
    message.maximumEventAge = (object.maximumEventAge !== undefined && object.maximumEventAge !== null)
      ? Duration.fromPartial(object.maximumEventAge)
      : undefined;
    return message;
  },
};

function createBaseIndexDefinition(): IndexDefinition {
  return {
    name: "",
    displayName: "",
    description: undefined,
    retentionPeriod: undefined,
    ingestionAccess: 0,
    searchAccess: 0,
    defaultSourcetype: undefined,
    limits: undefined,
  };
}

export const IndexDefinition: MessageFns<IndexDefinition> = {
  encode(message: IndexDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.displayName !== "") {
      writer.uint32(18).string(message.displayName);
    }
    if (message.description !== undefined) {
      writer.uint32(26).string(message.description);
    }
    if (message.retentionPeriod !== undefined) {
      Duration.encode(message.retentionPeriod, writer.uint32(34).fork()).join();
    }
    if (message.ingestionAccess !== 0) {
      writer.uint32(40).int32(message.ingestionAccess);
    }
    if (message.searchAccess !== 0) {
      writer.uint32(48).int32(message.searchAccess);
    }
    if (message.defaultSourcetype !== undefined) {
      writer.uint32(58).string(message.defaultSourcetype);
    }
    if (message.limits !== undefined) {
      IndexLimits.encode(message.limits, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IndexDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIndexDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.retentionPeriod = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.ingestionAccess = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.searchAccess = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.defaultSourcetype = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.limits = IndexLimits.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IndexDefinition {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
      retentionPeriod: isSet(object.retentionPeriod)
        ? Duration.fromJSON(object.retentionPeriod)
        : isSet(object.retention_period)
        ? Duration.fromJSON(object.retention_period)
        : undefined,
      ingestionAccess: isSet(object.ingestionAccess)
        ? indexAccessStateFromJSON(object.ingestionAccess)
        : isSet(object.ingestion_access)
        ? indexAccessStateFromJSON(object.ingestion_access)
        : 0,
      searchAccess: isSet(object.searchAccess)
        ? indexAccessStateFromJSON(object.searchAccess)
        : isSet(object.search_access)
        ? indexAccessStateFromJSON(object.search_access)
        : 0,
      defaultSourcetype: isSet(object.defaultSourcetype)
        ? globalThis.String(object.defaultSourcetype)
        : isSet(object.default_sourcetype)
        ? globalThis.String(object.default_sourcetype)
        : undefined,
      limits: isSet(object.limits) ? IndexLimits.fromJSON(object.limits) : undefined,
    };
  },

  toJSON(message: IndexDefinition): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.retentionPeriod !== undefined) {
      obj.retentionPeriod = Duration.toJSON(message.retentionPeriod);
    }
    if (message.ingestionAccess !== 0) {
      obj.ingestionAccess = indexAccessStateToJSON(message.ingestionAccess);
    }
    if (message.searchAccess !== 0) {
      obj.searchAccess = indexAccessStateToJSON(message.searchAccess);
    }
    if (message.defaultSourcetype !== undefined) {
      obj.defaultSourcetype = message.defaultSourcetype;
    }
    if (message.limits !== undefined) {
      obj.limits = IndexLimits.toJSON(message.limits);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IndexDefinition>, I>>(base?: I): IndexDefinition {
    return IndexDefinition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IndexDefinition>, I>>(object: I): IndexDefinition {
    const message = createBaseIndexDefinition();
    message.name = object.name ?? "";
    message.displayName = object.displayName ?? "";
    message.description = object.description ?? undefined;
    message.retentionPeriod = (object.retentionPeriod !== undefined && object.retentionPeriod !== null)
      ? Duration.fromPartial(object.retentionPeriod)
      : undefined;
    message.ingestionAccess = object.ingestionAccess ?? 0;
    message.searchAccess = object.searchAccess ?? 0;
    message.defaultSourcetype = object.defaultSourcetype ?? undefined;
    message.limits = (object.limits !== undefined && object.limits !== null)
      ? IndexLimits.fromPartial(object.limits)
      : undefined;
    return message;
  },
};

function createBaseIndex(): Index {
  return { indexId: "", version: 0n, definition: undefined, state: 0, createdAt: undefined, updatedAt: undefined };
}

export const Index: MessageFns<Index> = {
  encode(message: Index, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.indexId !== "") {
      writer.uint32(10).string(message.indexId);
    }
    if (message.version !== 0n) {
      if (BigInt.asUintN(64, message.version) !== message.version) {
        throw new globalThis.Error("value provided for field message.version of type uint64 too large");
      }
      writer.uint32(16).uint64(message.version);
    }
    if (message.definition !== undefined) {
      IndexDefinition.encode(message.definition, writer.uint32(26).fork()).join();
    }
    if (message.state !== 0) {
      writer.uint32(32).int32(message.state);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(42).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Index {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIndex();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.indexId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.definition = IndexDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Index {
    return {
      indexId: isSet(object.indexId)
        ? globalThis.String(object.indexId)
        : isSet(object.index_id)
        ? globalThis.String(object.index_id)
        : "",
      version: isSet(object.version) ? BigInt(object.version) : 0n,
      definition: isSet(object.definition) ? IndexDefinition.fromJSON(object.definition) : undefined,
      state: isSet(object.state) ? indexStateFromJSON(object.state) : 0,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      updatedAt: isSet(object.updatedAt)
        ? fromJsonTimestamp(object.updatedAt)
        : isSet(object.updated_at)
        ? fromJsonTimestamp(object.updated_at)
        : undefined,
    };
  },

  toJSON(message: Index): unknown {
    const obj: any = {};
    if (message.indexId !== "") {
      obj.indexId = message.indexId;
    }
    if (message.version !== 0n) {
      obj.version = message.version.toString();
    }
    if (message.definition !== undefined) {
      obj.definition = IndexDefinition.toJSON(message.definition);
    }
    if (message.state !== 0) {
      obj.state = indexStateToJSON(message.state);
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Index>, I>>(base?: I): Index {
    return Index.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Index>, I>>(object: I): Index {
    const message = createBaseIndex();
    message.indexId = object.indexId ?? "";
    message.version = (object.version !== undefined && object.version !== null) ? BigInt(object.version) : 0n;
    message.definition = (object.definition !== undefined && object.definition !== null)
      ? IndexDefinition.fromPartial(object.definition)
      : undefined;
    message.state = object.state ?? 0;
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    return message;
  },
};

function createBaseIndexStats(): IndexStats {
  return {
    indexId: "",
    eventCount: 0n,
    storageBytes: 0n,
    earliestEventTime: undefined,
    latestEventTime: undefined,
    measuredAt: undefined,
    estimates: false,
  };
}

export const IndexStats: MessageFns<IndexStats> = {
  encode(message: IndexStats, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.indexId !== "") {
      writer.uint32(10).string(message.indexId);
    }
    if (message.eventCount !== 0n) {
      if (BigInt.asUintN(64, message.eventCount) !== message.eventCount) {
        throw new globalThis.Error("value provided for field message.eventCount of type uint64 too large");
      }
      writer.uint32(16).uint64(message.eventCount);
    }
    if (message.storageBytes !== 0n) {
      if (BigInt.asUintN(64, message.storageBytes) !== message.storageBytes) {
        throw new globalThis.Error("value provided for field message.storageBytes of type uint64 too large");
      }
      writer.uint32(24).uint64(message.storageBytes);
    }
    if (message.earliestEventTime !== undefined) {
      Timestamp.encode(toTimestamp(message.earliestEventTime), writer.uint32(34).fork()).join();
    }
    if (message.latestEventTime !== undefined) {
      Timestamp.encode(toTimestamp(message.latestEventTime), writer.uint32(42).fork()).join();
    }
    if (message.measuredAt !== undefined) {
      Timestamp.encode(toTimestamp(message.measuredAt), writer.uint32(50).fork()).join();
    }
    if (message.estimates !== false) {
      writer.uint32(56).bool(message.estimates);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IndexStats {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIndexStats();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.indexId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.eventCount = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.storageBytes = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.earliestEventTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.latestEventTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.measuredAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.estimates = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IndexStats {
    return {
      indexId: isSet(object.indexId)
        ? globalThis.String(object.indexId)
        : isSet(object.index_id)
        ? globalThis.String(object.index_id)
        : "",
      eventCount: isSet(object.eventCount)
        ? BigInt(object.eventCount)
        : isSet(object.event_count)
        ? BigInt(object.event_count)
        : 0n,
      storageBytes: isSet(object.storageBytes)
        ? BigInt(object.storageBytes)
        : isSet(object.storage_bytes)
        ? BigInt(object.storage_bytes)
        : 0n,
      earliestEventTime: isSet(object.earliestEventTime)
        ? fromJsonTimestamp(object.earliestEventTime)
        : isSet(object.earliest_event_time)
        ? fromJsonTimestamp(object.earliest_event_time)
        : undefined,
      latestEventTime: isSet(object.latestEventTime)
        ? fromJsonTimestamp(object.latestEventTime)
        : isSet(object.latest_event_time)
        ? fromJsonTimestamp(object.latest_event_time)
        : undefined,
      measuredAt: isSet(object.measuredAt)
        ? fromJsonTimestamp(object.measuredAt)
        : isSet(object.measured_at)
        ? fromJsonTimestamp(object.measured_at)
        : undefined,
      estimates: isSet(object.estimates) ? globalThis.Boolean(object.estimates) : false,
    };
  },

  toJSON(message: IndexStats): unknown {
    const obj: any = {};
    if (message.indexId !== "") {
      obj.indexId = message.indexId;
    }
    if (message.eventCount !== 0n) {
      obj.eventCount = message.eventCount.toString();
    }
    if (message.storageBytes !== 0n) {
      obj.storageBytes = message.storageBytes.toString();
    }
    if (message.earliestEventTime !== undefined) {
      obj.earliestEventTime = message.earliestEventTime.toISOString();
    }
    if (message.latestEventTime !== undefined) {
      obj.latestEventTime = message.latestEventTime.toISOString();
    }
    if (message.measuredAt !== undefined) {
      obj.measuredAt = message.measuredAt.toISOString();
    }
    if (message.estimates !== false) {
      obj.estimates = message.estimates;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IndexStats>, I>>(base?: I): IndexStats {
    return IndexStats.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IndexStats>, I>>(object: I): IndexStats {
    const message = createBaseIndexStats();
    message.indexId = object.indexId ?? "";
    message.eventCount = (object.eventCount !== undefined && object.eventCount !== null)
      ? BigInt(object.eventCount)
      : 0n;
    message.storageBytes = (object.storageBytes !== undefined && object.storageBytes !== null)
      ? BigInt(object.storageBytes)
      : 0n;
    message.earliestEventTime = object.earliestEventTime ?? undefined;
    message.latestEventTime = object.latestEventTime ?? undefined;
    message.measuredAt = object.measuredAt ?? undefined;
    message.estimates = object.estimates ?? false;
    return message;
  },
};

function createBaseIndexSummary(): IndexSummary {
  return { indexId: "", name: "", displayName: "", state: 0, ingestionAccess: 0, searchAccess: 0 };
}

export const IndexSummary: MessageFns<IndexSummary> = {
  encode(message: IndexSummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.indexId !== "") {
      writer.uint32(10).string(message.indexId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.displayName !== "") {
      writer.uint32(26).string(message.displayName);
    }
    if (message.state !== 0) {
      writer.uint32(32).int32(message.state);
    }
    if (message.ingestionAccess !== 0) {
      writer.uint32(40).int32(message.ingestionAccess);
    }
    if (message.searchAccess !== 0) {
      writer.uint32(48).int32(message.searchAccess);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IndexSummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIndexSummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.indexId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.ingestionAccess = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.searchAccess = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IndexSummary {
    return {
      indexId: isSet(object.indexId)
        ? globalThis.String(object.indexId)
        : isSet(object.index_id)
        ? globalThis.String(object.index_id)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      state: isSet(object.state) ? indexStateFromJSON(object.state) : 0,
      ingestionAccess: isSet(object.ingestionAccess)
        ? indexAccessStateFromJSON(object.ingestionAccess)
        : isSet(object.ingestion_access)
        ? indexAccessStateFromJSON(object.ingestion_access)
        : 0,
      searchAccess: isSet(object.searchAccess)
        ? indexAccessStateFromJSON(object.searchAccess)
        : isSet(object.search_access)
        ? indexAccessStateFromJSON(object.search_access)
        : 0,
    };
  },

  toJSON(message: IndexSummary): unknown {
    const obj: any = {};
    if (message.indexId !== "") {
      obj.indexId = message.indexId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.state !== 0) {
      obj.state = indexStateToJSON(message.state);
    }
    if (message.ingestionAccess !== 0) {
      obj.ingestionAccess = indexAccessStateToJSON(message.ingestionAccess);
    }
    if (message.searchAccess !== 0) {
      obj.searchAccess = indexAccessStateToJSON(message.searchAccess);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IndexSummary>, I>>(base?: I): IndexSummary {
    return IndexSummary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IndexSummary>, I>>(object: I): IndexSummary {
    const message = createBaseIndexSummary();
    message.indexId = object.indexId ?? "";
    message.name = object.name ?? "";
    message.displayName = object.displayName ?? "";
    message.state = object.state ?? 0;
    message.ingestionAccess = object.ingestionAccess ?? 0;
    message.searchAccess = object.searchAccess ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
