// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: open_splunk/v1/common.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";

export enum SortDirection {
  SORT_DIRECTION_UNSPECIFIED = 0,
  SORT_DIRECTION_ASCENDING = 1,
  SORT_DIRECTION_DESCENDING = 2,
  UNRECOGNIZED = -1,
}

export function sortDirectionFromJSON(object: any): SortDirection {
  switch (object) {
    case 0:
    case "SORT_DIRECTION_UNSPECIFIED":
      return SortDirection.SORT_DIRECTION_UNSPECIFIED;
    case 1:
    case "SORT_DIRECTION_ASCENDING":
      return SortDirection.SORT_DIRECTION_ASCENDING;
    case 2:
    case "SORT_DIRECTION_DESCENDING":
      return SortDirection.SORT_DIRECTION_DESCENDING;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SortDirection.UNRECOGNIZED;
  }
}

export function sortDirectionToJSON(object: SortDirection): string {
  switch (object) {
    case SortDirection.SORT_DIRECTION_UNSPECIFIED:
      return "SORT_DIRECTION_UNSPECIFIED";
    case SortDirection.SORT_DIRECTION_ASCENDING:
      return "SORT_DIRECTION_ASCENDING";
    case SortDirection.SORT_DIRECTION_DESCENDING:
      return "SORT_DIRECTION_DESCENDING";
    case SortDirection.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum DiagnosticSeverity {
  DIAGNOSTIC_SEVERITY_UNSPECIFIED = 0,
  DIAGNOSTIC_SEVERITY_INFO = 1,
  DIAGNOSTIC_SEVERITY_WARNING = 2,
  DIAGNOSTIC_SEVERITY_ERROR = 3,
  UNRECOGNIZED = -1,
}

export function diagnosticSeverityFromJSON(object: any): DiagnosticSeverity {
  switch (object) {
    case 0:
    case "DIAGNOSTIC_SEVERITY_UNSPECIFIED":
      return DiagnosticSeverity.DIAGNOSTIC_SEVERITY_UNSPECIFIED;
    case 1:
    case "DIAGNOSTIC_SEVERITY_INFO":
      return DiagnosticSeverity.DIAGNOSTIC_SEVERITY_INFO;
    case 2:
    case "DIAGNOSTIC_SEVERITY_WARNING":
      return DiagnosticSeverity.DIAGNOSTIC_SEVERITY_WARNING;
    case 3:
    case "DIAGNOSTIC_SEVERITY_ERROR":
      return DiagnosticSeverity.DIAGNOSTIC_SEVERITY_ERROR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DiagnosticSeverity.UNRECOGNIZED;
  }
}

export function diagnosticSeverityToJSON(object: DiagnosticSeverity): string {
  switch (object) {
    case DiagnosticSeverity.DIAGNOSTIC_SEVERITY_UNSPECIFIED:
      return "DIAGNOSTIC_SEVERITY_UNSPECIFIED";
    case DiagnosticSeverity.DIAGNOSTIC_SEVERITY_INFO:
      return "DIAGNOSTIC_SEVERITY_INFO";
    case DiagnosticSeverity.DIAGNOSTIC_SEVERITY_WARNING:
      return "DIAGNOSTIC_SEVERITY_WARNING";
    case DiagnosticSeverity.DIAGNOSTIC_SEVERITY_ERROR:
      return "DIAGNOSTIC_SEVERITY_ERROR";
    case DiagnosticSeverity.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum SharingScope {
  SHARING_SCOPE_UNSPECIFIED = 0,
  SHARING_SCOPE_PRIVATE = 1,
  SHARING_SCOPE_APP = 2,
  SHARING_SCOPE_GLOBAL = 3,
  UNRECOGNIZED = -1,
}

export function sharingScopeFromJSON(object: any): SharingScope {
  switch (object) {
    case 0:
    case "SHARING_SCOPE_UNSPECIFIED":
      return SharingScope.SHARING_SCOPE_UNSPECIFIED;
    case 1:
    case "SHARING_SCOPE_PRIVATE":
      return SharingScope.SHARING_SCOPE_PRIVATE;
    case 2:
    case "SHARING_SCOPE_APP":
      return SharingScope.SHARING_SCOPE_APP;
    case 3:
    case "SHARING_SCOPE_GLOBAL":
      return SharingScope.SHARING_SCOPE_GLOBAL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SharingScope.UNRECOGNIZED;
  }
}

export function sharingScopeToJSON(object: SharingScope): string {
  switch (object) {
    case SharingScope.SHARING_SCOPE_UNSPECIFIED:
      return "SHARING_SCOPE_UNSPECIFIED";
    case SharingScope.SHARING_SCOPE_PRIVATE:
      return "SHARING_SCOPE_PRIVATE";
    case SharingScope.SHARING_SCOPE_APP:
      return "SHARING_SCOPE_APP";
    case SharingScope.SHARING_SCOPE_GLOBAL:
      return "SHARING_SCOPE_GLOBAL";
    case SharingScope.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * BuildMetadata reports one immutable server source identity and the exact
 * embedded UI, contracts, and migrations produced from it.
 */
export interface BuildMetadata {
  applicationVersion: string;
  sourceRevision: string;
  uiBuildId: string;
  uiSha256: string;
  protobufSchemaSha256: string;
  sqliteMigrationsSha256: string;
  sqliteMigrationVersion: number;
  clickhouseMigrationsSha256: string;
  clickhouseMigrationVersion: number;
  assetManifestFormatVersion: number;
}

/**
 * PageRequest uses an opaque server cursor. page_token must only be replayed
 * with the same endpoint and filter/sort parameters that produced it.
 */
export interface PageRequest {
  pageSize?: number | undefined;
  pageToken?: string | undefined;
  includeTotalSize: boolean;
}

export interface PageResponse {
  nextPageToken?: string | undefined;
  totalSize?: bigint | undefined;
  totalSizeExact: boolean;
}

/**
 * TimeRangeSpec preserves user-entered SPL time expressions. earliest and
 * latest may be absolute timestamps or relative expressions such as -24h and
 * now. Omission semantics belong to the consuming endpoint; in particular,
 * /indexes/fields/list requires both bounds.
 */
export interface TimeRangeSpec {
  earliest?: string | undefined;
  latest?: string | undefined;
  timezone?: string | undefined;
}

/** ResolvedTimeRange is an absolute half-open interval [earliest, latest). */
export interface ResolvedTimeRange {
  earliest: Date | undefined;
  latest: Date | undefined;
  timezone: string;
}

/**
 * SourcePosition is one UTF-8 source location. byte_offset is zero-based;
 * line and column are one-based when known and zero otherwise.
 */
export interface SourcePosition {
  byteOffset: bigint;
  line: number;
  column: number;
}

export interface SourceRange {
  start: SourcePosition | undefined;
  end: SourcePosition | undefined;
}

/**
 * Diagnostic is a source-located SPL parse, semantic, or planning message.
 * code is stable and intended for programmatic handling; message is for users.
 */
export interface Diagnostic {
  code: string;
  severity: DiagnosticSeverity;
  message: string;
  sourceRange?: SourceRange | undefined;
  suggestions: string[];
}

/** ApiWarning is a non-fatal, stable-code warning attached to a job or event. */
export interface ApiWarning {
  code: string;
  message: string;
  sourceRange?: SourceRange | undefined;
  occurredAt: Date | undefined;
}

/**
 * FieldViolation identifies one invalid field without coupling transport errors
 * to a human-readable message.
 */
export interface FieldViolation {
  fieldPath: string;
  code: string;
  message: string;
}

function createBaseBuildMetadata(): BuildMetadata {
  return {
    applicationVersion: "",
    sourceRevision: "",
    uiBuildId: "",
    uiSha256: "",
    protobufSchemaSha256: "",
    sqliteMigrationsSha256: "",
    sqliteMigrationVersion: 0,
    clickhouseMigrationsSha256: "",
    clickhouseMigrationVersion: 0,
    assetManifestFormatVersion: 0,
  };
}

export const BuildMetadata: MessageFns<BuildMetadata> = {
  encode(message: BuildMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.applicationVersion !== "") {
      writer.uint32(10).string(message.applicationVersion);
    }
    if (message.sourceRevision !== "") {
      writer.uint32(18).string(message.sourceRevision);
    }
    if (message.uiBuildId !== "") {
      writer.uint32(26).string(message.uiBuildId);
    }
    if (message.uiSha256 !== "") {
      writer.uint32(34).string(message.uiSha256);
    }
    if (message.protobufSchemaSha256 !== "") {
      writer.uint32(42).string(message.protobufSchemaSha256);
    }
    if (message.sqliteMigrationsSha256 !== "") {
      writer.uint32(50).string(message.sqliteMigrationsSha256);
    }
    if (message.sqliteMigrationVersion !== 0) {
      writer.uint32(56).uint32(message.sqliteMigrationVersion);
    }
    if (message.clickhouseMigrationsSha256 !== "") {
      writer.uint32(66).string(message.clickhouseMigrationsSha256);
    }
    if (message.clickhouseMigrationVersion !== 0) {
      writer.uint32(72).uint32(message.clickhouseMigrationVersion);
    }
    if (message.assetManifestFormatVersion !== 0) {
      writer.uint32(80).uint32(message.assetManifestFormatVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BuildMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBuildMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.applicationVersion = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sourceRevision = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.uiBuildId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.uiSha256 = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.protobufSchemaSha256 = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.sqliteMigrationsSha256 = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.sqliteMigrationVersion = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.clickhouseMigrationsSha256 = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.clickhouseMigrationVersion = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.assetManifestFormatVersion = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BuildMetadata {
    return {
      applicationVersion: isSet(object.applicationVersion)
        ? globalThis.String(object.applicationVersion)
        : isSet(object.application_version)
        ? globalThis.String(object.application_version)
        : "",
      sourceRevision: isSet(object.sourceRevision)
        ? globalThis.String(object.sourceRevision)
        : isSet(object.source_revision)
        ? globalThis.String(object.source_revision)
        : "",
      uiBuildId: isSet(object.uiBuildId)
        ? globalThis.String(object.uiBuildId)
        : isSet(object.ui_build_id)
        ? globalThis.String(object.ui_build_id)
        : "",
      uiSha256: isSet(object.uiSha256)
        ? globalThis.String(object.uiSha256)
        : isSet(object.ui_sha256)
        ? globalThis.String(object.ui_sha256)
        : "",
      protobufSchemaSha256: isSet(object.protobufSchemaSha256)
        ? globalThis.String(object.protobufSchemaSha256)
        : isSet(object.protobuf_schema_sha256)
        ? globalThis.String(object.protobuf_schema_sha256)
        : "",
      sqliteMigrationsSha256: isSet(object.sqliteMigrationsSha256)
        ? globalThis.String(object.sqliteMigrationsSha256)
        : isSet(object.sqlite_migrations_sha256)
        ? globalThis.String(object.sqlite_migrations_sha256)
        : "",
      sqliteMigrationVersion: isSet(object.sqliteMigrationVersion)
        ? globalThis.Number(object.sqliteMigrationVersion)
        : isSet(object.sqlite_migration_version)
        ? globalThis.Number(object.sqlite_migration_version)
        : 0,
      clickhouseMigrationsSha256: isSet(object.clickhouseMigrationsSha256)
        ? globalThis.String(object.clickhouseMigrationsSha256)
        : isSet(object.clickhouse_migrations_sha256)
        ? globalThis.String(object.clickhouse_migrations_sha256)
        : "",
      clickhouseMigrationVersion: isSet(object.clickhouseMigrationVersion)
        ? globalThis.Number(object.clickhouseMigrationVersion)
        : isSet(object.clickhouse_migration_version)
        ? globalThis.Number(object.clickhouse_migration_version)
        : 0,
      assetManifestFormatVersion: isSet(object.assetManifestFormatVersion)
        ? globalThis.Number(object.assetManifestFormatVersion)
        : isSet(object.asset_manifest_format_version)
        ? globalThis.Number(object.asset_manifest_format_version)
        : 0,
    };
  },

  toJSON(message: BuildMetadata): unknown {
    const obj: any = {};
    if (message.applicationVersion !== "") {
      obj.applicationVersion = message.applicationVersion;
    }
    if (message.sourceRevision !== "") {
      obj.sourceRevision = message.sourceRevision;
    }
    if (message.uiBuildId !== "") {
      obj.uiBuildId = message.uiBuildId;
    }
    if (message.uiSha256 !== "") {
      obj.uiSha256 = message.uiSha256;
    }
    if (message.protobufSchemaSha256 !== "") {
      obj.protobufSchemaSha256 = message.protobufSchemaSha256;
    }
    if (message.sqliteMigrationsSha256 !== "") {
      obj.sqliteMigrationsSha256 = message.sqliteMigrationsSha256;
    }
    if (message.sqliteMigrationVersion !== 0) {
      obj.sqliteMigrationVersion = Math.round(message.sqliteMigrationVersion);
    }
    if (message.clickhouseMigrationsSha256 !== "") {
      obj.clickhouseMigrationsSha256 = message.clickhouseMigrationsSha256;
    }
    if (message.clickhouseMigrationVersion !== 0) {
      obj.clickhouseMigrationVersion = Math.round(message.clickhouseMigrationVersion);
    }
    if (message.assetManifestFormatVersion !== 0) {
      obj.assetManifestFormatVersion = Math.round(message.assetManifestFormatVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BuildMetadata>, I>>(base?: I): BuildMetadata {
    return BuildMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BuildMetadata>, I>>(object: I): BuildMetadata {
    const message = createBaseBuildMetadata();
    message.applicationVersion = object.applicationVersion ?? "";
    message.sourceRevision = object.sourceRevision ?? "";
    message.uiBuildId = object.uiBuildId ?? "";
    message.uiSha256 = object.uiSha256 ?? "";
    message.protobufSchemaSha256 = object.protobufSchemaSha256 ?? "";
    message.sqliteMigrationsSha256 = object.sqliteMigrationsSha256 ?? "";
    message.sqliteMigrationVersion = object.sqliteMigrationVersion ?? 0;
    message.clickhouseMigrationsSha256 = object.clickhouseMigrationsSha256 ?? "";
    message.clickhouseMigrationVersion = object.clickhouseMigrationVersion ?? 0;
    message.assetManifestFormatVersion = object.assetManifestFormatVersion ?? 0;
    return message;
  },
};

function createBasePageRequest(): PageRequest {
  return { pageSize: undefined, pageToken: undefined, includeTotalSize: false };
}

export const PageRequest: MessageFns<PageRequest> = {
  encode(message: PageRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pageSize !== undefined) {
      writer.uint32(8).uint32(message.pageSize);
    }
    if (message.pageToken !== undefined) {
      writer.uint32(18).string(message.pageToken);
    }
    if (message.includeTotalSize !== false) {
      writer.uint32(24).bool(message.includeTotalSize);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PageRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pageSize = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pageToken = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.includeTotalSize = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PageRequest {
    return {
      pageSize: isSet(object.pageSize)
        ? globalThis.Number(object.pageSize)
        : isSet(object.page_size)
        ? globalThis.Number(object.page_size)
        : undefined,
      pageToken: isSet(object.pageToken)
        ? globalThis.String(object.pageToken)
        : isSet(object.page_token)
        ? globalThis.String(object.page_token)
        : undefined,
      includeTotalSize: isSet(object.includeTotalSize)
        ? globalThis.Boolean(object.includeTotalSize)
        : isSet(object.include_total_size)
        ? globalThis.Boolean(object.include_total_size)
        : false,
    };
  },

  toJSON(message: PageRequest): unknown {
    const obj: any = {};
    if (message.pageSize !== undefined) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.pageToken !== undefined) {
      obj.pageToken = message.pageToken;
    }
    if (message.includeTotalSize !== false) {
      obj.includeTotalSize = message.includeTotalSize;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PageRequest>, I>>(base?: I): PageRequest {
    return PageRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PageRequest>, I>>(object: I): PageRequest {
    const message = createBasePageRequest();
    message.pageSize = object.pageSize ?? undefined;
    message.pageToken = object.pageToken ?? undefined;
    message.includeTotalSize = object.includeTotalSize ?? false;
    return message;
  },
};

function createBasePageResponse(): PageResponse {
  return { nextPageToken: undefined, totalSize: undefined, totalSizeExact: false };
}

export const PageResponse: MessageFns<PageResponse> = {
  encode(message: PageResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nextPageToken !== undefined) {
      writer.uint32(10).string(message.nextPageToken);
    }
    if (message.totalSize !== undefined) {
      if (BigInt.asUintN(64, message.totalSize) !== message.totalSize) {
        throw new globalThis.Error("value provided for field message.totalSize of type uint64 too large");
      }
      writer.uint32(16).uint64(message.totalSize);
    }
    if (message.totalSizeExact !== false) {
      writer.uint32(24).bool(message.totalSizeExact);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PageResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nextPageToken = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalSize = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.totalSizeExact = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PageResponse {
    return {
      nextPageToken: isSet(object.nextPageToken)
        ? globalThis.String(object.nextPageToken)
        : isSet(object.next_page_token)
        ? globalThis.String(object.next_page_token)
        : undefined,
      totalSize: isSet(object.totalSize)
        ? BigInt(object.totalSize)
        : isSet(object.total_size)
        ? BigInt(object.total_size)
        : undefined,
      totalSizeExact: isSet(object.totalSizeExact)
        ? globalThis.Boolean(object.totalSizeExact)
        : isSet(object.total_size_exact)
        ? globalThis.Boolean(object.total_size_exact)
        : false,
    };
  },

  toJSON(message: PageResponse): unknown {
    const obj: any = {};
    if (message.nextPageToken !== undefined) {
      obj.nextPageToken = message.nextPageToken;
    }
    if (message.totalSize !== undefined) {
      obj.totalSize = message.totalSize.toString();
    }
    if (message.totalSizeExact !== false) {
      obj.totalSizeExact = message.totalSizeExact;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PageResponse>, I>>(base?: I): PageResponse {
    return PageResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PageResponse>, I>>(object: I): PageResponse {
    const message = createBasePageResponse();
    message.nextPageToken = object.nextPageToken ?? undefined;
    message.totalSize = (object.totalSize !== undefined && object.totalSize !== null)
      ? BigInt(object.totalSize)
      : undefined;
    message.totalSizeExact = object.totalSizeExact ?? false;
    return message;
  },
};

function createBaseTimeRangeSpec(): TimeRangeSpec {
  return { earliest: undefined, latest: undefined, timezone: undefined };
}

export const TimeRangeSpec: MessageFns<TimeRangeSpec> = {
  encode(message: TimeRangeSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.earliest !== undefined) {
      writer.uint32(10).string(message.earliest);
    }
    if (message.latest !== undefined) {
      writer.uint32(18).string(message.latest);
    }
    if (message.timezone !== undefined) {
      writer.uint32(26).string(message.timezone);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TimeRangeSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTimeRangeSpec();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.earliest = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.latest = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.timezone = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TimeRangeSpec {
    return {
      earliest: isSet(object.earliest) ? globalThis.String(object.earliest) : undefined,
      latest: isSet(object.latest) ? globalThis.String(object.latest) : undefined,
      timezone: isSet(object.timezone) ? globalThis.String(object.timezone) : undefined,
    };
  },

  toJSON(message: TimeRangeSpec): unknown {
    const obj: any = {};
    if (message.earliest !== undefined) {
      obj.earliest = message.earliest;
    }
    if (message.latest !== undefined) {
      obj.latest = message.latest;
    }
    if (message.timezone !== undefined) {
      obj.timezone = message.timezone;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TimeRangeSpec>, I>>(base?: I): TimeRangeSpec {
    return TimeRangeSpec.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TimeRangeSpec>, I>>(object: I): TimeRangeSpec {
    const message = createBaseTimeRangeSpec();
    message.earliest = object.earliest ?? undefined;
    message.latest = object.latest ?? undefined;
    message.timezone = object.timezone ?? undefined;
    return message;
  },
};

function createBaseResolvedTimeRange(): ResolvedTimeRange {
  return { earliest: undefined, latest: undefined, timezone: "" };
}

export const ResolvedTimeRange: MessageFns<ResolvedTimeRange> = {
  encode(message: ResolvedTimeRange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.earliest !== undefined) {
      Timestamp.encode(toTimestamp(message.earliest), writer.uint32(10).fork()).join();
    }
    if (message.latest !== undefined) {
      Timestamp.encode(toTimestamp(message.latest), writer.uint32(18).fork()).join();
    }
    if (message.timezone !== "") {
      writer.uint32(26).string(message.timezone);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolvedTimeRange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolvedTimeRange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.earliest = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.latest = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.timezone = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResolvedTimeRange {
    return {
      earliest: isSet(object.earliest) ? fromJsonTimestamp(object.earliest) : undefined,
      latest: isSet(object.latest) ? fromJsonTimestamp(object.latest) : undefined,
      timezone: isSet(object.timezone) ? globalThis.String(object.timezone) : "",
    };
  },

  toJSON(message: ResolvedTimeRange): unknown {
    const obj: any = {};
    if (message.earliest !== undefined) {
      obj.earliest = message.earliest.toISOString();
    }
    if (message.latest !== undefined) {
      obj.latest = message.latest.toISOString();
    }
    if (message.timezone !== "") {
      obj.timezone = message.timezone;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResolvedTimeRange>, I>>(base?: I): ResolvedTimeRange {
    return ResolvedTimeRange.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolvedTimeRange>, I>>(object: I): ResolvedTimeRange {
    const message = createBaseResolvedTimeRange();
    message.earliest = object.earliest ?? undefined;
    message.latest = object.latest ?? undefined;
    message.timezone = object.timezone ?? "";
    return message;
  },
};

function createBaseSourcePosition(): SourcePosition {
  return { byteOffset: 0n, line: 0, column: 0 };
}

export const SourcePosition: MessageFns<SourcePosition> = {
  encode(message: SourcePosition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.byteOffset !== 0n) {
      if (BigInt.asUintN(64, message.byteOffset) !== message.byteOffset) {
        throw new globalThis.Error("value provided for field message.byteOffset of type uint64 too large");
      }
      writer.uint32(8).uint64(message.byteOffset);
    }
    if (message.line !== 0) {
      writer.uint32(16).uint32(message.line);
    }
    if (message.column !== 0) {
      writer.uint32(24).uint32(message.column);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SourcePosition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSourcePosition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.byteOffset = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.line = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.column = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SourcePosition {
    return {
      byteOffset: isSet(object.byteOffset)
        ? BigInt(object.byteOffset)
        : isSet(object.byte_offset)
        ? BigInt(object.byte_offset)
        : 0n,
      line: isSet(object.line) ? globalThis.Number(object.line) : 0,
      column: isSet(object.column) ? globalThis.Number(object.column) : 0,
    };
  },

  toJSON(message: SourcePosition): unknown {
    const obj: any = {};
    if (message.byteOffset !== 0n) {
      obj.byteOffset = message.byteOffset.toString();
    }
    if (message.line !== 0) {
      obj.line = Math.round(message.line);
    }
    if (message.column !== 0) {
      obj.column = Math.round(message.column);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SourcePosition>, I>>(base?: I): SourcePosition {
    return SourcePosition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SourcePosition>, I>>(object: I): SourcePosition {
    const message = createBaseSourcePosition();
    message.byteOffset = (object.byteOffset !== undefined && object.byteOffset !== null)
      ? BigInt(object.byteOffset)
      : 0n;
    message.line = object.line ?? 0;
    message.column = object.column ?? 0;
    return message;
  },
};

function createBaseSourceRange(): SourceRange {
  return { start: undefined, end: undefined };
}

export const SourceRange: MessageFns<SourceRange> = {
  encode(message: SourceRange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.start !== undefined) {
      SourcePosition.encode(message.start, writer.uint32(10).fork()).join();
    }
    if (message.end !== undefined) {
      SourcePosition.encode(message.end, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SourceRange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSourceRange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.start = SourcePosition.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.end = SourcePosition.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SourceRange {
    return {
      start: isSet(object.start) ? SourcePosition.fromJSON(object.start) : undefined,
      end: isSet(object.end) ? SourcePosition.fromJSON(object.end) : undefined,
    };
  },

  toJSON(message: SourceRange): unknown {
    const obj: any = {};
    if (message.start !== undefined) {
      obj.start = SourcePosition.toJSON(message.start);
    }
    if (message.end !== undefined) {
      obj.end = SourcePosition.toJSON(message.end);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SourceRange>, I>>(base?: I): SourceRange {
    return SourceRange.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SourceRange>, I>>(object: I): SourceRange {
    const message = createBaseSourceRange();
    message.start = (object.start !== undefined && object.start !== null)
      ? SourcePosition.fromPartial(object.start)
      : undefined;
    message.end = (object.end !== undefined && object.end !== null)
      ? SourcePosition.fromPartial(object.end)
      : undefined;
    return message;
  },
};

function createBaseDiagnostic(): Diagnostic {
  return { code: "", severity: 0, message: "", sourceRange: undefined, suggestions: [] };
}

export const Diagnostic: MessageFns<Diagnostic> = {
  encode(message: Diagnostic, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.severity !== 0) {
      writer.uint32(16).int32(message.severity);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    if (message.sourceRange !== undefined) {
      SourceRange.encode(message.sourceRange, writer.uint32(34).fork()).join();
    }
    for (const v of message.suggestions) {
      writer.uint32(42).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Diagnostic {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiagnostic();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.severity = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sourceRange = SourceRange.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.suggestions.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Diagnostic {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      severity: isSet(object.severity) ? diagnosticSeverityFromJSON(object.severity) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      sourceRange: isSet(object.sourceRange)
        ? SourceRange.fromJSON(object.sourceRange)
        : isSet(object.source_range)
        ? SourceRange.fromJSON(object.source_range)
        : undefined,
      suggestions: globalThis.Array.isArray(object?.suggestions)
        ? object.suggestions.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: Diagnostic): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.severity !== 0) {
      obj.severity = diagnosticSeverityToJSON(message.severity);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.sourceRange !== undefined) {
      obj.sourceRange = SourceRange.toJSON(message.sourceRange);
    }
    if (message.suggestions?.length) {
      obj.suggestions = message.suggestions;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Diagnostic>, I>>(base?: I): Diagnostic {
    return Diagnostic.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Diagnostic>, I>>(object: I): Diagnostic {
    const message = createBaseDiagnostic();
    message.code = object.code ?? "";
    message.severity = object.severity ?? 0;
    message.message = object.message ?? "";
    message.sourceRange = (object.sourceRange !== undefined && object.sourceRange !== null)
      ? SourceRange.fromPartial(object.sourceRange)
      : undefined;
    message.suggestions = object.suggestions?.map((e) => e) || [];
    return message;
  },
};

function createBaseApiWarning(): ApiWarning {
  return { code: "", message: "", sourceRange: undefined, occurredAt: undefined };
}

export const ApiWarning: MessageFns<ApiWarning> = {
  encode(message: ApiWarning, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.sourceRange !== undefined) {
      SourceRange.encode(message.sourceRange, writer.uint32(26).fork()).join();
    }
    if (message.occurredAt !== undefined) {
      Timestamp.encode(toTimestamp(message.occurredAt), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApiWarning {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApiWarning();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sourceRange = SourceRange.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.occurredAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApiWarning {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      sourceRange: isSet(object.sourceRange)
        ? SourceRange.fromJSON(object.sourceRange)
        : isSet(object.source_range)
        ? SourceRange.fromJSON(object.source_range)
        : undefined,
      occurredAt: isSet(object.occurredAt)
        ? fromJsonTimestamp(object.occurredAt)
        : isSet(object.occurred_at)
        ? fromJsonTimestamp(object.occurred_at)
        : undefined,
    };
  },

  toJSON(message: ApiWarning): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.sourceRange !== undefined) {
      obj.sourceRange = SourceRange.toJSON(message.sourceRange);
    }
    if (message.occurredAt !== undefined) {
      obj.occurredAt = message.occurredAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ApiWarning>, I>>(base?: I): ApiWarning {
    return ApiWarning.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ApiWarning>, I>>(object: I): ApiWarning {
    const message = createBaseApiWarning();
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    message.sourceRange = (object.sourceRange !== undefined && object.sourceRange !== null)
      ? SourceRange.fromPartial(object.sourceRange)
      : undefined;
    message.occurredAt = object.occurredAt ?? undefined;
    return message;
  },
};

function createBaseFieldViolation(): FieldViolation {
  return { fieldPath: "", code: "", message: "" };
}

export const FieldViolation: MessageFns<FieldViolation> = {
  encode(message: FieldViolation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fieldPath !== "") {
      writer.uint32(10).string(message.fieldPath);
    }
    if (message.code !== "") {
      writer.uint32(18).string(message.code);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FieldViolation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFieldViolation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.fieldPath = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FieldViolation {
    return {
      fieldPath: isSet(object.fieldPath)
        ? globalThis.String(object.fieldPath)
        : isSet(object.field_path)
        ? globalThis.String(object.field_path)
        : "",
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: FieldViolation): unknown {
    const obj: any = {};
    if (message.fieldPath !== "") {
      obj.fieldPath = message.fieldPath;
    }
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FieldViolation>, I>>(base?: I): FieldViolation {
    return FieldViolation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FieldViolation>, I>>(object: I): FieldViolation {
    const message = createBaseFieldViolation();
    message.fieldPath = object.fieldPath ?? "";
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
