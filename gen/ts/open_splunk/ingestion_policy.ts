// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: open_splunk/ingestion_policy.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

/**
 * IngestionRateLimits configures independently optional event and encoded-byte
 * rates. A zero or omitted field leaves that dimension unlimited.
 */
export interface IngestionRateLimits {
  maxEventsPerSecond?: bigint | undefined;
  maxUncompressedBytesPerSecond?: bigint | undefined;
}

function createBaseIngestionRateLimits(): IngestionRateLimits {
  return { maxEventsPerSecond: undefined, maxUncompressedBytesPerSecond: undefined };
}

export const IngestionRateLimits: MessageFns<IngestionRateLimits> = {
  encode(message: IngestionRateLimits, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maxEventsPerSecond !== undefined) {
      if (BigInt.asUintN(64, message.maxEventsPerSecond) !== message.maxEventsPerSecond) {
        throw new globalThis.Error("value provided for field message.maxEventsPerSecond of type uint64 too large");
      }
      writer.uint32(8).uint64(message.maxEventsPerSecond);
    }
    if (message.maxUncompressedBytesPerSecond !== undefined) {
      if (BigInt.asUintN(64, message.maxUncompressedBytesPerSecond) !== message.maxUncompressedBytesPerSecond) {
        throw new globalThis.Error(
          "value provided for field message.maxUncompressedBytesPerSecond of type uint64 too large",
        );
      }
      writer.uint32(16).uint64(message.maxUncompressedBytesPerSecond);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IngestionRateLimits {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIngestionRateLimits();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.maxEventsPerSecond = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.maxUncompressedBytesPerSecond = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IngestionRateLimits {
    return {
      maxEventsPerSecond: isSet(object.maxEventsPerSecond)
        ? BigInt(object.maxEventsPerSecond)
        : isSet(object.max_events_per_second)
        ? BigInt(object.max_events_per_second)
        : undefined,
      maxUncompressedBytesPerSecond: isSet(object.maxUncompressedBytesPerSecond)
        ? BigInt(object.maxUncompressedBytesPerSecond)
        : isSet(object.max_uncompressed_bytes_per_second)
        ? BigInt(object.max_uncompressed_bytes_per_second)
        : undefined,
    };
  },

  toJSON(message: IngestionRateLimits): unknown {
    const obj: any = {};
    if (message.maxEventsPerSecond !== undefined) {
      obj.maxEventsPerSecond = message.maxEventsPerSecond.toString();
    }
    if (message.maxUncompressedBytesPerSecond !== undefined) {
      obj.maxUncompressedBytesPerSecond = message.maxUncompressedBytesPerSecond.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IngestionRateLimits>, I>>(base?: I): IngestionRateLimits {
    return IngestionRateLimits.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IngestionRateLimits>, I>>(object: I): IngestionRateLimits {
    const message = createBaseIngestionRateLimits();
    message.maxEventsPerSecond = (object.maxEventsPerSecond !== undefined && object.maxEventsPerSecond !== null)
      ? BigInt(object.maxEventsPerSecond)
      : undefined;
    message.maxUncompressedBytesPerSecond =
      (object.maxUncompressedBytesPerSecond !== undefined && object.maxUncompressedBytesPerSecond !== null)
        ? BigInt(object.maxUncompressedBytesPerSecond)
        : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
