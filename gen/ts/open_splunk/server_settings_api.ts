// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: open_splunk/server_settings_api.proto

import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../google/protobuf/duration";
import { Timestamp } from "../google/protobuf/timestamp";

/**
 * UiPalette is the instance-wide aesthetic every browser session paints,
 * including the sign-in page. Light and dark stay each user's own choice.
 */
export enum UiPalette {
  UI_PALETTE_UNSPECIFIED = 0,
  UI_PALETTE_CLASSIC = 1,
  UI_PALETTE_OCEAN = 2,
  UI_PALETTE_EMBER = 3,
  UI_PALETTE_GRAPHITE = 4,
  UI_PALETTE_GLASS = 5,
  UI_PALETTE_TERMINAL = 6,
  UNRECOGNIZED = -1,
}

export function uiPaletteFromJSON(object: any): UiPalette {
  switch (object) {
    case 0:
    case "UI_PALETTE_UNSPECIFIED":
      return UiPalette.UI_PALETTE_UNSPECIFIED;
    case 1:
    case "UI_PALETTE_CLASSIC":
      return UiPalette.UI_PALETTE_CLASSIC;
    case 2:
    case "UI_PALETTE_OCEAN":
      return UiPalette.UI_PALETTE_OCEAN;
    case 3:
    case "UI_PALETTE_EMBER":
      return UiPalette.UI_PALETTE_EMBER;
    case 4:
    case "UI_PALETTE_GRAPHITE":
      return UiPalette.UI_PALETTE_GRAPHITE;
    case 5:
    case "UI_PALETTE_GLASS":
      return UiPalette.UI_PALETTE_GLASS;
    case 6:
    case "UI_PALETTE_TERMINAL":
      return UiPalette.UI_PALETTE_TERMINAL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return UiPalette.UNRECOGNIZED;
  }
}

export function uiPaletteToJSON(object: UiPalette): string {
  switch (object) {
    case UiPalette.UI_PALETTE_UNSPECIFIED:
      return "UI_PALETTE_UNSPECIFIED";
    case UiPalette.UI_PALETTE_CLASSIC:
      return "UI_PALETTE_CLASSIC";
    case UiPalette.UI_PALETTE_OCEAN:
      return "UI_PALETTE_OCEAN";
    case UiPalette.UI_PALETTE_EMBER:
      return "UI_PALETTE_EMBER";
    case UiPalette.UI_PALETTE_GRAPHITE:
      return "UI_PALETTE_GRAPHITE";
    case UiPalette.UI_PALETTE_GLASS:
      return "UI_PALETTE_GLASS";
    case UiPalette.UI_PALETTE_TERMINAL:
      return "UI_PALETTE_TERMINAL";
    case UiPalette.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * SearchLimits is the complete node-wide search resource policy. Updates are
 * full replacements so related limits are validated as one coherent unit.
 */
export interface SearchLimits {
  maximumRuntime: Duration | undefined;
  maximumMemoryBytes: bigint;
  maximumRowsToRead: bigint;
  maximumBytesToRead: bigint;
  maximumGroupedRows: bigint;
  maximumThreads: bigint;
  maximumResultRows: bigint;
  maximumResultBytes: bigint;
  maximumTotalResultBytes: bigint;
  maximumConcurrentSearches: number;
  resultRetention: Duration | undefined;
}

export interface VersionedSearchLimits {
  version: bigint;
  limits: SearchLimits | undefined;
  updatedAt?: Date | undefined;
}

export interface GetServerSettingsRequest {
}

export interface GetServerSettingsResponse {
  current: VersionedSearchLimits | undefined;
  defaults: SearchLimits | undefined;
  minimums: SearchLimits | undefined;
  maximums: SearchLimits | undefined;
}

export interface UpdateServerSettingsRequest {
  expectedVersion: bigint;
  limits: SearchLimits | undefined;
}

export interface UpdateServerSettingsResponse {
  current: VersionedSearchLimits | undefined;
  defaults: SearchLimits | undefined;
  minimums: SearchLimits | undefined;
  maximums: SearchLimits | undefined;
}

export interface VersionedUiAppearance {
  version: bigint;
  palette: UiPalette;
  updatedAt?: Date | undefined;
}

export interface GetServerAppearanceRequest {
}

export interface GetServerAppearanceResponse {
  current: VersionedUiAppearance | undefined;
  defaultPalette: UiPalette;
}

export interface UpdateServerAppearanceRequest {
  expectedVersion: bigint;
  palette: UiPalette;
}

export interface UpdateServerAppearanceResponse {
  current: VersionedUiAppearance | undefined;
  defaultPalette: UiPalette;
}

function createBaseSearchLimits(): SearchLimits {
  return {
    maximumRuntime: undefined,
    maximumMemoryBytes: 0n,
    maximumRowsToRead: 0n,
    maximumBytesToRead: 0n,
    maximumGroupedRows: 0n,
    maximumThreads: 0n,
    maximumResultRows: 0n,
    maximumResultBytes: 0n,
    maximumTotalResultBytes: 0n,
    maximumConcurrentSearches: 0,
    resultRetention: undefined,
  };
}

export const SearchLimits: MessageFns<SearchLimits> = {
  encode(message: SearchLimits, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maximumRuntime !== undefined) {
      Duration.encode(message.maximumRuntime, writer.uint32(10).fork()).join();
    }
    if (message.maximumMemoryBytes !== 0n) {
      if (BigInt.asUintN(64, message.maximumMemoryBytes) !== message.maximumMemoryBytes) {
        throw new globalThis.Error("value provided for field message.maximumMemoryBytes of type uint64 too large");
      }
      writer.uint32(16).uint64(message.maximumMemoryBytes);
    }
    if (message.maximumRowsToRead !== 0n) {
      if (BigInt.asUintN(64, message.maximumRowsToRead) !== message.maximumRowsToRead) {
        throw new globalThis.Error("value provided for field message.maximumRowsToRead of type uint64 too large");
      }
      writer.uint32(24).uint64(message.maximumRowsToRead);
    }
    if (message.maximumBytesToRead !== 0n) {
      if (BigInt.asUintN(64, message.maximumBytesToRead) !== message.maximumBytesToRead) {
        throw new globalThis.Error("value provided for field message.maximumBytesToRead of type uint64 too large");
      }
      writer.uint32(32).uint64(message.maximumBytesToRead);
    }
    if (message.maximumGroupedRows !== 0n) {
      if (BigInt.asUintN(64, message.maximumGroupedRows) !== message.maximumGroupedRows) {
        throw new globalThis.Error("value provided for field message.maximumGroupedRows of type uint64 too large");
      }
      writer.uint32(40).uint64(message.maximumGroupedRows);
    }
    if (message.maximumThreads !== 0n) {
      if (BigInt.asUintN(64, message.maximumThreads) !== message.maximumThreads) {
        throw new globalThis.Error("value provided for field message.maximumThreads of type uint64 too large");
      }
      writer.uint32(48).uint64(message.maximumThreads);
    }
    if (message.maximumResultRows !== 0n) {
      if (BigInt.asUintN(64, message.maximumResultRows) !== message.maximumResultRows) {
        throw new globalThis.Error("value provided for field message.maximumResultRows of type uint64 too large");
      }
      writer.uint32(56).uint64(message.maximumResultRows);
    }
    if (message.maximumResultBytes !== 0n) {
      if (BigInt.asUintN(64, message.maximumResultBytes) !== message.maximumResultBytes) {
        throw new globalThis.Error("value provided for field message.maximumResultBytes of type uint64 too large");
      }
      writer.uint32(64).uint64(message.maximumResultBytes);
    }
    if (message.maximumTotalResultBytes !== 0n) {
      if (BigInt.asUintN(64, message.maximumTotalResultBytes) !== message.maximumTotalResultBytes) {
        throw new globalThis.Error("value provided for field message.maximumTotalResultBytes of type uint64 too large");
      }
      writer.uint32(72).uint64(message.maximumTotalResultBytes);
    }
    if (message.maximumConcurrentSearches !== 0) {
      writer.uint32(80).uint32(message.maximumConcurrentSearches);
    }
    if (message.resultRetention !== undefined) {
      Duration.encode(message.resultRetention, writer.uint32(90).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchLimits {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSearchLimits();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.maximumRuntime = Duration.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.maximumMemoryBytes = reader.uint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.maximumRowsToRead = reader.uint64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.maximumBytesToRead = reader.uint64() as bigint;
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.maximumGroupedRows = reader.uint64() as bigint;
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.maximumThreads = reader.uint64() as bigint;
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.maximumResultRows = reader.uint64() as bigint;
            continue;
          }
          case 8: {
            if (tag !== 64) {
              break;
            }

            message.maximumResultBytes = reader.uint64() as bigint;
            continue;
          }
          case 9: {
            if (tag !== 72) {
              break;
            }

            message.maximumTotalResultBytes = reader.uint64() as bigint;
            continue;
          }
          case 10: {
            if (tag !== 80) {
              break;
            }

            message.maximumConcurrentSearches = reader.uint32();
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.resultRetention = Duration.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): SearchLimits {
    return {
      maximumRuntime: isSet(object.maximumRuntime)
        ? Duration.fromJSON(object.maximumRuntime)
        : isSet(object.maximum_runtime)
        ? Duration.fromJSON(object.maximum_runtime)
        : undefined,
      maximumMemoryBytes: isSet(object.maximumMemoryBytes)
        ? BigInt(object.maximumMemoryBytes)
        : isSet(object.maximum_memory_bytes)
        ? BigInt(object.maximum_memory_bytes)
        : 0n,
      maximumRowsToRead: isSet(object.maximumRowsToRead)
        ? BigInt(object.maximumRowsToRead)
        : isSet(object.maximum_rows_to_read)
        ? BigInt(object.maximum_rows_to_read)
        : 0n,
      maximumBytesToRead: isSet(object.maximumBytesToRead)
        ? BigInt(object.maximumBytesToRead)
        : isSet(object.maximum_bytes_to_read)
        ? BigInt(object.maximum_bytes_to_read)
        : 0n,
      maximumGroupedRows: isSet(object.maximumGroupedRows)
        ? BigInt(object.maximumGroupedRows)
        : isSet(object.maximum_grouped_rows)
        ? BigInt(object.maximum_grouped_rows)
        : 0n,
      maximumThreads: isSet(object.maximumThreads)
        ? BigInt(object.maximumThreads)
        : isSet(object.maximum_threads)
        ? BigInt(object.maximum_threads)
        : 0n,
      maximumResultRows: isSet(object.maximumResultRows)
        ? BigInt(object.maximumResultRows)
        : isSet(object.maximum_result_rows)
        ? BigInt(object.maximum_result_rows)
        : 0n,
      maximumResultBytes: isSet(object.maximumResultBytes)
        ? BigInt(object.maximumResultBytes)
        : isSet(object.maximum_result_bytes)
        ? BigInt(object.maximum_result_bytes)
        : 0n,
      maximumTotalResultBytes: isSet(object.maximumTotalResultBytes)
        ? BigInt(object.maximumTotalResultBytes)
        : isSet(object.maximum_total_result_bytes)
        ? BigInt(object.maximum_total_result_bytes)
        : 0n,
      maximumConcurrentSearches: isSet(object.maximumConcurrentSearches)
        ? globalThis.Number(object.maximumConcurrentSearches)
        : isSet(object.maximum_concurrent_searches)
        ? globalThis.Number(object.maximum_concurrent_searches)
        : 0,
      resultRetention: isSet(object.resultRetention)
        ? Duration.fromJSON(object.resultRetention)
        : isSet(object.result_retention)
        ? Duration.fromJSON(object.result_retention)
        : undefined,
    };
  },

  toJSON(message: SearchLimits): unknown {
    const obj: any = {};
    if (message.maximumRuntime !== undefined) {
      obj.maximumRuntime = Duration.toJSON(message.maximumRuntime);
    }
    if (message.maximumMemoryBytes !== 0n) {
      obj.maximumMemoryBytes = message.maximumMemoryBytes.toString();
    }
    if (message.maximumRowsToRead !== 0n) {
      obj.maximumRowsToRead = message.maximumRowsToRead.toString();
    }
    if (message.maximumBytesToRead !== 0n) {
      obj.maximumBytesToRead = message.maximumBytesToRead.toString();
    }
    if (message.maximumGroupedRows !== 0n) {
      obj.maximumGroupedRows = message.maximumGroupedRows.toString();
    }
    if (message.maximumThreads !== 0n) {
      obj.maximumThreads = message.maximumThreads.toString();
    }
    if (message.maximumResultRows !== 0n) {
      obj.maximumResultRows = message.maximumResultRows.toString();
    }
    if (message.maximumResultBytes !== 0n) {
      obj.maximumResultBytes = message.maximumResultBytes.toString();
    }
    if (message.maximumTotalResultBytes !== 0n) {
      obj.maximumTotalResultBytes = message.maximumTotalResultBytes.toString();
    }
    if (message.maximumConcurrentSearches !== 0) {
      obj.maximumConcurrentSearches = Math.round(message.maximumConcurrentSearches);
    }
    if (message.resultRetention !== undefined) {
      obj.resultRetention = Duration.toJSON(message.resultRetention);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchLimits>, I>>(base?: I): SearchLimits {
    return SearchLimits.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchLimits>, I>>(object: I): SearchLimits {
    const message = createBaseSearchLimits();
    message.maximumRuntime = (object.maximumRuntime !== undefined && object.maximumRuntime !== null)
      ? Duration.fromPartial(object.maximumRuntime)
      : undefined;
    message.maximumMemoryBytes = (object.maximumMemoryBytes !== undefined && object.maximumMemoryBytes !== null)
      ? BigInt(object.maximumMemoryBytes)
      : 0n;
    message.maximumRowsToRead = (object.maximumRowsToRead !== undefined && object.maximumRowsToRead !== null)
      ? BigInt(object.maximumRowsToRead)
      : 0n;
    message.maximumBytesToRead = (object.maximumBytesToRead !== undefined && object.maximumBytesToRead !== null)
      ? BigInt(object.maximumBytesToRead)
      : 0n;
    message.maximumGroupedRows = (object.maximumGroupedRows !== undefined && object.maximumGroupedRows !== null)
      ? BigInt(object.maximumGroupedRows)
      : 0n;
    message.maximumThreads = (object.maximumThreads !== undefined && object.maximumThreads !== null)
      ? BigInt(object.maximumThreads)
      : 0n;
    message.maximumResultRows = (object.maximumResultRows !== undefined && object.maximumResultRows !== null)
      ? BigInt(object.maximumResultRows)
      : 0n;
    message.maximumResultBytes = (object.maximumResultBytes !== undefined && object.maximumResultBytes !== null)
      ? BigInt(object.maximumResultBytes)
      : 0n;
    message.maximumTotalResultBytes =
      (object.maximumTotalResultBytes !== undefined && object.maximumTotalResultBytes !== null)
        ? BigInt(object.maximumTotalResultBytes)
        : 0n;
    message.maximumConcurrentSearches = object.maximumConcurrentSearches ?? 0;
    message.resultRetention = (object.resultRetention !== undefined && object.resultRetention !== null)
      ? Duration.fromPartial(object.resultRetention)
      : undefined;
    return message;
  },
};

function createBaseVersionedSearchLimits(): VersionedSearchLimits {
  return { version: 0n, limits: undefined, updatedAt: undefined };
}

export const VersionedSearchLimits: MessageFns<VersionedSearchLimits> = {
  encode(message: VersionedSearchLimits, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== 0n) {
      if (BigInt.asUintN(64, message.version) !== message.version) {
        throw new globalThis.Error("value provided for field message.version of type uint64 too large");
      }
      writer.uint32(8).uint64(message.version);
    }
    if (message.limits !== undefined) {
      SearchLimits.encode(message.limits, writer.uint32(18).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VersionedSearchLimits {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseVersionedSearchLimits();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.version = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.limits = SearchLimits.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): VersionedSearchLimits {
    return {
      version: isSet(object.version) ? BigInt(object.version) : 0n,
      limits: isSet(object.limits) ? SearchLimits.fromJSON(object.limits) : undefined,
      updatedAt: isSet(object.updatedAt)
        ? fromJsonTimestamp(object.updatedAt)
        : isSet(object.updated_at)
        ? fromJsonTimestamp(object.updated_at)
        : undefined,
    };
  },

  toJSON(message: VersionedSearchLimits): unknown {
    const obj: any = {};
    if (message.version !== 0n) {
      obj.version = message.version.toString();
    }
    if (message.limits !== undefined) {
      obj.limits = SearchLimits.toJSON(message.limits);
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VersionedSearchLimits>, I>>(base?: I): VersionedSearchLimits {
    return VersionedSearchLimits.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VersionedSearchLimits>, I>>(object: I): VersionedSearchLimits {
    const message = createBaseVersionedSearchLimits();
    message.version = (object.version !== undefined && object.version !== null) ? BigInt(object.version) : 0n;
    message.limits = (object.limits !== undefined && object.limits !== null)
      ? SearchLimits.fromPartial(object.limits)
      : undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    return message;
  },
};

function createBaseGetServerSettingsRequest(): GetServerSettingsRequest {
  return {};
}

export const GetServerSettingsRequest: MessageFns<GetServerSettingsRequest> = {
  encode(_: GetServerSettingsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServerSettingsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetServerSettingsRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): GetServerSettingsRequest {
    return {};
  },

  toJSON(_: GetServerSettingsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetServerSettingsRequest>, I>>(base?: I): GetServerSettingsRequest {
    return GetServerSettingsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetServerSettingsRequest>, I>>(_: I): GetServerSettingsRequest {
    const message = createBaseGetServerSettingsRequest();
    return message;
  },
};

function createBaseGetServerSettingsResponse(): GetServerSettingsResponse {
  return { current: undefined, defaults: undefined, minimums: undefined, maximums: undefined };
}

export const GetServerSettingsResponse: MessageFns<GetServerSettingsResponse> = {
  encode(message: GetServerSettingsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.current !== undefined) {
      VersionedSearchLimits.encode(message.current, writer.uint32(10).fork()).join();
    }
    if (message.defaults !== undefined) {
      SearchLimits.encode(message.defaults, writer.uint32(18).fork()).join();
    }
    if (message.minimums !== undefined) {
      SearchLimits.encode(message.minimums, writer.uint32(26).fork()).join();
    }
    if (message.maximums !== undefined) {
      SearchLimits.encode(message.maximums, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServerSettingsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetServerSettingsResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.current = VersionedSearchLimits.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.defaults = SearchLimits.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.minimums = SearchLimits.decode(reader, reader.uint32());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.maximums = SearchLimits.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GetServerSettingsResponse {
    return {
      current: isSet(object.current) ? VersionedSearchLimits.fromJSON(object.current) : undefined,
      defaults: isSet(object.defaults) ? SearchLimits.fromJSON(object.defaults) : undefined,
      minimums: isSet(object.minimums) ? SearchLimits.fromJSON(object.minimums) : undefined,
      maximums: isSet(object.maximums) ? SearchLimits.fromJSON(object.maximums) : undefined,
    };
  },

  toJSON(message: GetServerSettingsResponse): unknown {
    const obj: any = {};
    if (message.current !== undefined) {
      obj.current = VersionedSearchLimits.toJSON(message.current);
    }
    if (message.defaults !== undefined) {
      obj.defaults = SearchLimits.toJSON(message.defaults);
    }
    if (message.minimums !== undefined) {
      obj.minimums = SearchLimits.toJSON(message.minimums);
    }
    if (message.maximums !== undefined) {
      obj.maximums = SearchLimits.toJSON(message.maximums);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetServerSettingsResponse>, I>>(base?: I): GetServerSettingsResponse {
    return GetServerSettingsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetServerSettingsResponse>, I>>(object: I): GetServerSettingsResponse {
    const message = createBaseGetServerSettingsResponse();
    message.current = (object.current !== undefined && object.current !== null)
      ? VersionedSearchLimits.fromPartial(object.current)
      : undefined;
    message.defaults = (object.defaults !== undefined && object.defaults !== null)
      ? SearchLimits.fromPartial(object.defaults)
      : undefined;
    message.minimums = (object.minimums !== undefined && object.minimums !== null)
      ? SearchLimits.fromPartial(object.minimums)
      : undefined;
    message.maximums = (object.maximums !== undefined && object.maximums !== null)
      ? SearchLimits.fromPartial(object.maximums)
      : undefined;
    return message;
  },
};

function createBaseUpdateServerSettingsRequest(): UpdateServerSettingsRequest {
  return { expectedVersion: 0n, limits: undefined };
}

export const UpdateServerSettingsRequest: MessageFns<UpdateServerSettingsRequest> = {
  encode(message: UpdateServerSettingsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.expectedVersion !== 0n) {
      if (BigInt.asUintN(64, message.expectedVersion) !== message.expectedVersion) {
        throw new globalThis.Error("value provided for field message.expectedVersion of type uint64 too large");
      }
      writer.uint32(8).uint64(message.expectedVersion);
    }
    if (message.limits !== undefined) {
      SearchLimits.encode(message.limits, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateServerSettingsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseUpdateServerSettingsRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.expectedVersion = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.limits = SearchLimits.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): UpdateServerSettingsRequest {
    return {
      expectedVersion: isSet(object.expectedVersion)
        ? BigInt(object.expectedVersion)
        : isSet(object.expected_version)
        ? BigInt(object.expected_version)
        : 0n,
      limits: isSet(object.limits) ? SearchLimits.fromJSON(object.limits) : undefined,
    };
  },

  toJSON(message: UpdateServerSettingsRequest): unknown {
    const obj: any = {};
    if (message.expectedVersion !== 0n) {
      obj.expectedVersion = message.expectedVersion.toString();
    }
    if (message.limits !== undefined) {
      obj.limits = SearchLimits.toJSON(message.limits);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateServerSettingsRequest>, I>>(base?: I): UpdateServerSettingsRequest {
    return UpdateServerSettingsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateServerSettingsRequest>, I>>(object: I): UpdateServerSettingsRequest {
    const message = createBaseUpdateServerSettingsRequest();
    message.expectedVersion = (object.expectedVersion !== undefined && object.expectedVersion !== null)
      ? BigInt(object.expectedVersion)
      : 0n;
    message.limits = (object.limits !== undefined && object.limits !== null)
      ? SearchLimits.fromPartial(object.limits)
      : undefined;
    return message;
  },
};

function createBaseUpdateServerSettingsResponse(): UpdateServerSettingsResponse {
  return { current: undefined, defaults: undefined, minimums: undefined, maximums: undefined };
}

export const UpdateServerSettingsResponse: MessageFns<UpdateServerSettingsResponse> = {
  encode(message: UpdateServerSettingsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.current !== undefined) {
      VersionedSearchLimits.encode(message.current, writer.uint32(10).fork()).join();
    }
    if (message.defaults !== undefined) {
      SearchLimits.encode(message.defaults, writer.uint32(18).fork()).join();
    }
    if (message.minimums !== undefined) {
      SearchLimits.encode(message.minimums, writer.uint32(26).fork()).join();
    }
    if (message.maximums !== undefined) {
      SearchLimits.encode(message.maximums, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateServerSettingsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseUpdateServerSettingsResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.current = VersionedSearchLimits.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.defaults = SearchLimits.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.minimums = SearchLimits.decode(reader, reader.uint32());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.maximums = SearchLimits.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): UpdateServerSettingsResponse {
    return {
      current: isSet(object.current) ? VersionedSearchLimits.fromJSON(object.current) : undefined,
      defaults: isSet(object.defaults) ? SearchLimits.fromJSON(object.defaults) : undefined,
      minimums: isSet(object.minimums) ? SearchLimits.fromJSON(object.minimums) : undefined,
      maximums: isSet(object.maximums) ? SearchLimits.fromJSON(object.maximums) : undefined,
    };
  },

  toJSON(message: UpdateServerSettingsResponse): unknown {
    const obj: any = {};
    if (message.current !== undefined) {
      obj.current = VersionedSearchLimits.toJSON(message.current);
    }
    if (message.defaults !== undefined) {
      obj.defaults = SearchLimits.toJSON(message.defaults);
    }
    if (message.minimums !== undefined) {
      obj.minimums = SearchLimits.toJSON(message.minimums);
    }
    if (message.maximums !== undefined) {
      obj.maximums = SearchLimits.toJSON(message.maximums);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateServerSettingsResponse>, I>>(base?: I): UpdateServerSettingsResponse {
    return UpdateServerSettingsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateServerSettingsResponse>, I>>(object: I): UpdateServerSettingsResponse {
    const message = createBaseUpdateServerSettingsResponse();
    message.current = (object.current !== undefined && object.current !== null)
      ? VersionedSearchLimits.fromPartial(object.current)
      : undefined;
    message.defaults = (object.defaults !== undefined && object.defaults !== null)
      ? SearchLimits.fromPartial(object.defaults)
      : undefined;
    message.minimums = (object.minimums !== undefined && object.minimums !== null)
      ? SearchLimits.fromPartial(object.minimums)
      : undefined;
    message.maximums = (object.maximums !== undefined && object.maximums !== null)
      ? SearchLimits.fromPartial(object.maximums)
      : undefined;
    return message;
  },
};

function createBaseVersionedUiAppearance(): VersionedUiAppearance {
  return { version: 0n, palette: 0, updatedAt: undefined };
}

export const VersionedUiAppearance: MessageFns<VersionedUiAppearance> = {
  encode(message: VersionedUiAppearance, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== 0n) {
      if (BigInt.asUintN(64, message.version) !== message.version) {
        throw new globalThis.Error("value provided for field message.version of type uint64 too large");
      }
      writer.uint32(8).uint64(message.version);
    }
    if (message.palette !== 0) {
      writer.uint32(16).int32(message.palette);
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VersionedUiAppearance {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseVersionedUiAppearance();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.version = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.palette = reader.int32() as any;
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): VersionedUiAppearance {
    return {
      version: isSet(object.version) ? BigInt(object.version) : 0n,
      palette: isSet(object.palette) ? uiPaletteFromJSON(object.palette) : 0,
      updatedAt: isSet(object.updatedAt)
        ? fromJsonTimestamp(object.updatedAt)
        : isSet(object.updated_at)
        ? fromJsonTimestamp(object.updated_at)
        : undefined,
    };
  },

  toJSON(message: VersionedUiAppearance): unknown {
    const obj: any = {};
    if (message.version !== 0n) {
      obj.version = message.version.toString();
    }
    if (message.palette !== 0) {
      obj.palette = uiPaletteToJSON(message.palette);
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VersionedUiAppearance>, I>>(base?: I): VersionedUiAppearance {
    return VersionedUiAppearance.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VersionedUiAppearance>, I>>(object: I): VersionedUiAppearance {
    const message = createBaseVersionedUiAppearance();
    message.version = (object.version !== undefined && object.version !== null) ? BigInt(object.version) : 0n;
    message.palette = object.palette ?? 0;
    message.updatedAt = object.updatedAt ?? undefined;
    return message;
  },
};

function createBaseGetServerAppearanceRequest(): GetServerAppearanceRequest {
  return {};
}

export const GetServerAppearanceRequest: MessageFns<GetServerAppearanceRequest> = {
  encode(_: GetServerAppearanceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServerAppearanceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetServerAppearanceRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): GetServerAppearanceRequest {
    return {};
  },

  toJSON(_: GetServerAppearanceRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetServerAppearanceRequest>, I>>(base?: I): GetServerAppearanceRequest {
    return GetServerAppearanceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetServerAppearanceRequest>, I>>(_: I): GetServerAppearanceRequest {
    const message = createBaseGetServerAppearanceRequest();
    return message;
  },
};

function createBaseGetServerAppearanceResponse(): GetServerAppearanceResponse {
  return { current: undefined, defaultPalette: 0 };
}

export const GetServerAppearanceResponse: MessageFns<GetServerAppearanceResponse> = {
  encode(message: GetServerAppearanceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.current !== undefined) {
      VersionedUiAppearance.encode(message.current, writer.uint32(10).fork()).join();
    }
    if (message.defaultPalette !== 0) {
      writer.uint32(16).int32(message.defaultPalette);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServerAppearanceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetServerAppearanceResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.current = VersionedUiAppearance.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.defaultPalette = reader.int32() as any;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GetServerAppearanceResponse {
    return {
      current: isSet(object.current) ? VersionedUiAppearance.fromJSON(object.current) : undefined,
      defaultPalette: isSet(object.defaultPalette)
        ? uiPaletteFromJSON(object.defaultPalette)
        : isSet(object.default_palette)
        ? uiPaletteFromJSON(object.default_palette)
        : 0,
    };
  },

  toJSON(message: GetServerAppearanceResponse): unknown {
    const obj: any = {};
    if (message.current !== undefined) {
      obj.current = VersionedUiAppearance.toJSON(message.current);
    }
    if (message.defaultPalette !== 0) {
      obj.defaultPalette = uiPaletteToJSON(message.defaultPalette);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetServerAppearanceResponse>, I>>(base?: I): GetServerAppearanceResponse {
    return GetServerAppearanceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetServerAppearanceResponse>, I>>(object: I): GetServerAppearanceResponse {
    const message = createBaseGetServerAppearanceResponse();
    message.current = (object.current !== undefined && object.current !== null)
      ? VersionedUiAppearance.fromPartial(object.current)
      : undefined;
    message.defaultPalette = object.defaultPalette ?? 0;
    return message;
  },
};

function createBaseUpdateServerAppearanceRequest(): UpdateServerAppearanceRequest {
  return { expectedVersion: 0n, palette: 0 };
}

export const UpdateServerAppearanceRequest: MessageFns<UpdateServerAppearanceRequest> = {
  encode(message: UpdateServerAppearanceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.expectedVersion !== 0n) {
      if (BigInt.asUintN(64, message.expectedVersion) !== message.expectedVersion) {
        throw new globalThis.Error("value provided for field message.expectedVersion of type uint64 too large");
      }
      writer.uint32(8).uint64(message.expectedVersion);
    }
    if (message.palette !== 0) {
      writer.uint32(16).int32(message.palette);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateServerAppearanceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseUpdateServerAppearanceRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.expectedVersion = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.palette = reader.int32() as any;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): UpdateServerAppearanceRequest {
    return {
      expectedVersion: isSet(object.expectedVersion)
        ? BigInt(object.expectedVersion)
        : isSet(object.expected_version)
        ? BigInt(object.expected_version)
        : 0n,
      palette: isSet(object.palette) ? uiPaletteFromJSON(object.palette) : 0,
    };
  },

  toJSON(message: UpdateServerAppearanceRequest): unknown {
    const obj: any = {};
    if (message.expectedVersion !== 0n) {
      obj.expectedVersion = message.expectedVersion.toString();
    }
    if (message.palette !== 0) {
      obj.palette = uiPaletteToJSON(message.palette);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateServerAppearanceRequest>, I>>(base?: I): UpdateServerAppearanceRequest {
    return UpdateServerAppearanceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateServerAppearanceRequest>, I>>(
    object: I,
  ): UpdateServerAppearanceRequest {
    const message = createBaseUpdateServerAppearanceRequest();
    message.expectedVersion = (object.expectedVersion !== undefined && object.expectedVersion !== null)
      ? BigInt(object.expectedVersion)
      : 0n;
    message.palette = object.palette ?? 0;
    return message;
  },
};

function createBaseUpdateServerAppearanceResponse(): UpdateServerAppearanceResponse {
  return { current: undefined, defaultPalette: 0 };
}

export const UpdateServerAppearanceResponse: MessageFns<UpdateServerAppearanceResponse> = {
  encode(message: UpdateServerAppearanceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.current !== undefined) {
      VersionedUiAppearance.encode(message.current, writer.uint32(10).fork()).join();
    }
    if (message.defaultPalette !== 0) {
      writer.uint32(16).int32(message.defaultPalette);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateServerAppearanceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseUpdateServerAppearanceResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.current = VersionedUiAppearance.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.defaultPalette = reader.int32() as any;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): UpdateServerAppearanceResponse {
    return {
      current: isSet(object.current) ? VersionedUiAppearance.fromJSON(object.current) : undefined,
      defaultPalette: isSet(object.defaultPalette)
        ? uiPaletteFromJSON(object.defaultPalette)
        : isSet(object.default_palette)
        ? uiPaletteFromJSON(object.default_palette)
        : 0,
    };
  },

  toJSON(message: UpdateServerAppearanceResponse): unknown {
    const obj: any = {};
    if (message.current !== undefined) {
      obj.current = VersionedUiAppearance.toJSON(message.current);
    }
    if (message.defaultPalette !== 0) {
      obj.defaultPalette = uiPaletteToJSON(message.defaultPalette);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateServerAppearanceResponse>, I>>(base?: I): UpdateServerAppearanceResponse {
    return UpdateServerAppearanceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateServerAppearanceResponse>, I>>(
    object: I,
  ): UpdateServerAppearanceResponse {
    const message = createBaseUpdateServerAppearanceResponse();
    message.current = (object.current !== undefined && object.current !== null)
      ? VersionedUiAppearance.fromPartial(object.current)
      : undefined;
    message.defaultPalette = object.defaultPalette ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
