// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: open_splunk/lookup.proto

import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../google/protobuf/timestamp";
import { SharingScope, sharingScopeFromJSON, sharingScopeToJSON } from "./common";
import {
  KnowledgeOverwriteBehavior,
  knowledgeOverwriteBehaviorFromJSON,
  knowledgeOverwriteBehaviorToJSON,
  KnowledgeSelector,
} from "./knowledge";

/**
 * LookupState is the durable lifecycle of one logical lookup. Staged upload
 * bytes are private implementation state and never appear in this projection.
 */
export enum LookupState {
  LOOKUP_STATE_UNSPECIFIED = 0,
  LOOKUP_STATE_ACTIVE = 1,
  LOOKUP_STATE_DISABLED = 2,
  LOOKUP_STATE_DELETED = 3,
  UNRECOGNIZED = -1,
}

export function lookupStateFromJSON(object: any): LookupState {
  switch (object) {
    case 0:
    case "LOOKUP_STATE_UNSPECIFIED":
      return LookupState.LOOKUP_STATE_UNSPECIFIED;
    case 1:
    case "LOOKUP_STATE_ACTIVE":
      return LookupState.LOOKUP_STATE_ACTIVE;
    case 2:
    case "LOOKUP_STATE_DISABLED":
      return LookupState.LOOKUP_STATE_DISABLED;
    case 3:
    case "LOOKUP_STATE_DELETED":
      return LookupState.LOOKUP_STATE_DELETED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return LookupState.UNRECOGNIZED;
  }
}

export function lookupStateToJSON(object: LookupState): string {
  switch (object) {
    case LookupState.LOOKUP_STATE_UNSPECIFIED:
      return "LOOKUP_STATE_UNSPECIFIED";
    case LookupState.LOOKUP_STATE_ACTIVE:
      return "LOOKUP_STATE_ACTIVE";
    case LookupState.LOOKUP_STATE_DISABLED:
      return "LOOKUP_STATE_DISABLED";
    case LookupState.LOOKUP_STATE_DELETED:
      return "LOOKUP_STATE_DELETED";
    case LookupState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface LookupFieldMapping {
  lookupField: string;
  eventField: string;
}

/**
 * LookupDefinition owns logical matching and enrichment. csv_data is carried
 * only by mutation requests and is never echoed through catalog responses.
 */
export interface LookupDefinition {
  appId: string;
  name: string;
  description?: string | undefined;
  sharingScope: SharingScope;
  selector: KnowledgeSelector | undefined;
  automatic: boolean;
  keyMappings: LookupFieldMapping[];
  outputMappings: LookupFieldMapping[];
  overwriteBehavior: KnowledgeOverwriteBehavior;
}

/** Lookup is the bounded safe projection of one immutable published version. */
export interface Lookup {
  lookupId: string;
  tenantId: string;
  ownerId: string;
  version: bigint;
  state: LookupState;
  definition: LookupDefinition | undefined;
  columns: string[];
  rowCount: bigint;
  canonicalSizeBytes: bigint;
  sourceSha256: Uint8Array;
  contentSha256: Uint8Array;
  createdAt: Date | undefined;
  updatedAt: Date | undefined;
  disabledAt?: Date | undefined;
  deletedAt?: Date | undefined;
}

export interface LookupVersionReference {
  lookupId: string;
  version: bigint;
  contentSha256: Uint8Array;
  canonicalSizeBytes: bigint;
  rowCount: bigint;
}

function createBaseLookupFieldMapping(): LookupFieldMapping {
  return { lookupField: "", eventField: "" };
}

export const LookupFieldMapping: MessageFns<LookupFieldMapping> = {
  encode(message: LookupFieldMapping, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lookupField !== "") {
      writer.uint32(10).string(message.lookupField);
    }
    if (message.eventField !== "") {
      writer.uint32(18).string(message.eventField);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LookupFieldMapping {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseLookupFieldMapping();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.lookupField = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.eventField = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): LookupFieldMapping {
    return {
      lookupField: isSet(object.lookupField)
        ? globalThis.String(object.lookupField)
        : isSet(object.lookup_field)
        ? globalThis.String(object.lookup_field)
        : "",
      eventField: isSet(object.eventField)
        ? globalThis.String(object.eventField)
        : isSet(object.event_field)
        ? globalThis.String(object.event_field)
        : "",
    };
  },

  toJSON(message: LookupFieldMapping): unknown {
    const obj: any = {};
    if (message.lookupField !== "") {
      obj.lookupField = message.lookupField;
    }
    if (message.eventField !== "") {
      obj.eventField = message.eventField;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LookupFieldMapping>, I>>(base?: I): LookupFieldMapping {
    return LookupFieldMapping.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LookupFieldMapping>, I>>(object: I): LookupFieldMapping {
    const message = createBaseLookupFieldMapping();
    message.lookupField = object.lookupField ?? "";
    message.eventField = object.eventField ?? "";
    return message;
  },
};

function createBaseLookupDefinition(): LookupDefinition {
  return {
    appId: "",
    name: "",
    description: undefined,
    sharingScope: 0,
    selector: undefined,
    automatic: false,
    keyMappings: [],
    outputMappings: [],
    overwriteBehavior: 0,
  };
}

export const LookupDefinition: MessageFns<LookupDefinition> = {
  encode(message: LookupDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.appId !== "") {
      writer.uint32(10).string(message.appId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.description !== undefined) {
      writer.uint32(26).string(message.description);
    }
    if (message.sharingScope !== 0) {
      writer.uint32(32).int32(message.sharingScope);
    }
    if (message.selector !== undefined) {
      KnowledgeSelector.encode(message.selector, writer.uint32(42).fork()).join();
    }
    if (message.automatic !== false) {
      writer.uint32(48).bool(message.automatic);
    }
    for (const v of message.keyMappings) {
      LookupFieldMapping.encode(v!, writer.uint32(58).fork()).join();
    }
    for (const v of message.outputMappings) {
      LookupFieldMapping.encode(v!, writer.uint32(66).fork()).join();
    }
    if (message.overwriteBehavior !== 0) {
      writer.uint32(72).int32(message.overwriteBehavior);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LookupDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseLookupDefinition();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.appId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.name = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.description = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.sharingScope = reader.int32() as any;
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.selector = KnowledgeSelector.decode(reader, reader.uint32());
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.automatic = reader.bool();
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.keyMappings.push(LookupFieldMapping.decode(reader, reader.uint32()));
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.outputMappings.push(LookupFieldMapping.decode(reader, reader.uint32()));
            continue;
          }
          case 9: {
            if (tag !== 72) {
              break;
            }

            message.overwriteBehavior = reader.int32() as any;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): LookupDefinition {
    return {
      appId: isSet(object.appId)
        ? globalThis.String(object.appId)
        : isSet(object.app_id)
        ? globalThis.String(object.app_id)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
      sharingScope: isSet(object.sharingScope)
        ? sharingScopeFromJSON(object.sharingScope)
        : isSet(object.sharing_scope)
        ? sharingScopeFromJSON(object.sharing_scope)
        : 0,
      selector: isSet(object.selector) ? KnowledgeSelector.fromJSON(object.selector) : undefined,
      automatic: isSet(object.automatic) ? globalThis.Boolean(object.automatic) : false,
      keyMappings: globalThis.Array.isArray(object?.keyMappings)
        ? object.keyMappings.map((e: any) => LookupFieldMapping.fromJSON(e))
        : globalThis.Array.isArray(object?.key_mappings)
        ? object.key_mappings.map((e: any) => LookupFieldMapping.fromJSON(e))
        : [],
      outputMappings: globalThis.Array.isArray(object?.outputMappings)
        ? object.outputMappings.map((e: any) => LookupFieldMapping.fromJSON(e))
        : globalThis.Array.isArray(object?.output_mappings)
        ? object.output_mappings.map((e: any) => LookupFieldMapping.fromJSON(e))
        : [],
      overwriteBehavior: isSet(object.overwriteBehavior)
        ? knowledgeOverwriteBehaviorFromJSON(object.overwriteBehavior)
        : isSet(object.overwrite_behavior)
        ? knowledgeOverwriteBehaviorFromJSON(object.overwrite_behavior)
        : 0,
    };
  },

  toJSON(message: LookupDefinition): unknown {
    const obj: any = {};
    if (message.appId !== "") {
      obj.appId = message.appId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.sharingScope !== 0) {
      obj.sharingScope = sharingScopeToJSON(message.sharingScope);
    }
    if (message.selector !== undefined) {
      obj.selector = KnowledgeSelector.toJSON(message.selector);
    }
    if (message.automatic !== false) {
      obj.automatic = message.automatic;
    }
    if (message.keyMappings?.length) {
      obj.keyMappings = message.keyMappings.map((e) => LookupFieldMapping.toJSON(e));
    }
    if (message.outputMappings?.length) {
      obj.outputMappings = message.outputMappings.map((e) => LookupFieldMapping.toJSON(e));
    }
    if (message.overwriteBehavior !== 0) {
      obj.overwriteBehavior = knowledgeOverwriteBehaviorToJSON(message.overwriteBehavior);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LookupDefinition>, I>>(base?: I): LookupDefinition {
    return LookupDefinition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LookupDefinition>, I>>(object: I): LookupDefinition {
    const message = createBaseLookupDefinition();
    message.appId = object.appId ?? "";
    message.name = object.name ?? "";
    message.description = object.description ?? undefined;
    message.sharingScope = object.sharingScope ?? 0;
    message.selector = (object.selector !== undefined && object.selector !== null)
      ? KnowledgeSelector.fromPartial(object.selector)
      : undefined;
    message.automatic = object.automatic ?? false;
    message.keyMappings = object.keyMappings?.map((e) => LookupFieldMapping.fromPartial(e)) || [];
    message.outputMappings = object.outputMappings?.map((e) => LookupFieldMapping.fromPartial(e)) || [];
    message.overwriteBehavior = object.overwriteBehavior ?? 0;
    return message;
  },
};

function createBaseLookup(): Lookup {
  return {
    lookupId: "",
    tenantId: "",
    ownerId: "",
    version: 0n,
    state: 0,
    definition: undefined,
    columns: [],
    rowCount: 0n,
    canonicalSizeBytes: 0n,
    sourceSha256: new Uint8Array(0),
    contentSha256: new Uint8Array(0),
    createdAt: undefined,
    updatedAt: undefined,
    disabledAt: undefined,
    deletedAt: undefined,
  };
}

export const Lookup: MessageFns<Lookup> = {
  encode(message: Lookup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lookupId !== "") {
      writer.uint32(10).string(message.lookupId);
    }
    if (message.tenantId !== "") {
      writer.uint32(18).string(message.tenantId);
    }
    if (message.ownerId !== "") {
      writer.uint32(26).string(message.ownerId);
    }
    if (message.version !== 0n) {
      if (BigInt.asUintN(64, message.version) !== message.version) {
        throw new globalThis.Error("value provided for field message.version of type uint64 too large");
      }
      writer.uint32(32).uint64(message.version);
    }
    if (message.state !== 0) {
      writer.uint32(40).int32(message.state);
    }
    if (message.definition !== undefined) {
      LookupDefinition.encode(message.definition, writer.uint32(50).fork()).join();
    }
    for (const v of message.columns) {
      writer.uint32(58).string(v!);
    }
    if (message.rowCount !== 0n) {
      if (BigInt.asUintN(64, message.rowCount) !== message.rowCount) {
        throw new globalThis.Error("value provided for field message.rowCount of type uint64 too large");
      }
      writer.uint32(64).uint64(message.rowCount);
    }
    if (message.canonicalSizeBytes !== 0n) {
      if (BigInt.asUintN(64, message.canonicalSizeBytes) !== message.canonicalSizeBytes) {
        throw new globalThis.Error("value provided for field message.canonicalSizeBytes of type uint64 too large");
      }
      writer.uint32(72).uint64(message.canonicalSizeBytes);
    }
    if (message.sourceSha256.length !== 0) {
      writer.uint32(82).bytes(message.sourceSha256);
    }
    if (message.contentSha256.length !== 0) {
      writer.uint32(90).bytes(message.contentSha256);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(98).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(106).fork()).join();
    }
    if (message.disabledAt !== undefined) {
      Timestamp.encode(toTimestamp(message.disabledAt), writer.uint32(114).fork()).join();
    }
    if (message.deletedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.deletedAt), writer.uint32(122).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Lookup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseLookup();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.lookupId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.tenantId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.ownerId = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.version = reader.uint64() as bigint;
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.state = reader.int32() as any;
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.definition = LookupDefinition.decode(reader, reader.uint32());
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.columns.push(reader.string());
            continue;
          }
          case 8: {
            if (tag !== 64) {
              break;
            }

            message.rowCount = reader.uint64() as bigint;
            continue;
          }
          case 9: {
            if (tag !== 72) {
              break;
            }

            message.canonicalSizeBytes = reader.uint64() as bigint;
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.sourceSha256 = reader.bytes();
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.contentSha256 = reader.bytes();
            continue;
          }
          case 12: {
            if (tag !== 98) {
              break;
            }

            message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
          case 13: {
            if (tag !== 106) {
              break;
            }

            message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
          case 14: {
            if (tag !== 114) {
              break;
            }

            message.disabledAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
          case 15: {
            if (tag !== 122) {
              break;
            }

            message.deletedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Lookup {
    return {
      lookupId: isSet(object.lookupId)
        ? globalThis.String(object.lookupId)
        : isSet(object.lookup_id)
        ? globalThis.String(object.lookup_id)
        : "",
      tenantId: isSet(object.tenantId)
        ? globalThis.String(object.tenantId)
        : isSet(object.tenant_id)
        ? globalThis.String(object.tenant_id)
        : "",
      ownerId: isSet(object.ownerId)
        ? globalThis.String(object.ownerId)
        : isSet(object.owner_id)
        ? globalThis.String(object.owner_id)
        : "",
      version: isSet(object.version) ? BigInt(object.version) : 0n,
      state: isSet(object.state) ? lookupStateFromJSON(object.state) : 0,
      definition: isSet(object.definition) ? LookupDefinition.fromJSON(object.definition) : undefined,
      columns: globalThis.Array.isArray(object?.columns) ? object.columns.map((e: any) => globalThis.String(e)) : [],
      rowCount: isSet(object.rowCount)
        ? BigInt(object.rowCount)
        : isSet(object.row_count)
        ? BigInt(object.row_count)
        : 0n,
      canonicalSizeBytes: isSet(object.canonicalSizeBytes)
        ? BigInt(object.canonicalSizeBytes)
        : isSet(object.canonical_size_bytes)
        ? BigInt(object.canonical_size_bytes)
        : 0n,
      sourceSha256: isSet(object.sourceSha256)
        ? bytesFromBase64(object.sourceSha256)
        : isSet(object.source_sha256)
        ? bytesFromBase64(object.source_sha256)
        : new Uint8Array(0),
      contentSha256: isSet(object.contentSha256)
        ? bytesFromBase64(object.contentSha256)
        : isSet(object.content_sha256)
        ? bytesFromBase64(object.content_sha256)
        : new Uint8Array(0),
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      updatedAt: isSet(object.updatedAt)
        ? fromJsonTimestamp(object.updatedAt)
        : isSet(object.updated_at)
        ? fromJsonTimestamp(object.updated_at)
        : undefined,
      disabledAt: isSet(object.disabledAt)
        ? fromJsonTimestamp(object.disabledAt)
        : isSet(object.disabled_at)
        ? fromJsonTimestamp(object.disabled_at)
        : undefined,
      deletedAt: isSet(object.deletedAt)
        ? fromJsonTimestamp(object.deletedAt)
        : isSet(object.deleted_at)
        ? fromJsonTimestamp(object.deleted_at)
        : undefined,
    };
  },

  toJSON(message: Lookup): unknown {
    const obj: any = {};
    if (message.lookupId !== "") {
      obj.lookupId = message.lookupId;
    }
    if (message.tenantId !== "") {
      obj.tenantId = message.tenantId;
    }
    if (message.ownerId !== "") {
      obj.ownerId = message.ownerId;
    }
    if (message.version !== 0n) {
      obj.version = message.version.toString();
    }
    if (message.state !== 0) {
      obj.state = lookupStateToJSON(message.state);
    }
    if (message.definition !== undefined) {
      obj.definition = LookupDefinition.toJSON(message.definition);
    }
    if (message.columns?.length) {
      obj.columns = message.columns;
    }
    if (message.rowCount !== 0n) {
      obj.rowCount = message.rowCount.toString();
    }
    if (message.canonicalSizeBytes !== 0n) {
      obj.canonicalSizeBytes = message.canonicalSizeBytes.toString();
    }
    if (message.sourceSha256.length !== 0) {
      obj.sourceSha256 = base64FromBytes(message.sourceSha256);
    }
    if (message.contentSha256.length !== 0) {
      obj.contentSha256 = base64FromBytes(message.contentSha256);
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.disabledAt !== undefined) {
      obj.disabledAt = message.disabledAt.toISOString();
    }
    if (message.deletedAt !== undefined) {
      obj.deletedAt = message.deletedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Lookup>, I>>(base?: I): Lookup {
    return Lookup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Lookup>, I>>(object: I): Lookup {
    const message = createBaseLookup();
    message.lookupId = object.lookupId ?? "";
    message.tenantId = object.tenantId ?? "";
    message.ownerId = object.ownerId ?? "";
    message.version = (object.version !== undefined && object.version !== null) ? BigInt(object.version) : 0n;
    message.state = object.state ?? 0;
    message.definition = (object.definition !== undefined && object.definition !== null)
      ? LookupDefinition.fromPartial(object.definition)
      : undefined;
    message.columns = object.columns?.map((e) => e) || [];
    message.rowCount = (object.rowCount !== undefined && object.rowCount !== null) ? BigInt(object.rowCount) : 0n;
    message.canonicalSizeBytes = (object.canonicalSizeBytes !== undefined && object.canonicalSizeBytes !== null)
      ? BigInt(object.canonicalSizeBytes)
      : 0n;
    message.sourceSha256 = object.sourceSha256 ?? new Uint8Array(0);
    message.contentSha256 = object.contentSha256 ?? new Uint8Array(0);
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.disabledAt = object.disabledAt ?? undefined;
    message.deletedAt = object.deletedAt ?? undefined;
    return message;
  },
};

function createBaseLookupVersionReference(): LookupVersionReference {
  return { lookupId: "", version: 0n, contentSha256: new Uint8Array(0), canonicalSizeBytes: 0n, rowCount: 0n };
}

export const LookupVersionReference: MessageFns<LookupVersionReference> = {
  encode(message: LookupVersionReference, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lookupId !== "") {
      writer.uint32(10).string(message.lookupId);
    }
    if (message.version !== 0n) {
      if (BigInt.asUintN(64, message.version) !== message.version) {
        throw new globalThis.Error("value provided for field message.version of type uint64 too large");
      }
      writer.uint32(16).uint64(message.version);
    }
    if (message.contentSha256.length !== 0) {
      writer.uint32(26).bytes(message.contentSha256);
    }
    if (message.canonicalSizeBytes !== 0n) {
      if (BigInt.asUintN(64, message.canonicalSizeBytes) !== message.canonicalSizeBytes) {
        throw new globalThis.Error("value provided for field message.canonicalSizeBytes of type uint64 too large");
      }
      writer.uint32(32).uint64(message.canonicalSizeBytes);
    }
    if (message.rowCount !== 0n) {
      if (BigInt.asUintN(64, message.rowCount) !== message.rowCount) {
        throw new globalThis.Error("value provided for field message.rowCount of type uint64 too large");
      }
      writer.uint32(40).uint64(message.rowCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LookupVersionReference {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseLookupVersionReference();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.lookupId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.version = reader.uint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.contentSha256 = reader.bytes();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.canonicalSizeBytes = reader.uint64() as bigint;
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.rowCount = reader.uint64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): LookupVersionReference {
    return {
      lookupId: isSet(object.lookupId)
        ? globalThis.String(object.lookupId)
        : isSet(object.lookup_id)
        ? globalThis.String(object.lookup_id)
        : "",
      version: isSet(object.version) ? BigInt(object.version) : 0n,
      contentSha256: isSet(object.contentSha256)
        ? bytesFromBase64(object.contentSha256)
        : isSet(object.content_sha256)
        ? bytesFromBase64(object.content_sha256)
        : new Uint8Array(0),
      canonicalSizeBytes: isSet(object.canonicalSizeBytes)
        ? BigInt(object.canonicalSizeBytes)
        : isSet(object.canonical_size_bytes)
        ? BigInt(object.canonical_size_bytes)
        : 0n,
      rowCount: isSet(object.rowCount)
        ? BigInt(object.rowCount)
        : isSet(object.row_count)
        ? BigInt(object.row_count)
        : 0n,
    };
  },

  toJSON(message: LookupVersionReference): unknown {
    const obj: any = {};
    if (message.lookupId !== "") {
      obj.lookupId = message.lookupId;
    }
    if (message.version !== 0n) {
      obj.version = message.version.toString();
    }
    if (message.contentSha256.length !== 0) {
      obj.contentSha256 = base64FromBytes(message.contentSha256);
    }
    if (message.canonicalSizeBytes !== 0n) {
      obj.canonicalSizeBytes = message.canonicalSizeBytes.toString();
    }
    if (message.rowCount !== 0n) {
      obj.rowCount = message.rowCount.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LookupVersionReference>, I>>(base?: I): LookupVersionReference {
    return LookupVersionReference.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LookupVersionReference>, I>>(object: I): LookupVersionReference {
    const message = createBaseLookupVersionReference();
    message.lookupId = object.lookupId ?? "";
    message.version = (object.version !== undefined && object.version !== null) ? BigInt(object.version) : 0n;
    message.contentSha256 = object.contentSha256 ?? new Uint8Array(0);
    message.canonicalSizeBytes = (object.canonicalSizeBytes !== undefined && object.canonicalSizeBytes !== null)
      ? BigInt(object.canonicalSizeBytes)
      : 0n;
    message.rowCount = (object.rowCount !== undefined && object.rowCount !== null) ? BigInt(object.rowCount) : 0n;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
