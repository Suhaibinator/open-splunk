// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: open_splunk/hec_admin_api.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../google/protobuf/duration";
import { Timestamp } from "../google/protobuf/timestamp";

/**
 * POST /api/hec/operations/get
 * This route is administrator-only. The request is intentionally empty: HEC
 * The contract exposes one process-wide bounded aggregate and no per-token selector.
 */
export interface GetHECOperationalSnapshotRequest {
}

export interface HECRequestOperationalMetrics {
  requests: bigint;
  events: bigint;
  uncompressedBytes: bigint;
  authenticationFailures: bigint;
  decodeFailures: bigint;
  eventPolicyFailures: bigint;
  acceptedRequests: bigint;
  rateLimitedRequests: bigint;
  stagingFailures: bigint;
  stagingDuration: Duration | undefined;
  shutdownRejections: bigint;
}

export interface HECDurableOperationalMetrics {
  queueAvailable: boolean;
  pendingOutboxReservations: bigint;
  pendingOutboxBytes: bigint;
  oldestPendingOutboxAge: Duration | undefined;
  requestCapacityAvailable: boolean;
  retainedRequests: bigint;
}

export interface HECReconciliationOperationalMetrics {
  available: boolean;
  successes: bigint;
  retries: bigint;
  ambiguities: bigint;
}

export interface HECAcknowledgmentOperationalMetrics {
  available: boolean;
  activeChannels: bigint;
  retainedChannels: bigint;
  pendingRows: bigint;
  indexedRows: bigint;
  expiredRows: bigint;
  terminalFailedRequests: bigint;
  queries: bigint;
  idsQueried: bigint;
  misses: bigint;
}

/**
 * code is one of the closed non-success Splunk-compatible HEC response codes.
 * This list has exactly the bounded domain 1 through 27 and never carries
 * request-provided labels.
 */
export interface HECProtocolFailureMetric {
  code: number;
  count: bigint;
}

export interface GetHECOperationalSnapshotResponse {
  observedAt: Date | undefined;
  request: HECRequestOperationalMetrics | undefined;
  durable: HECDurableOperationalMetrics | undefined;
  reconciliation: HECReconciliationOperationalMetrics | undefined;
  acknowledgments: HECAcknowledgmentOperationalMetrics | undefined;
  protocolFailures: HECProtocolFailureMetric[];
}

function createBaseGetHECOperationalSnapshotRequest(): GetHECOperationalSnapshotRequest {
  return {};
}

export const GetHECOperationalSnapshotRequest: MessageFns<GetHECOperationalSnapshotRequest> = {
  encode(_: GetHECOperationalSnapshotRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetHECOperationalSnapshotRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetHECOperationalSnapshotRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetHECOperationalSnapshotRequest {
    return {};
  },

  toJSON(_: GetHECOperationalSnapshotRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetHECOperationalSnapshotRequest>, I>>(
    base?: I,
  ): GetHECOperationalSnapshotRequest {
    return GetHECOperationalSnapshotRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetHECOperationalSnapshotRequest>, I>>(
    _: I,
  ): GetHECOperationalSnapshotRequest {
    const message = createBaseGetHECOperationalSnapshotRequest();
    return message;
  },
};

function createBaseHECRequestOperationalMetrics(): HECRequestOperationalMetrics {
  return {
    requests: 0n,
    events: 0n,
    uncompressedBytes: 0n,
    authenticationFailures: 0n,
    decodeFailures: 0n,
    eventPolicyFailures: 0n,
    acceptedRequests: 0n,
    rateLimitedRequests: 0n,
    stagingFailures: 0n,
    stagingDuration: undefined,
    shutdownRejections: 0n,
  };
}

export const HECRequestOperationalMetrics: MessageFns<HECRequestOperationalMetrics> = {
  encode(message: HECRequestOperationalMetrics, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requests !== 0n) {
      if (BigInt.asUintN(64, message.requests) !== message.requests) {
        throw new globalThis.Error("value provided for field message.requests of type uint64 too large");
      }
      writer.uint32(8).uint64(message.requests);
    }
    if (message.events !== 0n) {
      if (BigInt.asUintN(64, message.events) !== message.events) {
        throw new globalThis.Error("value provided for field message.events of type uint64 too large");
      }
      writer.uint32(16).uint64(message.events);
    }
    if (message.uncompressedBytes !== 0n) {
      if (BigInt.asUintN(64, message.uncompressedBytes) !== message.uncompressedBytes) {
        throw new globalThis.Error("value provided for field message.uncompressedBytes of type uint64 too large");
      }
      writer.uint32(24).uint64(message.uncompressedBytes);
    }
    if (message.authenticationFailures !== 0n) {
      if (BigInt.asUintN(64, message.authenticationFailures) !== message.authenticationFailures) {
        throw new globalThis.Error("value provided for field message.authenticationFailures of type uint64 too large");
      }
      writer.uint32(32).uint64(message.authenticationFailures);
    }
    if (message.decodeFailures !== 0n) {
      if (BigInt.asUintN(64, message.decodeFailures) !== message.decodeFailures) {
        throw new globalThis.Error("value provided for field message.decodeFailures of type uint64 too large");
      }
      writer.uint32(40).uint64(message.decodeFailures);
    }
    if (message.eventPolicyFailures !== 0n) {
      if (BigInt.asUintN(64, message.eventPolicyFailures) !== message.eventPolicyFailures) {
        throw new globalThis.Error("value provided for field message.eventPolicyFailures of type uint64 too large");
      }
      writer.uint32(48).uint64(message.eventPolicyFailures);
    }
    if (message.acceptedRequests !== 0n) {
      if (BigInt.asUintN(64, message.acceptedRequests) !== message.acceptedRequests) {
        throw new globalThis.Error("value provided for field message.acceptedRequests of type uint64 too large");
      }
      writer.uint32(56).uint64(message.acceptedRequests);
    }
    if (message.rateLimitedRequests !== 0n) {
      if (BigInt.asUintN(64, message.rateLimitedRequests) !== message.rateLimitedRequests) {
        throw new globalThis.Error("value provided for field message.rateLimitedRequests of type uint64 too large");
      }
      writer.uint32(64).uint64(message.rateLimitedRequests);
    }
    if (message.stagingFailures !== 0n) {
      if (BigInt.asUintN(64, message.stagingFailures) !== message.stagingFailures) {
        throw new globalThis.Error("value provided for field message.stagingFailures of type uint64 too large");
      }
      writer.uint32(72).uint64(message.stagingFailures);
    }
    if (message.stagingDuration !== undefined) {
      Duration.encode(message.stagingDuration, writer.uint32(82).fork()).join();
    }
    if (message.shutdownRejections !== 0n) {
      if (BigInt.asUintN(64, message.shutdownRejections) !== message.shutdownRejections) {
        throw new globalThis.Error("value provided for field message.shutdownRejections of type uint64 too large");
      }
      writer.uint32(88).uint64(message.shutdownRejections);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HECRequestOperationalMetrics {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHECRequestOperationalMetrics();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.requests = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.events = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.uncompressedBytes = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.authenticationFailures = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.decodeFailures = reader.uint64() as bigint;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.eventPolicyFailures = reader.uint64() as bigint;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.acceptedRequests = reader.uint64() as bigint;
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.rateLimitedRequests = reader.uint64() as bigint;
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.stagingFailures = reader.uint64() as bigint;
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.stagingDuration = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.shutdownRejections = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HECRequestOperationalMetrics {
    return {
      requests: isSet(object.requests) ? BigInt(object.requests) : 0n,
      events: isSet(object.events) ? BigInt(object.events) : 0n,
      uncompressedBytes: isSet(object.uncompressedBytes)
        ? BigInt(object.uncompressedBytes)
        : isSet(object.uncompressed_bytes)
        ? BigInt(object.uncompressed_bytes)
        : 0n,
      authenticationFailures: isSet(object.authenticationFailures)
        ? BigInt(object.authenticationFailures)
        : isSet(object.authentication_failures)
        ? BigInt(object.authentication_failures)
        : 0n,
      decodeFailures: isSet(object.decodeFailures)
        ? BigInt(object.decodeFailures)
        : isSet(object.decode_failures)
        ? BigInt(object.decode_failures)
        : 0n,
      eventPolicyFailures: isSet(object.eventPolicyFailures)
        ? BigInt(object.eventPolicyFailures)
        : isSet(object.event_policy_failures)
        ? BigInt(object.event_policy_failures)
        : 0n,
      acceptedRequests: isSet(object.acceptedRequests)
        ? BigInt(object.acceptedRequests)
        : isSet(object.accepted_requests)
        ? BigInt(object.accepted_requests)
        : 0n,
      rateLimitedRequests: isSet(object.rateLimitedRequests)
        ? BigInt(object.rateLimitedRequests)
        : isSet(object.rate_limited_requests)
        ? BigInt(object.rate_limited_requests)
        : 0n,
      stagingFailures: isSet(object.stagingFailures)
        ? BigInt(object.stagingFailures)
        : isSet(object.staging_failures)
        ? BigInt(object.staging_failures)
        : 0n,
      stagingDuration: isSet(object.stagingDuration)
        ? Duration.fromJSON(object.stagingDuration)
        : isSet(object.staging_duration)
        ? Duration.fromJSON(object.staging_duration)
        : undefined,
      shutdownRejections: isSet(object.shutdownRejections)
        ? BigInt(object.shutdownRejections)
        : isSet(object.shutdown_rejections)
        ? BigInt(object.shutdown_rejections)
        : 0n,
    };
  },

  toJSON(message: HECRequestOperationalMetrics): unknown {
    const obj: any = {};
    if (message.requests !== 0n) {
      obj.requests = message.requests.toString();
    }
    if (message.events !== 0n) {
      obj.events = message.events.toString();
    }
    if (message.uncompressedBytes !== 0n) {
      obj.uncompressedBytes = message.uncompressedBytes.toString();
    }
    if (message.authenticationFailures !== 0n) {
      obj.authenticationFailures = message.authenticationFailures.toString();
    }
    if (message.decodeFailures !== 0n) {
      obj.decodeFailures = message.decodeFailures.toString();
    }
    if (message.eventPolicyFailures !== 0n) {
      obj.eventPolicyFailures = message.eventPolicyFailures.toString();
    }
    if (message.acceptedRequests !== 0n) {
      obj.acceptedRequests = message.acceptedRequests.toString();
    }
    if (message.rateLimitedRequests !== 0n) {
      obj.rateLimitedRequests = message.rateLimitedRequests.toString();
    }
    if (message.stagingFailures !== 0n) {
      obj.stagingFailures = message.stagingFailures.toString();
    }
    if (message.stagingDuration !== undefined) {
      obj.stagingDuration = Duration.toJSON(message.stagingDuration);
    }
    if (message.shutdownRejections !== 0n) {
      obj.shutdownRejections = message.shutdownRejections.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HECRequestOperationalMetrics>, I>>(base?: I): HECRequestOperationalMetrics {
    return HECRequestOperationalMetrics.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HECRequestOperationalMetrics>, I>>(object: I): HECRequestOperationalMetrics {
    const message = createBaseHECRequestOperationalMetrics();
    message.requests = (object.requests !== undefined && object.requests !== null) ? BigInt(object.requests) : 0n;
    message.events = (object.events !== undefined && object.events !== null) ? BigInt(object.events) : 0n;
    message.uncompressedBytes = (object.uncompressedBytes !== undefined && object.uncompressedBytes !== null)
      ? BigInt(object.uncompressedBytes)
      : 0n;
    message.authenticationFailures =
      (object.authenticationFailures !== undefined && object.authenticationFailures !== null)
        ? BigInt(object.authenticationFailures)
        : 0n;
    message.decodeFailures = (object.decodeFailures !== undefined && object.decodeFailures !== null)
      ? BigInt(object.decodeFailures)
      : 0n;
    message.eventPolicyFailures = (object.eventPolicyFailures !== undefined && object.eventPolicyFailures !== null)
      ? BigInt(object.eventPolicyFailures)
      : 0n;
    message.acceptedRequests = (object.acceptedRequests !== undefined && object.acceptedRequests !== null)
      ? BigInt(object.acceptedRequests)
      : 0n;
    message.rateLimitedRequests = (object.rateLimitedRequests !== undefined && object.rateLimitedRequests !== null)
      ? BigInt(object.rateLimitedRequests)
      : 0n;
    message.stagingFailures = (object.stagingFailures !== undefined && object.stagingFailures !== null)
      ? BigInt(object.stagingFailures)
      : 0n;
    message.stagingDuration = (object.stagingDuration !== undefined && object.stagingDuration !== null)
      ? Duration.fromPartial(object.stagingDuration)
      : undefined;
    message.shutdownRejections = (object.shutdownRejections !== undefined && object.shutdownRejections !== null)
      ? BigInt(object.shutdownRejections)
      : 0n;
    return message;
  },
};

function createBaseHECDurableOperationalMetrics(): HECDurableOperationalMetrics {
  return {
    queueAvailable: false,
    pendingOutboxReservations: 0n,
    pendingOutboxBytes: 0n,
    oldestPendingOutboxAge: undefined,
    requestCapacityAvailable: false,
    retainedRequests: 0n,
  };
}

export const HECDurableOperationalMetrics: MessageFns<HECDurableOperationalMetrics> = {
  encode(message: HECDurableOperationalMetrics, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.queueAvailable !== false) {
      writer.uint32(8).bool(message.queueAvailable);
    }
    if (message.pendingOutboxReservations !== 0n) {
      if (BigInt.asUintN(64, message.pendingOutboxReservations) !== message.pendingOutboxReservations) {
        throw new globalThis.Error(
          "value provided for field message.pendingOutboxReservations of type uint64 too large",
        );
      }
      writer.uint32(16).uint64(message.pendingOutboxReservations);
    }
    if (message.pendingOutboxBytes !== 0n) {
      if (BigInt.asUintN(64, message.pendingOutboxBytes) !== message.pendingOutboxBytes) {
        throw new globalThis.Error("value provided for field message.pendingOutboxBytes of type uint64 too large");
      }
      writer.uint32(24).uint64(message.pendingOutboxBytes);
    }
    if (message.oldestPendingOutboxAge !== undefined) {
      Duration.encode(message.oldestPendingOutboxAge, writer.uint32(34).fork()).join();
    }
    if (message.requestCapacityAvailable !== false) {
      writer.uint32(40).bool(message.requestCapacityAvailable);
    }
    if (message.retainedRequests !== 0n) {
      if (BigInt.asUintN(64, message.retainedRequests) !== message.retainedRequests) {
        throw new globalThis.Error("value provided for field message.retainedRequests of type uint64 too large");
      }
      writer.uint32(48).uint64(message.retainedRequests);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HECDurableOperationalMetrics {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHECDurableOperationalMetrics();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.queueAvailable = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.pendingOutboxReservations = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.pendingOutboxBytes = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.oldestPendingOutboxAge = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.requestCapacityAvailable = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.retainedRequests = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HECDurableOperationalMetrics {
    return {
      queueAvailable: isSet(object.queueAvailable)
        ? globalThis.Boolean(object.queueAvailable)
        : isSet(object.queue_available)
        ? globalThis.Boolean(object.queue_available)
        : false,
      pendingOutboxReservations: isSet(object.pendingOutboxReservations)
        ? BigInt(object.pendingOutboxReservations)
        : isSet(object.pending_outbox_reservations)
        ? BigInt(object.pending_outbox_reservations)
        : 0n,
      pendingOutboxBytes: isSet(object.pendingOutboxBytes)
        ? BigInt(object.pendingOutboxBytes)
        : isSet(object.pending_outbox_bytes)
        ? BigInt(object.pending_outbox_bytes)
        : 0n,
      oldestPendingOutboxAge: isSet(object.oldestPendingOutboxAge)
        ? Duration.fromJSON(object.oldestPendingOutboxAge)
        : isSet(object.oldest_pending_outbox_age)
        ? Duration.fromJSON(object.oldest_pending_outbox_age)
        : undefined,
      requestCapacityAvailable: isSet(object.requestCapacityAvailable)
        ? globalThis.Boolean(object.requestCapacityAvailable)
        : isSet(object.request_capacity_available)
        ? globalThis.Boolean(object.request_capacity_available)
        : false,
      retainedRequests: isSet(object.retainedRequests)
        ? BigInt(object.retainedRequests)
        : isSet(object.retained_requests)
        ? BigInt(object.retained_requests)
        : 0n,
    };
  },

  toJSON(message: HECDurableOperationalMetrics): unknown {
    const obj: any = {};
    if (message.queueAvailable !== false) {
      obj.queueAvailable = message.queueAvailable;
    }
    if (message.pendingOutboxReservations !== 0n) {
      obj.pendingOutboxReservations = message.pendingOutboxReservations.toString();
    }
    if (message.pendingOutboxBytes !== 0n) {
      obj.pendingOutboxBytes = message.pendingOutboxBytes.toString();
    }
    if (message.oldestPendingOutboxAge !== undefined) {
      obj.oldestPendingOutboxAge = Duration.toJSON(message.oldestPendingOutboxAge);
    }
    if (message.requestCapacityAvailable !== false) {
      obj.requestCapacityAvailable = message.requestCapacityAvailable;
    }
    if (message.retainedRequests !== 0n) {
      obj.retainedRequests = message.retainedRequests.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HECDurableOperationalMetrics>, I>>(base?: I): HECDurableOperationalMetrics {
    return HECDurableOperationalMetrics.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HECDurableOperationalMetrics>, I>>(object: I): HECDurableOperationalMetrics {
    const message = createBaseHECDurableOperationalMetrics();
    message.queueAvailable = object.queueAvailable ?? false;
    message.pendingOutboxReservations =
      (object.pendingOutboxReservations !== undefined && object.pendingOutboxReservations !== null)
        ? BigInt(object.pendingOutboxReservations)
        : 0n;
    message.pendingOutboxBytes = (object.pendingOutboxBytes !== undefined && object.pendingOutboxBytes !== null)
      ? BigInt(object.pendingOutboxBytes)
      : 0n;
    message.oldestPendingOutboxAge =
      (object.oldestPendingOutboxAge !== undefined && object.oldestPendingOutboxAge !== null)
        ? Duration.fromPartial(object.oldestPendingOutboxAge)
        : undefined;
    message.requestCapacityAvailable = object.requestCapacityAvailable ?? false;
    message.retainedRequests = (object.retainedRequests !== undefined && object.retainedRequests !== null)
      ? BigInt(object.retainedRequests)
      : 0n;
    return message;
  },
};

function createBaseHECReconciliationOperationalMetrics(): HECReconciliationOperationalMetrics {
  return { available: false, successes: 0n, retries: 0n, ambiguities: 0n };
}

export const HECReconciliationOperationalMetrics: MessageFns<HECReconciliationOperationalMetrics> = {
  encode(message: HECReconciliationOperationalMetrics, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.available !== false) {
      writer.uint32(8).bool(message.available);
    }
    if (message.successes !== 0n) {
      if (BigInt.asUintN(64, message.successes) !== message.successes) {
        throw new globalThis.Error("value provided for field message.successes of type uint64 too large");
      }
      writer.uint32(16).uint64(message.successes);
    }
    if (message.retries !== 0n) {
      if (BigInt.asUintN(64, message.retries) !== message.retries) {
        throw new globalThis.Error("value provided for field message.retries of type uint64 too large");
      }
      writer.uint32(24).uint64(message.retries);
    }
    if (message.ambiguities !== 0n) {
      if (BigInt.asUintN(64, message.ambiguities) !== message.ambiguities) {
        throw new globalThis.Error("value provided for field message.ambiguities of type uint64 too large");
      }
      writer.uint32(32).uint64(message.ambiguities);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HECReconciliationOperationalMetrics {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHECReconciliationOperationalMetrics();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.available = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.successes = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.retries = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.ambiguities = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HECReconciliationOperationalMetrics {
    return {
      available: isSet(object.available) ? globalThis.Boolean(object.available) : false,
      successes: isSet(object.successes) ? BigInt(object.successes) : 0n,
      retries: isSet(object.retries) ? BigInt(object.retries) : 0n,
      ambiguities: isSet(object.ambiguities) ? BigInt(object.ambiguities) : 0n,
    };
  },

  toJSON(message: HECReconciliationOperationalMetrics): unknown {
    const obj: any = {};
    if (message.available !== false) {
      obj.available = message.available;
    }
    if (message.successes !== 0n) {
      obj.successes = message.successes.toString();
    }
    if (message.retries !== 0n) {
      obj.retries = message.retries.toString();
    }
    if (message.ambiguities !== 0n) {
      obj.ambiguities = message.ambiguities.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HECReconciliationOperationalMetrics>, I>>(
    base?: I,
  ): HECReconciliationOperationalMetrics {
    return HECReconciliationOperationalMetrics.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HECReconciliationOperationalMetrics>, I>>(
    object: I,
  ): HECReconciliationOperationalMetrics {
    const message = createBaseHECReconciliationOperationalMetrics();
    message.available = object.available ?? false;
    message.successes = (object.successes !== undefined && object.successes !== null) ? BigInt(object.successes) : 0n;
    message.retries = (object.retries !== undefined && object.retries !== null) ? BigInt(object.retries) : 0n;
    message.ambiguities = (object.ambiguities !== undefined && object.ambiguities !== null)
      ? BigInt(object.ambiguities)
      : 0n;
    return message;
  },
};

function createBaseHECAcknowledgmentOperationalMetrics(): HECAcknowledgmentOperationalMetrics {
  return {
    available: false,
    activeChannels: 0n,
    retainedChannels: 0n,
    pendingRows: 0n,
    indexedRows: 0n,
    expiredRows: 0n,
    terminalFailedRequests: 0n,
    queries: 0n,
    idsQueried: 0n,
    misses: 0n,
  };
}

export const HECAcknowledgmentOperationalMetrics: MessageFns<HECAcknowledgmentOperationalMetrics> = {
  encode(message: HECAcknowledgmentOperationalMetrics, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.available !== false) {
      writer.uint32(8).bool(message.available);
    }
    if (message.activeChannels !== 0n) {
      if (BigInt.asUintN(64, message.activeChannels) !== message.activeChannels) {
        throw new globalThis.Error("value provided for field message.activeChannels of type uint64 too large");
      }
      writer.uint32(16).uint64(message.activeChannels);
    }
    if (message.retainedChannels !== 0n) {
      if (BigInt.asUintN(64, message.retainedChannels) !== message.retainedChannels) {
        throw new globalThis.Error("value provided for field message.retainedChannels of type uint64 too large");
      }
      writer.uint32(24).uint64(message.retainedChannels);
    }
    if (message.pendingRows !== 0n) {
      if (BigInt.asUintN(64, message.pendingRows) !== message.pendingRows) {
        throw new globalThis.Error("value provided for field message.pendingRows of type uint64 too large");
      }
      writer.uint32(32).uint64(message.pendingRows);
    }
    if (message.indexedRows !== 0n) {
      if (BigInt.asUintN(64, message.indexedRows) !== message.indexedRows) {
        throw new globalThis.Error("value provided for field message.indexedRows of type uint64 too large");
      }
      writer.uint32(40).uint64(message.indexedRows);
    }
    if (message.expiredRows !== 0n) {
      if (BigInt.asUintN(64, message.expiredRows) !== message.expiredRows) {
        throw new globalThis.Error("value provided for field message.expiredRows of type uint64 too large");
      }
      writer.uint32(48).uint64(message.expiredRows);
    }
    if (message.terminalFailedRequests !== 0n) {
      if (BigInt.asUintN(64, message.terminalFailedRequests) !== message.terminalFailedRequests) {
        throw new globalThis.Error("value provided for field message.terminalFailedRequests of type uint64 too large");
      }
      writer.uint32(56).uint64(message.terminalFailedRequests);
    }
    if (message.queries !== 0n) {
      if (BigInt.asUintN(64, message.queries) !== message.queries) {
        throw new globalThis.Error("value provided for field message.queries of type uint64 too large");
      }
      writer.uint32(64).uint64(message.queries);
    }
    if (message.idsQueried !== 0n) {
      if (BigInt.asUintN(64, message.idsQueried) !== message.idsQueried) {
        throw new globalThis.Error("value provided for field message.idsQueried of type uint64 too large");
      }
      writer.uint32(72).uint64(message.idsQueried);
    }
    if (message.misses !== 0n) {
      if (BigInt.asUintN(64, message.misses) !== message.misses) {
        throw new globalThis.Error("value provided for field message.misses of type uint64 too large");
      }
      writer.uint32(80).uint64(message.misses);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HECAcknowledgmentOperationalMetrics {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHECAcknowledgmentOperationalMetrics();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.available = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.activeChannels = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.retainedChannels = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.pendingRows = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.indexedRows = reader.uint64() as bigint;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.expiredRows = reader.uint64() as bigint;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.terminalFailedRequests = reader.uint64() as bigint;
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.queries = reader.uint64() as bigint;
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.idsQueried = reader.uint64() as bigint;
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.misses = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HECAcknowledgmentOperationalMetrics {
    return {
      available: isSet(object.available) ? globalThis.Boolean(object.available) : false,
      activeChannels: isSet(object.activeChannels)
        ? BigInt(object.activeChannels)
        : isSet(object.active_channels)
        ? BigInt(object.active_channels)
        : 0n,
      retainedChannels: isSet(object.retainedChannels)
        ? BigInt(object.retainedChannels)
        : isSet(object.retained_channels)
        ? BigInt(object.retained_channels)
        : 0n,
      pendingRows: isSet(object.pendingRows)
        ? BigInt(object.pendingRows)
        : isSet(object.pending_rows)
        ? BigInt(object.pending_rows)
        : 0n,
      indexedRows: isSet(object.indexedRows)
        ? BigInt(object.indexedRows)
        : isSet(object.indexed_rows)
        ? BigInt(object.indexed_rows)
        : 0n,
      expiredRows: isSet(object.expiredRows)
        ? BigInt(object.expiredRows)
        : isSet(object.expired_rows)
        ? BigInt(object.expired_rows)
        : 0n,
      terminalFailedRequests: isSet(object.terminalFailedRequests)
        ? BigInt(object.terminalFailedRequests)
        : isSet(object.terminal_failed_requests)
        ? BigInt(object.terminal_failed_requests)
        : 0n,
      queries: isSet(object.queries) ? BigInt(object.queries) : 0n,
      idsQueried: isSet(object.idsQueried)
        ? BigInt(object.idsQueried)
        : isSet(object.ids_queried)
        ? BigInt(object.ids_queried)
        : 0n,
      misses: isSet(object.misses) ? BigInt(object.misses) : 0n,
    };
  },

  toJSON(message: HECAcknowledgmentOperationalMetrics): unknown {
    const obj: any = {};
    if (message.available !== false) {
      obj.available = message.available;
    }
    if (message.activeChannels !== 0n) {
      obj.activeChannels = message.activeChannels.toString();
    }
    if (message.retainedChannels !== 0n) {
      obj.retainedChannels = message.retainedChannels.toString();
    }
    if (message.pendingRows !== 0n) {
      obj.pendingRows = message.pendingRows.toString();
    }
    if (message.indexedRows !== 0n) {
      obj.indexedRows = message.indexedRows.toString();
    }
    if (message.expiredRows !== 0n) {
      obj.expiredRows = message.expiredRows.toString();
    }
    if (message.terminalFailedRequests !== 0n) {
      obj.terminalFailedRequests = message.terminalFailedRequests.toString();
    }
    if (message.queries !== 0n) {
      obj.queries = message.queries.toString();
    }
    if (message.idsQueried !== 0n) {
      obj.idsQueried = message.idsQueried.toString();
    }
    if (message.misses !== 0n) {
      obj.misses = message.misses.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HECAcknowledgmentOperationalMetrics>, I>>(
    base?: I,
  ): HECAcknowledgmentOperationalMetrics {
    return HECAcknowledgmentOperationalMetrics.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HECAcknowledgmentOperationalMetrics>, I>>(
    object: I,
  ): HECAcknowledgmentOperationalMetrics {
    const message = createBaseHECAcknowledgmentOperationalMetrics();
    message.available = object.available ?? false;
    message.activeChannels = (object.activeChannels !== undefined && object.activeChannels !== null)
      ? BigInt(object.activeChannels)
      : 0n;
    message.retainedChannels = (object.retainedChannels !== undefined && object.retainedChannels !== null)
      ? BigInt(object.retainedChannels)
      : 0n;
    message.pendingRows = (object.pendingRows !== undefined && object.pendingRows !== null)
      ? BigInt(object.pendingRows)
      : 0n;
    message.indexedRows = (object.indexedRows !== undefined && object.indexedRows !== null)
      ? BigInt(object.indexedRows)
      : 0n;
    message.expiredRows = (object.expiredRows !== undefined && object.expiredRows !== null)
      ? BigInt(object.expiredRows)
      : 0n;
    message.terminalFailedRequests =
      (object.terminalFailedRequests !== undefined && object.terminalFailedRequests !== null)
        ? BigInt(object.terminalFailedRequests)
        : 0n;
    message.queries = (object.queries !== undefined && object.queries !== null) ? BigInt(object.queries) : 0n;
    message.idsQueried = (object.idsQueried !== undefined && object.idsQueried !== null)
      ? BigInt(object.idsQueried)
      : 0n;
    message.misses = (object.misses !== undefined && object.misses !== null) ? BigInt(object.misses) : 0n;
    return message;
  },
};

function createBaseHECProtocolFailureMetric(): HECProtocolFailureMetric {
  return { code: 0, count: 0n };
}

export const HECProtocolFailureMetric: MessageFns<HECProtocolFailureMetric> = {
  encode(message: HECProtocolFailureMetric, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== 0) {
      writer.uint32(8).uint32(message.code);
    }
    if (message.count !== 0n) {
      if (BigInt.asUintN(64, message.count) !== message.count) {
        throw new globalThis.Error("value provided for field message.count of type uint64 too large");
      }
      writer.uint32(16).uint64(message.count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HECProtocolFailureMetric {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHECProtocolFailureMetric();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.code = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.count = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HECProtocolFailureMetric {
    return {
      code: isSet(object.code) ? globalThis.Number(object.code) : 0,
      count: isSet(object.count) ? BigInt(object.count) : 0n,
    };
  },

  toJSON(message: HECProtocolFailureMetric): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = Math.round(message.code);
    }
    if (message.count !== 0n) {
      obj.count = message.count.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HECProtocolFailureMetric>, I>>(base?: I): HECProtocolFailureMetric {
    return HECProtocolFailureMetric.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HECProtocolFailureMetric>, I>>(object: I): HECProtocolFailureMetric {
    const message = createBaseHECProtocolFailureMetric();
    message.code = object.code ?? 0;
    message.count = (object.count !== undefined && object.count !== null) ? BigInt(object.count) : 0n;
    return message;
  },
};

function createBaseGetHECOperationalSnapshotResponse(): GetHECOperationalSnapshotResponse {
  return {
    observedAt: undefined,
    request: undefined,
    durable: undefined,
    reconciliation: undefined,
    acknowledgments: undefined,
    protocolFailures: [],
  };
}

export const GetHECOperationalSnapshotResponse: MessageFns<GetHECOperationalSnapshotResponse> = {
  encode(message: GetHECOperationalSnapshotResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.observedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.observedAt), writer.uint32(10).fork()).join();
    }
    if (message.request !== undefined) {
      HECRequestOperationalMetrics.encode(message.request, writer.uint32(18).fork()).join();
    }
    if (message.durable !== undefined) {
      HECDurableOperationalMetrics.encode(message.durable, writer.uint32(26).fork()).join();
    }
    if (message.reconciliation !== undefined) {
      HECReconciliationOperationalMetrics.encode(message.reconciliation, writer.uint32(34).fork()).join();
    }
    if (message.acknowledgments !== undefined) {
      HECAcknowledgmentOperationalMetrics.encode(message.acknowledgments, writer.uint32(42).fork()).join();
    }
    for (const v of message.protocolFailures) {
      HECProtocolFailureMetric.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetHECOperationalSnapshotResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetHECOperationalSnapshotResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.observedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.request = HECRequestOperationalMetrics.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.durable = HECDurableOperationalMetrics.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.reconciliation = HECReconciliationOperationalMetrics.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.acknowledgments = HECAcknowledgmentOperationalMetrics.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.protocolFailures.push(HECProtocolFailureMetric.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetHECOperationalSnapshotResponse {
    return {
      observedAt: isSet(object.observedAt)
        ? fromJsonTimestamp(object.observedAt)
        : isSet(object.observed_at)
        ? fromJsonTimestamp(object.observed_at)
        : undefined,
      request: isSet(object.request) ? HECRequestOperationalMetrics.fromJSON(object.request) : undefined,
      durable: isSet(object.durable) ? HECDurableOperationalMetrics.fromJSON(object.durable) : undefined,
      reconciliation: isSet(object.reconciliation)
        ? HECReconciliationOperationalMetrics.fromJSON(object.reconciliation)
        : undefined,
      acknowledgments: isSet(object.acknowledgments)
        ? HECAcknowledgmentOperationalMetrics.fromJSON(object.acknowledgments)
        : undefined,
      protocolFailures: globalThis.Array.isArray(object?.protocolFailures)
        ? object.protocolFailures.map((e: any) => HECProtocolFailureMetric.fromJSON(e))
        : globalThis.Array.isArray(object?.protocol_failures)
        ? object.protocol_failures.map((e: any) => HECProtocolFailureMetric.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetHECOperationalSnapshotResponse): unknown {
    const obj: any = {};
    if (message.observedAt !== undefined) {
      obj.observedAt = message.observedAt.toISOString();
    }
    if (message.request !== undefined) {
      obj.request = HECRequestOperationalMetrics.toJSON(message.request);
    }
    if (message.durable !== undefined) {
      obj.durable = HECDurableOperationalMetrics.toJSON(message.durable);
    }
    if (message.reconciliation !== undefined) {
      obj.reconciliation = HECReconciliationOperationalMetrics.toJSON(message.reconciliation);
    }
    if (message.acknowledgments !== undefined) {
      obj.acknowledgments = HECAcknowledgmentOperationalMetrics.toJSON(message.acknowledgments);
    }
    if (message.protocolFailures?.length) {
      obj.protocolFailures = message.protocolFailures.map((e) => HECProtocolFailureMetric.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetHECOperationalSnapshotResponse>, I>>(
    base?: I,
  ): GetHECOperationalSnapshotResponse {
    return GetHECOperationalSnapshotResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetHECOperationalSnapshotResponse>, I>>(
    object: I,
  ): GetHECOperationalSnapshotResponse {
    const message = createBaseGetHECOperationalSnapshotResponse();
    message.observedAt = object.observedAt ?? undefined;
    message.request = (object.request !== undefined && object.request !== null)
      ? HECRequestOperationalMetrics.fromPartial(object.request)
      : undefined;
    message.durable = (object.durable !== undefined && object.durable !== null)
      ? HECDurableOperationalMetrics.fromPartial(object.durable)
      : undefined;
    message.reconciliation = (object.reconciliation !== undefined && object.reconciliation !== null)
      ? HECReconciliationOperationalMetrics.fromPartial(object.reconciliation)
      : undefined;
    message.acknowledgments = (object.acknowledgments !== undefined && object.acknowledgments !== null)
      ? HECAcknowledgmentOperationalMetrics.fromPartial(object.acknowledgments)
      : undefined;
    message.protocolFailures = object.protocolFailures?.map((e) => HECProtocolFailureMetric.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
