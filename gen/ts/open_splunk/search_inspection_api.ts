// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: open_splunk/search_inspection_api.proto

import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { SourceRange } from "./common";
import { KnowledgeProvenance, KnowledgeSnapshotSummary } from "./knowledge";

export enum SearchInspectionOutputKind {
  SEARCH_INSPECTION_OUTPUT_KIND_UNSPECIFIED = 0,
  SEARCH_INSPECTION_OUTPUT_KIND_OPEN = 1,
  SEARCH_INSPECTION_OUTPUT_KIND_STATIC = 2,
  SEARCH_INSPECTION_OUTPUT_KIND_DYNAMIC = 3,
  UNRECOGNIZED = -1,
}

export function searchInspectionOutputKindFromJSON(object: any): SearchInspectionOutputKind {
  switch (object) {
    case 0:
    case "SEARCH_INSPECTION_OUTPUT_KIND_UNSPECIFIED":
      return SearchInspectionOutputKind.SEARCH_INSPECTION_OUTPUT_KIND_UNSPECIFIED;
    case 1:
    case "SEARCH_INSPECTION_OUTPUT_KIND_OPEN":
      return SearchInspectionOutputKind.SEARCH_INSPECTION_OUTPUT_KIND_OPEN;
    case 2:
    case "SEARCH_INSPECTION_OUTPUT_KIND_STATIC":
      return SearchInspectionOutputKind.SEARCH_INSPECTION_OUTPUT_KIND_STATIC;
    case 3:
    case "SEARCH_INSPECTION_OUTPUT_KIND_DYNAMIC":
      return SearchInspectionOutputKind.SEARCH_INSPECTION_OUTPUT_KIND_DYNAMIC;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SearchInspectionOutputKind.UNRECOGNIZED;
  }
}

export function searchInspectionOutputKindToJSON(object: SearchInspectionOutputKind): string {
  switch (object) {
    case SearchInspectionOutputKind.SEARCH_INSPECTION_OUTPUT_KIND_UNSPECIFIED:
      return "SEARCH_INSPECTION_OUTPUT_KIND_UNSPECIFIED";
    case SearchInspectionOutputKind.SEARCH_INSPECTION_OUTPUT_KIND_OPEN:
      return "SEARCH_INSPECTION_OUTPUT_KIND_OPEN";
    case SearchInspectionOutputKind.SEARCH_INSPECTION_OUTPUT_KIND_STATIC:
      return "SEARCH_INSPECTION_OUTPUT_KIND_STATIC";
    case SearchInspectionOutputKind.SEARCH_INSPECTION_OUTPUT_KIND_DYNAMIC:
      return "SEARCH_INSPECTION_OUTPUT_KIND_DYNAMIC";
    case SearchInspectionOutputKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * POST /api/search/jobs/inspect
 * The authenticated administrator's tenant and owner identity are derived
 * from the browser principal and are deliberately not accepted on the wire.
 */
export interface InspectSearchJobRequest {
  searchJobId: string;
}

export interface SearchInspectionLogicalStage {
  stageIndex: number;
  operator: string;
  inputFields: string[];
  outputFields: string[];
  /**
   * Present only for authored SPL stages. Generated knowledge stages have no
   * authored source coordinate and leave this message absent.
   */
  sourceRange:
    | SourceRange
    | undefined;
  /**
   * One entry per canonical knowledge object represented by this physical
   * logical stage, ordered by ascending redacted object ordinal. Current
   * browser responses use only redacted_object.
   */
  operatorProvenance: KnowledgeProvenance[];
  /**
   * Output occurrences remain distinct because selector-disjoint objects may
   * legitimately target the same field. Entries are ordered by output field,
   * then by ascending redacted object ordinal.
   */
  outputProvenance: SearchInspectionOutputProvenance[];
}

export interface SearchInspectionOutputProvenance {
  outputField: string;
  provenance: KnowledgeProvenance | undefined;
}

/**
 * SearchInspectionOutputShape describes the final logical relation. fields is
 * the complete ordered schema for STATIC output and the fixed prefix for
 * DYNAMIC output; max_dynamic_fields is nonzero only for DYNAMIC output.
 */
export interface SearchInspectionOutputShape {
  kind: SearchInspectionOutputKind;
  fields: string[];
  maxDynamicFields: number;
}

export interface SearchInspectionLogicalPlan {
  stages: SearchInspectionLogicalStage[];
  referencedFields: string[];
  output: SearchInspectionOutputShape | undefined;
}

/**
 * SearchInspectionPhysicalIndex exposes only safe schema names and selection
 * counters. It never contains predicates, rendered expressions, or literals.
 */
export interface SearchInspectionPhysicalIndex {
  type: string;
  name: string;
  keys: string[];
  initialParts: bigint;
  selectedParts: bigint;
  initialGranules: bigint;
  selectedGranules: bigint;
}

export interface SearchInspectionPhysicalRead {
  columns: string[];
  indexes: SearchInspectionPhysicalIndex[];
}

/**
 * SearchInspectionPhysicalPlan is the bounded, literal-free structural
 * projection parsed from the raw ClickHouse structured PLAN response.
 */
export interface SearchInspectionPhysicalPlan {
  nodeTypes: string[];
  reads: SearchInspectionPhysicalRead[];
}

/**
 * The SQL and raw EXPLAIN text are administrator-sensitive diagnostics. The
 * response does not expose the compiler argument vector as a separate field;
 * raw EXPLAIN may contain rendered bound values. Execution rows are excluded.
 */
export interface InspectSearchJobResponse {
  searchJobId: string;
  logicalPlan: SearchInspectionLogicalPlan | undefined;
  physicalPlan: SearchInspectionPhysicalPlan | undefined;
  generatedSql: string;
  explainText: string;
  diagnosticQueryId: string;
  knowledgeSnapshot?: KnowledgeSnapshotSummary | undefined;
}

function createBaseInspectSearchJobRequest(): InspectSearchJobRequest {
  return { searchJobId: "" };
}

export const InspectSearchJobRequest: MessageFns<InspectSearchJobRequest> = {
  encode(message: InspectSearchJobRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.searchJobId !== "") {
      writer.uint32(10).string(message.searchJobId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InspectSearchJobRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseInspectSearchJobRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.searchJobId = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): InspectSearchJobRequest {
    return {
      searchJobId: isSet(object.searchJobId)
        ? globalThis.String(object.searchJobId)
        : isSet(object.search_job_id)
        ? globalThis.String(object.search_job_id)
        : "",
    };
  },

  toJSON(message: InspectSearchJobRequest): unknown {
    const obj: any = {};
    if (message.searchJobId !== "") {
      obj.searchJobId = message.searchJobId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InspectSearchJobRequest>, I>>(base?: I): InspectSearchJobRequest {
    return InspectSearchJobRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InspectSearchJobRequest>, I>>(object: I): InspectSearchJobRequest {
    const message = createBaseInspectSearchJobRequest();
    message.searchJobId = object.searchJobId ?? "";
    return message;
  },
};

function createBaseSearchInspectionLogicalStage(): SearchInspectionLogicalStage {
  return {
    stageIndex: 0,
    operator: "",
    inputFields: [],
    outputFields: [],
    sourceRange: undefined,
    operatorProvenance: [],
    outputProvenance: [],
  };
}

export const SearchInspectionLogicalStage: MessageFns<SearchInspectionLogicalStage> = {
  encode(message: SearchInspectionLogicalStage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stageIndex !== 0) {
      writer.uint32(8).uint32(message.stageIndex);
    }
    if (message.operator !== "") {
      writer.uint32(18).string(message.operator);
    }
    for (const v of message.inputFields) {
      writer.uint32(26).string(v!);
    }
    for (const v of message.outputFields) {
      writer.uint32(34).string(v!);
    }
    if (message.sourceRange !== undefined) {
      SourceRange.encode(message.sourceRange, writer.uint32(42).fork()).join();
    }
    for (const v of message.operatorProvenance) {
      KnowledgeProvenance.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.outputProvenance) {
      SearchInspectionOutputProvenance.encode(v!, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchInspectionLogicalStage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSearchInspectionLogicalStage();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.stageIndex = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.operator = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.inputFields.push(reader.string());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.outputFields.push(reader.string());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.sourceRange = SourceRange.decode(reader, reader.uint32());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.operatorProvenance.push(KnowledgeProvenance.decode(reader, reader.uint32()));
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.outputProvenance.push(SearchInspectionOutputProvenance.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): SearchInspectionLogicalStage {
    return {
      stageIndex: isSet(object.stageIndex)
        ? globalThis.Number(object.stageIndex)
        : isSet(object.stage_index)
        ? globalThis.Number(object.stage_index)
        : 0,
      operator: isSet(object.operator) ? globalThis.String(object.operator) : "",
      inputFields: globalThis.Array.isArray(object?.inputFields)
        ? object.inputFields.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.input_fields)
        ? object.input_fields.map((e: any) => globalThis.String(e))
        : [],
      outputFields: globalThis.Array.isArray(object?.outputFields)
        ? object.outputFields.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.output_fields)
        ? object.output_fields.map((e: any) => globalThis.String(e))
        : [],
      sourceRange: isSet(object.sourceRange)
        ? SourceRange.fromJSON(object.sourceRange)
        : isSet(object.source_range)
        ? SourceRange.fromJSON(object.source_range)
        : undefined,
      operatorProvenance: globalThis.Array.isArray(object?.operatorProvenance)
        ? object.operatorProvenance.map((e: any) => KnowledgeProvenance.fromJSON(e))
        : globalThis.Array.isArray(object?.operator_provenance)
        ? object.operator_provenance.map((e: any) => KnowledgeProvenance.fromJSON(e))
        : [],
      outputProvenance: globalThis.Array.isArray(object?.outputProvenance)
        ? object.outputProvenance.map((e: any) => SearchInspectionOutputProvenance.fromJSON(e))
        : globalThis.Array.isArray(object?.output_provenance)
        ? object.output_provenance.map((e: any) => SearchInspectionOutputProvenance.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SearchInspectionLogicalStage): unknown {
    const obj: any = {};
    if (message.stageIndex !== 0) {
      obj.stageIndex = Math.round(message.stageIndex);
    }
    if (message.operator !== "") {
      obj.operator = message.operator;
    }
    if (message.inputFields?.length) {
      obj.inputFields = message.inputFields;
    }
    if (message.outputFields?.length) {
      obj.outputFields = message.outputFields;
    }
    if (message.sourceRange !== undefined) {
      obj.sourceRange = SourceRange.toJSON(message.sourceRange);
    }
    if (message.operatorProvenance?.length) {
      obj.operatorProvenance = message.operatorProvenance.map((e) => KnowledgeProvenance.toJSON(e));
    }
    if (message.outputProvenance?.length) {
      obj.outputProvenance = message.outputProvenance.map((e) => SearchInspectionOutputProvenance.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchInspectionLogicalStage>, I>>(base?: I): SearchInspectionLogicalStage {
    return SearchInspectionLogicalStage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchInspectionLogicalStage>, I>>(object: I): SearchInspectionLogicalStage {
    const message = createBaseSearchInspectionLogicalStage();
    message.stageIndex = object.stageIndex ?? 0;
    message.operator = object.operator ?? "";
    message.inputFields = object.inputFields?.map((e) => e) || [];
    message.outputFields = object.outputFields?.map((e) => e) || [];
    message.sourceRange = (object.sourceRange !== undefined && object.sourceRange !== null)
      ? SourceRange.fromPartial(object.sourceRange)
      : undefined;
    message.operatorProvenance = object.operatorProvenance?.map((e) => KnowledgeProvenance.fromPartial(e)) || [];
    message.outputProvenance = object.outputProvenance?.map((e) => SearchInspectionOutputProvenance.fromPartial(e)) ||
      [];
    return message;
  },
};

function createBaseSearchInspectionOutputProvenance(): SearchInspectionOutputProvenance {
  return { outputField: "", provenance: undefined };
}

export const SearchInspectionOutputProvenance: MessageFns<SearchInspectionOutputProvenance> = {
  encode(message: SearchInspectionOutputProvenance, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.outputField !== "") {
      writer.uint32(10).string(message.outputField);
    }
    if (message.provenance !== undefined) {
      KnowledgeProvenance.encode(message.provenance, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchInspectionOutputProvenance {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSearchInspectionOutputProvenance();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.outputField = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.provenance = KnowledgeProvenance.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): SearchInspectionOutputProvenance {
    return {
      outputField: isSet(object.outputField)
        ? globalThis.String(object.outputField)
        : isSet(object.output_field)
        ? globalThis.String(object.output_field)
        : "",
      provenance: isSet(object.provenance) ? KnowledgeProvenance.fromJSON(object.provenance) : undefined,
    };
  },

  toJSON(message: SearchInspectionOutputProvenance): unknown {
    const obj: any = {};
    if (message.outputField !== "") {
      obj.outputField = message.outputField;
    }
    if (message.provenance !== undefined) {
      obj.provenance = KnowledgeProvenance.toJSON(message.provenance);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchInspectionOutputProvenance>, I>>(
    base?: I,
  ): SearchInspectionOutputProvenance {
    return SearchInspectionOutputProvenance.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchInspectionOutputProvenance>, I>>(
    object: I,
  ): SearchInspectionOutputProvenance {
    const message = createBaseSearchInspectionOutputProvenance();
    message.outputField = object.outputField ?? "";
    message.provenance = (object.provenance !== undefined && object.provenance !== null)
      ? KnowledgeProvenance.fromPartial(object.provenance)
      : undefined;
    return message;
  },
};

function createBaseSearchInspectionOutputShape(): SearchInspectionOutputShape {
  return { kind: 0, fields: [], maxDynamicFields: 0 };
}

export const SearchInspectionOutputShape: MessageFns<SearchInspectionOutputShape> = {
  encode(message: SearchInspectionOutputShape, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.kind !== 0) {
      writer.uint32(8).int32(message.kind);
    }
    for (const v of message.fields) {
      writer.uint32(18).string(v!);
    }
    if (message.maxDynamicFields !== 0) {
      writer.uint32(24).uint32(message.maxDynamicFields);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchInspectionOutputShape {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSearchInspectionOutputShape();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.kind = reader.int32() as any;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.fields.push(reader.string());
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.maxDynamicFields = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): SearchInspectionOutputShape {
    return {
      kind: isSet(object.kind) ? searchInspectionOutputKindFromJSON(object.kind) : 0,
      fields: globalThis.Array.isArray(object?.fields) ? object.fields.map((e: any) => globalThis.String(e)) : [],
      maxDynamicFields: isSet(object.maxDynamicFields)
        ? globalThis.Number(object.maxDynamicFields)
        : isSet(object.max_dynamic_fields)
        ? globalThis.Number(object.max_dynamic_fields)
        : 0,
    };
  },

  toJSON(message: SearchInspectionOutputShape): unknown {
    const obj: any = {};
    if (message.kind !== 0) {
      obj.kind = searchInspectionOutputKindToJSON(message.kind);
    }
    if (message.fields?.length) {
      obj.fields = message.fields;
    }
    if (message.maxDynamicFields !== 0) {
      obj.maxDynamicFields = Math.round(message.maxDynamicFields);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchInspectionOutputShape>, I>>(base?: I): SearchInspectionOutputShape {
    return SearchInspectionOutputShape.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchInspectionOutputShape>, I>>(object: I): SearchInspectionOutputShape {
    const message = createBaseSearchInspectionOutputShape();
    message.kind = object.kind ?? 0;
    message.fields = object.fields?.map((e) => e) || [];
    message.maxDynamicFields = object.maxDynamicFields ?? 0;
    return message;
  },
};

function createBaseSearchInspectionLogicalPlan(): SearchInspectionLogicalPlan {
  return { stages: [], referencedFields: [], output: undefined };
}

export const SearchInspectionLogicalPlan: MessageFns<SearchInspectionLogicalPlan> = {
  encode(message: SearchInspectionLogicalPlan, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.stages) {
      SearchInspectionLogicalStage.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.referencedFields) {
      writer.uint32(18).string(v!);
    }
    if (message.output !== undefined) {
      SearchInspectionOutputShape.encode(message.output, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchInspectionLogicalPlan {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSearchInspectionLogicalPlan();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.stages.push(SearchInspectionLogicalStage.decode(reader, reader.uint32()));
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.referencedFields.push(reader.string());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.output = SearchInspectionOutputShape.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): SearchInspectionLogicalPlan {
    return {
      stages: globalThis.Array.isArray(object?.stages)
        ? object.stages.map((e: any) => SearchInspectionLogicalStage.fromJSON(e))
        : [],
      referencedFields: globalThis.Array.isArray(object?.referencedFields)
        ? object.referencedFields.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.referenced_fields)
        ? object.referenced_fields.map((e: any) => globalThis.String(e))
        : [],
      output: isSet(object.output) ? SearchInspectionOutputShape.fromJSON(object.output) : undefined,
    };
  },

  toJSON(message: SearchInspectionLogicalPlan): unknown {
    const obj: any = {};
    if (message.stages?.length) {
      obj.stages = message.stages.map((e) => SearchInspectionLogicalStage.toJSON(e));
    }
    if (message.referencedFields?.length) {
      obj.referencedFields = message.referencedFields;
    }
    if (message.output !== undefined) {
      obj.output = SearchInspectionOutputShape.toJSON(message.output);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchInspectionLogicalPlan>, I>>(base?: I): SearchInspectionLogicalPlan {
    return SearchInspectionLogicalPlan.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchInspectionLogicalPlan>, I>>(object: I): SearchInspectionLogicalPlan {
    const message = createBaseSearchInspectionLogicalPlan();
    message.stages = object.stages?.map((e) => SearchInspectionLogicalStage.fromPartial(e)) || [];
    message.referencedFields = object.referencedFields?.map((e) => e) || [];
    message.output = (object.output !== undefined && object.output !== null)
      ? SearchInspectionOutputShape.fromPartial(object.output)
      : undefined;
    return message;
  },
};

function createBaseSearchInspectionPhysicalIndex(): SearchInspectionPhysicalIndex {
  return {
    type: "",
    name: "",
    keys: [],
    initialParts: 0n,
    selectedParts: 0n,
    initialGranules: 0n,
    selectedGranules: 0n,
  };
}

export const SearchInspectionPhysicalIndex: MessageFns<SearchInspectionPhysicalIndex> = {
  encode(message: SearchInspectionPhysicalIndex, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== "") {
      writer.uint32(10).string(message.type);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    for (const v of message.keys) {
      writer.uint32(26).string(v!);
    }
    if (message.initialParts !== 0n) {
      if (BigInt.asUintN(64, message.initialParts) !== message.initialParts) {
        throw new globalThis.Error("value provided for field message.initialParts of type uint64 too large");
      }
      writer.uint32(32).uint64(message.initialParts);
    }
    if (message.selectedParts !== 0n) {
      if (BigInt.asUintN(64, message.selectedParts) !== message.selectedParts) {
        throw new globalThis.Error("value provided for field message.selectedParts of type uint64 too large");
      }
      writer.uint32(40).uint64(message.selectedParts);
    }
    if (message.initialGranules !== 0n) {
      if (BigInt.asUintN(64, message.initialGranules) !== message.initialGranules) {
        throw new globalThis.Error("value provided for field message.initialGranules of type uint64 too large");
      }
      writer.uint32(48).uint64(message.initialGranules);
    }
    if (message.selectedGranules !== 0n) {
      if (BigInt.asUintN(64, message.selectedGranules) !== message.selectedGranules) {
        throw new globalThis.Error("value provided for field message.selectedGranules of type uint64 too large");
      }
      writer.uint32(56).uint64(message.selectedGranules);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchInspectionPhysicalIndex {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSearchInspectionPhysicalIndex();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.type = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.name = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.keys.push(reader.string());
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.initialParts = reader.uint64() as bigint;
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.selectedParts = reader.uint64() as bigint;
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.initialGranules = reader.uint64() as bigint;
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.selectedGranules = reader.uint64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): SearchInspectionPhysicalIndex {
    return {
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      keys: globalThis.Array.isArray(object?.keys) ? object.keys.map((e: any) => globalThis.String(e)) : [],
      initialParts: isSet(object.initialParts)
        ? BigInt(object.initialParts)
        : isSet(object.initial_parts)
        ? BigInt(object.initial_parts)
        : 0n,
      selectedParts: isSet(object.selectedParts)
        ? BigInt(object.selectedParts)
        : isSet(object.selected_parts)
        ? BigInt(object.selected_parts)
        : 0n,
      initialGranules: isSet(object.initialGranules)
        ? BigInt(object.initialGranules)
        : isSet(object.initial_granules)
        ? BigInt(object.initial_granules)
        : 0n,
      selectedGranules: isSet(object.selectedGranules)
        ? BigInt(object.selectedGranules)
        : isSet(object.selected_granules)
        ? BigInt(object.selected_granules)
        : 0n,
    };
  },

  toJSON(message: SearchInspectionPhysicalIndex): unknown {
    const obj: any = {};
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.keys?.length) {
      obj.keys = message.keys;
    }
    if (message.initialParts !== 0n) {
      obj.initialParts = message.initialParts.toString();
    }
    if (message.selectedParts !== 0n) {
      obj.selectedParts = message.selectedParts.toString();
    }
    if (message.initialGranules !== 0n) {
      obj.initialGranules = message.initialGranules.toString();
    }
    if (message.selectedGranules !== 0n) {
      obj.selectedGranules = message.selectedGranules.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchInspectionPhysicalIndex>, I>>(base?: I): SearchInspectionPhysicalIndex {
    return SearchInspectionPhysicalIndex.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchInspectionPhysicalIndex>, I>>(
    object: I,
  ): SearchInspectionPhysicalIndex {
    const message = createBaseSearchInspectionPhysicalIndex();
    message.type = object.type ?? "";
    message.name = object.name ?? "";
    message.keys = object.keys?.map((e) => e) || [];
    message.initialParts = (object.initialParts !== undefined && object.initialParts !== null)
      ? BigInt(object.initialParts)
      : 0n;
    message.selectedParts = (object.selectedParts !== undefined && object.selectedParts !== null)
      ? BigInt(object.selectedParts)
      : 0n;
    message.initialGranules = (object.initialGranules !== undefined && object.initialGranules !== null)
      ? BigInt(object.initialGranules)
      : 0n;
    message.selectedGranules = (object.selectedGranules !== undefined && object.selectedGranules !== null)
      ? BigInt(object.selectedGranules)
      : 0n;
    return message;
  },
};

function createBaseSearchInspectionPhysicalRead(): SearchInspectionPhysicalRead {
  return { columns: [], indexes: [] };
}

export const SearchInspectionPhysicalRead: MessageFns<SearchInspectionPhysicalRead> = {
  encode(message: SearchInspectionPhysicalRead, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.columns) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.indexes) {
      SearchInspectionPhysicalIndex.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchInspectionPhysicalRead {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSearchInspectionPhysicalRead();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.columns.push(reader.string());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.indexes.push(SearchInspectionPhysicalIndex.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): SearchInspectionPhysicalRead {
    return {
      columns: globalThis.Array.isArray(object?.columns) ? object.columns.map((e: any) => globalThis.String(e)) : [],
      indexes: globalThis.Array.isArray(object?.indexes)
        ? object.indexes.map((e: any) => SearchInspectionPhysicalIndex.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SearchInspectionPhysicalRead): unknown {
    const obj: any = {};
    if (message.columns?.length) {
      obj.columns = message.columns;
    }
    if (message.indexes?.length) {
      obj.indexes = message.indexes.map((e) => SearchInspectionPhysicalIndex.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchInspectionPhysicalRead>, I>>(base?: I): SearchInspectionPhysicalRead {
    return SearchInspectionPhysicalRead.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchInspectionPhysicalRead>, I>>(object: I): SearchInspectionPhysicalRead {
    const message = createBaseSearchInspectionPhysicalRead();
    message.columns = object.columns?.map((e) => e) || [];
    message.indexes = object.indexes?.map((e) => SearchInspectionPhysicalIndex.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSearchInspectionPhysicalPlan(): SearchInspectionPhysicalPlan {
  return { nodeTypes: [], reads: [] };
}

export const SearchInspectionPhysicalPlan: MessageFns<SearchInspectionPhysicalPlan> = {
  encode(message: SearchInspectionPhysicalPlan, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.nodeTypes) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.reads) {
      SearchInspectionPhysicalRead.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchInspectionPhysicalPlan {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSearchInspectionPhysicalPlan();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.nodeTypes.push(reader.string());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.reads.push(SearchInspectionPhysicalRead.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): SearchInspectionPhysicalPlan {
    return {
      nodeTypes: globalThis.Array.isArray(object?.nodeTypes)
        ? object.nodeTypes.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.node_types)
        ? object.node_types.map((e: any) => globalThis.String(e))
        : [],
      reads: globalThis.Array.isArray(object?.reads)
        ? object.reads.map((e: any) => SearchInspectionPhysicalRead.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SearchInspectionPhysicalPlan): unknown {
    const obj: any = {};
    if (message.nodeTypes?.length) {
      obj.nodeTypes = message.nodeTypes;
    }
    if (message.reads?.length) {
      obj.reads = message.reads.map((e) => SearchInspectionPhysicalRead.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchInspectionPhysicalPlan>, I>>(base?: I): SearchInspectionPhysicalPlan {
    return SearchInspectionPhysicalPlan.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchInspectionPhysicalPlan>, I>>(object: I): SearchInspectionPhysicalPlan {
    const message = createBaseSearchInspectionPhysicalPlan();
    message.nodeTypes = object.nodeTypes?.map((e) => e) || [];
    message.reads = object.reads?.map((e) => SearchInspectionPhysicalRead.fromPartial(e)) || [];
    return message;
  },
};

function createBaseInspectSearchJobResponse(): InspectSearchJobResponse {
  return {
    searchJobId: "",
    logicalPlan: undefined,
    physicalPlan: undefined,
    generatedSql: "",
    explainText: "",
    diagnosticQueryId: "",
    knowledgeSnapshot: undefined,
  };
}

export const InspectSearchJobResponse: MessageFns<InspectSearchJobResponse> = {
  encode(message: InspectSearchJobResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.searchJobId !== "") {
      writer.uint32(10).string(message.searchJobId);
    }
    if (message.logicalPlan !== undefined) {
      SearchInspectionLogicalPlan.encode(message.logicalPlan, writer.uint32(18).fork()).join();
    }
    if (message.physicalPlan !== undefined) {
      SearchInspectionPhysicalPlan.encode(message.physicalPlan, writer.uint32(26).fork()).join();
    }
    if (message.generatedSql !== "") {
      writer.uint32(34).string(message.generatedSql);
    }
    if (message.explainText !== "") {
      writer.uint32(42).string(message.explainText);
    }
    if (message.diagnosticQueryId !== "") {
      writer.uint32(50).string(message.diagnosticQueryId);
    }
    if (message.knowledgeSnapshot !== undefined) {
      KnowledgeSnapshotSummary.encode(message.knowledgeSnapshot, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InspectSearchJobResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseInspectSearchJobResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.searchJobId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.logicalPlan = SearchInspectionLogicalPlan.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.physicalPlan = SearchInspectionPhysicalPlan.decode(reader, reader.uint32());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.generatedSql = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.explainText = reader.string();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.diagnosticQueryId = reader.string();
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.knowledgeSnapshot = KnowledgeSnapshotSummary.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): InspectSearchJobResponse {
    return {
      searchJobId: isSet(object.searchJobId)
        ? globalThis.String(object.searchJobId)
        : isSet(object.search_job_id)
        ? globalThis.String(object.search_job_id)
        : "",
      logicalPlan: isSet(object.logicalPlan)
        ? SearchInspectionLogicalPlan.fromJSON(object.logicalPlan)
        : isSet(object.logical_plan)
        ? SearchInspectionLogicalPlan.fromJSON(object.logical_plan)
        : undefined,
      physicalPlan: isSet(object.physicalPlan)
        ? SearchInspectionPhysicalPlan.fromJSON(object.physicalPlan)
        : isSet(object.physical_plan)
        ? SearchInspectionPhysicalPlan.fromJSON(object.physical_plan)
        : undefined,
      generatedSql: isSet(object.generatedSql)
        ? globalThis.String(object.generatedSql)
        : isSet(object.generated_sql)
        ? globalThis.String(object.generated_sql)
        : "",
      explainText: isSet(object.explainText)
        ? globalThis.String(object.explainText)
        : isSet(object.explain_text)
        ? globalThis.String(object.explain_text)
        : "",
      diagnosticQueryId: isSet(object.diagnosticQueryId)
        ? globalThis.String(object.diagnosticQueryId)
        : isSet(object.diagnostic_query_id)
        ? globalThis.String(object.diagnostic_query_id)
        : "",
      knowledgeSnapshot: isSet(object.knowledgeSnapshot)
        ? KnowledgeSnapshotSummary.fromJSON(object.knowledgeSnapshot)
        : isSet(object.knowledge_snapshot)
        ? KnowledgeSnapshotSummary.fromJSON(object.knowledge_snapshot)
        : undefined,
    };
  },

  toJSON(message: InspectSearchJobResponse): unknown {
    const obj: any = {};
    if (message.searchJobId !== "") {
      obj.searchJobId = message.searchJobId;
    }
    if (message.logicalPlan !== undefined) {
      obj.logicalPlan = SearchInspectionLogicalPlan.toJSON(message.logicalPlan);
    }
    if (message.physicalPlan !== undefined) {
      obj.physicalPlan = SearchInspectionPhysicalPlan.toJSON(message.physicalPlan);
    }
    if (message.generatedSql !== "") {
      obj.generatedSql = message.generatedSql;
    }
    if (message.explainText !== "") {
      obj.explainText = message.explainText;
    }
    if (message.diagnosticQueryId !== "") {
      obj.diagnosticQueryId = message.diagnosticQueryId;
    }
    if (message.knowledgeSnapshot !== undefined) {
      obj.knowledgeSnapshot = KnowledgeSnapshotSummary.toJSON(message.knowledgeSnapshot);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InspectSearchJobResponse>, I>>(base?: I): InspectSearchJobResponse {
    return InspectSearchJobResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InspectSearchJobResponse>, I>>(object: I): InspectSearchJobResponse {
    const message = createBaseInspectSearchJobResponse();
    message.searchJobId = object.searchJobId ?? "";
    message.logicalPlan = (object.logicalPlan !== undefined && object.logicalPlan !== null)
      ? SearchInspectionLogicalPlan.fromPartial(object.logicalPlan)
      : undefined;
    message.physicalPlan = (object.physicalPlan !== undefined && object.physicalPlan !== null)
      ? SearchInspectionPhysicalPlan.fromPartial(object.physicalPlan)
      : undefined;
    message.generatedSql = object.generatedSql ?? "";
    message.explainText = object.explainText ?? "";
    message.diagnosticQueryId = object.diagnosticQueryId ?? "";
    message.knowledgeSnapshot = (object.knowledgeSnapshot !== undefined && object.knowledgeSnapshot !== null)
      ? KnowledgeSnapshotSummary.fromPartial(object.knowledgeSnapshot)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
