// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: open_splunk/audit_api.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  AuditAction,
  auditActionFromJSON,
  auditActionToJSON,
  AuditEvent,
  AuditTargetKind,
  auditTargetKindFromJSON,
  auditTargetKindToJSON,
} from "./audit";
import { PageRequest, PageResponse } from "./common";

/**
 * POST /api/audit/events/list
 * Lists retained events in descending sequence order. Filters are exact, and
 * page tokens are bound to the filter set that produced them.
 */
export interface ListAuditEventsRequest {
  page: PageRequest | undefined;
  actionFilters: AuditAction[];
  actorIdFilter?: string | undefined;
  targetKindFilter?: AuditTargetKind | undefined;
}

export interface ListAuditEventsResponse {
  auditEvents: AuditEvent[];
  page: PageResponse | undefined;
}

function createBaseListAuditEventsRequest(): ListAuditEventsRequest {
  return { page: undefined, actionFilters: [], actorIdFilter: undefined, targetKindFilter: undefined };
}

export const ListAuditEventsRequest: MessageFns<ListAuditEventsRequest> = {
  encode(message: ListAuditEventsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.page !== undefined) {
      PageRequest.encode(message.page, writer.uint32(10).fork()).join();
    }
    writer.uint32(18).fork();
    for (const v of message.actionFilters) {
      writer.int32(v);
    }
    writer.join();
    if (message.actorIdFilter !== undefined) {
      writer.uint32(26).string(message.actorIdFilter);
    }
    if (message.targetKindFilter !== undefined) {
      writer.uint32(32).int32(message.targetKindFilter);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAuditEventsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAuditEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.page = PageRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.actionFilters.push(reader.int32() as any);

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.actionFilters.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.actorIdFilter = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.targetKindFilter = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListAuditEventsRequest {
    return {
      page: isSet(object.page) ? PageRequest.fromJSON(object.page) : undefined,
      actionFilters: globalThis.Array.isArray(object?.actionFilters)
        ? object.actionFilters.map((e: any) => auditActionFromJSON(e))
        : globalThis.Array.isArray(object?.action_filters)
        ? object.action_filters.map((e: any) => auditActionFromJSON(e))
        : [],
      actorIdFilter: isSet(object.actorIdFilter)
        ? globalThis.String(object.actorIdFilter)
        : isSet(object.actor_id_filter)
        ? globalThis.String(object.actor_id_filter)
        : undefined,
      targetKindFilter: isSet(object.targetKindFilter)
        ? auditTargetKindFromJSON(object.targetKindFilter)
        : isSet(object.target_kind_filter)
        ? auditTargetKindFromJSON(object.target_kind_filter)
        : undefined,
    };
  },

  toJSON(message: ListAuditEventsRequest): unknown {
    const obj: any = {};
    if (message.page !== undefined) {
      obj.page = PageRequest.toJSON(message.page);
    }
    if (message.actionFilters?.length) {
      obj.actionFilters = message.actionFilters.map((e) => auditActionToJSON(e));
    }
    if (message.actorIdFilter !== undefined) {
      obj.actorIdFilter = message.actorIdFilter;
    }
    if (message.targetKindFilter !== undefined) {
      obj.targetKindFilter = auditTargetKindToJSON(message.targetKindFilter);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListAuditEventsRequest>, I>>(base?: I): ListAuditEventsRequest {
    return ListAuditEventsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListAuditEventsRequest>, I>>(object: I): ListAuditEventsRequest {
    const message = createBaseListAuditEventsRequest();
    message.page = (object.page !== undefined && object.page !== null)
      ? PageRequest.fromPartial(object.page)
      : undefined;
    message.actionFilters = object.actionFilters?.map((e) => e) || [];
    message.actorIdFilter = object.actorIdFilter ?? undefined;
    message.targetKindFilter = object.targetKindFilter ?? undefined;
    return message;
  },
};

function createBaseListAuditEventsResponse(): ListAuditEventsResponse {
  return { auditEvents: [], page: undefined };
}

export const ListAuditEventsResponse: MessageFns<ListAuditEventsResponse> = {
  encode(message: ListAuditEventsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.auditEvents) {
      AuditEvent.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      PageResponse.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAuditEventsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAuditEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.auditEvents.push(AuditEvent.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListAuditEventsResponse {
    return {
      auditEvents: globalThis.Array.isArray(object?.auditEvents)
        ? object.auditEvents.map((e: any) => AuditEvent.fromJSON(e))
        : globalThis.Array.isArray(object?.audit_events)
        ? object.audit_events.map((e: any) => AuditEvent.fromJSON(e))
        : [],
      page: isSet(object.page) ? PageResponse.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListAuditEventsResponse): unknown {
    const obj: any = {};
    if (message.auditEvents?.length) {
      obj.auditEvents = message.auditEvents.map((e) => AuditEvent.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = PageResponse.toJSON(message.page);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListAuditEventsResponse>, I>>(base?: I): ListAuditEventsResponse {
    return ListAuditEventsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListAuditEventsResponse>, I>>(object: I): ListAuditEventsResponse {
    const message = createBaseListAuditEventsResponse();
    message.auditEvents = object.auditEvents?.map((e) => AuditEvent.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResponse.fromPartial(object.page)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
