// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: open_splunk/lookup_api.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  FieldViolation,
  PageRequest,
  PageResponse,
  SortDirection,
  sortDirectionFromJSON,
  sortDirectionToJSON,
} from "./common";
import { Lookup, LookupDefinition, LookupState, lookupStateFromJSON, lookupStateToJSON } from "./lookup";

export enum LookupSortBy {
  LOOKUP_SORT_BY_UNSPECIFIED = 0,
  LOOKUP_SORT_BY_NAME = 1,
  LOOKUP_SORT_BY_CREATED_AT = 2,
  LOOKUP_SORT_BY_UPDATED_AT = 3,
  UNRECOGNIZED = -1,
}

export function lookupSortByFromJSON(object: any): LookupSortBy {
  switch (object) {
    case 0:
    case "LOOKUP_SORT_BY_UNSPECIFIED":
      return LookupSortBy.LOOKUP_SORT_BY_UNSPECIFIED;
    case 1:
    case "LOOKUP_SORT_BY_NAME":
      return LookupSortBy.LOOKUP_SORT_BY_NAME;
    case 2:
    case "LOOKUP_SORT_BY_CREATED_AT":
      return LookupSortBy.LOOKUP_SORT_BY_CREATED_AT;
    case 3:
    case "LOOKUP_SORT_BY_UPDATED_AT":
      return LookupSortBy.LOOKUP_SORT_BY_UPDATED_AT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return LookupSortBy.UNRECOGNIZED;
  }
}

export function lookupSortByToJSON(object: LookupSortBy): string {
  switch (object) {
    case LookupSortBy.LOOKUP_SORT_BY_UNSPECIFIED:
      return "LOOKUP_SORT_BY_UNSPECIFIED";
    case LookupSortBy.LOOKUP_SORT_BY_NAME:
      return "LOOKUP_SORT_BY_NAME";
    case LookupSortBy.LOOKUP_SORT_BY_CREATED_AT:
      return "LOOKUP_SORT_BY_CREATED_AT";
    case LookupSortBy.LOOKUP_SORT_BY_UPDATED_AT:
      return "LOOKUP_SORT_BY_UPDATED_AT";
    case LookupSortBy.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** POST /api/knowledge/lookups/create */
export interface CreateLookupRequest {
  definition: LookupDefinition | undefined;
  csvData: Uint8Array;
  clientRequestId?: string | undefined;
}

export interface CreateLookupResponse {
  lookup: Lookup | undefined;
}

/** POST /api/knowledge/lookups/get */
export interface GetLookupRequest {
  lookupId: string;
  version?: bigint | undefined;
}

export interface GetLookupResponse {
  lookup: Lookup | undefined;
}

/** POST /api/knowledge/lookups/list */
export interface ListLookupsRequest {
  page: PageRequest | undefined;
  appId?: string | undefined;
  stateFilters: LookupState[];
  textFilter?: string | undefined;
  sortBy: LookupSortBy;
  sortDirection: SortDirection;
}

export interface ListLookupsResponse {
  lookups: Lookup[];
  page: PageResponse | undefined;
}

/** POST /api/knowledge/lookups/replace */
export interface ReplaceLookupRequest {
  lookupId: string;
  expectedVersion: bigint;
  definition: LookupDefinition | undefined;
  csvData?: Uint8Array | undefined;
}

export interface ReplaceLookupResponse {
  lookup: Lookup | undefined;
}

/** POST /api/knowledge/lookups/state/set */
export interface SetLookupStateRequest {
  lookupId: string;
  expectedVersion: bigint;
  state: LookupState;
}

export interface SetLookupStateResponse {
  lookup: Lookup | undefined;
}

/** POST /api/knowledge/lookups/delete */
export interface DeleteLookupRequest {
  lookupId: string;
  expectedVersion: bigint;
  confirmationName: string;
}

export interface DeleteLookupResponse {
  lookupId: string;
  version: bigint;
}

/** POST /api/knowledge/lookups/preview */
export interface PreviewLookupRequest {
  definition: LookupDefinition | undefined;
  csvData: Uint8Array;
  maximumRows?: number | undefined;
}

export interface LookupPreviewRow {
  values: string[];
}

export interface PreviewLookupResponse {
  columns: string[];
  rows: LookupPreviewRow[];
  totalRows: bigint;
  truncated: boolean;
  sourceSha256: Uint8Array;
  contentSha256: Uint8Array;
  violations: FieldViolation[];
}

function createBaseCreateLookupRequest(): CreateLookupRequest {
  return { definition: undefined, csvData: new Uint8Array(0), clientRequestId: undefined };
}

export const CreateLookupRequest: MessageFns<CreateLookupRequest> = {
  encode(message: CreateLookupRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.definition !== undefined) {
      LookupDefinition.encode(message.definition, writer.uint32(10).fork()).join();
    }
    if (message.csvData.length !== 0) {
      writer.uint32(18).bytes(message.csvData);
    }
    if (message.clientRequestId !== undefined) {
      writer.uint32(26).string(message.clientRequestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateLookupRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateLookupRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.definition = LookupDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.csvData = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.clientRequestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateLookupRequest {
    return {
      definition: isSet(object.definition) ? LookupDefinition.fromJSON(object.definition) : undefined,
      csvData: isSet(object.csvData)
        ? bytesFromBase64(object.csvData)
        : isSet(object.csv_data)
        ? bytesFromBase64(object.csv_data)
        : new Uint8Array(0),
      clientRequestId: isSet(object.clientRequestId)
        ? globalThis.String(object.clientRequestId)
        : isSet(object.client_request_id)
        ? globalThis.String(object.client_request_id)
        : undefined,
    };
  },

  toJSON(message: CreateLookupRequest): unknown {
    const obj: any = {};
    if (message.definition !== undefined) {
      obj.definition = LookupDefinition.toJSON(message.definition);
    }
    if (message.csvData.length !== 0) {
      obj.csvData = base64FromBytes(message.csvData);
    }
    if (message.clientRequestId !== undefined) {
      obj.clientRequestId = message.clientRequestId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateLookupRequest>, I>>(base?: I): CreateLookupRequest {
    return CreateLookupRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateLookupRequest>, I>>(object: I): CreateLookupRequest {
    const message = createBaseCreateLookupRequest();
    message.definition = (object.definition !== undefined && object.definition !== null)
      ? LookupDefinition.fromPartial(object.definition)
      : undefined;
    message.csvData = object.csvData ?? new Uint8Array(0);
    message.clientRequestId = object.clientRequestId ?? undefined;
    return message;
  },
};

function createBaseCreateLookupResponse(): CreateLookupResponse {
  return { lookup: undefined };
}

export const CreateLookupResponse: MessageFns<CreateLookupResponse> = {
  encode(message: CreateLookupResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lookup !== undefined) {
      Lookup.encode(message.lookup, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateLookupResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateLookupResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.lookup = Lookup.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateLookupResponse {
    return { lookup: isSet(object.lookup) ? Lookup.fromJSON(object.lookup) : undefined };
  },

  toJSON(message: CreateLookupResponse): unknown {
    const obj: any = {};
    if (message.lookup !== undefined) {
      obj.lookup = Lookup.toJSON(message.lookup);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateLookupResponse>, I>>(base?: I): CreateLookupResponse {
    return CreateLookupResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateLookupResponse>, I>>(object: I): CreateLookupResponse {
    const message = createBaseCreateLookupResponse();
    message.lookup = (object.lookup !== undefined && object.lookup !== null)
      ? Lookup.fromPartial(object.lookup)
      : undefined;
    return message;
  },
};

function createBaseGetLookupRequest(): GetLookupRequest {
  return { lookupId: "", version: undefined };
}

export const GetLookupRequest: MessageFns<GetLookupRequest> = {
  encode(message: GetLookupRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lookupId !== "") {
      writer.uint32(10).string(message.lookupId);
    }
    if (message.version !== undefined) {
      if (BigInt.asUintN(64, message.version) !== message.version) {
        throw new globalThis.Error("value provided for field message.version of type uint64 too large");
      }
      writer.uint32(16).uint64(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLookupRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetLookupRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.lookupId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetLookupRequest {
    return {
      lookupId: isSet(object.lookupId)
        ? globalThis.String(object.lookupId)
        : isSet(object.lookup_id)
        ? globalThis.String(object.lookup_id)
        : "",
      version: isSet(object.version) ? BigInt(object.version) : undefined,
    };
  },

  toJSON(message: GetLookupRequest): unknown {
    const obj: any = {};
    if (message.lookupId !== "") {
      obj.lookupId = message.lookupId;
    }
    if (message.version !== undefined) {
      obj.version = message.version.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetLookupRequest>, I>>(base?: I): GetLookupRequest {
    return GetLookupRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetLookupRequest>, I>>(object: I): GetLookupRequest {
    const message = createBaseGetLookupRequest();
    message.lookupId = object.lookupId ?? "";
    message.version = (object.version !== undefined && object.version !== null) ? BigInt(object.version) : undefined;
    return message;
  },
};

function createBaseGetLookupResponse(): GetLookupResponse {
  return { lookup: undefined };
}

export const GetLookupResponse: MessageFns<GetLookupResponse> = {
  encode(message: GetLookupResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lookup !== undefined) {
      Lookup.encode(message.lookup, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLookupResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetLookupResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.lookup = Lookup.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetLookupResponse {
    return { lookup: isSet(object.lookup) ? Lookup.fromJSON(object.lookup) : undefined };
  },

  toJSON(message: GetLookupResponse): unknown {
    const obj: any = {};
    if (message.lookup !== undefined) {
      obj.lookup = Lookup.toJSON(message.lookup);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetLookupResponse>, I>>(base?: I): GetLookupResponse {
    return GetLookupResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetLookupResponse>, I>>(object: I): GetLookupResponse {
    const message = createBaseGetLookupResponse();
    message.lookup = (object.lookup !== undefined && object.lookup !== null)
      ? Lookup.fromPartial(object.lookup)
      : undefined;
    return message;
  },
};

function createBaseListLookupsRequest(): ListLookupsRequest {
  return { page: undefined, appId: undefined, stateFilters: [], textFilter: undefined, sortBy: 0, sortDirection: 0 };
}

export const ListLookupsRequest: MessageFns<ListLookupsRequest> = {
  encode(message: ListLookupsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.page !== undefined) {
      PageRequest.encode(message.page, writer.uint32(10).fork()).join();
    }
    if (message.appId !== undefined) {
      writer.uint32(18).string(message.appId);
    }
    writer.uint32(26).fork();
    for (const v of message.stateFilters) {
      writer.int32(v);
    }
    writer.join();
    if (message.textFilter !== undefined) {
      writer.uint32(34).string(message.textFilter);
    }
    if (message.sortBy !== 0) {
      writer.uint32(40).int32(message.sortBy);
    }
    if (message.sortDirection !== 0) {
      writer.uint32(48).int32(message.sortDirection);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListLookupsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListLookupsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.page = PageRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.appId = reader.string();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.stateFilters.push(reader.int32() as any);

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.stateFilters.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.textFilter = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.sortBy = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.sortDirection = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListLookupsRequest {
    return {
      page: isSet(object.page) ? PageRequest.fromJSON(object.page) : undefined,
      appId: isSet(object.appId)
        ? globalThis.String(object.appId)
        : isSet(object.app_id)
        ? globalThis.String(object.app_id)
        : undefined,
      stateFilters: globalThis.Array.isArray(object?.stateFilters)
        ? object.stateFilters.map((e: any) => lookupStateFromJSON(e))
        : globalThis.Array.isArray(object?.state_filters)
        ? object.state_filters.map((e: any) => lookupStateFromJSON(e))
        : [],
      textFilter: isSet(object.textFilter)
        ? globalThis.String(object.textFilter)
        : isSet(object.text_filter)
        ? globalThis.String(object.text_filter)
        : undefined,
      sortBy: isSet(object.sortBy)
        ? lookupSortByFromJSON(object.sortBy)
        : isSet(object.sort_by)
        ? lookupSortByFromJSON(object.sort_by)
        : 0,
      sortDirection: isSet(object.sortDirection)
        ? sortDirectionFromJSON(object.sortDirection)
        : isSet(object.sort_direction)
        ? sortDirectionFromJSON(object.sort_direction)
        : 0,
    };
  },

  toJSON(message: ListLookupsRequest): unknown {
    const obj: any = {};
    if (message.page !== undefined) {
      obj.page = PageRequest.toJSON(message.page);
    }
    if (message.appId !== undefined) {
      obj.appId = message.appId;
    }
    if (message.stateFilters?.length) {
      obj.stateFilters = message.stateFilters.map((e) => lookupStateToJSON(e));
    }
    if (message.textFilter !== undefined) {
      obj.textFilter = message.textFilter;
    }
    if (message.sortBy !== 0) {
      obj.sortBy = lookupSortByToJSON(message.sortBy);
    }
    if (message.sortDirection !== 0) {
      obj.sortDirection = sortDirectionToJSON(message.sortDirection);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListLookupsRequest>, I>>(base?: I): ListLookupsRequest {
    return ListLookupsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListLookupsRequest>, I>>(object: I): ListLookupsRequest {
    const message = createBaseListLookupsRequest();
    message.page = (object.page !== undefined && object.page !== null)
      ? PageRequest.fromPartial(object.page)
      : undefined;
    message.appId = object.appId ?? undefined;
    message.stateFilters = object.stateFilters?.map((e) => e) || [];
    message.textFilter = object.textFilter ?? undefined;
    message.sortBy = object.sortBy ?? 0;
    message.sortDirection = object.sortDirection ?? 0;
    return message;
  },
};

function createBaseListLookupsResponse(): ListLookupsResponse {
  return { lookups: [], page: undefined };
}

export const ListLookupsResponse: MessageFns<ListLookupsResponse> = {
  encode(message: ListLookupsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.lookups) {
      Lookup.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      PageResponse.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListLookupsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListLookupsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.lookups.push(Lookup.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListLookupsResponse {
    return {
      lookups: globalThis.Array.isArray(object?.lookups) ? object.lookups.map((e: any) => Lookup.fromJSON(e)) : [],
      page: isSet(object.page) ? PageResponse.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListLookupsResponse): unknown {
    const obj: any = {};
    if (message.lookups?.length) {
      obj.lookups = message.lookups.map((e) => Lookup.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = PageResponse.toJSON(message.page);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListLookupsResponse>, I>>(base?: I): ListLookupsResponse {
    return ListLookupsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListLookupsResponse>, I>>(object: I): ListLookupsResponse {
    const message = createBaseListLookupsResponse();
    message.lookups = object.lookups?.map((e) => Lookup.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResponse.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseReplaceLookupRequest(): ReplaceLookupRequest {
  return { lookupId: "", expectedVersion: 0n, definition: undefined, csvData: undefined };
}

export const ReplaceLookupRequest: MessageFns<ReplaceLookupRequest> = {
  encode(message: ReplaceLookupRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lookupId !== "") {
      writer.uint32(10).string(message.lookupId);
    }
    if (message.expectedVersion !== 0n) {
      if (BigInt.asUintN(64, message.expectedVersion) !== message.expectedVersion) {
        throw new globalThis.Error("value provided for field message.expectedVersion of type uint64 too large");
      }
      writer.uint32(16).uint64(message.expectedVersion);
    }
    if (message.definition !== undefined) {
      LookupDefinition.encode(message.definition, writer.uint32(26).fork()).join();
    }
    if (message.csvData !== undefined) {
      writer.uint32(34).bytes(message.csvData);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReplaceLookupRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReplaceLookupRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.lookupId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.expectedVersion = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.definition = LookupDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.csvData = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReplaceLookupRequest {
    return {
      lookupId: isSet(object.lookupId)
        ? globalThis.String(object.lookupId)
        : isSet(object.lookup_id)
        ? globalThis.String(object.lookup_id)
        : "",
      expectedVersion: isSet(object.expectedVersion)
        ? BigInt(object.expectedVersion)
        : isSet(object.expected_version)
        ? BigInt(object.expected_version)
        : 0n,
      definition: isSet(object.definition) ? LookupDefinition.fromJSON(object.definition) : undefined,
      csvData: isSet(object.csvData)
        ? bytesFromBase64(object.csvData)
        : isSet(object.csv_data)
        ? bytesFromBase64(object.csv_data)
        : undefined,
    };
  },

  toJSON(message: ReplaceLookupRequest): unknown {
    const obj: any = {};
    if (message.lookupId !== "") {
      obj.lookupId = message.lookupId;
    }
    if (message.expectedVersion !== 0n) {
      obj.expectedVersion = message.expectedVersion.toString();
    }
    if (message.definition !== undefined) {
      obj.definition = LookupDefinition.toJSON(message.definition);
    }
    if (message.csvData !== undefined) {
      obj.csvData = base64FromBytes(message.csvData);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReplaceLookupRequest>, I>>(base?: I): ReplaceLookupRequest {
    return ReplaceLookupRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReplaceLookupRequest>, I>>(object: I): ReplaceLookupRequest {
    const message = createBaseReplaceLookupRequest();
    message.lookupId = object.lookupId ?? "";
    message.expectedVersion = (object.expectedVersion !== undefined && object.expectedVersion !== null)
      ? BigInt(object.expectedVersion)
      : 0n;
    message.definition = (object.definition !== undefined && object.definition !== null)
      ? LookupDefinition.fromPartial(object.definition)
      : undefined;
    message.csvData = object.csvData ?? undefined;
    return message;
  },
};

function createBaseReplaceLookupResponse(): ReplaceLookupResponse {
  return { lookup: undefined };
}

export const ReplaceLookupResponse: MessageFns<ReplaceLookupResponse> = {
  encode(message: ReplaceLookupResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lookup !== undefined) {
      Lookup.encode(message.lookup, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReplaceLookupResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReplaceLookupResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.lookup = Lookup.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReplaceLookupResponse {
    return { lookup: isSet(object.lookup) ? Lookup.fromJSON(object.lookup) : undefined };
  },

  toJSON(message: ReplaceLookupResponse): unknown {
    const obj: any = {};
    if (message.lookup !== undefined) {
      obj.lookup = Lookup.toJSON(message.lookup);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReplaceLookupResponse>, I>>(base?: I): ReplaceLookupResponse {
    return ReplaceLookupResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReplaceLookupResponse>, I>>(object: I): ReplaceLookupResponse {
    const message = createBaseReplaceLookupResponse();
    message.lookup = (object.lookup !== undefined && object.lookup !== null)
      ? Lookup.fromPartial(object.lookup)
      : undefined;
    return message;
  },
};

function createBaseSetLookupStateRequest(): SetLookupStateRequest {
  return { lookupId: "", expectedVersion: 0n, state: 0 };
}

export const SetLookupStateRequest: MessageFns<SetLookupStateRequest> = {
  encode(message: SetLookupStateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lookupId !== "") {
      writer.uint32(10).string(message.lookupId);
    }
    if (message.expectedVersion !== 0n) {
      if (BigInt.asUintN(64, message.expectedVersion) !== message.expectedVersion) {
        throw new globalThis.Error("value provided for field message.expectedVersion of type uint64 too large");
      }
      writer.uint32(16).uint64(message.expectedVersion);
    }
    if (message.state !== 0) {
      writer.uint32(24).int32(message.state);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetLookupStateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetLookupStateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.lookupId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.expectedVersion = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetLookupStateRequest {
    return {
      lookupId: isSet(object.lookupId)
        ? globalThis.String(object.lookupId)
        : isSet(object.lookup_id)
        ? globalThis.String(object.lookup_id)
        : "",
      expectedVersion: isSet(object.expectedVersion)
        ? BigInt(object.expectedVersion)
        : isSet(object.expected_version)
        ? BigInt(object.expected_version)
        : 0n,
      state: isSet(object.state) ? lookupStateFromJSON(object.state) : 0,
    };
  },

  toJSON(message: SetLookupStateRequest): unknown {
    const obj: any = {};
    if (message.lookupId !== "") {
      obj.lookupId = message.lookupId;
    }
    if (message.expectedVersion !== 0n) {
      obj.expectedVersion = message.expectedVersion.toString();
    }
    if (message.state !== 0) {
      obj.state = lookupStateToJSON(message.state);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetLookupStateRequest>, I>>(base?: I): SetLookupStateRequest {
    return SetLookupStateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetLookupStateRequest>, I>>(object: I): SetLookupStateRequest {
    const message = createBaseSetLookupStateRequest();
    message.lookupId = object.lookupId ?? "";
    message.expectedVersion = (object.expectedVersion !== undefined && object.expectedVersion !== null)
      ? BigInt(object.expectedVersion)
      : 0n;
    message.state = object.state ?? 0;
    return message;
  },
};

function createBaseSetLookupStateResponse(): SetLookupStateResponse {
  return { lookup: undefined };
}

export const SetLookupStateResponse: MessageFns<SetLookupStateResponse> = {
  encode(message: SetLookupStateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lookup !== undefined) {
      Lookup.encode(message.lookup, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetLookupStateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetLookupStateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.lookup = Lookup.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetLookupStateResponse {
    return { lookup: isSet(object.lookup) ? Lookup.fromJSON(object.lookup) : undefined };
  },

  toJSON(message: SetLookupStateResponse): unknown {
    const obj: any = {};
    if (message.lookup !== undefined) {
      obj.lookup = Lookup.toJSON(message.lookup);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetLookupStateResponse>, I>>(base?: I): SetLookupStateResponse {
    return SetLookupStateResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetLookupStateResponse>, I>>(object: I): SetLookupStateResponse {
    const message = createBaseSetLookupStateResponse();
    message.lookup = (object.lookup !== undefined && object.lookup !== null)
      ? Lookup.fromPartial(object.lookup)
      : undefined;
    return message;
  },
};

function createBaseDeleteLookupRequest(): DeleteLookupRequest {
  return { lookupId: "", expectedVersion: 0n, confirmationName: "" };
}

export const DeleteLookupRequest: MessageFns<DeleteLookupRequest> = {
  encode(message: DeleteLookupRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lookupId !== "") {
      writer.uint32(10).string(message.lookupId);
    }
    if (message.expectedVersion !== 0n) {
      if (BigInt.asUintN(64, message.expectedVersion) !== message.expectedVersion) {
        throw new globalThis.Error("value provided for field message.expectedVersion of type uint64 too large");
      }
      writer.uint32(16).uint64(message.expectedVersion);
    }
    if (message.confirmationName !== "") {
      writer.uint32(26).string(message.confirmationName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteLookupRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteLookupRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.lookupId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.expectedVersion = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.confirmationName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteLookupRequest {
    return {
      lookupId: isSet(object.lookupId)
        ? globalThis.String(object.lookupId)
        : isSet(object.lookup_id)
        ? globalThis.String(object.lookup_id)
        : "",
      expectedVersion: isSet(object.expectedVersion)
        ? BigInt(object.expectedVersion)
        : isSet(object.expected_version)
        ? BigInt(object.expected_version)
        : 0n,
      confirmationName: isSet(object.confirmationName)
        ? globalThis.String(object.confirmationName)
        : isSet(object.confirmation_name)
        ? globalThis.String(object.confirmation_name)
        : "",
    };
  },

  toJSON(message: DeleteLookupRequest): unknown {
    const obj: any = {};
    if (message.lookupId !== "") {
      obj.lookupId = message.lookupId;
    }
    if (message.expectedVersion !== 0n) {
      obj.expectedVersion = message.expectedVersion.toString();
    }
    if (message.confirmationName !== "") {
      obj.confirmationName = message.confirmationName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteLookupRequest>, I>>(base?: I): DeleteLookupRequest {
    return DeleteLookupRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteLookupRequest>, I>>(object: I): DeleteLookupRequest {
    const message = createBaseDeleteLookupRequest();
    message.lookupId = object.lookupId ?? "";
    message.expectedVersion = (object.expectedVersion !== undefined && object.expectedVersion !== null)
      ? BigInt(object.expectedVersion)
      : 0n;
    message.confirmationName = object.confirmationName ?? "";
    return message;
  },
};

function createBaseDeleteLookupResponse(): DeleteLookupResponse {
  return { lookupId: "", version: 0n };
}

export const DeleteLookupResponse: MessageFns<DeleteLookupResponse> = {
  encode(message: DeleteLookupResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lookupId !== "") {
      writer.uint32(10).string(message.lookupId);
    }
    if (message.version !== 0n) {
      if (BigInt.asUintN(64, message.version) !== message.version) {
        throw new globalThis.Error("value provided for field message.version of type uint64 too large");
      }
      writer.uint32(16).uint64(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteLookupResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteLookupResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.lookupId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteLookupResponse {
    return {
      lookupId: isSet(object.lookupId)
        ? globalThis.String(object.lookupId)
        : isSet(object.lookup_id)
        ? globalThis.String(object.lookup_id)
        : "",
      version: isSet(object.version) ? BigInt(object.version) : 0n,
    };
  },

  toJSON(message: DeleteLookupResponse): unknown {
    const obj: any = {};
    if (message.lookupId !== "") {
      obj.lookupId = message.lookupId;
    }
    if (message.version !== 0n) {
      obj.version = message.version.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteLookupResponse>, I>>(base?: I): DeleteLookupResponse {
    return DeleteLookupResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteLookupResponse>, I>>(object: I): DeleteLookupResponse {
    const message = createBaseDeleteLookupResponse();
    message.lookupId = object.lookupId ?? "";
    message.version = (object.version !== undefined && object.version !== null) ? BigInt(object.version) : 0n;
    return message;
  },
};

function createBasePreviewLookupRequest(): PreviewLookupRequest {
  return { definition: undefined, csvData: new Uint8Array(0), maximumRows: undefined };
}

export const PreviewLookupRequest: MessageFns<PreviewLookupRequest> = {
  encode(message: PreviewLookupRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.definition !== undefined) {
      LookupDefinition.encode(message.definition, writer.uint32(10).fork()).join();
    }
    if (message.csvData.length !== 0) {
      writer.uint32(18).bytes(message.csvData);
    }
    if (message.maximumRows !== undefined) {
      writer.uint32(24).uint32(message.maximumRows);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PreviewLookupRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePreviewLookupRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.definition = LookupDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.csvData = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maximumRows = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PreviewLookupRequest {
    return {
      definition: isSet(object.definition) ? LookupDefinition.fromJSON(object.definition) : undefined,
      csvData: isSet(object.csvData)
        ? bytesFromBase64(object.csvData)
        : isSet(object.csv_data)
        ? bytesFromBase64(object.csv_data)
        : new Uint8Array(0),
      maximumRows: isSet(object.maximumRows)
        ? globalThis.Number(object.maximumRows)
        : isSet(object.maximum_rows)
        ? globalThis.Number(object.maximum_rows)
        : undefined,
    };
  },

  toJSON(message: PreviewLookupRequest): unknown {
    const obj: any = {};
    if (message.definition !== undefined) {
      obj.definition = LookupDefinition.toJSON(message.definition);
    }
    if (message.csvData.length !== 0) {
      obj.csvData = base64FromBytes(message.csvData);
    }
    if (message.maximumRows !== undefined) {
      obj.maximumRows = Math.round(message.maximumRows);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PreviewLookupRequest>, I>>(base?: I): PreviewLookupRequest {
    return PreviewLookupRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PreviewLookupRequest>, I>>(object: I): PreviewLookupRequest {
    const message = createBasePreviewLookupRequest();
    message.definition = (object.definition !== undefined && object.definition !== null)
      ? LookupDefinition.fromPartial(object.definition)
      : undefined;
    message.csvData = object.csvData ?? new Uint8Array(0);
    message.maximumRows = object.maximumRows ?? undefined;
    return message;
  },
};

function createBaseLookupPreviewRow(): LookupPreviewRow {
  return { values: [] };
}

export const LookupPreviewRow: MessageFns<LookupPreviewRow> = {
  encode(message: LookupPreviewRow, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.values) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LookupPreviewRow {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLookupPreviewRow();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.values.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LookupPreviewRow {
    return {
      values: globalThis.Array.isArray(object?.values) ? object.values.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: LookupPreviewRow): unknown {
    const obj: any = {};
    if (message.values?.length) {
      obj.values = message.values;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LookupPreviewRow>, I>>(base?: I): LookupPreviewRow {
    return LookupPreviewRow.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LookupPreviewRow>, I>>(object: I): LookupPreviewRow {
    const message = createBaseLookupPreviewRow();
    message.values = object.values?.map((e) => e) || [];
    return message;
  },
};

function createBasePreviewLookupResponse(): PreviewLookupResponse {
  return {
    columns: [],
    rows: [],
    totalRows: 0n,
    truncated: false,
    sourceSha256: new Uint8Array(0),
    contentSha256: new Uint8Array(0),
    violations: [],
  };
}

export const PreviewLookupResponse: MessageFns<PreviewLookupResponse> = {
  encode(message: PreviewLookupResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.columns) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.rows) {
      LookupPreviewRow.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.totalRows !== 0n) {
      if (BigInt.asUintN(64, message.totalRows) !== message.totalRows) {
        throw new globalThis.Error("value provided for field message.totalRows of type uint64 too large");
      }
      writer.uint32(24).uint64(message.totalRows);
    }
    if (message.truncated !== false) {
      writer.uint32(32).bool(message.truncated);
    }
    if (message.sourceSha256.length !== 0) {
      writer.uint32(42).bytes(message.sourceSha256);
    }
    if (message.contentSha256.length !== 0) {
      writer.uint32(50).bytes(message.contentSha256);
    }
    for (const v of message.violations) {
      FieldViolation.encode(v!, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PreviewLookupResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePreviewLookupResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.columns.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rows.push(LookupPreviewRow.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.totalRows = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.truncated = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.sourceSha256 = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.contentSha256 = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.violations.push(FieldViolation.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PreviewLookupResponse {
    return {
      columns: globalThis.Array.isArray(object?.columns) ? object.columns.map((e: any) => globalThis.String(e)) : [],
      rows: globalThis.Array.isArray(object?.rows) ? object.rows.map((e: any) => LookupPreviewRow.fromJSON(e)) : [],
      totalRows: isSet(object.totalRows)
        ? BigInt(object.totalRows)
        : isSet(object.total_rows)
        ? BigInt(object.total_rows)
        : 0n,
      truncated: isSet(object.truncated) ? globalThis.Boolean(object.truncated) : false,
      sourceSha256: isSet(object.sourceSha256)
        ? bytesFromBase64(object.sourceSha256)
        : isSet(object.source_sha256)
        ? bytesFromBase64(object.source_sha256)
        : new Uint8Array(0),
      contentSha256: isSet(object.contentSha256)
        ? bytesFromBase64(object.contentSha256)
        : isSet(object.content_sha256)
        ? bytesFromBase64(object.content_sha256)
        : new Uint8Array(0),
      violations: globalThis.Array.isArray(object?.violations)
        ? object.violations.map((e: any) => FieldViolation.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PreviewLookupResponse): unknown {
    const obj: any = {};
    if (message.columns?.length) {
      obj.columns = message.columns;
    }
    if (message.rows?.length) {
      obj.rows = message.rows.map((e) => LookupPreviewRow.toJSON(e));
    }
    if (message.totalRows !== 0n) {
      obj.totalRows = message.totalRows.toString();
    }
    if (message.truncated !== false) {
      obj.truncated = message.truncated;
    }
    if (message.sourceSha256.length !== 0) {
      obj.sourceSha256 = base64FromBytes(message.sourceSha256);
    }
    if (message.contentSha256.length !== 0) {
      obj.contentSha256 = base64FromBytes(message.contentSha256);
    }
    if (message.violations?.length) {
      obj.violations = message.violations.map((e) => FieldViolation.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PreviewLookupResponse>, I>>(base?: I): PreviewLookupResponse {
    return PreviewLookupResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PreviewLookupResponse>, I>>(object: I): PreviewLookupResponse {
    const message = createBasePreviewLookupResponse();
    message.columns = object.columns?.map((e) => e) || [];
    message.rows = object.rows?.map((e) => LookupPreviewRow.fromPartial(e)) || [];
    message.totalRows = (object.totalRows !== undefined && object.totalRows !== null) ? BigInt(object.totalRows) : 0n;
    message.truncated = object.truncated ?? false;
    message.sourceSha256 = object.sourceSha256 ?? new Uint8Array(0);
    message.contentSha256 = object.contentSha256 ?? new Uint8Array(0);
    message.violations = object.violations?.map((e) => FieldViolation.fromPartial(e)) || [];
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
